[package]
name = "sinarca_token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "26.0.0" }


[profile.release]
opt-level = "z"
codegen-units = 1
lto = true
panic = "abort"

