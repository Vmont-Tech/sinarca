---
phase: "03"
plan: "03-05"
title: "Fechar dossiê, timeline, cobertura e UAT da originação"
type: "execute"
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - "src/pages/Dashboard/MrcaDetails.tsx"
  - "src/services/database.ts"
  - "tests/contract/test_frontend_project_links.py"
  - ".planning/phases/03-project-origination-and-documents/03-VERIFICATION.md"
autonomous: true
requirements:
  - "CHECKLIST-3-geofence"
  - "CHECKLIST-3-timeline"
  - "PHASE3-uat"
---

<objective>
Garantir que o projeto originado aparece no dossiê interno/público com QTAGs, geofence, baseline, documentos e timeline operacional vindos da API, e registrar a verificação/UAT da Phase 03.
</objective>

<context>
- `.planning/phases/03-project-origination-and-documents/03-CONTEXT.md`
- `.planning/phases/03-project-origination-and-documents/03-RESEARCH.md`
- `.planning/phases/03-project-origination-and-documents/03-VALIDATION.md`
- `ui-ux-pro-max` aplicado: dossiê deve manter hierarquia escaneável, estados vazios úteis, contraste AA e nenhum dado crítico comunicado só por cor.
- `src/pages/Dashboard/MrcaDetails.tsx`
- `src/services/database.ts`
- `tests/contract/test_frontend_project_links.py`
</context>

<coverage>
## Cobertura do checklist

- Cobre fechamento do Checklist 3:
  - polígono/geofence exibido a partir de `project_tags`;
  - documentos legais/inventário listados no dossiê;
  - baseline/hash/Sentinel status explícitos;
  - timeline cobre de criação até fila de certificação e prepara `AVAILABLE` para fases seguintes.
- Fora de escopo:
  - certificação e auditoria decisória ficam nas Phases 4 e 5;
  - marketplace/aposentadoria ficam na Phase 6.
- Gate de dados:
  - nada novo pode vir de mock runtime; dossiê lê `/api/v1/projects/{id}/public-dossier`.
- UAT obrigatório:
  - evidência manual em `03-VERIFICATION.md`.
</coverage>

<threat_model>
## Threat Model

- **Ameaça:** dossiê continuar mostrando dados legados/fixos e mascarar falha de originação.
  **Mitigação:** testes/grep bloqueiam stubs e exigem render de `dossier.tags`, `dossier.documents`, `project.timeline`.
- **Ameaça:** dados sensíveis de CMAC/documentos serem expostos de forma ampla.
  **Mitigação:** manter exibição mínima/truncada quando público e deixar detalhes completos para painel protegido quando necessário.
- **Ameaça:** fase marcada completa sem UAT de fluxo crítico.
  **Mitigação:** `03-VERIFICATION.md` obrigatório com passos e resultados.
</threat_model>

<tasks>
<task id="T1" type="execute">
  <title>Renderizar geofence e status técnico no dossiê</title>
  <read_first>
    - `src/pages/Dashboard/MrcaDetails.tsx`
    - `src/components/ProjectGeofencePreview.tsx`
    - `src/services/database.ts`
  </read_first>
  <action>
    Atualizar `MrcaDetails.tsx` para:
    - renderizar `ProjectGeofencePreview` ou equivalente somente com dados de `dossier.tags`;
    - manter lista de QTAGs por vértice A/B/C/D;
    - mostrar baseline hash, pontos analisados, NDVI e status técnico de Sentinel quando disponível em metadata/response;
    - não exibir texto que sugira Sentinel live se `baseline_source` for `deterministic_baseline`;
    - manter estados vazios com ação/explicação curta, sem cards decorativos aninhados.
  </action>
  <verify>
    `rg -n "ProjectGeofencePreview|dossier.tags|baseline|deterministic_baseline|Sentinel" src/pages/Dashboard/MrcaDetails.tsx && npm run build`
  </verify>
  <acceptance_criteria>
    - Geofence no detalhe usa `dossier.tags`.
    - Baseline determinístico é rotulado corretamente se exposto.
    - QTAGs ausentes mostram empty state, não dados inventados.
  </acceptance_criteria>
</task>

<task id="T2" type="execute">
  <title>Garantir timeline operacional sem stubs</title>
  <read_first>
    - `src/pages/Dashboard/MrcaDetails.tsx`
    - `backend_app/modules/projects/service.py`
    - `.planning/ROADMAP.md`
  </read_first>
  <action>
    Ajustar render de timeline para aceitar eventos com `code` e exibir os eventos da API em ordem. A tela deve reconhecer e nomear:
    - `CREATED`;
    - `QTAGS_RECORDED`;
    - `BASELINE_CREATED`;
    - `DOCUMENTS_PENDING` ou documento enviado;
    - `AWAITING_CERTIFICATION`;
    - estados futuros `AWAITING_AUDIT`, `ACTIVE`, `AVAILABLE` sem quebrar.
  </action>
  <verify>
    `rg -n "CREATED|QTAGS_RECORDED|BASELINE_CREATED|DOCUMENTS_PENDING|AWAITING_CERTIFICATION|AVAILABLE" src/pages/Dashboard/MrcaDetails.tsx backend_app/modules/projects/service.py && npm run build`
  </verify>
  <acceptance_criteria>
    - Timeline renderiza eventos da API e não substitui por lista fixa.
    - Estados futuros não causam tela quebrada.
  </acceptance_criteria>
</task>

<task id="T3" type="execute">
  <title>Adicionar testes/contratos de fonte de dados do frontend</title>
  <read_first>
    - `tests/contract/test_frontend_project_links.py`
    - `src/pages/Dashboard/AddProject.tsx`
    - `src/pages/Dashboard/MrcaDetails.tsx`
  </read_first>
  <action>
    Expandir `tests/contract/test_frontend_project_links.py` ou criar teste similar para verificar:
    - `AddProject.tsx` contém `apiPost<any>('/projects'` e propriedade `tags`;
    - `AddProject.tsx` contém upload real com `FormData`/`uploadProjectDocument`;
    - `MrcaDetails.tsx` consome `dossier.tags` e `dossier.documents`;
    - não há strings antigas que tratem documentos/QTAGs como exemplos fixos.
  </action>
  <verify>
    `uv run pytest -q tests/contract/test_frontend_project_links.py`
  </verify>
  <acceptance_criteria>
    - Teste falha se `tags` sair do payload da UI.
    - Teste falha se upload voltar a ser apenas decorativo.
  </acceptance_criteria>
</task>

<task id="T4" type="execute">
  <title>Registrar verificação e UAT da Phase 03</title>
  <read_first>
    - `.planning/phases/03-project-origination-and-documents/03-VALIDATION.md`
    - `.planning/docs/FLOW_SCREEN_CHECKLIST_AUDIT.md`
  </read_first>
  <action>
    Criar `.planning/phases/03-project-origination-and-documents/03-VERIFICATION.md` com:
    - comandos executados e resultado;
    - checklist seção 3 coberto;
    - itens fora de escopo redirecionados para Phases 4/5/6;
    - bloqueios externos de Web NFC/SUN/Sentinel quando aplicável;
    - passos de UAT manual para `/painel/adicionar-projeto`;
    - evidência de que documentos/QTAGs/timeline vêm da API.
  </action>
  <verify>
    `rg -n "Cobertura do checklist|UAT|Web NFC|SUN|Sentinel|QTAG|documentos" .planning/phases/03-project-origination-and-documents/03-VERIFICATION.md`
  </verify>
  <acceptance_criteria>
    - `03-VERIFICATION.md` existe e menciona comandos reais.
    - Bloqueios externos não são marcados como sucesso.
    - UAT manual tem passos claros e status.
  </acceptance_criteria>
</task>
</tasks>

<verification>
1. `npm run lint`
2. `npm run build`
3. `uv run --with pytest --with httpx pytest -q tests/contract/test_backend_app_api_v1.py -k "project or project_document"`
4. `uv run pytest -q tests/contract/test_frontend_project_links.py tests/db/test_schema_contract.py`
5. UAT manual em `/painel/adicionar-projeto`.
</verification>

<must_haves>
- Dossiê usa QTAGs, baseline, documentos e timeline da API.
- Verificação da fase registra UAT e bloqueios.
- Nenhum dado novo de originação vem de mock runtime.
</must_haves>

<success_criteria>
- Phase 03 fica pronta para execução com evidência objetiva de backend, frontend e UAT.
- Dependências para Phase 4/5 estão explícitas.
</success_criteria>
