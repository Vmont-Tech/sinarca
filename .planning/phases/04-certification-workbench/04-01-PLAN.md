---
phase: 04-certification-workbench
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/202608150001_certification_workbench.sql
  - supabase/seed.sql
  - backend_app/db/models.py
  - tests/test_certifier_workbench.py
autonomous: true
requirements: [CERT-01, CERT-02, CERT-03, CERT-04, CERT-05]

must_haves:
  truths:
    - "Duas decisões consecutivas no mesmo projeto geram duas linhas em certifications (nada é sobrescrito)"
    - "O banco tem uma tabela para pendências de correção e outra para autorizações de tesouraria"
    - "npx supabase db reset aplica migrations e seed sem erro"
    - "O arquivo de testes tests/test_certifier_workbench.py existe e declara os cinco testes do contrato CERT-01..05"
  artifacts:
    - path: "supabase/migrations/202608150001_certification_workbench.sql"
      provides: "Drop da constraint única de certifications, tabelas certification_pendencies e treasury_authorizations, RLS"
      contains: "create table if not exists treasury_authorizations"
    - path: "backend_app/db/models.py"
      provides: "Modelos CertificationPendency e TreasuryAuthorization; Certification sem UniqueConstraint"
      contains: "class TreasuryAuthorization(Base)"
    - path: "tests/test_certifier_workbench.py"
      provides: "Contrato de testes CERT-01..05 (Wave 0)"
      contains: "def test_approve_creates_treasury_authorization"
    - path: "supabase/seed.sql"
      provides: "Seed compatível com o schema novo e com dossiê mínimo completo para os projetos da fila"
      contains: "FOREST_INVENTORY"
  key_links:
    - from: "supabase/migrations/202608150001_certification_workbench.sql"
      to: "backend_app/db/models.py"
      via: "espelhamento de colunas SQL <-> SQLAlchemy"
      pattern: "certification_pendencies"
    - from: "supabase/seed.sql"
      to: "certifications"
      via: "insert idempotente sem ON CONFLICT (project_id, decision)"
      pattern: "delete from certifications"
---

<objective>
Criar a fundação de dados da bancada da certificadora e o contrato de testes da fase.

Purpose: A Phase 04 exige decisões append-only (D-09), pendências estruturadas de correção (D-04/D-08/D-10) e um pacote de autorização de tesouraria (D-18). Nenhuma dessas três coisas cabe no schema atual: `certifications` tem `UNIQUE (project_id, decision)` que sobrescreve decisões anteriores, e não existe tabela para pendência nem para autorização. Sem essa fundação, nenhum plano posterior pode ser implementado.
Output: Uma migration SQL nova, dois modelos SQLAlchemy novos, o seed ajustado ao schema novo e o arquivo de testes de integração que serve como contrato Nyquist de CERT-01..05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-RESEARCH.md
@.planning/phases/04-certification-workbench/04-PATTERNS.md
@.planning/phases/04-certification-workbench/04-VALIDATION.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual. Não explorar o codebase para descobrir isso. -->

De `backend_app/db/models.py` (helpers de coluna já existentes no arquivo):
```python
UserRoleEnum = Enum("producer", "auditor", "company", "certifier", "admin", name="user_role", create_type=False)

class Certification(Base):
    __tablename__ = "certifications"
    __table_args__ = (UniqueConstraint("project_id", "decision", name="certifications_project_decision_idx"),)  # ← REMOVER

    id: Mapped[uuid.UUID] = uuid_pk()
    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    certifier_organization_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("organizations.id"))
    certifier_profile_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("profiles.id"))
    methodology: Mapped[str] = mapped_column(String, nullable=False)
    credit_potential: Mapped[Decimal] = mapped_column(Numeric(18, 2), nullable=False)
    decision: Mapped[str] = mapped_column(String, nullable=False)
    notes: Mapped[str | None] = mapped_column(Text)
    signed_document_hash: Mapped[str | None] = mapped_column(String)
    signed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    created_at: Mapped[datetime] = created_at_column()
```

De `tests/contract/test_backend_app_api_v1.py` (helpers a replicar no arquivo de teste novo):
```python
client = TestClient(app)

def auth_headers(email: str = "produtor@sinarca.com.br", password: str = "produtor") -> dict[str, str]:
    response = client.post("/api/v1/auth/login", json={"email": email, "password": password})
    assert response.status_code == 200
    return {"Authorization": f"Bearer {response.json()['access_token']}"}
# certificadora: auth_headers("certificadora@sinarca.com.br", "certificadora")
```

De `supabase/migrations/202605270001_relax_document_hash_uniqueness.sql` (padrão idempotente):
```sql
drop index if exists documents_sha256_hash_idx;
do $$
begin
  if not exists (select 1 from pg_constraint where conname = 'x' and conrelid = 'y'::regclass) then
    alter table y add constraint x unique (...);
  end if;
end $$;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration de schema da bancada de certificação e ajustar o seed</name>
  <files>supabase/migrations/202608150001_certification_workbench.sql, supabase/seed.sql</files>
  <read_first>
    - supabase/migrations/202605220001_initial_schema.sql (convenções de CREATE TABLE, uuid_generate_v4, timestamptz)
    - supabase/migrations/202605260003_project_drafts.sql (padrão de tabela nova + índices + RLS + revoke)
    - supabase/migrations/202605270001_relax_document_hash_uniqueness.sql (padrão de drop/add idempotente)
    - supabase/migrations/202605220002_rls_policies.sql (padrão de enable RLS / revoke / grant)
    - supabase/seed.sql (blocos `insert into certifications` e `insert into documents`)
  </read_first>
  <action>
Criar o arquivo NOVO `supabase/migrations/202608150001_certification_workbench.sql` com exatamente este conteúdo (comentários em PT-BR com acentuação, como no restante das migrations):

```sql
-- Phase 04: bancada da certificadora.
-- 1. Decisoes de certificacao passam a ser append-only (D-09): a constraint
--    unica (project_id, decision) sobrescrevia a decisao anterior.
-- 2. Pendencias de correcao (D-04, D-08, D-10) e pacote de autorizacao de
--    tesouraria (D-18) ganham tabelas proprias.

alter table certifications drop constraint if exists certifications_project_decision_idx;
drop index if exists certifications_project_decision_idx;

create index if not exists certifications_project_created_idx
  on certifications (project_id, created_at desc);

create table if not exists certification_pendencies (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  certification_id uuid references certifications(id),
  certifier_organization_id uuid references organizations(id),
  raised_by_profile_id uuid references profiles(id),
  category text not null,
  description text not null,
  status text not null default 'OPEN' check (status in ('OPEN', 'RESOLVED', 'CANCELLED')),
  producer_response text,
  responded_by_profile_id uuid references profiles(id),
  responded_at timestamptz,
  resolved_at timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index if not exists certification_pendencies_project_status_idx
  on certification_pendencies (project_id, status, created_at desc);

create index if not exists certification_pendencies_status_idx
  on certification_pendencies (status, created_at desc);

create table if not exists treasury_authorizations (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  certification_id uuid not null references certifications(id),
  certifier_organization_id uuid references organizations(id),
  certifier_profile_id uuid references profiles(id),
  methodology text not null,
  approved_credit_potential numeric(18, 2) not null,
  certificate_document_id uuid references documents(id),
  certificate_sha256 text not null,
  status text not null default 'PENDING' check (status in ('PENDING', 'IN_PROGRESS', 'COMPLETED', 'CANCELLED')),
  authorized_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create unique index if not exists treasury_authorizations_certification_idx
  on treasury_authorizations (certification_id);

create index if not exists treasury_authorizations_status_idx
  on treasury_authorizations (status, created_at desc);

alter table certification_pendencies enable row level security;
alter table treasury_authorizations enable row level security;

revoke insert, update, delete on certification_pendencies, treasury_authorizations
  from anon, authenticated;

-- Sem politicas de select: pendencias e autorizacoes sao dados operacionais
-- internos, expostos apenas pelo backend_app com auth propria e guard de papel.
```

Depois ajustar `supabase/seed.sql` em três pontos:

(a) O bloco `insert into certifications (...) ... on conflict (project_id, decision) do update set ...` deixa de funcionar porque a constraint referenciada foi removida. Substituir a cláusula `on conflict (project_id, decision) do update set ...` (e todas as linhas `coluna = excluded.coluna` que a seguem, até o `;`) por nada, e inserir ANTES do `insert into certifications` a linha de limpeza idempotente:
```sql
delete from certifications
where project_id in (
  select id from projects where friendly_id in ('PRC-2024-002', 'PRC-2026-010', 'PRC-2026-011')
);
```
O `insert into certifications (...) values (...)` fica sem cláusula `on conflict`, terminando em `;`.

(b) O bloco `insert into documents (...) ... on conflict (sha256_hash) do update set ...` referencia um índice único que a migration `202605270001_relax_document_hash_uniqueness.sql` já removeu (hoje só existe `documents_project_sha256_key unique (project_id, sha256_hash)`). Trocar `on conflict (sha256_hash)` por `on conflict (project_id, sha256_hash)`.

(c) Adicionar ao mesmo bloco `insert into documents` as linhas de dossiê mínimo para os dois projetos da fila da certificadora, para que a UI e o UAT tenham projetos aprováveis (baseline + 4 QTAGs já existem no seed):
```sql
  ((select id from organizations where external_id = 'prd-001'), (select id from projects where friendly_id = 'PRC-2026-010'), 'LEGAL_OWNERSHIP', 's3://sinarca-seed/documents/prc-2026-010-legal.pdf', 'sha256-legal-prc-2026-010', 'application/pdf', 102400, jsonb_build_object('source', 'dossie minimo seed', 'filename', 'matricula.pdf')),
  ((select id from organizations where external_id = 'prd-001'), (select id from projects where friendly_id = 'PRC-2026-010'), 'FOREST_INVENTORY', 's3://sinarca-seed/documents/prc-2026-010-inventario.pdf', 'sha256-inventario-prc-2026-010', 'application/pdf', 153600, jsonb_build_object('source', 'dossie minimo seed', 'filename', 'inventario.pdf')),
  ((select id from organizations where external_id = 'prd-001'), (select id from projects where friendly_id = 'PRC-2026-011'), 'LEGAL_OWNERSHIP', 's3://sinarca-seed/documents/prc-2026-011-legal.pdf', 'sha256-legal-prc-2026-011', 'application/pdf', 102400, jsonb_build_object('source', 'dossie minimo seed', 'filename', 'matricula.pdf')),
  ((select id from organizations where external_id = 'prd-001'), (select id from projects where friendly_id = 'PRC-2026-011'), 'FOREST_INVENTORY', 's3://sinarca-seed/documents/prc-2026-011-inventario.pdf', 'sha256-inventario-prc-2026-011', 'application/pdf', 153600, jsonb_build_object('source', 'dossie minimo seed', 'filename', 'inventario.pdf')),
```
Antes de escrever, confirmar no seed qual é o `external_id` real da organização produtora (grep `insert into organizations` no seed) e usar esse valor no lugar de `prd-001` se for diferente. Se o projeto `PRC-2026-011` não existir no seed, incluir apenas as linhas de `PRC-2026-010`.

Aplicar tudo com `npx supabase db reset`.
  </action>
  <verify>
    <automated>npx supabase db reset --debug 2>&1 | tail -20 && psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -c "\d certification_pendencies" -c "\d treasury_authorizations" -c "select conname from pg_constraint where conname = 'certifications_project_decision_idx'"</automated>
  </verify>
  <acceptance_criteria>
    - `ls supabase/migrations/202608150001_certification_workbench.sql` retorna o arquivo
    - `grep -c "create table if not exists certification_pendencies" supabase/migrations/202608150001_certification_workbench.sql` == 1
    - `grep -c "create table if not exists treasury_authorizations" supabase/migrations/202608150001_certification_workbench.sql` == 1
    - `grep -c "alter table certifications drop constraint if exists certifications_project_decision_idx" supabase/migrations/202608150001_certification_workbench.sql` == 1
    - `grep -v '^--' supabase/seed.sql | grep -c "on conflict (project_id, decision)"` == 0
    - `grep -v '^--' supabase/seed.sql | grep -c "on conflict (sha256_hash)"` == 0
    - `grep -c "FOREST_INVENTORY" supabase/seed.sql` >= 1
    - `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from pg_constraint where conname='certifications_project_decision_idx'"` retorna `0`
    - `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from information_schema.tables where table_name in ('certification_pendencies','treasury_authorizations')"` retorna `2`
    - `npx supabase db reset` termina com exit code 0
  </acceptance_criteria>
  <done>Migration aplicada, seed executando sem erro, constraint única de certifications inexistente e duas tabelas novas criadas com RLS habilitada.</done>
</task>

<task type="auto">
  <name>Task 2: Espelhar o schema novo em backend_app/db/models.py</name>
  <files>backend_app/db/models.py</files>
  <read_first>
    - backend_app/db/models.py (classes Certification linha ~228, Audit, Document linha ~457, AuditEvent linha ~479, e os helpers uuid_pk/created_at_column no topo)
    - supabase/migrations/202608150001_certification_workbench.sql (criado na Task 1 — as colunas devem bater 1:1)
  </read_first>
  <action>
1. Em `class Certification`, REMOVER a linha `__table_args__ = (UniqueConstraint("project_id", "decision", name="certifications_project_decision_idx"),)` e substituí-la por:
```python
    __table_args__ = (Index("certifications_project_created_idx", "project_id", "created_at"),)
```
`Index` já está importado no topo do arquivo.

2. Adicionar, logo após `class Certification`, os dois modelos novos (mesmas convenções do arquivo: `uuid_pk()`, `created_at_column()`, `metadata_` mapeado para a coluna `metadata`):

```python
class CertificationPendency(Base):
    __tablename__ = "certification_pendencies"
    __table_args__ = (
        Index("certification_pendencies_project_status_idx", "project_id", "status", "created_at"),
    )

    id: Mapped[uuid.UUID] = uuid_pk()
    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    certification_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("certifications.id"))
    certifier_organization_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("organizations.id"))
    raised_by_profile_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("profiles.id"))
    category: Mapped[str] = mapped_column(String, nullable=False)
    description: Mapped[str] = mapped_column(Text, nullable=False)
    status: Mapped[str] = mapped_column(String, nullable=False, server_default=text("'OPEN'"))
    producer_response: Mapped[str | None] = mapped_column(Text)
    responded_by_profile_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("profiles.id"))
    responded_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    resolved_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    created_at: Mapped[datetime] = created_at_column()


class TreasuryAuthorization(Base):
    __tablename__ = "treasury_authorizations"
    __table_args__ = (
        UniqueConstraint("certification_id", name="treasury_authorizations_certification_idx"),
        Index("treasury_authorizations_status_idx", "status", "created_at"),
    )

    id: Mapped[uuid.UUID] = uuid_pk()
    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    certification_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("certifications.id"), nullable=False)
    certifier_organization_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("organizations.id"))
    certifier_profile_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("profiles.id"))
    methodology: Mapped[str] = mapped_column(String, nullable=False)
    approved_credit_potential: Mapped[Decimal] = mapped_column(Numeric(18, 2), nullable=False)
    certificate_document_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("documents.id"))
    certificate_sha256: Mapped[str] = mapped_column(String, nullable=False)
    status: Mapped[str] = mapped_column(String, nullable=False, server_default=text("'PENDING'"))
    authorized_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    created_at: Mapped[datetime] = created_at_column()
```

3. Confirmar que `func` está importado no topo (`from sqlalchemy import ... func, text`). Se não estiver, adicionar.

Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>uv run python -c "from backend_app.db.models import CertificationPendency, TreasuryAuthorization, Certification; assert not any(getattr(c, 'name', '') == 'certifications_project_decision_idx' for c in Certification.__table__.constraints); print('ok', CertificationPendency.__tablename__, TreasuryAuthorization.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CertificationPendency(Base)" backend_app/db/models.py` == 1
    - `grep -c "class TreasuryAuthorization(Base)" backend_app/db/models.py` == 1
    - `grep -c "certifications_project_decision_idx" backend_app/db/models.py` == 0
    - `grep -c "certifications_project_created_idx" backend_app/db/models.py` == 1
    - `grep -c "approved_credit_potential" backend_app/db/models.py` == 1
    - `uv run python -c "from backend_app.db.models import CertificationPendency, TreasuryAuthorization"` sai com código 0
  </acceptance_criteria>
  <done>Modelos importáveis, Certification sem UniqueConstraint e as colunas SQLAlchemy batendo 1:1 com o SQL da Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Criar o arquivo de contrato de testes tests/test_certifier_workbench.py (Wave 0)</name>
  <files>tests/test_certifier_workbench.py</files>
  <read_first>
    - tests/contract/test_backend_app_api_v1.py (linhas 1-145: client, auth_headers, tag_payload, project_payload, create_project_for_workflow)
    - tests/conftest.py (fixture autouse isolate_optional_storage_env)
    - .planning/phases/04-certification-workbench/04-VALIDATION.md (mapa requisito→teste, nomes obrigatórios dos testes)
  </read_first>
  <action>
Criar o arquivo NOVO `tests/test_certifier_workbench.py`. Ele é o contrato Nyquist da fase: os testes falham agora (RED) e ficam verdes conforme os planos 02-05 avançam. NÃO marcar com `xfail`/`skip`.

Estrutura obrigatória:

```python
from __future__ import annotations

import uuid

from fastapi.testclient import TestClient

from backend_app.main import app

client = TestClient(app)

PDF_BYTES = b"%PDF-1.4\n1 0 obj\n<< /Type /Catalog >>\nendobj\ntrailer\n%%EOF\n"
NOT_A_PDF_BYTES = b"MZ\x90\x00 este arquivo nao e um PDF"

CERTIFIER = ("certificadora@sinarca.com.br", "certificadora")
PRODUCER = ("produtor@sinarca.com.br", "produtor")
COMPANY = ("empresa@sinarca.com.br", "empresa")


def auth_headers(email: str, password: str) -> dict[str, str]:
    response = client.post("/api/v1/auth/login", json={"email": email, "password": password})
    assert response.status_code == 200
    return {"Authorization": f"Bearer {response.json()['access_token']}"}


def tag_payload(prefix: str) -> list[dict[str, object]]:
    # copiar de tests/contract/test_backend_app_api_v1.py::tag_payload
    ...


def create_project(prefix: str | None = None) -> dict[str, object]:
    # POST /api/v1/projects com 4 QTAGs; retorna project dict
    ...


def upload_minimum_documents(friendly_id: str) -> None:
    """Sobe LEGAL_OWNERSHIP e FOREST_INVENTORY para satisfazer o dossie minimo (D-03)."""
    for document_type, filename in (("LEGAL_OWNERSHIP", "matricula.pdf"), ("FOREST_INVENTORY", "inventario.pdf")):
        response = client.post(
            f"/api/v1/projects/{friendly_id}/documents",
            data={"document_type": document_type},
            files={"file": (filename, PDF_BYTES, "application/pdf")},
            headers=auth_headers(*PRODUCER),
        )
        assert response.status_code == 201, response.text


def create_certifiable_project(prefix: str | None = None) -> dict[str, object]:
    project = create_project(prefix)
    upload_minimum_documents(str(project["friendlyId"]))
    return project


def approve_payload(**overrides: object) -> dict[str, object]:
    payload = {
        "decision": "APPROVE",
        "methodology": "AR-ACM0003",
        "credit_potential": "1200.0",
        "credit_potential_adjustment_reason": "Ajuste conservador sobre o potencial sugerido.",
        "notes": "Certificacao aprovada no teste de integracao.",
    }
    payload.update(overrides)
    return payload
```

Testes obrigatórios (nomes exatos — são referenciados por `04-VALIDATION.md` e pelos planos 02-05):

1. `test_review_dossier_endpoint()` — CERT-01.
   - `GET /api/v1/certifier/projects/{friendlyId}/review` com header de certificadora → 200.
   - JSON contém as chaves `project`, `baseline`, `tags`, `documents`, `dossier`, `calculation`, `certifications`, `pendencies`.
   - `payload["dossier"]["complete"] is True` para um projeto criado por `create_certifiable_project()`.
   - `len(payload["tags"]) == 4`.
   - `payload["calculation"]["suggestedCreditPotential"] > 0`.
   - Chamada com header de `COMPANY` → 403.

2. `test_decision_requires_structured_fields()` — CERT-02.
   - `REJECT` sem `rejection_category` → 400.
   - `REJECT` com `rejection_category` mas sem `description`/`notes` → 400.
   - `REJECT` com `rejection_category="DOCUMENTACAO_INCOMPLETA"` e `notes` preenchido → 200 e `new_status == "SUSPENDED"`.
   - `APPROVE` com `credit_potential` diferente do sugerido e SEM `credit_potential_adjustment_reason` → 400.
   - Usar `client.patch(url, data={...}, files={...} | None, headers=...)` (multipart), nunca `json=`.

3. `test_approve_requires_real_pdf()` — CERT-03.
   - `APPROVE` sem campo `certificate` → 400 e nenhuma linha nova em `certifications`.
   - `APPROVE` com `files={"certificate": ("cert.txt", PDF_BYTES, "text/plain")}` → 415.
   - `APPROVE` com `files={"certificate": ("cert.pdf", NOT_A_PDF_BYTES, "application/pdf")}` → 400 (magic bytes).
   - `APPROVE` com `files={"certificate": ("cert.pdf", PDF_BYTES, "application/pdf")}` → 200; a resposta traz `certificate["sha256"]` com o sha256 real de `PDF_BYTES` e `certificate["documentType"] == "CERTIFICATION_CERTIFICATE"`.

4. `test_approve_creates_treasury_authorization()` — CERT-04.
   - Aprovar com certificado → 200.
   - `new_status == "CERTIFIED_AWAITING_TREASURY"`.
   - `payload["statusLabels"] == ["Certificação aprovada", "Mint autorizado", "Aguardando tesouraria"]`.
   - `GET /api/v1/treasury/authorizations` com header de certificadora → 200 e existe item com `projectId == friendlyId`, `approvedCreditPotential` igual ao aprovado, `methodology` preenchida e `certificate["sha256"]` não vazio.
   - `GET /api/v1/certifier/projects/{friendlyId}/history` contém as ações `CERTIFICATION_APPROVED`, `MINT_AUTHORIZED` e `TREASURY_QUEUE_CREATED`.

5. `test_public_dossier_hides_internal_notes()` — CERT-05.
   - Aprovar com `notes="NOTA INTERNA CONFIDENCIAL"` e certificado.
   - `GET /api/v1/projects/{friendlyId}/public-dossier` (SEM header de autenticação) → 200.
   - `"NOTA INTERNA CONFIDENCIAL" not in response.text`.
   - Nenhuma entrada de `payload["certifications"]` possui a chave `notes`.
   - `{doc["type"] for doc in payload["documents"]} <= {"CERTIFICATION_CERTIFICATE"}` (nenhum `LEGAL_OWNERSHIP`/`FOREST_INVENTORY` vaza).
   - `payload["certificate"]["sha256"]` presente e não vazio.
   - `payload["certificationHistory"]` só contém ações de `{"CERTIFICATION_APPROVED", "CERTIFICATION_REJECTED", "CERTIFICATION_CERTIFICATE_ATTACHED", "MINT_AUTHORIZED", "TREASURY_QUEUE_CREATED"}` e nenhum item traz a chave `metadata`.

6. `test_decisions_are_append_only()` — CERT-02/D-09.
   - Dois `REQUEST_CHANGES` seguidos no mesmo projeto (categoria + notas distintas) → dois 200.
   - `GET /api/v1/certifier/projects/{friendlyId}/history` contém DUAS ocorrências de `CERTIFICATION_CHANGES_REQUESTED`.
   - `GET /api/v1/certifier/projects/{friendlyId}/review` retorna `len(payload["certifications"]) == 2` e as duas com `notes` diferentes.

7. `test_incomplete_dossier_blocks_decision_and_creates_pendency()` — CERT-01/D-03/D-04.
   - `create_project()` SEM documentos.
   - `APPROVE` com certificado válido → 400 com `detail` contendo `"Dossiê incompleto"`.
   - `GET /api/v1/projects/{friendlyId}/pendencies` com header de produtor → 200 e ao menos uma pendência com `status == "OPEN"`.
   - Nenhuma linha nova em `certifications` (verificar via `/review` que `certifications` está vazio).

8. `test_correction_queue_split_and_producer_response()` — D-10.
   - `REQUEST_CHANGES` num projeto certificável.
   - `GET /api/v1/certifier/queue?scope=main` NÃO contém o `friendlyId`.
   - `GET /api/v1/certifier/queue?scope=corrections` CONTÉM o `friendlyId`; `counts["corrections"] >= 1`.
   - `POST /api/v1/projects/{friendlyId}/pendencies/{pendencyId}/respond` com `{"response": "Documento corrigido e reenviado."}` e header de produtor → 200.
   - Depois da resposta, `scope=main` volta a conter o `friendlyId` e `scope=corrections` não.

9. `test_approve_rolls_back_when_treasury_package_fails(monkeypatch)` — D-14/D-19.
   - `monkeypatch.setattr("backend_app.modules.certifier.service.upload_storage_object", _raise_502)` onde `_raise_502` levanta `HTTPException(status_code=502, detail="Não foi possível gravar o arquivo no Supabase Storage.")`.
   - `APPROVE` com certificado → 502.
   - `GET /api/v1/certifier/projects/{friendlyId}/review` mostra `certifications == []`.
   - `GET /api/v1/projects/{friendlyId}` mostra `status` diferente de `CERTIFIED_AWAITING_TREASURY`.
   - `GET /api/v1/treasury/authorizations` não contém o projeto.

Todos os testes usam `client.patch(..., data=..., files=..., headers=...)` (multipart). Nenhum usa `json=` na rota de decisão.
  </action>
  <verify>
    <automated>uv run python -m compileall -q tests/test_certifier_workbench.py && uv run pytest tests/test_certifier_workbench.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_certifier_workbench.py --collect-only -q` lista pelo menos 9 testes
    - `grep -c "def test_review_dossier_endpoint" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_decision_requires_structured_fields" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_approve_requires_real_pdf" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_approve_creates_treasury_authorization" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_public_dossier_hides_internal_notes" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_decisions_are_append_only" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_incomplete_dossier_blocks_decision_and_creates_pendency" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_correction_queue_split_and_producer_response" tests/test_certifier_workbench.py` == 1
    - `grep -c "def test_approve_rolls_back_when_treasury_package_fails" tests/test_certifier_workbench.py` == 1
    - `grep -c "xfail\|pytest.mark.skip" tests/test_certifier_workbench.py` == 0
    - `grep -c "json=" tests/test_certifier_workbench.py` — nenhuma ocorrência na rota `/decision` (permitido em `/auth/login` e `/projects`)
  </acceptance_criteria>
  <done>Arquivo coletável pelo pytest com os 9 testes nomeados; falhas RED esperadas até o plano 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated Postgres role → tabelas novas | O Supabase expõe as tabelas via PostgREST se RLS não for habilitada |
| seed.sql → banco local | Dados demonstrativos entram sem passar pela API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `certification_pendencies`, `treasury_authorizations` | mitigate | `enable row level security` sem policy de select + `revoke insert, update, delete ... from anon, authenticated` na migration (mesmo padrão de `project_drafts`) |
| T-04-02 | Tampering | `certifications` | mitigate | Remoção da constraint única é acompanhada de índice `(project_id, created_at desc)`; append-only é garantido no plano 03 (sempre INSERT, nunca UPDATE) |
| T-04-03 | Repudiation | `treasury_authorizations` | mitigate | `certification_id` NOT NULL + índice único garante 1 autorização por decisão, sem pacote órfão nem duplicado |
</threat_model>

<verification>
- `npx supabase db reset` aplica todas as migrations e o seed sem erro
- `uv run python -c "from backend_app.db.models import CertificationPendency, TreasuryAuthorization"` funciona
- `uv run pytest tests/test_certifier_workbench.py --collect-only -q` coleta 9 testes
- `uv run pytest tests/db -q` continua verde (contrato de schema)
</verification>

<success_criteria>
- Constraint `certifications_project_decision_idx` não existe mais no banco nem em `models.py`
- Tabelas `certification_pendencies` e `treasury_authorizations` existem com RLS habilitada
- `supabase/seed.sql` roda sem erro e cria dossiê mínimo para os projetos da fila
- `tests/test_certifier_workbench.py` é coletável e declara os 9 testes nomeados
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-01-SUMMARY.md`
</output>
