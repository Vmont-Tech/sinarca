---
phase: 04-certification-workbench
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - backend_app/modules/certifier/service.py
  - backend_app/modules/certifier/routes.py
  - backend_app/modules/audit/routes.py
  - tests/contract/test_backend_app_api_v1.py
autonomous: true
requirements: [CERT-02, CERT-03]

must_haves:
  truths:
    - "A certificadora não consegue decidir sem dossiê mínimo completo; o sistema cria uma pendência para o produtor"
    - "Aprovar exige certificado PDF real anexado na mesma requisição"
    - "Reprovar e solicitar ajustes exigem categoria estruturada e descrição"
    - "Duas decisões seguidas geram duas linhas em certifications, sem sobrescrever a anterior"
    - "Editar o potencial sugerido exige justificativa"
  artifacts:
    - path: "backend_app/modules/certifier/service.py"
      provides: "CertifierService.record_decision com gate, append-only, upload de certificado e commit único"
      contains: "class CertifierService"
    - path: "backend_app/modules/certifier/routes.py"
      provides: "PATCH /certifier/projects/{id}/decision multipart"
      contains: "certificate: UploadFile | None = File(default=None)"
  key_links:
    - from: "backend_app/modules/certifier/service.py"
      to: "backend_app.modules.supabase_storage.upload_storage_object"
      via: "upload do certificado antes do commit único"
      pattern: "upload_storage_object"
    - from: "backend_app/modules/certifier/service.py"
      to: "ProjectsService.assert_certification_dossier_complete"
      via: "gate server-side de dossiê mínimo"
      pattern: "assert_certification_dossier_complete"
---

<objective>
Reescrever a decisão da certificadora como endpoint multipart append-only, com gate de dossiê mínimo, categorias estruturadas e certificado PDF obrigatório na aprovação.

Purpose: Hoje `PATCH /certifier/projects/{id}/decision` aceita JSON, sobrescreve a decisão anterior via `_get_or_create_certification` (viola D-09), gera um `signed_document_hash` determinístico sem arquivo nenhum (viola D-11) e não valida o dossiê (viola D-03). CERT-02 e CERT-03 dependem inteiramente dessa reescrita.
Output: `CertifierService` novo com toda a regra de decisão, endpoint multipart, e a suíte de contrato existente ajustada ao novo contrato.

ATENÇÃO — mudança de contrato quebrando: a rota deixa de aceitar `application/json`. Os 4 pontos de chamada em `tests/contract/test_backend_app_api_v1.py` precisam migrar para multipart na Task 3 do mesmo plano; caso contrário a wave fica vermelha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-RESEARCH.md
@.planning/phases/04-certification-workbench/04-PATTERNS.md
@.planning/phases/04-certification-workbench/04-UI-SPEC.md
@.planning/phases/04-certification-workbench/04-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes. Usar diretamente. -->

`backend_app/modules/projects/routes.py` (pipeline de upload já validado — replicar, não reinventar):
```python
async def _validated_upload_payload(document_type: str, file: UploadFile) -> dict[str, object]:
    normalized_document_type = document_type.strip().upper()
    filename = file.filename or "upload"
    extension = PurePath(filename).suffix.lower()
    if extension not in ALLOWED_EXTENSIONS:
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="Extensão de arquivo não permitida")
    content = await file.read(MAX_UPLOAD_BYTES + 1)
    if len(content) > MAX_UPLOAD_BYTES:
        raise HTTPException(status_code=status.HTTP_413_REQUEST_ENTITY_TOO_LARGE, detail="Arquivo excede o limite configurado")
    validate_magic_bytes(extension, content)
    return {"document_type", "filename", "extension", "content", "content_type", "sha256", "mime_type", "size_bytes"}
```

`backend_app/modules/inventory/routes.py`:
```python
MAX_UPLOAD_BYTES = 10 * 1024 * 1024
ALLOWED_EXTENSIONS = {".pdf", ".png", ".jpg", ".jpeg", ".csv", ".xlsx"}
MIME_BY_EXTENSION = {".pdf": "application/pdf", ...}
def validate_magic_bytes(extension: str, content: bytes) -> None:  # .pdf exige prefixo b"%PDF-"
```

`backend_app/modules/storage_paths.py`:
```python
def project_document_location(project_friendly_id: str, document_type: str, sha256: str, extension: str) -> StorageLocation
# StorageLocation(bucket, object_path, uri)
```

`backend_app/modules/supabase_storage.py`:
```python
async def upload_storage_object(bucket: str, object_path: str, content: bytes, content_type: str | None) -> bool
# retorna False quando nao ha credenciais configuradas (ambiente de teste); levanta HTTPException 502 quando o Storage responde erro
```

`backend_app/db/models.py`:
```python
class Document(Base):  # UniqueConstraint("project_id", "sha256_hash", name="documents_project_sha256_key")
class Certification(Base)      # sem UniqueConstraint apos o plano 04-01
class CertificationPendency(Base)  # project_id, certification_id, certifier_organization_id, raised_by_profile_id, category, description, status, producer_response, responded_by_profile_id, responded_at, resolved_at, metadata_, created_at
```

`backend_app/modules/projects/service.py` (criado no plano 04-02):
```python
DOSSIER_INCOMPLETE_DETAIL: str
CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE = "CERTIFICATION_CERTIFICATE"
class ProjectsService:
    async def _get_project_model(self, project_id: str) -> Project
    async def certification_dossier_status(self, project) -> dict
    async def assert_certification_dossier_complete(self, project) -> dict
    async def suggested_credit_potential(self, project) -> dict   # {"suggestedCreditPotential": float, ...}
    async def _actor_profile(self, actor_id: str | None) -> Profile
```

`backend_app/modules/certifier/routes.py` (estado atual, a ser substituído):
```python
class CertifierDecisionRequest(BaseModel): decision, credit_potential, certifier_id, notes   # REMOVER
async def decide_project(project_id, payload: CertifierDecisionRequest, ...)                 # REESCREVER como multipart
async def _get_or_create_certification(session, project, decision) -> Certification          # REMOVER (viola D-09)
async def _ensure_locked_credit(session, project, amount) -> EnvironmentalCredit             # MANTER
def _decision_title(decision) -> str                                                          # MANTER/atualizar copy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar CertifierService com a regra completa de decisão</name>
  <files>backend_app/modules/certifier/service.py</files>
  <read_first>
    - backend_app/modules/certifier/routes.py (arquivo inteiro — decide_project, _get_or_create_certification, _ensure_locked_credit, _decision_title)
    - backend_app/modules/projects/routes.py (linhas 42-66 e 224-309 — pipeline de upload a replicar)
    - backend_app/modules/projects/service.py (helpers criados no plano 04-02)
    - backend_app/db/models.py (Certification, Document, CertificationPendency, EnvironmentalCredit, Project)
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (cópias exatas das mensagens de erro)
    - tests/test_certifier_workbench.py (test_decision_requires_structured_fields, test_approve_requires_real_pdf, test_decisions_are_append_only, test_incomplete_dossier_blocks_decision_and_creates_pendency)
  </read_first>
  <behavior>
    - `REJECT` sem `rejection_category` → HTTPException 400 "Categoria estruturada é obrigatória para reprovação ou pedido de ajustes."
    - `REJECT` com categoria mas com `notes` vazio → HTTPException 400 "Descrição do motivo é obrigatória para reprovação ou pedido de ajustes."
    - `rejection_category` fora do vocabulário fechado → HTTPException 400
    - `APPROVE` sem `certificate` → HTTPException 400 "Certificado em PDF é obrigatório para aprovação."
    - `APPROVE` com arquivo `.txt` → HTTPException 415
    - `APPROVE` com bytes que não começam com `%PDF-` → HTTPException 400
    - `APPROVE` com dossiê incompleto → HTTPException 400 com `DOSSIER_INCOMPLETE_DETAIL` e uma `CertificationPendency` OPEN criada
    - `APPROVE` com `credit_potential` != sugerido e sem `credit_potential_adjustment_reason` → HTTPException 400
    - Duas chamadas `REQUEST_CHANGES` seguidas → duas linhas em `certifications` (nunca UPDATE)
    - Qualquer exceção antes do commit deixa o banco sem nenhuma linha nova (commit único no fim)
  </behavior>
  <action>
Criar o arquivo NOVO `backend_app/modules/certifier/service.py`.

Constantes no topo:
```python
DECISION_APPROVE = "APPROVE"
DECISION_REJECT = "REJECT"
DECISION_REQUEST_CHANGES = "REQUEST_CHANGES"

PENDENCY_CATEGORIES: tuple[str, ...] = (
    "DOCUMENTACAO_INCOMPLETA",
    "GEOFENCE_INVALIDA",
    "BASELINE_INCONSISTENTE",
    "METODOLOGIA_INADEQUADA",
    "POTENCIAL_DIVERGENTE",
    "OUTRO",
)
PENDENCY_CATEGORY_LABELS: dict[str, str] = {
    "DOCUMENTACAO_INCOMPLETA": "Documentação incompleta",
    "GEOFENCE_INVALIDA": "Geofence/QTAGs inválidas",
    "BASELINE_INCONSISTENTE": "Baseline inconsistente",
    "METODOLOGIA_INADEQUADA": "Metodologia inadequada",
    "POTENCIAL_DIVERGENTE": "Potencial de crédito divergente",
    "OUTRO": "Outro",
}
CERTIFICATE_REQUIRED_DETAIL = "Certificado em PDF é obrigatório para aprovação."
CATEGORY_REQUIRED_DETAIL = "Categoria estruturada é obrigatória para reprovação ou pedido de ajustes."
DESCRIPTION_REQUIRED_DETAIL = "Descrição do motivo é obrigatória para reprovação ou pedido de ajustes."
ADJUSTMENT_REASON_REQUIRED_DETAIL = (
    "Justificativa é obrigatória quando o potencial de crédito difere do valor sugerido pelo sistema."
)
CERTIFICATE_EXTENSION = ".pdf"
CREDIT_POTENTIAL_TOLERANCE = 0.01
STATUS_LABELS_APPROVED = ["Certificação aprovada", "Mint autorizado", "Aguardando tesouraria"]
```

Classe:
```python
class CertifierService:
    def __init__(self, session: AsyncSession) -> None:
        self.session = session
        self.projects = ProjectsService(session)
```

Métodos privados:

1. `async def _validated_certificate(self, certificate: UploadFile) -> dict[str, object]`
   - `filename = certificate.filename or "certificado.pdf"`; `extension = PurePath(filename).suffix.lower()`
   - Se `extension != CERTIFICATE_EXTENSION` → `HTTPException(415, "Somente certificado em PDF é aceito nesta fase.")` (D-12)
   - `content = await certificate.read(MAX_UPLOAD_BYTES + 1)`; se maior → `HTTPException(413, "Arquivo excede o limite configurado")`
   - Se `not content` → `HTTPException(400, "Certificado enviado está vazio.")`
   - `validate_magic_bytes(extension, content)` (levanta 400 se não começar com `%PDF-`)
   - Retorna `{"filename", "extension", "content", "content_type": certificate.content_type, "sha256": hashlib.sha256(content).hexdigest(), "mime_type": MIME_BY_EXTENSION[extension], "size_bytes": len(content)}`

2. `async def _persist_certificate(self, project: Project, upload: dict[str, object]) -> Document`
   - Dedupe: `select(Document).where(Document.project_id == project.id, Document.sha256_hash == upload["sha256"])`; se existir, retorna a linha existente (a constraint `documents_project_sha256_key` impediria o INSERT).
   - `location = project_document_location(project.friendly_id, CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE, str(upload["sha256"]), str(upload["extension"]))`
   - `await upload_storage_object(location.bucket, location.object_path, bytes(upload["content"]), str(upload["mime_type"]))` — se levantar 502, a exceção sobe e nada é commitado (D-14)
   - `document = Document(project_id=project.id, document_type=CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE, storage_bucket=location.bucket, storage_object_path=location.object_path, storage_path=location.uri, sha256_hash=..., mime_type=..., size_bytes=..., metadata_={"filename": upload["filename"], "content_type": upload["content_type"], "source": "certifier_decision"})`
   - `self.session.add(document)`; `await self.session.flush()`; retorna `document`

3. `async def _create_pendency(self, project, *, category, description, actor, certification=None, metadata=None) -> CertificationPendency`
   - Cria `CertificationPendency(project_id=project.id, certification_id=(certification.id if certification else None), certifier_organization_id=project.certifier_organization_id, raised_by_profile_id=<profile.id do ator, via self.projects._actor_profile(actor.id) com try/except retornando None>, category=category, description=description, status="OPEN", metadata_=metadata or {})`
   - `session.add` + `flush`
   - `await create_audit_event(session, action="CERTIFICATION_PENDENCY_CREATED", entity_type="projects", entity_id=project.id, actor_role=actor.role, metadata={"actor_external_id": actor.id, "friendly_id": project.friendly_id, "category": category, "description": description, "pendency_id": str(pendency.id)})`

4. `def _append_timeline(self, project: Project, *, title: str, desc: str, status_value: str) -> None`
   - `project.timeline = [*(project.timeline or []), {"title": title, "date": datetime.now(timezone.utc).date().isoformat(), "status": status_value, "desc": desc}]`

Método público principal:

```python
    async def record_decision(
        self,
        project_id: str,
        *,
        decision: str,
        methodology: str | None,
        credit_potential: float | None,
        credit_potential_adjustment_reason: str | None,
        notes: str,
        rejection_category: str | None,
        certificate: UploadFile | None,
        actor: AuthenticatedUser,
    ) -> dict[str, object]:
```
Fluxo obrigatório, nesta ordem exata:

1. `project = await self.projects._get_project_model(project_id)`; `previous_status = project.status`.
2. Validação de campos por tipo de decisão (ANTES de qualquer escrita):
   - Se `decision in (DECISION_REJECT, DECISION_REQUEST_CHANGES)`:
     - `rejection_category` vazio → `HTTPException(400, CATEGORY_REQUIRED_DETAIL)`
     - `rejection_category.upper() not in PENDENCY_CATEGORIES` → `HTTPException(400, f"Categoria inválida. Use uma de: {', '.join(PENDENCY_CATEGORIES)}")`
     - `not (notes or "").strip()` → `HTTPException(400, DESCRIPTION_REQUIRED_DETAIL)`
   - Se `decision == DECISION_APPROVE` e `certificate is None` → `HTTPException(400, CERTIFICATE_REQUIRED_DETAIL)`
3. Gate de dossiê mínimo (D-03/D-04) — vale para APPROVE e REJECT (uma reprovação também precisa de dossiê para ser tecnicamente defensável); NÃO vale para `REQUEST_CHANGES`, que existe justamente para pedir o que falta:
```python
        dossier = await self.projects.certification_dossier_status(project)
        if decision != DECISION_REQUEST_CHANGES and not dossier["complete"]:
            await self._create_pendency(
                project,
                category="DOCUMENTACAO_INCOMPLETA",
                description="Itens faltantes no dossiê mínimo: " + ", ".join(dossier["missing"]) + ".",
                actor=actor,
                metadata={"missing": dossier["missing"], "trigger": "DECISION_BLOCKED"},
            )
            await self.session.commit()   # a pendencia PRECISA persistir; a decisao nao acontece
            raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=DOSSIER_INCOMPLETE_DETAIL)
```
4. Cálculo do potencial (D-07):
```python
        calculation = await self.projects.suggested_credit_potential(project)
        suggested = float(calculation["suggestedCreditPotential"])
        if decision == DECISION_APPROVE:
            final_potential = float(credit_potential) if credit_potential is not None else suggested
            if abs(final_potential - suggested) > CREDIT_POTENTIAL_TOLERANCE and not (credit_potential_adjustment_reason or "").strip():
                raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=ADJUSTMENT_REASON_REQUIRED_DETAIL)
        else:
            final_potential = 0.0
```
5. Certificado (só APPROVE): `upload = await self._validated_certificate(certificate)` e, mais adiante, `document = await self._persist_certificate(project, upload)`. Validar ANTES de inserir a `Certification` (falha de validação não pode deixar decisão parcial).
6. INSERT append-only da certificação — NUNCA reutilizar linha existente:
```python
        certification = Certification(
            project_id=project.id,
            certifier_organization_id=project.certifier_organization_id,
            certifier_profile_id=<profile.id do ator ou None>,
            methodology=(methodology or project.methodology),
            credit_potential=Decimal(str(final_potential)),
            decision=decision,
            notes=notes or None,
            signed_document_hash=(f"sha256-{upload['sha256']}" if decision == DECISION_APPROVE else None),
            signed_at=(datetime.now(timezone.utc) if decision == DECISION_APPROVE else None),
        )
        self.session.add(certification)
        await self.session.flush()
```
7. Transições de status e efeitos por decisão:
   - `APPROVE`: `project.status = "CERTIFIED_AWAITING_TREASURY"`; `await self._ensure_locked_credit(project, Decimal(str(final_potential)))` (mover a função `_ensure_locked_credit` de `routes.py` para cá como método); timeline `title="Certificação aprovada"`, `status_value="completed"`.
   - `REJECT`: `project.status = "SUSPENDED"`; cria pendência com `category=rejection_category.upper()` e `description=notes`; timeline `title="Projeto reprovado pela certificadora"`, `status_value="active"`.
   - `REQUEST_CHANGES`: `project.status` PERMANECE inalterado (a separação de fila é derivada de pendência aberta, não de status — ver plano 04-05); cria pendência com `category=rejection_category.upper()` e `description=notes`; timeline `title="Ajustes solicitados pela certificadora"`, `status_value="active"`.
8. Eventos de auditoria canônicos (D-21), todos antes do commit:
   - `APPROVE` → `CERTIFICATION_APPROVED` com `before_data={"status": previous_status}`, `after_data={"status": project.status, "credit_potential": final_potential}`, `metadata={"actor_external_id": actor.id, "friendly_id": project.friendly_id, "certification_id": str(certification.id), "methodology": certification.methodology, "credit_potential_suggested": suggested, "credit_potential_adjustment_reason": credit_potential_adjustment_reason}`; e `CERTIFICATION_CERTIFICATE_ATTACHED` com `metadata={"actor_external_id": actor.id, "document_id": str(document.id), "sha256": upload["sha256"], "storage_path": document.storage_path}`
   - `REJECT` → `CERTIFICATION_REJECTED`
   - `REQUEST_CHANGES` → `CERTIFICATION_CHANGES_REQUESTED`
   (Os eventos `MINT_AUTHORIZED` e `TREASURY_QUEUE_CREATED` entram no plano 04-04.)
9. `await self.session.commit()` — ÚNICO commit do caminho feliz (além do commit de pendência do passo 3, que é o caminho de bloqueio).
10. Retorno:
```python
        return {
            "success": True,
            "project_id": project.friendly_id,
            "new_status": project.status,
            "previous_status": previous_status,
            "decision": decision,
            "credit_potential": final_potential,
            "suggested_credit_potential": suggested,
            "certification_id": str(certification.id),
            "certificate": ({
                "documentId": str(document.id),
                "documentType": CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE,
                "sha256": upload["sha256"],
                "mimeType": upload["mime_type"],
                "sizeBytes": upload["size_bytes"],
                "storagePath": document.storage_path,
            } if decision == DECISION_APPROVE else None),
            "pendency_id": (str(pendency.id) if pendency is not None else None),
            "statusLabels": (STATUS_LABELS_APPROVED if decision == DECISION_APPROVE else []),
        }
```

Imports necessários: `hashlib`, `datetime/timezone`, `Decimal`, `PurePath`, `HTTPException`/`UploadFile`/`status` de fastapi, `select` de sqlalchemy, `AsyncSession`, `AuthenticatedUser`, modelos, `create_audit_event`, `ProjectsService` + constantes do plano 04-02, `ALLOWED_EXTENSIONS/MAX_UPLOAD_BYTES/MIME_BY_EXTENSION/validate_magic_bytes` de `backend_app.modules.inventory.routes`, `upload_storage_object` de `backend_app.modules.supabase_storage`, `project_document_location` de `backend_app.modules.storage_paths`.

IMPORTANTE: importar `upload_storage_object` como NOME DE MÓDULO neste arquivo (`from backend_app.modules.supabase_storage import upload_storage_object`) — o teste `test_approve_rolls_back_when_treasury_package_fails` faz `monkeypatch.setattr("backend_app.modules.certifier.service.upload_storage_object", ...)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_decision_requires_structured_fields tests/test_certifier_workbench.py::test_approve_requires_real_pdf tests/test_certifier_workbench.py::test_decisions_are_append_only tests/test_certifier_workbench.py::test_incomplete_dossier_blocks_decision_and_creates_pendency -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CertifierService" backend_app/modules/certifier/service.py` == 1
    - `grep -c "async def record_decision" backend_app/modules/certifier/service.py` == 1
    - `grep -c "_get_or_create_certification" backend_app/modules/certifier/service.py` == 0
    - `grep -c "self.session.add(certification)" backend_app/modules/certifier/service.py` == 1
    - `grep -c "await self.session.commit()" backend_app/modules/certifier/service.py` == 2 (um no bloqueio por dossiê, um no caminho feliz)
    - `grep -c "from backend_app.modules.supabase_storage import upload_storage_object" backend_app/modules/certifier/service.py` == 1
    - `grep -c "CERTIFIED_AWAITING_TREASURY" backend_app/modules/certifier/service.py` == 1
    - `grep -c "validate_magic_bytes" backend_app/modules/certifier/service.py` == 1
    - `grep -c "PENDENCY_CATEGORIES" backend_app/modules/certifier/service.py` >= 2
    - `uv run pytest tests/test_certifier_workbench.py::test_decisions_are_append_only -x` passa
  </acceptance_criteria>
  <done>Serviço de decisão com gate, append-only, categorias fechadas, certificado PDF real e commit único.</done>
</task>

<task type="auto">
  <name>Task 2: Trocar o endpoint de decisão para multipart e liberar a fila de auditoria</name>
  <files>backend_app/modules/certifier/routes.py, backend_app/modules/audit/routes.py</files>
  <read_first>
    - backend_app/modules/certifier/routes.py (arquivo inteiro, incluindo o endpoint de review adicionado no plano 04-02)
    - backend_app/modules/certifier/service.py (criado na Task 1)
    - backend_app/modules/audit/routes.py (linha ~39: filtro da fila de auditoria)
    - backend_app/modules/projects/routes.py (linhas 224-235 — assinatura de rota multipart com Form + File)
  </read_first>
  <action>
1. Em `backend_app/modules/certifier/routes.py`:
   - REMOVER `class CertifierDecisionRequest`, a função `_get_or_create_certification` e a função `_ensure_locked_credit` (essa última migrou para `CertifierService` na Task 1). Remover também os imports que ficarem órfãos (`hashlib`, `BaseModel`, `Field`, `EnvironmentalCredit`).
   - Substituir `decide_project` por:
```python
@router.patch("/certifier/projects/{project_id}/decision")
async def decide_project(
    project_id: str,
    decision: Literal["APPROVE", "REJECT", "REQUEST_CHANGES"] = Form(...),
    methodology: str | None = Form(default=None),
    credit_potential: float | None = Form(default=None),
    credit_potential_adjustment_reason: str | None = Form(default=None),
    notes: str = Form(default=""),
    rejection_category: str | None = Form(default=None),
    certificate: UploadFile | None = File(default=None),
    current_user: AuthenticatedUser = Depends(require_role("certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> dict[str, object]:
    return await CertifierService(session).record_decision(
        project_id,
        decision=decision,
        methodology=methodology,
        credit_potential=credit_potential,
        credit_potential_adjustment_reason=credit_potential_adjustment_reason,
        notes=notes,
        rejection_category=rejection_category,
        certificate=certificate,
        actor=current_user,
    )
```
   - Adicionar imports `File`, `Form`, `UploadFile` de `fastapi` e `from backend_app.modules.certifier.service import CertifierService`.
   - Manter `_decision_title` apenas se ainda houver uso; caso contrário, remover.

2. Em `backend_app/modules/audit/routes.py`, o filtro da fila de auditoria hoje é:
```python
    statement = select(Project).where(Project.status.in_(["AWAITING_AUDIT", "BLOCKED_AUDIT_REQUIRED"])).order_by(Project.created_at.asc())
```
Trocar por:
```python
    statement = (
        select(Project)
        .where(Project.status.in_(["CERTIFIED_AWAITING_TREASURY", "AWAITING_AUDIT", "BLOCKED_AUDIT_REQUIRED"]))
        .order_by(Project.created_at.asc())
    )
```
Motivo explícito: a aprovação da certificadora agora estaciona o projeto em `CERTIFIED_AWAITING_TREASURY` (D-17). Sem essa inclusão, todo o encadeamento certificação → auditoria → marketplace (usado por Phases 1, 2 e 6 e coberto pela suíte de contrato) fica quebrado, porque nada na Phase 04 move o projeto adiante — a execução do mint é da tesouraria, na Phase 08 (D-16).

3. Verificar em `backend_app/modules/projects/service.py` que `PRODUCER_PORTFOLIO_PROJECT_STATUSES` inclui `"CERTIFIED_AWAITING_TREASURY"`; se não incluir, adicionar, para o projeto não sumir do portfólio do produtor após a aprovação.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py -x -k "requires_real_pdf or structured_fields or append_only or incomplete_dossier"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CertifierDecisionRequest" backend_app/modules/certifier/routes.py` == 0
    - `grep -c "_get_or_create_certification" backend_app/modules/certifier/routes.py` == 0
    - `grep -c "certificate: UploadFile | None = File(default=None)" backend_app/modules/certifier/routes.py` == 1
    - `grep -c "CertifierService(session).record_decision" backend_app/modules/certifier/routes.py` == 1
    - `grep -c "CERTIFIED_AWAITING_TREASURY" backend_app/modules/audit/routes.py` == 1
    - `grep -c "CERTIFIED_AWAITING_TREASURY" backend_app/modules/projects/service.py` >= 2
    - `uv run python -c "from backend_app.main import app; print([r.path for r in app.routes if 'certifier' in r.path])"` lista `/api/v1/certifier/projects/{project_id}/decision`
  </acceptance_criteria>
  <done>Endpoint multipart operacional, fila de auditoria enxergando o novo status e sem código morto de upsert.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar a suíte de contrato existente para o novo contrato de decisão</name>
  <files>tests/contract/test_backend_app_api_v1.py</files>
  <read_first>
    - tests/contract/test_backend_app_api_v1.py (linhas 1-145: helpers; 300-400: test_certifier_queue_and_decision_apply_role_guard_and_status_transition e test_audit_queue_verify_and_monitoring_anomaly_block_project; ~385-400: activate_project_for_marketplace)
    - tests/test_certifier_workbench.py (helpers upload_minimum_documents e PDF_BYTES — replicar a mesma abordagem)
    - backend_app/modules/certifier/service.py (contrato de resposta de record_decision)
  </read_first>
  <action>
A rota de decisão deixou de aceitar JSON e passou a exigir dossiê mínimo + certificado PDF na aprovação. Ajustar `tests/contract/test_backend_app_api_v1.py`:

1. Adicionar no topo do arquivo, junto dos helpers existentes:
```python
CERTIFIER_PDF_BYTES = b"%PDF-1.4\n1 0 obj\n<< /Type /Catalog >>\nendobj\ntrailer\n%%EOF\n"


def upload_certification_minimum_documents(friendly_id: str) -> None:
    for document_type, filename in (("LEGAL_OWNERSHIP", "matricula.pdf"), ("FOREST_INVENTORY", "inventario.pdf")):
        response = client.post(
            f"/api/v1/projects/{friendly_id}/documents",
            data={"document_type": document_type},
            files={"file": (filename, CERTIFIER_PDF_BYTES, "application/pdf")},
            headers=auth_headers(),
        )
        assert response.status_code == 201, response.text


def certifier_approve(friendly_id: str, credit_potential: float, *, notes: str = "Certificação aprovada") -> dict[str, object]:
    upload_certification_minimum_documents(friendly_id)
    response = client.patch(
        f"/api/v1/certifier/projects/{friendly_id}/decision",
        data={
            "decision": "APPROVE",
            "methodology": "AR-ACM0003",
            "credit_potential": str(credit_potential),
            "credit_potential_adjustment_reason": "Potencial ajustado para o cenário de contrato.",
            "notes": notes,
        },
        files={"certificate": ("certificado.pdf", CERTIFIER_PDF_BYTES, "application/pdf")},
        headers=auth_headers("certificadora@sinarca.com.br", "certificadora"),
    )
    assert response.status_code == 200, response.text
    return response.json()
```
Cuidado: se o mesmo PDF for reaproveitado no mesmo projeto para `LEGAL_OWNERSHIP` e `FOREST_INVENTORY`, a constraint `documents_project_sha256_key` (unique por `project_id, sha256_hash`) bloqueia o segundo upload. Gerar bytes distintos por tipo, por exemplo `CERTIFIER_PDF_BYTES + document_type.encode()`, mantendo o prefixo `%PDF-`.

2. Em `test_certifier_queue_and_decision_apply_role_guard_and_status_transition`:
   - A chamada `forbidden` passa a usar `data={"decision": "APPROVE", "notes": "tentativa indevida"}` (multipart) e continua esperando 403.
   - A chamada de aprovação passa a usar `certifier_approve(project["friendlyId"], 1234)`.
   - A asserção `data["new_status"] == "AWAITING_AUDIT"` muda para `data["new_status"] == "CERTIFIED_AWAITING_TREASURY"`.
   - Acrescentar `assert data["certificate"]["documentType"] == "CERTIFICATION_CERTIFICATE"`.

3. Em `test_audit_queue_verify_and_monitoring_anomaly_block_project`: trocar a chamada JSON de certificação por `certifier_approve(project["friendlyId"], 900)`.

4. Em `activate_project_for_marketplace`: trocar a chamada JSON de certificação por `certifier_approve(project["friendlyId"], credit_potential)`.

5. Rodar a suíte inteira e corrigir qualquer outra asserção que dependa de `AWAITING_AUDIT` imediatamente após certificação.

NÃO relaxar asserções para "fazer passar": se um teste falhar por comportamento (não por contrato de chamada), corrigir o código, não o teste.
  </action>
  <verify>
    <automated>uv run pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def certifier_approve" tests/contract/test_backend_app_api_v1.py` == 1
    - `grep -c "def upload_certification_minimum_documents" tests/contract/test_backend_app_api_v1.py` == 1
    - `grep -c 'json={"decision"' tests/contract/test_backend_app_api_v1.py` == 0
    - `grep -c "CERTIFIED_AWAITING_TREASURY" tests/contract/test_backend_app_api_v1.py` >= 1
    - `uv run pytest tests/contract -q` termina com 0 falhas
    - `uv run pytest -q` não apresenta falhas novas em relação ao baseline registrado no SUMMARY do plano 04-01
  </acceptance_criteria>
  <done>Suíte de contrato verde contra o endpoint multipart e o novo status pós-aprovação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| certificadora autenticada → `PATCH /certifier/projects/{id}/decision` | Entrada não confiável: arquivo binário + campos livres |
| backend → Supabase Storage | Escrita de arquivo externo dentro de uma transação de banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering / DoS | campo `certificate` | mitigate | Allowlist de extensão `.pdf` (415), `MAX_UPLOAD_BYTES` de 10 MB (413) e `validate_magic_bytes` exigindo prefixo `%PDF-` (400) — reuso do pipeline já validado |
| T-04-08 | Elevation of Privilege | gate de dossiê mínimo | mitigate | `certification_dossier_status` avaliado no servidor dentro de `record_decision`; desabilitar o botão no React não é considerado controle |
| T-04-09 | Tampering | histórico de decisões | mitigate | `Certification` sempre inserida (`session.add`), nunca atualizada; constraint única removida no plano 04-01 |
| T-04-10 | Repudiation | decisão sem trilha | mitigate | `CERTIFICATION_APPROVED` / `CERTIFICATION_REJECTED` / `CERTIFICATION_CHANGES_REQUESTED` / `CERTIFICATION_CERTIFICATE_ATTACHED` gravados antes do commit único |
| T-04-11 | Tampering | estado parcial (decisão sem certificado) | mitigate | Commit único no fim de `record_decision`; falha de Storage levanta 502 e nada é persistido (D-14) |
| T-04-12 | Input Validation | `rejection_category` | mitigate | Vocabulário fechado `PENDENCY_CATEGORIES` validado no servidor; valor fora da lista → 400 |
</threat_model>

<verification>
- `uv run pytest tests/test_certifier_workbench.py -x -k "requires_real_pdf or structured_fields or append_only or incomplete_dossier"` passa
- `uv run pytest tests/contract -q` sem falhas
- `uv run pytest -q` sem regressões
</verification>

<success_criteria>
- Decisão registrada nunca sobrescreve decisão anterior
- Aprovação sem PDF real, com PDF falso ou com dossiê incompleto é recusada pelo servidor
- Reprovação e pedido de ajustes exigem categoria do vocabulário fechado e descrição
- Ajuste do potencial sugerido exige justificativa
- Suíte de contrato verde
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-03-SUMMARY.md`
</output>
