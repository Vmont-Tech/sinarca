---
phase: 04-certification-workbench
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/services/certifierReview.ts
  - src/pages/Dashboard/CertifierReview.tsx
autonomous: true
requirements: [CERT-01, CERT-02, CERT-03]

must_haves:
  truths:
    - "A certificadora expande o card do projeto na própria fila e navega por seis abas"
    - "A fila mostra dois estados: decisão principal e aguardando retorno do produtor, com contador"
    - "O formulário de decisão bloqueia aprovação sem dossiê mínimo e sem certificado PDF anexado"
    - "O certificado é enviado de verdade ao backend via FormData, nunca como blob local"
    - "A aba histórico mostra a linha do tempo do projeto com filtros por tipo de evento e ator"
  artifacts:
    - path: "src/services/certifierReview.ts"
      provides: "Cliente tipado da bancada: fila por escopo, dossiê, histórico e decisão multipart"
      contains: "new FormData()"
    - path: "src/pages/Dashboard/CertifierReview.tsx"
      provides: "Card expansível com seis abas, duas filas e formulário completo de decisão"
      contains: "QTAGs / Geofence"
  key_links:
    - from: "src/pages/Dashboard/CertifierReview.tsx"
      to: "src/services/certifierReview.ts"
      via: "import dos wrappers tipados"
      pattern: "from '../../services/certifierReview'"
    - from: "src/services/certifierReview.ts"
      to: "/certifier/projects/{id}/decision"
      via: "apiPatch com corpo FormData"
      pattern: "apiPatch"
---

<objective>
Transformar a fila da certificadora na bancada completa: card expansível com abas, duas filas com contador, formulário de decisão com upload real de certificado e linha do tempo filtrável.

Purpose: CERT-01/CERT-02/CERT-03 e D-01/D-02/D-05/D-06/D-10/D-11 exigem que toda a revisão aconteça no card da própria fila. Hoje `CertifierReview.tsx` tem 114 linhas: lista os projetos e dispara três decisões com notas hardcoded via JSON, sem dossiê, sem formulário e sem certificado.
Output: Um módulo de serviço tipado e a página reescrita conforme o contrato visual aprovado em `04-UI-SPEC.md`.

Restrições não negociáveis:
- Nenhum mock runtime novo no frontend. Todo dado vem de `/api/v1`.
- Nenhuma URL absoluta: usar `apiGet`/`apiPatch` de `src/services/api.ts`.
- NÃO copiar a lógica de arquivos de `AuditorReview.tsx` (`local://`, blob URLs). Só a casca visual do dropzone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-UI-SPEC.md
@.planning/phases/04-certification-workbench/04-PATTERNS.md
@.planning/phases/04-certification-workbench/04-05-SUMMARY.md

<interfaces>
`src/services/api.ts`:
```typescript
export const API_BASE_URL: string;
export async function apiGet<T>(path: string): Promise<T | null>;
export async function apiPost<T>(path: string, body?: unknown): Promise<T | null>;
export async function apiPatch<T>(path: string, body?: unknown): Promise<T | null>;
// request() ja trata `body instanceof FormData` e NAO seta Content-Type nesse caso.
// friendlyError() sobrescreve a mensagem do servidor para 401/403/409/422; para 400/415/5xx propaga `detail`.
```

`src/services/projectDocuments.ts` (padrão exato de FormData a espelhar):
```typescript
const body = new FormData();
body.append('document_type', documentType);
body.append('file', file);
const response = await apiPost<ProjectDocumentUploadResponse>(`/projects/${projectId}/documents`, body);
```

`src/components/ProjectGeofencePreview.tsx`:
```typescript
type ProjectGeofencePreviewProps = { tags: ProjectTagDraft[] };
// ProjectTagDraft: { vertex_label: VertexLabel; has_qtag: boolean; tag_uid: string; cmac: string; latitude: string; longitude: string; captureMode: string }
// Conversao ja usada em MrcaDetails.tsx:144-153 — replicar.
```

Contratos de API entregues pelos planos 04-02..04-05:
```
GET  /certifier/queue?scope=main|corrections
     -> { success, total, projects: ProjectMRCA[], scope, counts: { main: number; corrections: number } }

GET  /certifier/projects/{id}/review
     -> { success, project, baseline, tags, documents,
          dossier: { complete, missing: string[], baseline:{present,capturedAt,sentinelSceneId},
                     tags:{total,valid,required,complete},
                     documents:{presentTypes,missingGroups,complete} },
          calculation: { suggestedCreditPotential, formula, source, vegetationCoverPct, ndviMean, areaHectares, carbonStock, methodology },
          certifications: [{ id, methodology, creditPotential, decision, notes, signedDocumentHash, signedAt, createdAt }],
          pendencies: [{ id, category, description, status, producerResponse, respondedAt, resolvedAt, createdAt, metadata }],
          treasuryAuthorization: { id, methodology, approvedCreditPotential, certificateSha256, status, authorizedAt } | null,
          certificate: { documentId, sha256, mimeType, sizeBytes, uploadedAt, storagePath, filename, downloadAvailable } | null }

GET  /certifier/projects/{id}/history?event_type=&actor_role=
     -> { success, total, events: [{ id, action, label, actorRole, actorProfileId, createdAt, metadata, beforeData, afterData }],
          availableEventTypes: [{ value, label }], availableActorRoles: string[] }

PATCH /certifier/projects/{id}/decision   (multipart/form-data)
     campos: decision, methodology, credit_potential, credit_potential_adjustment_reason,
             notes, rejection_category, certificate (File)
     -> { success, project_id, new_status, previous_status, decision, credit_potential,
          suggested_credit_potential, certification_id, certificate, pendency_id,
          treasury_authorization_id, statusLabels }

GET  /projects/{id}/pendencies -> { success, total, pendencies: [...] }
```

Categorias estruturadas aceitas pelo backend (`PENDENCY_CATEGORIES`):
`DOCUMENTACAO_INCOMPLETA`, `GEOFENCE_INVALIDA`, `BASELINE_INCONSISTENTE`, `METODOLOGIA_INADEQUADA`, `POTENCIAL_DIVERGENTE`, `OUTRO`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/services/certifierReview.ts</name>
  <files>src/services/certifierReview.ts</files>
  <read_first>
    - src/services/projectDocuments.ts (arquivo inteiro — padrão do wrapper tipado com FormData)
    - src/services/api.ts (apiGet/apiPatch e o tratamento de friendlyError)
    - src/services/database.ts (tipos ProjectMRCA, QueueResponse)
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (cópias exatas das mensagens de erro)
  </read_first>
  <action>
Criar o arquivo NOVO `src/services/certifierReview.ts` espelhando a estrutura de `src/services/projectDocuments.ts`.

Tipos exportados (nomes exatos):
```typescript
export type CertifierQueueScope = 'main' | 'corrections';
export type CertificationDecision = 'APPROVE' | 'REJECT' | 'REQUEST_CHANGES';
export type PendencyCategory =
    | 'DOCUMENTACAO_INCOMPLETA'
    | 'GEOFENCE_INVALIDA'
    | 'BASELINE_INCONSISTENTE'
    | 'METODOLOGIA_INADEQUADA'
    | 'POTENCIAL_DIVERGENTE'
    | 'OUTRO';

export const PENDENCY_CATEGORY_OPTIONS: Array<{ value: PendencyCategory; label: string }> = [
    { value: 'DOCUMENTACAO_INCOMPLETA', label: 'Documentação incompleta' },
    { value: 'GEOFENCE_INVALIDA', label: 'Geofence/QTAGs inválidas' },
    { value: 'BASELINE_INCONSISTENTE', label: 'Baseline inconsistente' },
    { value: 'METODOLOGIA_INADEQUADA', label: 'Metodologia inadequada' },
    { value: 'POTENCIAL_DIVERGENTE', label: 'Potencial de crédito divergente' },
    { value: 'OUTRO', label: 'Outro' },
];

export const APPROVAL_STATUS_LABELS = ['Certificação aprovada', 'Mint autorizado', 'Aguardando tesouraria'] as const;

export type CertifierQueueResponse = { success: boolean; total: number; projects: any[]; scope: CertifierQueueScope; counts: { main: number; corrections: number } };
export type CertificationDossierStatus = { complete: boolean; missing: string[]; baseline: { present: boolean; capturedAt: string | null; sentinelSceneId: string | null }; tags: { total: number; valid: number; required: number; complete: boolean }; documents: { presentTypes: string[]; missingGroups: string[]; complete: boolean } };
export type CertificationCalculation = { suggestedCreditPotential: number; formula: string; source: string; vegetationCoverPct: number | null; ndviMean: number | null; areaHectares: number | null; carbonStock: number | null; methodology: string | null };
export type CertificationCertificate = { documentId: string; sha256: string; mimeType: string; sizeBytes: number; uploadedAt: string; storagePath: string; filename: string | null; downloadAvailable: boolean };
export type CertificationPendencyItem = { id: string; category: string; description: string; status: string; producerResponse: string | null; respondedAt: string | null; resolvedAt: string | null; createdAt: string; metadata: Record<string, any> };
export type CertificationHistoryEvent = { id: string; action: string; label: string; actorRole: string | null; actorProfileId: string | null; createdAt: string; metadata?: Record<string, any> };
export type CertifierReviewDossier = { success: boolean; project: any; baseline: Record<string, any> | null; tags: Array<Record<string, any>>; documents: Array<Record<string, any>>; dossier: CertificationDossierStatus; calculation: CertificationCalculation; certifications: Array<Record<string, any>>; pendencies: CertificationPendencyItem[]; treasuryAuthorization: Record<string, any> | null; certificate: CertificationCertificate | null };
export type CertificationHistoryResponse = { success: boolean; total: number; events: CertificationHistoryEvent[]; availableEventTypes: Array<{ value: string; label: string }>; availableActorRoles: string[] };
export type CertificationDecisionResult = { success: boolean; project_id: string; new_status: string; previous_status: string; decision: CertificationDecision; credit_potential: number; suggested_credit_potential: number; certification_id: string; certificate: Record<string, any> | null; pendency_id: string | null; treasury_authorization_id: string | null; statusLabels: string[] };
```

Funções exportadas:
```typescript
export const fetchCertifierQueue = async (scope: CertifierQueueScope): Promise<CertifierQueueResponse> => { ... apiGet(`/certifier/queue?scope=${scope}`) ... }
export const fetchCertifierReview = async (projectId: string): Promise<CertifierReviewDossier> => { ... apiGet(`/certifier/projects/${encodeURIComponent(projectId)}/review`) ... }
export const fetchCertificationHistory = async (projectId: string, filters?: { eventType?: string; actorRole?: string }): Promise<CertificationHistoryResponse> => { ... monta querystring com URLSearchParams (event_type, actor_role) omitindo vazios ... }
export const decideCertification = async (projectId: string, input: {
    decision: CertificationDecision;
    methodology?: string;
    creditPotential?: number;
    creditPotentialAdjustmentReason?: string;
    notes: string;
    rejectionCategory?: PendencyCategory;
    certificate?: File | null;
}): Promise<CertificationDecisionResult> => { ... }
```
`decideCertification` monta o corpo assim (nomes de campo idênticos aos `Form(...)` do backend):
```typescript
    const body = new FormData();
    body.append('decision', input.decision);
    if (input.methodology) body.append('methodology', input.methodology);
    if (input.creditPotential !== undefined && !Number.isNaN(input.creditPotential)) body.append('credit_potential', String(input.creditPotential));
    if (input.creditPotentialAdjustmentReason) body.append('credit_potential_adjustment_reason', input.creditPotentialAdjustmentReason);
    body.append('notes', input.notes);
    if (input.rejectionCategory) body.append('rejection_category', input.rejectionCategory);
    if (input.certificate) body.append('certificate', input.certificate);
    const response = await apiPatch<CertificationDecisionResult>(`/certifier/projects/${encodeURIComponent(projectId)}/decision`, body);
    if (!response) throw new Error('Decisão da certificadora sem resposta da API.');
    return response;
```
NUNCA setar `headers` nem `Content-Type` — o browser define o boundary do multipart.

Adicionar também o tradutor de erro para as cópias obrigatórias do `04-UI-SPEC.md`:
```typescript
export const decisionErrorMessage = (error: unknown, decision: CertificationDecision): string => {
    const raw = error instanceof Error ? error.message : String(error);
    if (raw.includes('Dossiê incompleto')) return raw;
    if (decision === 'APPROVE' && (raw.includes('Storage') || raw.includes('certificado') || raw.includes('Certificado') || raw.includes('PDF')))
        return 'Não foi possível anexar o certificado. A aprovação não foi concluída — verifique o arquivo PDF e tente novamente.';
    if (decision === 'APPROVE' && (raw.includes('tesouraria') || raw.includes('API indisponível')))
        return 'Não foi possível registrar a autorização para a tesouraria. A aprovação foi revertida — tente novamente.';
    return raw;
};
```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "certifierReview" | grep -q "^0$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/services/certifierReview.ts` retorna o arquivo
    - `grep -c "new FormData()" src/services/certifierReview.ts` == 1
    - `grep -c "Content-Type" src/services/certifierReview.ts` == 0
    - `grep -c "export const decideCertification" src/services/certifierReview.ts` == 1
    - `grep -c "export const fetchCertifierQueue" src/services/certifierReview.ts` == 1
    - `grep -c "export const fetchCertifierReview" src/services/certifierReview.ts` == 1
    - `grep -c "export const fetchCertificationHistory" src/services/certifierReview.ts` == 1
    - `grep -c "PENDENCY_CATEGORY_OPTIONS" src/services/certifierReview.ts` == 1
    - `grep -c "http://\|https://" src/services/certifierReview.ts` == 0
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <done>Cliente tipado da bancada compilando, com FormData e sem URL absoluta.</done>
</task>

<task type="auto">
  <name>Task 2: Card expansível com duas filas, contador e abas Resumo/QTAGs/Documentos/Cálculo</name>
  <files>src/pages/Dashboard/CertifierReview.tsx</files>
  <read_first>
    - src/pages/Dashboard/CertifierReview.tsx (arquivo inteiro — 114 linhas)
    - src/pages/Dashboard/AuditorReview.tsx (linhas 160-220 e 395-450: activeProjectId, cache por projeto, casca do card `article.rounded-3xl.border.border-gray-100.bg-white.p-6.shadow-sm`)
    - src/pages/Dashboard/MrcaDetails.tsx (linhas 140-155: conversão de tags do dossiê para ProjectTagDraft; linhas 199-220: nav de abas com sublinhado ativo)
    - src/components/ProjectGeofencePreview.tsx (props)
    - src/services/certifierReview.ts (criado na Task 1)
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (Component Inventory, Copywriting Contract, Color)
  </read_first>
  <action>
Reescrever `src/pages/Dashboard/CertifierReview.tsx` mantendo o cabeçalho existente (`Painel da Certificadora`, badge `ShieldCheck` em `bg-emerald-50 text-emerald-700`) e substituindo a listagem simples.

1. Estado:
```typescript
const [scope, setScope] = React.useState<CertifierQueueScope>('main');
const [items, setItems] = React.useState<any[]>([]);
const [counts, setCounts] = React.useState<{ main: number; corrections: number }>({ main: 0, corrections: 0 });
const [loading, setLoading] = React.useState(true);
const [activeProjectId, setActiveProjectId] = React.useState<string | null>(null);
const [activeTab, setActiveTab] = React.useState<TabId>('resumo');
const [reviewByProject, setReviewByProject] = React.useState<Record<string, CertifierReviewDossier>>({});
const [reviewLoading, setReviewLoading] = React.useState<string | null>(null);
const [message, setMessage] = React.useState('');
const [error, setError] = React.useState('');
```
`type TabId = 'resumo' | 'qtags' | 'documentos' | 'calculo' | 'decisao' | 'historico';`

2. Carga da fila: `loadQueue(scope)` chama `fetchCertifierQueue(scope)`, seta `items`, `counts`. `useEffect` dispara em `[scope]`.

3. Abas de fila (dois botões acima da lista), com o contador âmbar exigido pelo UI-SPEC:
```jsx
<div className="flex flex-wrap items-center gap-3 border-b border-gray-100 pb-4">
  <button onClick={() => setScope('main')} className={`pb-2 text-[11px] font-bold uppercase tracking-[0.1em] transition-all relative ${scope === 'main' ? 'text-emerald-600' : 'text-gray-400 hover:text-black'}`}>
    Fila de decisão
    {scope === 'main' && <div className="absolute -bottom-[17px] left-0 h-1 w-full rounded-full bg-emerald-600" />}
  </button>
  <button onClick={() => setScope('corrections')} className={`flex items-center gap-2 pb-2 text-[11px] font-bold uppercase tracking-[0.1em] transition-all relative ${scope === 'corrections' ? 'text-emerald-600' : 'text-gray-400 hover:text-black'}`}>
    Aguardando retorno do produtor
    <span className="rounded-full bg-amber-100 px-2 py-1 text-xs font-bold text-amber-700">{counts.corrections}</span>
    {scope === 'corrections' && <div className="absolute -bottom-[17px] left-0 h-1 w-full rounded-full bg-emerald-600" />}
  </button>
</div>
```

4. Estados vazios com as cópias EXATAS do UI-SPEC:
   - `main`: título "Nenhum projeto pendente de certificação." + corpo "Novos projetos auditados aparecerão aqui automaticamente para revisão."
   - `corrections`: título "Nenhum projeto aguardando retorno do produtor." + corpo "Pendências enviadas para o produtor aparecem aqui até serem respondidas ou corrigidas."

5. Card + toggle (padrão de `AuditorReview.tsx`), com cache por `friendlyId`:
```typescript
const toggleReview = async (project: any) => {
    const key = project.friendlyId || project.id;
    if (activeProjectId === key) { setActiveProjectId(null); return; }
    setActiveProjectId(key);
    setActiveTab('resumo');
    if (reviewByProject[key]) return;
    setReviewLoading(key);
    try {
        const review = await fetchCertifierReview(key);
        setReviewByProject((current) => ({ ...current, [key]: review }));
    } catch (err) {
        setError(err instanceof Error ? err.message : 'Não foi possível carregar o dossiê.');
    } finally {
        setReviewLoading(null);
    }
};
```
Botão de expandir no header do card: `<button onClick={() => toggleReview(project)}>` com rótulo "Abrir revisão detalhada" / "Fechar revisão".

6. Dentro da região expandida (`{activeProjectId === key && (<div className="mt-6 space-y-6 border-t border-gray-100 pt-6">...`), a nav de seis abas — presença e nomes obrigatórios (D-02 / UI-SPEC):
```jsx
<nav className="flex gap-6 border-b border-gray-100 overflow-x-auto">
  {[
    { id: 'resumo', label: 'Resumo', icon: FileText },
    { id: 'qtags', label: 'QTAGs / Geofence', icon: Tag },
    { id: 'documentos', label: 'Documentos', icon: FileCheck2 },
    { id: 'calculo', label: 'Cálculo', icon: Calculator },
    { id: 'decisao', label: 'Decisão', icon: ClipboardCheck },
    { id: 'historico', label: 'Histórico', icon: History },
  ].map((tab) => (
    <button key={tab.id} onClick={() => setActiveTab(tab.id as TabId)}
      className={`pb-5 flex items-center gap-2.5 text-[11px] font-bold uppercase tracking-[0.1em] whitespace-nowrap transition-all relative ${activeTab === tab.id ? 'text-emerald-600' : 'text-gray-400 hover:text-black'}`}>
      <tab.icon className="w-4 h-4" />
      {tab.label}
      {activeTab === tab.id && <div className="absolute bottom-0 left-0 w-full h-1 bg-emerald-600 rounded-full" />}
    </button>
  ))}
</nav>
```
Ícones de `lucide-react` (já instalado): `FileText, Tag, FileCheck2, Calculator, ClipboardCheck, History, CheckCircle2, XCircle, RotateCcw, ShieldCheck, AlertTriangle, Upload`.

7. Painel "Dossiê mínimo" fixo acima das abas, sempre visível quando expandido — três indicadores derivados de `review.dossier`:
   - baseline: `CheckCircle2` em `text-emerald-600` se `dossier.baseline.present`, senão `AlertTriangle` em `text-amber-500`
   - QTAGs: `{dossier.tags.valid}/{dossier.tags.required} vértices válidos`
   - documentos: lista `dossier.documents.missingGroups` quando houver
   Quando `dossier.complete === false`, renderizar inline o aviso com a cópia EXATA:
   "Dossiê incompleto: reúna baseline, as quatro QTAGs/geofence válidas e os documentos obrigatórios antes de decidir. Uma pendência será gerada para o produtor corrigir."
   em `rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-semibold text-amber-700`.

8. Conteúdo das quatro primeiras abas:
   - **Resumo**: nome, friendlyId, localização, área, estoque, metodologia, status atual, produtor/certificadora (de `review.project.entities`), e as pendências abertas (`review.pendencies` com `status === 'OPEN'`) em chips âmbar.
   - **QTAGs / Geofence**: `<ProjectGeofencePreview tags={qtagDrafts} />` onde `qtagDrafts` é construído a partir de `review.tags` exatamente como em `MrcaDetails.tsx:144-153`; abaixo, grid dos 4 vértices com `vertex`, `status`, `tagUid`/`cmac` (ou "Sem QTAG física") e coordenadas.
   - **Documentos**: tabela/lista de `review.documents` com `type`, `filename` (de `metadata.filename`), `mimeType`, `sizeBytes` formatado e `sha256Hash` em `font-mono text-[10px] break-all`; destacar em emerald os que satisfazem o dossiê mínimo; se `review.certificate` existir, mostrar o bloco "Certificado da certificação" com hash e `uploadedAt`.
   - **Cálculo**: `review.calculation` — potencial sugerido em destaque (`text-3xl font-black`), fórmula (`calculation.formula`), origem (`source`), `vegetationCoverPct`, `ndviMean`, `areaHectares`, `carbonStock`, `methodology`. Deixar explícito que o valor é sugestão editável na aba Decisão.

As abas `decisao` e `historico` ficam como placeholder nesta task (`<EmptyState text="..." />`) e são implementadas na Task 3.

Manter os três botões de decisão APENAS dentro da aba Decisão — remover os botões soltos do header do card (hoje em `CertifierReview.tsx:97-107`), porque D-05 exige formulário completo por decisão.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QTAGs / Geofence" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "'resumo'\|'qtags'\|'documentos'\|'calculo'\|'decisao'\|'historico'" src/pages/Dashboard/CertifierReview.tsx` >= 6
    - `grep -c "Nenhum projeto aguardando retorno do produtor." src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "Novos projetos auditados aparecerão aqui automaticamente para revisão." src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "bg-amber-100 px-2 py-1 text-xs font-bold text-amber-700" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "ProjectGeofencePreview" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "text-primary\|bg-primary" src/pages/Dashboard/CertifierReview.tsx` == 0 (paleta interna é emerald/amber/red, não a marca)
    - `grep -c "local://" src/pages/Dashboard/CertifierReview.tsx` == 0
    - `grep -c "apiPatch(\`/certifier" src/pages/Dashboard/CertifierReview.tsx` == 0 (chamadas só via certifierReview.ts)
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <done>Bancada com duas filas, contador âmbar, card expansível e quatro abas de dossiê alimentadas pela API.</done>
</task>

<task type="auto">
  <name>Task 3: Aba Decisão (formulário completo + upload de certificado) e aba Histórico com filtros</name>
  <files>src/pages/Dashboard/CertifierReview.tsx</files>
  <read_first>
    - src/pages/Dashboard/CertifierReview.tsx (estado após a Task 2)
    - src/pages/Dashboard/AuditorReview.tsx (linhas 375-395: casca do `select`; 517-531: casca visual do dropzone; 564-587: casca de `label` + `input`/`textarea`)
    - src/services/certifierReview.ts (decideCertification, decisionErrorMessage, PENDENCY_CATEGORY_OPTIONS, APPROVAL_STATUS_LABELS)
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (Copywriting Contract completo — todas as cópias são obrigatórias e literais)
  </read_first>
  <action>
1. Estado do rascunho de decisão, por projeto:
```typescript
type DecisionDraft = {
    decision: CertificationDecision;
    methodology: string;
    creditPotential: string;
    creditPotentialAdjustmentReason: string;
    notes: string;
    rejectionCategory: PendencyCategory | '';
    certificate: File | null;
};
const [draftByProject, setDraftByProject] = React.useState<Record<string, DecisionDraft>>({});
const [submitting, setSubmitting] = React.useState<string | null>(null);
```
Ao carregar o dossiê (`toggleReview`), inicializar o rascunho com `methodology = review.calculation.methodology ?? ''` e `creditPotential = String(review.calculation.suggestedCreditPotential)`.

2. Aba **Decisão** — layout:
   a. Seletor de decisão: três botões com as classes e cópias exatas do UI-SPEC:
      - `bg-emerald-600 hover:bg-emerald-700` + `<CheckCircle2 className="h-4 w-4" />` + **"Aprovar certificação"**
      - `bg-amber-500 hover:bg-amber-600` + `<RotateCcw className="h-4 w-4" />` + **"Solicitar ajustes"**
      - `bg-red-600 hover:bg-red-700` + `<XCircle className="h-4 w-4" />` + **"Reprovar projeto"**
      Todos com `inline-flex items-center justify-center gap-2 rounded-xl px-4 py-3 text-sm font-bold text-white`. O botão não selecionado fica com `opacity-40`.
   b. Campos condicionais (casca: `label.space-y-1` + `input/textarea.w-full.rounded-xl.border.border-gray-200.px-3.py-2.text-sm.focus:border-emerald-500`):
      - APPROVE: `Metodologia` (text, obrigatório), `Potencial de crédito (tCO₂e)` (number, obrigatório), `Justificativa do ajuste` (textarea, aparece e vira obrigatório quando `Number(creditPotential)` difere de `calculation.suggestedCreditPotential` em mais de 0.01), `Notas técnicas` (textarea), dropzone de certificado.
      - REJECT / REQUEST_CHANGES: `Categoria` (select com `PENDENCY_CATEGORY_OPTIONS`, obrigatório) e `Descrição do motivo` (textarea, obrigatório).
      Abaixo do potencial, mostrar sempre: `Sugerido pelo sistema: {calculation.suggestedCreditPotential} tCO₂e` com botão "Usar valor sugerido".
   c. Dropzone do certificado (SOMENTE APPROVE) — casca visual do `AuditorReview.tsx:517-531`, lógica nova:
```jsx
<label className="flex cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed border-gray-200 bg-gray-50 px-4 py-5 text-center transition hover:border-emerald-300 hover:bg-emerald-50">
    <Upload className="h-5 w-5 text-gray-400" />
    <span className="mt-2 text-sm font-bold text-gray-800">Anexar certificado (PDF)</span>
    <input type="file" accept="application/pdf" className="sr-only"
      onChange={(event) => updateDraft(key, { certificate: event.target.files?.[0] ?? null })} />
</label>
```
Quando houver arquivo, mostrar nome + tamanho e um botão de remover com `aria-label="Remover arquivo"`.
   d. Textos de confirmação destrutiva do UI-SPEC, renderizados acima do botão de submit quando a decisão selecionada for a correspondente:
      - Reprovar: "Esta decisão fica registrada permanentemente no histórico e não pode ser editada. Selecione uma categoria e descreva o motivo antes de confirmar."
      - Solicitar ajustes: "O projeto sai da fila principal e só retorna quando o produtor responder a esta pendência. Descreva claramente o que precisa ser corrigido."
   e. Regra de habilitação do submit (o servidor também valida; isto é UX):
```typescript
const canSubmit = (draft: DecisionDraft, review: CertifierReviewDossier): boolean => {
    if (draft.decision === 'APPROVE') {
        if (!review.dossier.complete) return false;
        if (!draft.certificate) return false;
        if (!draft.methodology.trim()) return false;
        const value = Number(draft.creditPotential);
        if (!Number.isFinite(value) || value <= 0) return false;
        if (Math.abs(value - review.calculation.suggestedCreditPotential) > 0.01 && !draft.creditPotentialAdjustmentReason.trim()) return false;
        return true;
    }
    return Boolean(draft.rejectionCategory) && draft.notes.trim().length > 0;
};
```
   f. Submit: chama `decideCertification(key, {...})`; em sucesso, seta `message` com os `statusLabels` quando APPROVE (renderizar os três rótulos como chips concluídos em `bg-emerald-50 text-emerald-700`), invalida o cache do projeto (`setReviewByProject` removendo a chave), recarrega a fila e recarrega o histórico. Em erro, `setError(decisionErrorMessage(err, draft.decision))` e manter o rascunho intacto (D-14: a decisão não conclui, o formulário não é perdido).

3. Aba **Histórico**:
   - Estado `historyByProject`, `historyLoading`, `historyFilters: { eventType: string; actorRole: string }`.
   - Carregar via `fetchCertificationHistory(key, filters)` ao entrar na aba e a cada mudança de filtro.
   - Dois `select` (casca de `AuditorReview.tsx:378-393`): "Tipo de evento" populado por `availableEventTypes` (+ opção "Todos") e "Ator" populado por `availableActorRoles` (+ "Todos").
   - Linha do tempo baseada em `MrcaDetails.tsx:250-262`:
```jsx
<div className="flex gap-5">
    <div className="w-9 h-9 rounded-full bg-emerald-50 text-emerald-700 flex items-center justify-center shrink-0">
        <CheckCircle2 className="w-5 h-5" />
    </div>
    <div>
        <p className="text-[10px] font-bold text-emerald-700 uppercase tracking-[0.2em]">{event.action} · {new Date(event.createdAt).toLocaleString('pt-BR')}</p>
        <h4 className="text-base font-bold text-black mt-1">{event.label}</h4>
        <p className="text-sm text-gray-500 mt-1">{event.actorRole || 'sistema'}</p>
    </div>
</div>
```
   - Quando `event.metadata?.description` ou `event.metadata?.response` existir, renderizar abaixo em `text-sm text-gray-500` (D-22: notas internas são visíveis para a certificadora).
   - Estado vazio: "Nenhum evento de certificação registrado para este projeto."

4. Remover completamente a função `decide()` antiga com notas hardcoded e a chamada `apiPatch(..., { decision, notes, certifier_id: 'std-001' })`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Aprovar certificação" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "Solicitar ajustes" src/pages/Dashboard/CertifierReview.tsx` >= 1
    - `grep -c "Reprovar projeto" src/pages/Dashboard/CertifierReview.tsx` >= 1
    - `grep -c "Rejeitar" src/pages/Dashboard/CertifierReview.tsx` == 0
    - `grep -c "Anexar certificado (PDF)" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c 'accept="application/pdf"' src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c 'aria-label="Remover arquivo"' src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "certifier_id: 'std-001'" src/pages/Dashboard/CertifierReview.tsx` == 0
    - `grep -c "decideCertification" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "decisionErrorMessage" src/pages/Dashboard/CertifierReview.tsx` >= 1
    - `grep -c "fetchCertificationHistory" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "Esta decisão fica registrada permanentemente no histórico" src/pages/Dashboard/CertifierReview.tsx` == 1
    - `grep -c "focus:border-blue-500" src/pages/Dashboard/CertifierReview.tsx` == 0
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <done>Formulário completo por decisão, com upload real de certificado, bloqueio por dossiê e linha do tempo filtrável na própria bancada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador da certificadora → API | Validação de formulário no cliente é apenas UX |
| arquivo escolhido pelo usuário → FormData | Conteúdo binário arbitrário sai do navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Elevation of Privilege | `canSubmit` no React | accept | Gating no cliente é conveniência; a validação autoritativa é `record_decision` no servidor (T-04-08, plano 04-03). Nenhum controle de segurança depende do botão desabilitado |
| T-04-23 | Tampering | upload do certificado | mitigate | `accept="application/pdf"` no input + validação real de extensão/tamanho/magic bytes no backend; o cliente nunca gera hash nem `local://` |
| T-04-24 | Information Disclosure | notas internas na aba Histórico | accept | D-22 autoriza a certificadora a ver notas internas; a rota exige `require_role("certifier","admin")` |
</threat_model>

<verification>
- `npm run build` verde
- `npx tsc -b` pode continuar bloqueado por dívida legada fora do escopo (registrado em STATE.md); não regredir o número de erros
- Verificação manual da UI está resolvida em 04-07 (seção `<verification>` — verificações manuais 1 a 5)
</verification>

<success_criteria>
- Card expansível com as seis abas exatas do contrato de UI
- Duas filas com contador âmbar
- Formulário completo por decisão com bloqueio por dossiê mínimo e certificado obrigatório na aprovação
- Upload real via FormData, sem blob local
- Histórico filtrável na própria bancada
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-06-SUMMARY.md`
</output>
