---
phase: 04-certification-workbench
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - backend_app/modules/certifier/service.py
  - backend_app/modules/treasury/routes.py
autonomous: true
requirements: [CERT-04]

must_haves:
  truths:
    - "Aprovar a certificação cria automaticamente um pacote de autorização para a tesouraria"
    - "Se a criação do pacote de tesouraria falhar, a aprovação inteira é revertida"
    - "A aprovação registra os três estados visíveis: Certificação aprovada, Mint autorizado e Aguardando tesouraria"
    - "A tesouraria consegue ler a fila de autorizações pendentes sem executar mint"
  artifacts:
    - path: "backend_app/modules/certifier/service.py"
      provides: "Criação de TreasuryAuthorization e eventos MINT_AUTHORIZED/TREASURY_QUEUE_CREATED dentro do commit único"
      contains: "TreasuryAuthorization("
    - path: "backend_app/modules/treasury/routes.py"
      provides: "GET /treasury/authorizations somente leitura"
      contains: "/treasury/authorizations"
  key_links:
    - from: "backend_app/modules/certifier/service.py"
      to: "treasury_authorizations"
      via: "INSERT dentro da mesma transação da aprovação"
      pattern: "TreasuryAuthorization\\("
    - from: "backend_app/modules/treasury/routes.py"
      to: "TreasuryAuthorization"
      via: "select filtrado por status"
      pattern: "select\\(TreasuryAuthorization\\)"
---

<objective>
Fechar a preparação explícita do lastro/mint bloqueado: a aprovação da certificadora passa a produzir, atomicamente, um pacote de autorização para a tesouraria com status visível.

Purpose: CERT-04 exige que a aprovação acione ou prepare explicitamente o fluxo de lastro/mint bloqueado com status visível. D-15/D-16 proíbem executar provider ou adapter externo nesta fase; D-18 define o pacote mínimo; D-19 exige atomicidade entre aprovação e criação da pendência de tesouraria.
Output: `TreasuryAuthorization` criada dentro do commit único da aprovação, os dois eventos de auditoria que faltavam, e o endpoint somente leitura da fila da tesouraria.

Fora de escopo (D-15/D-16): nenhuma chamada a `backend_app/adapters/stellar.py`, `backend_app/modules/blockchain/routes.py` ou qualquer provider. O projeto fica parado em `CERTIFIED_AWAITING_TREASURY` — a próxima transição é responsabilidade da Phase 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-RESEARCH.md
@.planning/phases/04-certification-workbench/04-03-SUMMARY.md

<interfaces>
`backend_app/db/models.py` (criado no plano 04-01):
```python
class TreasuryAuthorization(Base):
    __tablename__ = "treasury_authorizations"
    id, project_id, certification_id (UNIQUE), certifier_organization_id, certifier_profile_id,
    methodology: str, approved_credit_potential: Decimal, certificate_document_id: uuid | None,
    certificate_sha256: str, status: str (default 'PENDING'), authorized_at: datetime,
    metadata_: dict, created_at: datetime
```

`backend_app/core/roles.py`:
```python
UserRole = Literal["producer", "auditor", "company", "certifier", "admin"]
# NAO existe papel "treasury". A leitura da fila usa require_role("admin", "certifier").
```

`backend_app/modules/treasury/routes.py` (estado atual):
```python
router = APIRouter(tags=["treasury"])

@router.post("/treasury/harvest")
async def harvest_treasury_yield(payload: TreasuryHarvestRequest,
    _: AuthenticatedUser = Depends(require_role("admin")),
    session: AsyncSession = Depends(get_session)) -> dict[str, Any]
```

`backend_app/modules/audit/routes.py::audit_queue` (padrão de fila a espelhar):
```python
@router.get("/audit/queue", response_model=QueueResponse)
async def audit_queue(
    _: AuthenticatedUser = Depends(require_role("auditor", "admin")),
    session: AsyncSession = Depends(get_session),
) -> QueueResponse:
    statement = select(Project).where(...).order_by(Project.created_at.asc())
```

`backend_app/modules/certifier/routes.py` (criado no plano 04-02):
```python
def treasury_authorization_item(authorization: TreasuryAuthorization) -> dict[str, object]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar o pacote de autorização de tesouraria dentro da transação de aprovação</name>
  <files>backend_app/modules/certifier/service.py</files>
  <read_first>
    - backend_app/modules/certifier/service.py (arquivo inteiro — método record_decision criado no plano 04-03)
    - backend_app/db/models.py (class TreasuryAuthorization)
    - tests/test_certifier_workbench.py (test_approve_creates_treasury_authorization e test_approve_rolls_back_when_treasury_package_fails)
    - .planning/phases/04-certification-workbench/04-CONTEXT.md (D-17, D-18, D-19)
  </read_first>
  <behavior>
    - Aprovação bem-sucedida cria exatamente 1 linha em `treasury_authorizations` com `status = 'PENDING'`
    - A linha traz projeto, organização certificadora, potencial aprovado, metodologia, sha256 do certificado e `authorized_at`
    - A resposta da aprovação traz `statusLabels == ["Certificação aprovada", "Mint autorizado", "Aguardando tesouraria"]`
    - O histórico do projeto passa a conter `MINT_AUTHORIZED` e `TREASURY_QUEUE_CREATED`
    - Falha no upload do certificado (502 simulado) → nenhuma linha em `certifications`, `documents` ou `treasury_authorizations`, e `project.status` inalterado
    - Falha ao inserir a autorização → aprovação inteira revertida (mesma transação)
  </behavior>
  <action>
Editar `backend_app/modules/certifier/service.py`, no ramo `APPROVE` de `record_decision`, entre o INSERT da `Certification` (com `flush`) e os eventos de auditoria já existentes.

1. Após `await self.session.flush()` da `Certification` e após `document = await self._persist_certificate(...)`, adicionar a criação do pacote (D-18):
```python
        authorization = TreasuryAuthorization(
            project_id=project.id,
            certification_id=certification.id,
            certifier_organization_id=project.certifier_organization_id,
            certifier_profile_id=certification.certifier_profile_id,
            methodology=certification.methodology,
            approved_credit_potential=certification.credit_potential,
            certificate_document_id=document.id,
            certificate_sha256=str(upload["sha256"]),
            status="PENDING",
            authorized_at=datetime.now(timezone.utc),
            metadata_={
                "friendly_id": project.friendly_id,
                "project_name": project.name,
                "vintage": project.vintage,
                "certificate_storage_path": document.storage_path,
                "credit_potential_suggested": suggested,
                "credit_potential_adjustment_reason": credit_potential_adjustment_reason,
                "authorized_by_external_id": actor.id,
                "authorized_by_role": actor.role,
                "source": "certifier_decision",
            },
        )
        self.session.add(authorization)
        await self.session.flush()
```
(Confirmar os nomes reais dos atributos de `Project` usados em `metadata_` — `name`, `vintage` — antes de escrever; usar os que existirem no modelo.)

2. Depois dos eventos `CERTIFICATION_APPROVED` e `CERTIFICATION_CERTIFICATE_ATTACHED`, acrescentar os dois eventos que faltavam (D-21), ainda antes do commit:
```python
        await create_audit_event(
            self.session,
            action="MINT_AUTHORIZED",
            entity_type="projects",
            entity_id=project.id,
            actor_role=actor.role,
            after_data={"credit_potential": final_potential, "status": project.status},
            metadata={
                "actor_external_id": actor.id,
                "friendly_id": project.friendly_id,
                "certification_id": str(certification.id),
                "authorization_id": str(authorization.id),
                "execution": "DEFERRED_TO_TREASURY",
            },
        )
        await create_audit_event(
            self.session,
            action="TREASURY_QUEUE_CREATED",
            entity_type="treasury_authorizations",
            entity_id=authorization.id,
            actor_role=actor.role,
            after_data={"status": authorization.status},
            metadata={
                "actor_external_id": actor.id,
                "friendly_id": project.friendly_id,
                "project_id": str(project.id),
                "certification_id": str(certification.id),
                "approved_credit_potential": final_potential,
                "methodology": certification.methodology,
                "certificate_sha256": str(upload["sha256"]),
            },
        )
```
NOTA: `TREASURY_QUEUE_CREATED` usa `entity_type="treasury_authorizations"`, mas o histórico do projeto (plano 04-05) filtra por `entity_type == "projects"`. Para o evento aparecer na linha do tempo do projeto, gravar `entity_type="projects"` e `entity_id=project.id`, mantendo `authorization_id` dentro de `metadata`. Usar `entity_type="projects"` nos DOIS eventos.

3. Acrescentar `"treasury_authorization_id": str(authorization.id)` e `"treasury_status": authorization.status` ao dicionário de retorno de `record_decision` no ramo APPROVE (`None` nos demais ramos).

4. Importar `TreasuryAuthorization` de `backend_app.db.models`.

5. Não adicionar nenhum `try/except` em volta dessas escritas: qualquer exceção deve subir e abortar a transação inteira antes do `session.commit()` final (D-19). Não chamar `session.commit()` em nenhum ponto novo.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_approve_creates_treasury_authorization tests/test_certifier_workbench.py::test_approve_rolls_back_when_treasury_package_fails -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TreasuryAuthorization(" backend_app/modules/certifier/service.py` == 1
    - `grep -c "MINT_AUTHORIZED" backend_app/modules/certifier/service.py` == 1
    - `grep -c "TREASURY_QUEUE_CREATED" backend_app/modules/certifier/service.py` == 1
    - `grep -c "await self.session.commit()" backend_app/modules/certifier/service.py` == 2 (inalterado em relação ao plano 04-03)
    - `grep -c "try:" backend_app/modules/certifier/service.py` == 0
    - `grep -c "treasury_authorization_id" backend_app/modules/certifier/service.py` >= 1
    - `grep -c "STATUS_LABELS_APPROVED" backend_app/modules/certifier/service.py` >= 2
    - `uv run pytest tests/test_certifier_workbench.py::test_approve_rolls_back_when_treasury_package_fails -x` passa
  </acceptance_criteria>
  <done>Aprovação e pacote de tesouraria commitados juntos ou revertidos juntos, com os três rótulos de status e os cinco eventos de auditoria da aprovação.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoint somente leitura GET /treasury/authorizations</name>
  <files>backend_app/modules/treasury/routes.py</files>
  <read_first>
    - backend_app/modules/treasury/routes.py (arquivo inteiro)
    - backend_app/modules/audit/routes.py (linhas 30-45 — padrão de fila com require_role)
    - backend_app/db/models.py (TreasuryAuthorization, Project, Organization)
    - tests/test_certifier_workbench.py (test_approve_creates_treasury_authorization — chaves exatas esperadas na resposta)
  </read_first>
  <behavior>
    - `GET /api/v1/treasury/authorizations` com token de certificadora → 200 com `{"success": true, "total": n, "authorizations": [...]}`
    - Cada item traz `projectId` (friendly id), `projectName`, `certifierOrganization`, `approvedCreditPotential`, `methodology`, `certificate.sha256`, `authorizedAt`, `status` e `auditTrail`
    - `?status=PENDING` filtra por status
    - Token de `company` → 403; sem token → 401
    - Nenhum adapter/provider externo é chamado
  </behavior>
  <action>
Adicionar em `backend_app/modules/treasury/routes.py`:

```python
@router.get("/treasury/authorizations")
async def list_treasury_authorizations(
    status_filter: str | None = Query(default=None, alias="status"),
    _: AuthenticatedUser = Depends(require_role("admin", "certifier")),
    session: AsyncSession = Depends(get_session),
) -> dict[str, Any]:
```
Corpo:
1. Montar `statement = select(TreasuryAuthorization, Project, Organization).join(Project, Project.id == TreasuryAuthorization.project_id).outerjoin(Organization, Organization.id == TreasuryAuthorization.certifier_organization_id).order_by(TreasuryAuthorization.created_at.desc())`.
2. Se `status_filter` informado, adicionar `.where(TreasuryAuthorization.status == status_filter.upper())`.
3. Carregar a trilha de auditoria de cada autorização em UMA query só (evitar N+1): `select(AuditEvent).where(AuditEvent.entity_type == "projects", AuditEvent.entity_id.in_(project_ids), AuditEvent.action.in_(("CERTIFICATION_APPROVED", "CERTIFICATION_CERTIFICATE_ATTACHED", "MINT_AUTHORIZED", "TREASURY_QUEUE_CREATED"))).order_by(AuditEvent.created_at.asc())` e agrupar por `entity_id`.
4. Serializar cada item (pacote mínimo da D-18):
```python
        {
            "id": str(authorization.id),
            "projectId": project.friendly_id,
            "projectName": project.name,
            "certificationId": str(authorization.certification_id),
            "certifierOrganization": organization.name if organization is not None else None,
            "methodology": authorization.methodology,
            "approvedCreditPotential": float(authorization.approved_credit_potential),
            "certificate": {
                "documentId": str(authorization.certificate_document_id) if authorization.certificate_document_id else None,
                "sha256": authorization.certificate_sha256,
                "storagePath": (authorization.metadata_ or {}).get("certificate_storage_path"),
            },
            "status": authorization.status,
            "authorizedAt": authorization.authorized_at.isoformat(),
            "createdAt": authorization.created_at.isoformat(),
            "auditTrail": [
                {"id": str(event.id), "action": event.action, "actorRole": event.actor_role, "createdAt": event.created_at.isoformat()}
                for event in trail_by_project.get(project.id, [])
            ],
            "metadata": authorization.metadata_ or {},
        }
```
5. Retornar `{"success": True, "total": len(items), "authorizations": items}`.

Imports a acrescentar: `Query` de `fastapi`, `select` de `sqlalchemy`, `AuditEvent`, `Organization`, `Project`, `TreasuryAuthorization` de `backend_app.db.models`.

PROIBIDO neste endpoint: qualquer import de `backend_app.adapters.*` ou chamada a `backend_app.modules.blockchain` — a execução do mint é da Phase 08 (D-15/D-16). O endpoint é estritamente somente leitura (nenhum `session.add`, nenhum `commit`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_approve_creates_treasury_authorization -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@router.get("/treasury/authorizations")' backend_app/modules/treasury/routes.py` == 1
    - `grep -c 'require_role("admin", "certifier")' backend_app/modules/treasury/routes.py` == 1
    - `grep -c "session.add\|session.commit" backend_app/modules/treasury/routes.py` == 0
    - `grep -c "adapters\|blockchain" backend_app/modules/treasury/routes.py` == 0
    - `grep -c "approvedCreditPotential" backend_app/modules/treasury/routes.py` == 1
    - `uv run python -c "from backend_app.main import app; assert '/api/v1/treasury/authorizations' in [r.path for r in app.routes]; print('ok')"` sai com código 0
    - `uv run pytest tests/test_certifier_workbench.py -x` — os testes 1 a 9 do arquivo passam, exceto os que dependem dos planos 04-05 (fila por escopo, histórico filtrado, resposta do produtor)
  </acceptance_criteria>
  <done>Fila de tesouraria legível com o pacote mínimo da D-18, sem execução de mint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → `GET /treasury/authorizations` | Pacote operacional com hash de certificado e potencial aprovado |
| transação de aprovação → `treasury_authorizations` | Escrita crítica cujo fracasso não pode deixar aprovação órfã |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | `GET /treasury/authorizations` | mitigate | `Depends(require_role("admin", "certifier"))`; nenhum acesso anônimo; teste assegura 403 para `company` |
| T-04-14 | Tampering | aprovação sem pacote de tesouraria | mitigate | INSERT de `TreasuryAuthorization` dentro do mesmo `session` antes do commit único; sem `try/except` que engula falha (D-19) |
| T-04-15 | Repudiation | autorização de mint sem rastro | mitigate | `MINT_AUTHORIZED` e `TREASURY_QUEUE_CREATED` com `actor_external_id`, `certification_id` e `authorization_id` |
| T-04-16 | Elevation of Privilege | execução prematura de mint | accept | Nenhum adapter é chamado nesta fase por decisão de escopo (D-15/D-16); risco residual é operacional (projeto parado em `CERTIFIED_AWAITING_TREASURY` até a Phase 08), documentado no ROADMAP |
</threat_model>

<verification>
- `uv run pytest tests/test_certifier_workbench.py::test_approve_creates_treasury_authorization tests/test_certifier_workbench.py::test_approve_rolls_back_when_treasury_package_fails -x` passa
- `uv run pytest tests/contract -q` sem regressão
- `uv run pytest -q` sem falhas novas
</verification>

<success_criteria>
- Uma aprovação produz exatamente uma `TreasuryAuthorization` PENDING com o pacote mínimo da D-18
- Falha de Storage ou de banco reverte tudo (nenhuma certificação, documento ou autorização órfã)
- `statusLabels` da resposta traz os três rótulos exatos da D-17
- A fila da tesouraria é legível e não executa nada
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-04-SUMMARY.md`
</output>
