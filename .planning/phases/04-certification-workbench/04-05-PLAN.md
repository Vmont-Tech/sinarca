---
phase: 04-certification-workbench
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - backend_app/modules/certifier/routes.py
  - backend_app/modules/projects/routes.py
  - backend_app/modules/projects/schemas.py
autonomous: true
requirements: [CERT-01, CERT-05]

must_haves:
  truths:
    - "A fila da certificadora tem dois escopos: decisão principal e aguardando retorno do produtor, com contadores"
    - "Um projeto com pendência aberta sai da fila principal e aparece na fila de ajustes"
    - "O produtor vê e responde a pendência do seu próprio projeto; o projeto volta para a fila principal"
    - "A certificadora consulta a linha do tempo completa de certificação por projeto, com filtro por tipo de evento e ator"
  artifacts:
    - path: "backend_app/modules/certifier/routes.py"
      provides: "GET /certifier/queue?scope + GET /certifier/projects/{id}/history"
      contains: "scope"
    - path: "backend_app/modules/projects/routes.py"
      provides: "GET /projects/{id}/pendencies + POST /projects/{id}/pendencies/{pendency_id}/respond"
      contains: "pendencies"
    - path: "backend_app/modules/projects/schemas.py"
      provides: "CertifierQueueResponse e CertificationHistoryResponse"
      contains: "class CertifierQueueResponse"
  key_links:
    - from: "backend_app/modules/certifier/routes.py::certifier_queue"
      to: "certification_pendencies"
      via: "EXISTS/NOT EXISTS de pendência OPEN por projeto"
      pattern: "CertificationPendency"
    - from: "backend_app/modules/projects/routes.py"
      to: "ProjectsService._assert_project_edit_permission"
      via: "autorização por organização do projeto para o produtor"
      pattern: "_assert_project_edit_permission"
---

<objective>
Entregar a separação de filas com contador, a linha do tempo filtrável e o ciclo de resposta do produtor à pendência.

Purpose: D-10 exige que o pedido de ajustes tire o projeto da fila principal e o coloque numa fila de "aguardando retorno do produtor" com contador no dashboard, voltando à fila principal quando o produtor responder. D-21/D-23 exigem histórico completo por projeto como linha do tempo com filtros por tipo de evento/status e ator. D-04 exige que a pendência seja vinculada ao projeto e visível ao produtor/origem. Nada disso existe hoje.
Output: `GET /certifier/queue?scope=`, `GET /certifier/projects/{id}/history`, `GET /projects/{id}/pendencies` e `POST /projects/{id}/pendencies/{pendency_id}/respond`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-RESEARCH.md
@.planning/phases/04-certification-workbench/04-UI-SPEC.md
@.planning/phases/04-certification-workbench/04-04-SUMMARY.md

<interfaces>
`backend_app/modules/certifier/routes.py` (estado atual, após planos 02-04):
```python
@router.get("/certifier/queue", response_model=QueueResponse)
async def certifier_queue(_: AuthenticatedUser = Depends(require_role("certifier", "admin")),
                          session: AsyncSession = Depends(get_session)) -> QueueResponse:
    service = ProjectsService(session)
    statement = (select(Project)
        .where(Project.status.in_(["CREATED", "REGISTERED", "AWAITING_CERTIFICATION"]))
        .order_by(Project.created_at.asc()))
    projects = [await service.project_to_mrca(p) for p in (await session.execute(statement)).scalars().all()]
    return QueueResponse(total=len(projects), projects=projects)

def pendency_item(pendency: CertificationPendency) -> dict[str, object]        # criado no plano 04-02
def treasury_authorization_item(authorization) -> dict[str, object]            # criado no plano 04-02
```

`backend_app/modules/projects/schemas.py`:
```python
class QueueResponse(BaseModel):
    success: bool = True
    total: int
    projects: list[ProjectMRCA]
```

`backend_app/modules/projects/service.py` (planos 04-02):
```python
CERTIFICATION_HISTORY_ACTIONS: tuple[str, ...]
CERTIFICATION_HISTORY_LABELS: dict[str, str]
class ProjectsService:
    async def certification_history(self, project, *, actions=None, actor_role=None, include_metadata=True) -> list[dict]
    async def _get_project_model(self, project_id: str) -> Project
    async def _assert_project_edit_permission(self, project, *, actor_id, actor_role) -> None
    async def _actor_profile(self, actor_id: str | None) -> Profile
```

`backend_app/modules/projects/routes.py` (padrão de rota com papel):
```python
@router.post("/projects/{project_id}/documents", status_code=status.HTTP_201_CREATED)
async def upload_project_document(project_id: str, document_type: str = Form(...), file: UploadFile = File(...),
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session)) -> dict[str, object]
```

`backend_app/db/models.py`:
```python
class CertificationPendency(Base):  # status in ('OPEN','RESOLVED','CANCELLED'); producer_response, responded_by_profile_id, responded_at, resolved_at
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fila da certificadora com escopo e contadores</name>
  <files>backend_app/modules/certifier/routes.py, backend_app/modules/projects/schemas.py</files>
  <read_first>
    - backend_app/modules/certifier/routes.py (arquivo inteiro)
    - backend_app/modules/projects/schemas.py (class QueueResponse linha ~174)
    - backend_app/db/models.py (CertificationPendency)
    - tests/test_certifier_workbench.py (test_correction_queue_split_and_producer_response)
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (cópias dos dois estados vazios)
  </read_first>
  <behavior>
    - `GET /certifier/queue` sem parâmetro → escopo `main` (compatibilidade com o cliente atual)
    - `GET /certifier/queue?scope=main` → só projetos elegíveis SEM pendência `OPEN`
    - `GET /certifier/queue?scope=corrections` → só projetos elegíveis COM pendência `OPEN`
    - Ambas as respostas trazem `counts = {"main": n, "corrections": m}` independentemente do escopo pedido
    - `scope` inválido → 422
  </behavior>
  <action>
1. Em `backend_app/modules/projects/schemas.py`, adicionar:
```python
class CertifierQueueResponse(QueueResponse):
    scope: Literal["main", "corrections"] = "main"
    counts: dict[str, int] = {}
```
(`Literal` já é importado no arquivo; confirmar.)

2. Em `backend_app/modules/certifier/routes.py`, substituir `certifier_queue` por:
```python
CERTIFIER_QUEUE_STATUSES = ("CREATED", "REGISTERED", "AWAITING_CERTIFICATION")


def _open_pendency_exists():
    return (
        select(CertificationPendency.id)
        .where(
            CertificationPendency.project_id == Project.id,
            CertificationPendency.status == "OPEN",
        )
        .exists()
    )


@router.get("/certifier/queue", response_model=CertifierQueueResponse)
async def certifier_queue(
    scope: Literal["main", "corrections"] = Query(default="main"),
    _: AuthenticatedUser = Depends(require_role("certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> CertifierQueueResponse:
    service = ProjectsService(session)
    base = select(Project).where(Project.status.in_(CERTIFIER_QUEUE_STATUSES))
    has_open = _open_pendency_exists()

    statement = base.where(has_open if scope == "corrections" else ~has_open).order_by(Project.created_at.asc())
    projects = [await service.project_to_mrca(project) for project in (await session.execute(statement)).scalars().all()]

    main_count = (await session.execute(
        select(func.count()).select_from(Project).where(Project.status.in_(CERTIFIER_QUEUE_STATUSES), ~has_open)
    )).scalar_one()
    corrections_count = (await session.execute(
        select(func.count()).select_from(Project).where(Project.status.in_(CERTIFIER_QUEUE_STATUSES), has_open)
    )).scalar_one()

    return CertifierQueueResponse(
        total=len(projects),
        projects=projects,
        scope=scope,
        counts={"main": int(main_count), "corrections": int(corrections_count)},
    )
```
Imports a acrescentar: `Query` de `fastapi`, `func` de `sqlalchemy`, `CertificationPendency` de `backend_app.db.models`, `CertifierQueueResponse` de `backend_app.modules.projects.schemas`.

Justificativa da modelagem (documentar em comentário no código): a separação de filas é derivada de `certification_pendencies.status = 'OPEN'`, não de um valor novo em `project_status`. Adicionar um valor ao enum obrigaria a tocar `PROJECT_STATUS_TO_LIFECYCLE_CODE`, `MARKETPLACE_READY_PROJECT_STATUSES`, `PROJECT_STATUS_PRESENTATION` e as policies de RLS, com risco desproporcional ao ganho.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py -x -k "correction_queue"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CertifierQueueResponse" backend_app/modules/projects/schemas.py` == 1
    - `grep -c 'scope: Literal\["main", "corrections"\]' backend_app/modules/certifier/routes.py` == 1
    - `grep -c "CERTIFIER_QUEUE_STATUSES" backend_app/modules/certifier/routes.py` >= 3
    - `grep -c "counts=" backend_app/modules/certifier/routes.py` == 1
    - `curl -s -H "Authorization: Bearer $TOKEN" "http://127.0.0.1:8000/api/v1/certifier/queue?scope=corrections" | jq -r '.scope'` retorna `corrections`
    - `uv run pytest tests/contract -q` sem regressão (a resposta continua compatível com `QueueResponse`)
  </acceptance_criteria>
  <done>Fila com dois escopos e contadores, derivada de pendências abertas, sem alterar o enum de status do projeto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linha do tempo de certificação por projeto com filtros</name>
  <files>backend_app/modules/certifier/routes.py, backend_app/modules/projects/schemas.py</files>
  <read_first>
    - backend_app/modules/certifier/routes.py (arquivo inteiro, após a Task 1)
    - backend_app/modules/projects/service.py (certification_history, CERTIFICATION_HISTORY_ACTIONS, CERTIFICATION_HISTORY_LABELS)
    - tests/test_certifier_workbench.py (test_approve_creates_treasury_authorization e test_decisions_are_append_only — ambos consultam o histórico)
    - .planning/phases/04-certification-workbench/04-CONTEXT.md (D-21, D-22, D-23)
  </read_first>
  <behavior>
    - `GET /certifier/projects/{id}/history` com token de certificadora → 200 com `events` em ordem cronológica crescente
    - Cada evento traz `action`, `label` PT-BR, `actorRole`, `createdAt` e `metadata`
    - `?event_type=CERTIFICATION_APPROVED` filtra por ação
    - `?actor_role=certifier` filtra por papel do ator
    - `availableEventTypes` lista os tipos presentes no projeto, para popular o select do frontend
    - Token de `company` → 403
  </behavior>
  <action>
1. Em `backend_app/modules/projects/schemas.py`:
```python
class CertificationHistoryResponse(BaseModel):
    success: bool = True
    total: int
    events: list[dict[str, Any]]
    availableEventTypes: list[dict[str, str]] = []
    availableActorRoles: list[str] = []
```

2. Em `backend_app/modules/certifier/routes.py`:
```python
@router.get("/certifier/projects/{project_id}/history", response_model=CertificationHistoryResponse)
async def certifier_project_history(
    project_id: str,
    event_type: str | None = Query(default=None),
    actor_role: str | None = Query(default=None),
    _: AuthenticatedUser = Depends(require_role("certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> CertificationHistoryResponse:
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    all_events = await service.certification_history(project)
    events = all_events
    if event_type:
        events = [event for event in events if event["action"] == event_type.upper()]
    if actor_role:
        events = [event for event in events if (event["actorRole"] or "") == actor_role.lower()]
    available_types = sorted({event["action"] for event in all_events})
    return CertificationHistoryResponse(
        total=len(events),
        events=events,
        availableEventTypes=[
            {"value": action, "label": CERTIFICATION_HISTORY_LABELS.get(action, action)} for action in available_types
        ],
        availableActorRoles=sorted({event["actorRole"] for event in all_events if event["actorRole"]}),
    )
```
Filtrar em memória é aceitável: a linha do tempo de certificação de um projeto tem dezenas de eventos, não milhares, e a query base já é restrita por `entity_id` (índice de `audit_events`). Não introduzir paginação nesta fase.

Imports a acrescentar: `CertificationHistoryResponse` de `backend_app.modules.projects.schemas`; `CERTIFICATION_HISTORY_LABELS` de `backend_app.modules.projects.service`.

3. O acesso do PRODUTOR ao histórico completo (D-22: notas internas visíveis para certificadora, admin, produtor e tesouraria) é resolvido no endpoint de pendências da Task 3 mais o dossiê do próprio projeto; NÃO expor `/certifier/projects/{id}/history` para o papel `producer` (a rota é do módulo da certificadora e o guard permanece `require_role("certifier", "admin")`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_approve_creates_treasury_authorization tests/test_certifier_workbench.py::test_decisions_are_append_only -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'router.get("/certifier/projects/{project_id}/history"' backend_app/modules/certifier/routes.py` == 1
    - `grep -c "class CertificationHistoryResponse" backend_app/modules/projects/schemas.py` == 1
    - `grep -c "availableEventTypes" backend_app/modules/certifier/routes.py` == 1
    - `grep -c "CERTIFICATION_HISTORY_LABELS" backend_app/modules/certifier/routes.py` == 1
    - `uv run pytest tests/test_certifier_workbench.py::test_decisions_are_append_only -x` passa
  </acceptance_criteria>
  <done>Histórico canônico por projeto, ordenado, rotulado em PT-BR e filtrável por tipo de evento e ator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pendências visíveis e respondíveis pelo produtor</name>
  <files>backend_app/modules/projects/routes.py, backend_app/modules/projects/schemas.py</files>
  <read_first>
    - backend_app/modules/projects/routes.py (linhas 1-70 e 220-310 — imports e padrão de rota autenticada)
    - backend_app/modules/projects/service.py (_assert_project_edit_permission linhas 991-1009; _actor_profile; _get_project_model)
    - backend_app/db/models.py (CertificationPendency)
    - backend_app/modules/certifier/routes.py (pendency_item — reutilizar o serializador, não duplicar)
    - tests/test_certifier_workbench.py (test_correction_queue_split_and_producer_response, test_incomplete_dossier_blocks_decision_and_creates_pendency)
  </read_first>
  <behavior>
    - `GET /projects/{id}/pendencies` com token do produtor dono → 200 com a lista de pendências
    - Mesma rota com token de produtor de OUTRA organização → 403
    - `POST /projects/{id}/pendencies/{pendency_id}/respond` com `{"response": "..."}` → 200; a pendência passa a `RESOLVED` com `producer_response`, `responded_at` e `responded_by_profile_id`
    - Responder pendência já `RESOLVED` → 409
    - Resposta vazia → 400
    - Após responder a última pendência aberta, o projeto volta a aparecer em `GET /certifier/queue?scope=main`
    - A resposta gera `audit_event` com `action="CERTIFICATION_PENDENCY_ANSWERED"`
  </behavior>
  <action>
1. Em `backend_app/modules/projects/schemas.py`:
```python
class ProjectPendenciesResponse(BaseModel):
    success: bool = True
    total: int
    pendencies: list[dict[str, Any]]


class PendencyRespondRequest(BaseModel):
    response: str = Field(min_length=1)
```

2. Em `backend_app/modules/projects/routes.py`, adicionar duas rotas:

```python
@router.get("/projects/{project_id}/pendencies", response_model=ProjectPendenciesResponse)
async def list_project_pendencies(
    project_id: str,
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> ProjectPendenciesResponse:
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)
    rows = (await session.execute(
        select(CertificationPendency)
        .where(CertificationPendency.project_id == project.id)
        .order_by(CertificationPendency.created_at.desc())
    )).scalars().all()
    return ProjectPendenciesResponse(total=len(rows), pendencies=[pendency_item(row) for row in rows])
```

```python
@router.post("/projects/{project_id}/pendencies/{pendency_id}/respond")
async def respond_project_pendency(
    project_id: str,
    pendency_id: str,
    payload: PendencyRespondRequest,
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> dict[str, object]:
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)

    pendency = (await session.execute(
        select(CertificationPendency).where(
            CertificationPendency.id == uuid.UUID(pendency_id),
            CertificationPendency.project_id == project.id,
        )
    )).scalar_one_or_none()
    if pendency is None:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Pendência não encontrada")
    if pendency.status != "OPEN":
        raise HTTPException(status_code=status.HTTP_409_CONFLICT, detail="Esta pendência já foi respondida")

    text_response = payload.response.strip()
    if not text_response:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="A resposta à pendência é obrigatória")

    now = datetime.now(timezone.utc)
    pendency.producer_response = text_response
    pendency.responded_at = now
    pendency.resolved_at = now
    pendency.status = "RESOLVED"
    try:
        profile = await service._actor_profile(current_user.id)
        pendency.responded_by_profile_id = profile.id
    except HTTPException:
        pendency.responded_by_profile_id = None

    project.timeline = [
        *(project.timeline or []),
        {
            "title": "Produtor respondeu à pendência",
            "date": now.date().isoformat(),
            "status": "completed",
            "desc": text_response,
        },
    ]

    await create_audit_event(
        session,
        action="CERTIFICATION_PENDENCY_ANSWERED",
        entity_type="projects",
        entity_id=project.id,
        actor_role=current_user.role,
        before_data={"pendency_status": "OPEN"},
        after_data={"pendency_status": pendency.status},
        metadata={
            "actor_external_id": current_user.id,
            "friendly_id": project.friendly_id,
            "pendency_id": str(pendency.id),
            "category": pendency.category,
            "response": text_response,
        },
    )
    await session.commit()

    open_remaining = (await session.execute(
        select(func.count()).select_from(CertificationPendency).where(
            CertificationPendency.project_id == project.id,
            CertificationPendency.status == "OPEN",
        )
    )).scalar_one()

    return {
        "success": True,
        "project_id": project.friendly_id,
        "pendency_id": str(pendency.id),
        "status": pendency.status,
        "open_pendencies": int(open_remaining),
        "back_to_main_queue": int(open_remaining) == 0,
    }
```

3. Imports a acrescentar em `projects/routes.py`: `uuid`, `datetime`/`timezone`, `func` de `sqlalchemy`, `CertificationPendency` de `backend_app.db.models`, `pendency_item` de `backend_app.modules.certifier.routes`, `ProjectPendenciesResponse`/`PendencyRespondRequest` de `backend_app.modules.projects.schemas`.
   ATENÇÃO a import circular: `certifier/routes.py` importa de `projects/service.py` e `projects/schemas.py`, mas NÃO de `projects/routes.py`; ainda assim, se o import de `pendency_item` gerar ciclo, mover `pendency_item` e `treasury_authorization_item` para `backend_app/modules/projects/service.py` e ajustar os dois importadores.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'router.get("/projects/{project_id}/pendencies"' backend_app/modules/projects/routes.py` == 1
    - `grep -c 'router.post("/projects/{project_id}/pendencies/{pendency_id}/respond"' backend_app/modules/projects/routes.py` == 1
    - `grep -c "_assert_project_edit_permission" backend_app/modules/projects/routes.py` >= 2
    - `grep -c "CERTIFICATION_PENDENCY_ANSWERED" backend_app/modules/projects/routes.py` == 1
    - `grep -c "back_to_main_queue" backend_app/modules/projects/routes.py` == 1
    - `uv run pytest tests/test_certifier_workbench.py -x` — TODOS os 9 testes passam
    - `uv run pytest -q` sem falhas
  </acceptance_criteria>
  <done>Ciclo de correção fechado: pendência criada pela certificadora, visível e respondível pelo produtor dono, com retorno automático à fila principal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| produtor autenticado → `GET/POST /projects/{id}/pendencies` | Papel `producer` é amplo; o dado é específico de UMA organização |
| certificadora autenticada → `GET /certifier/projects/{id}/history` | Histórico traz notas internas em `metadata` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | `GET /projects/{id}/pendencies` | mitigate | `require_role` NÃO basta: `_assert_project_edit_permission` valida `organization_id ∈ {producer_organization_id, developer_organization_id}` (ou `certifier_organization_id`), impedindo leitura por ID adivinhado |
| T-04-18 | Elevation of Privilege | `POST .../respond` | mitigate | Mesmo guard org-scoped + verificação de que a pendência pertence ao `project.id` da rota (`where project_id == project.id`) |
| T-04-19 | Tampering | resposta em pendência já resolvida | mitigate | 409 quando `pendency.status != "OPEN"`; pendência resolvida é imutável (D-09 por analogia) |
| T-04-20 | Repudiation | resposta do produtor sem rastro | mitigate | `CERTIFICATION_PENDENCY_ANSWERED` com `actor_external_id`, `pendency_id` e o texto da resposta |
| T-04-21 | Information Disclosure | histórico com notas internas | accept | D-22 autoriza explicitamente certificadora, admin, produtor e tesouraria a ver notas internas; o público consome o caminho minimizado do plano 04-02 |
</threat_model>

<verification>
- `uv run pytest tests/test_certifier_workbench.py -x` — 9/9 verdes
- `uv run pytest -q` — suíte completa sem falhas
- `npm run build` continua passando (nenhuma mudança de frontend neste plano)
</verification>

<success_criteria>
- Fila principal e fila de ajustes separadas, com contadores em toda resposta
- Projeto com pendência aberta some da fila principal e volta ao ser respondido
- Histórico completo, ordenado, rotulado e filtrável por tipo de evento e ator
- Produtor de outra organização recebe 403 ao tentar ler ou responder pendência
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-05-SUMMARY.md`
</output>
