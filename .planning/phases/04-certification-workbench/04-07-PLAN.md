---
phase: 04-certification-workbench
plan: 07
type: execute
wave: 7
depends_on: ["04-06"]
gap_closure: true
files_modified:
  - backend_app/modules/supabase_storage.py
  - backend_app/modules/projects/routes.py
  - backend_app/modules/projects/schemas.py
  - tests/test_certifier_workbench.py
  - src/services/database.ts
  - src/pages/Dashboard/MrcaDetails.tsx
autonomous: true
requirements: [CERT-03, CERT-05]

must_haves:
  truths:
    - "O produtor dono do projeto consulta a trilha interna completa de certificação, incluindo as notas da certificadora"
    - "Um produtor de outra organização recebe 403 ao tentar ler a trilha interna de um projeto que não é dele"
    - "O dossiê público mostra a referência e o hash do certificado da certificação"
    - "O download do certificado só é entregue a produtor dono, certificadora do projeto ou admin; visitante anônimo recebe 403 com mensagem explícita"
    - "O dossiê público mostra o histórico de decisões finais da certificação, sem notas internas"
  artifacts:
    - path: "backend_app/modules/projects/routes.py"
      provides: "GET /projects/{id}/certification-history (trilha interna do produtor) e GET /projects/{id}/certificate (download condicional)"
      contains: "certification-history"
    - path: "backend_app/modules/projects/schemas.py"
      provides: "ProjectCertificationHistoryResponse"
      contains: "class ProjectCertificationHistoryResponse"
    - path: "backend_app/modules/supabase_storage.py"
      provides: "download_storage_object com falha fechada quando não há credenciais"
      contains: "async def download_storage_object"
    - path: "src/services/database.ts"
      provides: "Tipos certificate/certificationHistory no dossiê público e downloadProjectCertificate"
      contains: "downloadProjectCertificate"
    - path: "src/pages/Dashboard/MrcaDetails.tsx"
      provides: "Bloco de certificado (referência/hash/download condicional) e histórico público de certificação"
      contains: "Certificado da certificação"
  key_links:
    - from: "backend_app/modules/projects/routes.py"
      to: "ProjectsService._assert_project_edit_permission"
      via: "autorização por organização do projeto nas duas rotas novas"
      pattern: "_assert_project_edit_permission"
    - from: "backend_app/modules/projects/routes.py::download_project_certificate"
      to: "download_storage_object"
      via: "leitura do objeto privado do bucket projects com service role"
      pattern: "download_storage_object"
    - from: "src/pages/Dashboard/MrcaDetails.tsx"
      to: "database.downloadProjectCertificate"
      via: "botão de download do certificado no dossiê público"
      pattern: "downloadProjectCertificate"
    - from: "src/pages/Dashboard/MrcaDetails.tsx"
      to: "dossier.certificationHistory"
      via: "renderização da linha do tempo pública de certificação"
      pattern: "certificationHistory"
---

<objective>
Fechar as duas lacunas de consumo da fase: o dossiê público nunca foi ligado aos campos `certificate`/`certificationHistory` criados no plano 04-02, e nenhum endpoint dá ao produtor a trilha interna completa exigida pela D-22.

Purpose:
- **D-13 / CERT-03 / CERT-05:** `MrcaDetails.tsx` é o único consumidor de `GET /projects/{id}/public-dossier` e nenhum plano anterior o modifica. Depois do 04-02, o dossiê público passa a devolver `certificate` e `certificationHistory`, mas a tela ignora os dois campos e ainda carrega o código morto `{cert.notes && ...}`. Sem este plano, o certificado exigido pela D-13 ("o dossiê público mostra referência/hash e download quando permitido") simplesmente não aparece para ninguém.
- **D-22:** "Notas internas completas ficam visíveis para certificadora, admin, produtor e tesouraria." Hoje o único endpoint de trilha é `GET /certifier/projects/{id}/history`, guardado por `require_role("certifier","admin")` — o produtor não entra. O dossiê público foi deliberadamente minimizado pelo 04-02 e não tem `notes`. Logo, o produtor não tem NENHUM caminho para as notas internas do próprio projeto.

Output: duas rotas novas em `backend_app/modules/projects/routes.py`, um helper de leitura no Supabase Storage, os tipos e o cliente de download em `src/services/database.ts` e o bloco de certificado + histórico público em `src/pages/Dashboard/MrcaDetails.tsx`.

Este plano é o "04-07" referenciado pelo plano 04-02 (remoção do código morto de `cert.notes`) e substitui a referência ao inexistente "04-08" do plano 04-06 (verificação manual da UI, agora na seção `<verification>` deste plano).

Restrições não negociáveis:
- O bucket `projects` é PRIVADO (`supabase/migrations/202605260006_supabase_storage_buckets.sql`: `('projects', 'projects', false)`). Não existe URL pública utilizável para o PDF do certificado — o download tem que passar pelo backend com service role. Não inventar URL pública, não tornar o bucket público.
- Não reintroduzir `notes` no dossiê público. A minimização do 04-02 é um controle de segurança (ASVS V8), não uma preferência de UX.
- Nenhum mock runtime novo no frontend. Nenhuma URL absoluta hardcoded — usar `API_BASE_URL` de `src/services/api.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-certification-workbench/04-CONTEXT.md
@.planning/phases/04-certification-workbench/04-PATTERNS.md
@.planning/phases/04-certification-workbench/04-UI-SPEC.md
@.planning/phases/04-certification-workbench/04-02-SUMMARY.md
@.planning/phases/04-certification-workbench/04-05-SUMMARY.md
@.planning/phases/04-certification-workbench/04-06-SUMMARY.md

<interfaces>
<!-- Contratos já existentes ou entregues pelos planos 04-01..04-06. Usar diretamente; não explorar o codebase atrás deles. -->

`backend_app/modules/projects/service.py` (entregue no plano 04-02):
```python
CERTIFICATION_HISTORY_ACTIONS: tuple[str, ...]
CERTIFICATION_HISTORY_LABELS: dict[str, str]
PUBLIC_CERTIFICATION_HISTORY_ACTIONS: frozenset[str]
CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE = "CERTIFICATION_CERTIFICATE"

class ProjectsService:
    async def _get_project_model(self, project_id: str) -> Project          # aceita UUID, friendly_id ou source_hash; 404 se não achar
    async def _assert_project_edit_permission(self, project, *, actor_id, actor_role) -> None
    async def certification_history(self, project, *, actions=None, actor_role=None, include_metadata=True) -> list[dict]
    async def certification_certificate(self, project) -> dict | None
    # certificação: {"documentId","sha256","mimeType","sizeBytes","uploadedAt","storagePath",
    #                "storageBucket","storageObjectPath","filename","downloadAvailable"}

def certification_item(certification: Certification) -> dict[str, Any]
    # visão INTERNA: {"id","methodology","creditPotential","decision","notes","signedDocumentHash","signedAt","createdAt"}
```

`backend_app/modules/projects/service.py` (pré-existente, linhas 991-1009):
```python
async def _assert_project_edit_permission(self, project, *, actor_id, actor_role) -> None:
    if actor_role == "admin":
        return
    profile = await self._actor_profile(actor_id)          # 401 se não houver perfil
    organization_id = profile.organization_id
    allowed = False
    if organization_id is not None and actor_role == "producer":
        allowed = organization_id in {project.producer_organization_id, project.developer_organization_id}
    elif organization_id is not None and actor_role == "certifier":
        allowed = organization_id == project.certifier_organization_id
    if not allowed:
        raise HTTPException(403, detail="Seu perfil não pode editar este projeto")
```

`backend_app/core/roles.py`:
```python
UserRole = Literal["producer", "auditor", "company", "certifier", "admin"]
# NÃO existe papel "treasury". A tesouraria opera hoje sob "admin"
# (ver backend_app/modules/treasury/routes.py, guardado por require_role("admin")).
# Logo, "certificadora, admin, produtor e tesouraria" da D-22 mapeia para
# require_role("producer", "certifier", "admin") + _assert_project_edit_permission.
```

`backend_app/core/security.py`:
```python
def optional_user(authorization: str | None = Header(default=None)) -> AuthenticatedUser | None:
    if not authorization:
        return None
    return require_user(authorization)
# já importado em backend_app/modules/projects/routes.py:11
```

`backend_app/modules/supabase_storage.py` (estado atual):
```python
@dataclass(frozen=True)
class SupabaseStorageClient:
    supabase_url: str
    service_role_key: str
    public_url: str | None = None
    def upload_object(self, bucket, object_path, content, content_type) -> None
    def copy_object(self, bucket, source_object_path, destination_object_path) -> None
    def public_object_url(self, bucket, object_path) -> str
    def _object_url(self, bucket, object_path) -> str
    def _open(self, request) -> None          # consome e DESCARTA o corpo da resposta

class SupabaseStorageError(RuntimeError): ...
def get_supabase_storage_client(settings=None) -> SupabaseStorageClient | None   # None quando não há credenciais
async def upload_storage_object(bucket, object_path, content, content_type) -> bool
```
Não existe leitura de objeto — é o que a Task 2 acrescenta.

`backend_app/modules/projects/routes.py` (padrões a repetir):
```python
from backend_app.core.security import AuthenticatedUser, optional_user      # linha 11

@router.get("/projects/{project_id}/public-dossier", response_model=ProjectPublicDossierResponse)
async def get_project_public_dossier(project_id: str, session: AsyncSession = Depends(get_session)):
    return await ProjectsService(session).get_public_dossier(project_id)     # rota ANÔNIMA

@router.get("/projects/{project_id}/pendencies", response_model=ProjectPendenciesResponse)   # plano 04-05
async def list_project_pendencies(
    project_id: str,
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
):
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)
    ...
```

`src/services/api.ts`:
```typescript
export const API_BASE_URL: string;              // '/api/v1' por padrão
async function request<T>(path, options): Promise<T | null>
// ATENÇÃO: request() sempre faz response.json() e chama clearAuthSession() em 401.
// Download binário NÃO pode passar por apiGet — precisa de fetch cru com o mesmo header Bearer.
```

`src/services/database.ts` (estado atual, linhas 1-2 e 104-128):
```typescript
import { type ProjectMRCA, type InventoryItem } from '../data/mrca_db';
import { apiGet, apiPatch } from './api';

export type ProjectPublicDossier = {
    success: boolean;
    project: ProjectMRCA;
    tags: Array<Record<string, any>>;
    baseline: Record<string, any> | null;
    certifications: Array<Record<string, any>>;
    audits: Array<Record<string, any>>;
    documents: ProjectDossierDocument[];
    credits: Array<Record<string, any>>;
    transactions: TransactionRecord[];
    chainEvents: Array<Record<string, any>>;
};

getProjectPublicDossier: async (id: string): Promise<ProjectPublicDossier> => { ... }   // linha 292
```

Contrato do dossiê público APÓS o plano 04-02:
```
GET /projects/{id}/public-dossier   (anônimo)
 -> { success, project, tags, baseline,
      certifications: [{ id, methodology, creditPotential, decision, signedDocumentHash, signedAt, createdAt }],  // SEM notes
      audits, documents: [...apenas CERTIFICATION_CERTIFICATE...], credits, transactions, chainEvents,
      certificate: { documentId, sha256, mimeType, sizeBytes, uploadedAt, storagePath, storageBucket,
                     storageObjectPath, filename, downloadAvailable } | null,
      certificationHistory: [{ id, action, label, createdAt, ... }] }   // sem metadata/beforeData/afterData
```

`tests/test_certifier_workbench.py` (criado no plano 04-01 — helpers a reutilizar, não reescrever):
```python
client = TestClient(app)
PDF_BYTES = b"%PDF-1.4\n..."
CERTIFIER = ("certificadora@sinarca.com.br", "certificadora")
PRODUCER  = ("produtor@sinarca.com.br", "produtor")
COMPANY   = ("empresa@sinarca.com.br", "empresa")
def auth_headers(email: str, password: str) -> dict[str, str]
def create_certifiable_project(prefix: str | None = None) -> dict[str, object]
def approve_payload(**overrides: object) -> dict[str, object]
```

`src/pages/Dashboard/MrcaDetails.tsx` (âncoras exatas):
- linha 71-75: `const EmptyState = ({ text }) => (...)` — reutilizar sem modificar
- linha 200-219: nav de abas (`overview | integrity | documents | transactions`)
- linha 250-262: linha do tempo (`w-9 h-9 rounded-full bg-primary/10 text-primary` + eyebrow `text-[10px] font-bold uppercase tracking-[0.2em]`)
- linha 339-361: aba `documents` -> bloco "Certificações" (alvo desta fase)
- linha 354: `{cert.notes && <p className="text-sm text-gray-500 mt-4">{cert.notes}</p>}` — CÓDIGO MORTO após o 04-02
- `MrcaDetails` é montado em DUAS rotas (`src/App.tsx:72` público `projeto/:id`, `src/App.tsx:139` interno `mrca/:id`) — o mesmo componente atende visitante anônimo e usuário logado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endpoint GET /projects/{id}/certification-history com trilha interna para o produtor (D-22)</name>
  <files>backend_app/modules/projects/routes.py, backend_app/modules/projects/schemas.py, tests/test_certifier_workbench.py</files>
  <read_first>
    - backend_app/modules/projects/routes.py (linhas 1-40: imports; linhas 216-224: rota do dossiê público; a rota `/projects/{project_id}/pendencies` criada pelo plano 04-05 — copiar o padrão de guard dela)
    - backend_app/modules/projects/service.py (linhas 991-1009 `_assert_project_edit_permission`; os métodos `certification_history` e `certification_certificate` criados no 04-02; a função `certification_item`)
    - backend_app/modules/projects/schemas.py (onde o 04-05 adicionou `CertificationHistoryResponse` e `ProjectPendenciesResponse` — o novo schema entra ao lado, sem substituí-los)
    - backend_app/core/roles.py (arquivo inteiro, 35 linhas — confirmar que NÃO existe papel "treasury")
    - tests/test_certifier_workbench.py (helpers `auth_headers`, `create_certifiable_project`, `approve_payload`, constantes CERTIFIER/PRODUCER/COMPANY)
    - .planning/phases/04-certification-workbench/04-CONTEXT.md (D-20, D-21, D-22)
  </read_first>
  <behavior>
    - `GET /api/v1/projects/{friendlyId}/certification-history` com token do PRODUTOR dono → 200, e `certifications[0]` contém a chave `notes` com o texto interno da certificadora
    - Mesma rota com token de `company` (organização diferente) → 403
    - Mesma rota SEM token → 401 (`require_role` já rejeita antes do handler)
    - Mesma rota com token da certificadora do projeto → 200
    - Projeto inexistente → 404
    - `events` vem em ordem cronológica crescente e cada item traz `action`, `label` PT-BR, `actorRole`, `createdAt` e `metadata` (trilha INTERNA, com metadata — ao contrário do dossiê público)
    - `?event_type=CERTIFICATION_APPROVED` filtra por ação; `?actor_role=certifier` filtra por papel
  </behavior>
  <action>
1. Em `backend_app/modules/projects/schemas.py`, adicionar (NÃO alterar `CertificationHistoryResponse`, que pertence à rota da certificadora criada no 04-05):

```python
class ProjectCertificationHistoryResponse(BaseModel):
    success: bool = True
    total: int
    events: list[dict[str, Any]]
    certifications: list[dict[str, Any]] = []
    certificate: dict[str, Any] | None = None
    availableEventTypes: list[dict[str, str]] = []
    availableActorRoles: list[str] = []
```
(`Any` já é importado no arquivo; confirmar antes de escrever.)

2. Em `backend_app/modules/projects/routes.py`, adicionar a rota abaixo LOGO APÓS `get_project_public_dossier` (linha ~222), mantendo tudo o mais intacto:

```python
@router.get("/projects/{project_id}/certification-history", response_model=ProjectCertificationHistoryResponse)
async def get_project_certification_history(
    project_id: str,
    event_type: str | None = Query(default=None),
    actor_role: str | None = Query(default=None),
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> ProjectCertificationHistoryResponse:
    """Trilha INTERNA de certificação do projeto (D-22).

    A rota do módulo da certificadora (`/certifier/projects/{id}/history`) é fechada em
    require_role("certifier", "admin"). Esta rota existe para o produtor dono do projeto,
    que a D-22 lista explicitamente entre quem vê notas internas completas, junto com
    certificadora, admin e tesouraria — e a tesouraria opera hoje sob o papel `admin`,
    porque `backend_app/core/roles.py` não define papel `treasury`.
    O acesso NÃO é garantido só pelo papel: `_assert_project_edit_permission` restringe
    à organização dona (producer/developer) ou à certificadora do projeto.
    """
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(
        project, actor_id=current_user.id, actor_role=current_user.role
    )

    all_events = await service.certification_history(project)
    events = all_events
    if event_type:
        events = [event for event in events if event["action"] == event_type.upper()]
    if actor_role:
        events = [event for event in events if (event["actorRole"] or "") == actor_role.lower()]

    certifications = (
        await session.execute(
            select(Certification)
            .where(Certification.project_id == project.id)
            .order_by(Certification.created_at.desc())
        )
    ).scalars().all()

    available_types = sorted({event["action"] for event in all_events})
    return ProjectCertificationHistoryResponse(
        total=len(events),
        events=events,
        certifications=[certification_item(item) for item in certifications],
        certificate=await service.certification_certificate(project),
        availableEventTypes=[
            {"value": action, "label": CERTIFICATION_HISTORY_LABELS.get(action, action)}
            for action in available_types
        ],
        availableActorRoles=sorted({event["actorRole"] for event in all_events if event["actorRole"]}),
    )
```

Imports a acrescentar em `projects/routes.py`:
- `Certification` de `backend_app.db.models` (o arquivo hoje importa só `Document`; o plano 04-05 já acrescentou `CertificationPendency` — somar, não substituir)
- `ProjectCertificationHistoryResponse` no bloco de `backend_app.modules.projects.schemas`
- `certification_item` e `CERTIFICATION_HISTORY_LABELS` de `backend_app.modules.projects.service`
- `Query` de `fastapi` já está importado (linha 6); `select` de `sqlalchemy` já está importado (linha 7)

`certification_item` é o serializador INTERNO — ele inclui `notes`. É exatamente o que a D-22 exige aqui. NÃO usar `public_certification_item` nesta rota.

3. Em `tests/test_certifier_workbench.py`, acrescentar ao final do arquivo (sem tocar nos testes existentes):

```python
def test_certification_history_visible_to_producer() -> None:
    """D-22: produtor dono ve a trilha interna completa, incluindo notas da certificadora."""
    project = create_certifiable_project("hist")
    friendly_id = str(project["friendlyId"])

    decision = client.patch(
        f"/api/v1/certifier/projects/{friendly_id}/decision",
        data=approve_payload(
            decision="REQUEST_CHANGES",
            rejection_category="DOCUMENTACAO_INCOMPLETA",
            notes="NOTA INTERNA DA CERTIFICADORA",
        ),
        headers=auth_headers(*CERTIFIER),
    )
    assert decision.status_code == 200, decision.text

    owner = client.get(
        f"/api/v1/projects/{friendly_id}/certification-history",
        headers=auth_headers(*PRODUCER),
    )
    assert owner.status_code == 200, owner.text
    body = owner.json()
    assert body["certifications"], "produtor deve ver as certificacoes do proprio projeto"
    assert "notes" in body["certifications"][0]
    assert "NOTA INTERNA DA CERTIFICADORA" in owner.text
    assert body["events"] == sorted(body["events"], key=lambda event: event["createdAt"])

    outsider = client.get(
        f"/api/v1/projects/{friendly_id}/certification-history",
        headers=auth_headers(*COMPANY),
    )
    assert outsider.status_code == 403

    anonymous = client.get(f"/api/v1/projects/{friendly_id}/certification-history")
    assert anonymous.status_code == 401

    missing = client.get(
        "/api/v1/projects/PRC-INEXISTENTE-0000/certification-history",
        headers=auth_headers(*PRODUCER),
    )
    assert missing.status_code == 404
```
Se `approve_payload` não aceitar `rejection_category` como override, montar o `data` literal no teste — o contrato do endpoint de decisão é do plano 04-03; ler o `04-03-SUMMARY.md` para os nomes de campo reais antes de escrever o teste.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_certification_history_visible_to_producer -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'router.get("/projects/{project_id}/certification-history"' backend_app/modules/projects/routes.py` == 1
    - `grep -c "class ProjectCertificationHistoryResponse" backend_app/modules/projects/schemas.py` == 1
    - `grep -c "certification_item" backend_app/modules/projects/routes.py` >= 1
    - `grep -v '^#' backend_app/modules/projects/routes.py | grep -c "_assert_project_edit_permission"` >= 3 (pendências do 04-05 + esta rota)
    - `grep -c "public_certification_item" backend_app/modules/projects/routes.py` == 0 (a rota é interna, não minimizada)
    - `grep -c "def test_certification_history_visible_to_producer" tests/test_certifier_workbench.py` == 1
    - `uv run pytest tests/test_certifier_workbench.py::test_certification_history_visible_to_producer -x` passa
    - `uv run pytest tests/contract -q` sem novas falhas
  </acceptance_criteria>
  <done>Produtor dono, certificadora do projeto e admin leem a trilha interna completa com `notes`; qualquer outro perfil recebe 403.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Download condicional do certificado a partir do bucket privado (D-13)</name>
  <files>backend_app/modules/supabase_storage.py, backend_app/modules/projects/routes.py, tests/test_certifier_workbench.py</files>
  <read_first>
    - backend_app/modules/supabase_storage.py (arquivo inteiro — 170 linhas; atenção a `_open`, que descarta o corpo, e a `upload_storage_object`, que devolve `False` quando não há credenciais)
    - backend_app/modules/projects/routes.py (a rota criada na Task 1 deste plano; linha 11 já importa `optional_user`)
    - backend_app/modules/projects/service.py (`certification_certificate` do plano 04-02 — chaves `storageBucket`, `storageObjectPath`, `mimeType`, `filename`)
    - supabase/migrations/202605260006_supabase_storage_buckets.sql (linha com `('projects', 'projects', false)` — o bucket é privado)
    - tests/conftest.py (fixture autouse `isolate_optional_storage_env`, que limpa as variáveis do Supabase Storage em TODO teste)
  </read_first>
  <behavior>
    - `GET /api/v1/projects/{friendlyId}/certificate` SEM token → 403 com a mensagem de restrição (nunca 401, para não disparar `clearAuthSession()` no cliente)
    - Mesma rota com token de `company` de outra organização → 403 com a mesma mensagem
    - Mesma rota com token do produtor dono, com storage disponível → 200, `content-type` do documento e header `Content-Disposition: attachment`
    - Projeto sem certificado anexado → 404
    - Storage sem credenciais configuradas → 502 (falha fechada, nunca 200 com corpo vazio)
  </behavior>
  <action>
1. Em `backend_app/modules/supabase_storage.py`, adicionar a leitura de objeto.

Dentro de `SupabaseStorageClient`, logo depois de `copy_object`:
```python
    def download_object(self, bucket: str, object_path: str) -> bytes:
        request = urllib.request.Request(
            self._object_url(bucket, object_path),
            method="GET",
            headers={
                "Authorization": f"Bearer {self.service_role_key}",
                "Apikey": self.service_role_key,
            },
        )
        return self._read(request)
```
E, ao lado de `_open` (que descarta o corpo e por isso não serve aqui):
```python
    def _read(self, request: urllib.request.Request) -> bytes:
        try:
            with urllib.request.urlopen(request, timeout=30) as response:
                return response.read()
        except urllib.error.HTTPError as exc:
            body = exc.read().decode("utf-8", errors="replace")
            raise SupabaseStorageError(f"Supabase Storage respondeu {exc.code}: {body}") from exc
        except urllib.error.URLError as exc:
            raise SupabaseStorageError(f"Supabase Storage indisponível: {exc.reason}") from exc
```

No nível do módulo, ao lado de `upload_storage_object`:
```python
async def download_storage_object(bucket: str, object_path: str) -> bytes | None:
    """Le um objeto privado usando a service role. Devolve None quando nao ha credenciais."""
    client = get_supabase_storage_client()
    if client is None:
        return None
    try:
        return await asyncio.to_thread(client.download_object, bucket, object_path)
    except SupabaseStorageError as exc:
        raise HTTPException(
            status_code=status.HTTP_502_BAD_GATEWAY,
            detail="Não foi possível ler o certificado no Supabase Storage.",
        ) from exc
```
`asyncio`, `HTTPException` e `status` já estão importados no arquivo.

2. Em `backend_app/modules/projects/routes.py`, adicionar a constante e a rota logo após a rota da Task 1:

```python
CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL = (
    "Download do certificado disponível apenas para o produtor, a certificadora e a "
    "administração do projeto."
)


@router.get("/projects/{project_id}/certificate")
async def download_project_certificate(
    project_id: str,
    current_user: AuthenticatedUser | None = Depends(optional_user),
    session: AsyncSession = Depends(get_session),
) -> Response:
    """Download do certificado da certificação (D-13, "quando permitido").

    O bucket `projects` é privado (supabase/migrations/202605260006_...): não existe URL
    pública utilizável, então o arquivo é servido pelo backend com a service role.
    Falhas de permissão devolvem 403 (e não 401) de propósito: o cliente `src/services/api.ts`
    limpa a sessão em 401, e esta rota é linkada a partir do dossiê PÚBLICO, onde o visitante
    legitimamente não tem token.
    """
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)

    if current_user is None:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN, detail=CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL
        )
    try:
        await service._assert_project_edit_permission(
            project, actor_id=current_user.id, actor_role=current_user.role
        )
    except HTTPException as exc:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN, detail=CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL
        ) from exc

    certificate = await service.certification_certificate(project)
    if certificate is None or not certificate.get("storageObjectPath"):
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="Certificado ainda não anexado a este projeto.",
        )

    content = await download_storage_object(
        str(certificate["storageBucket"]), str(certificate["storageObjectPath"])
    )
    if content is None:
        raise HTTPException(
            status_code=status.HTTP_502_BAD_GATEWAY,
            detail="Certificado indisponível: Supabase Storage não está configurado.",
        )

    filename = str(certificate.get("filename") or f"certificado-{project.friendly_id}.pdf")
    safe_filename = PurePath(filename).name.replace('"', "")
    return Response(
        content=content,
        media_type=str(certificate.get("mimeType") or "application/pdf"),
        headers={"Content-Disposition": f'attachment; filename="{safe_filename}"'},
    )
```
Imports a acrescentar: `Response` de `fastapi`; `download_storage_object` de `backend_app.modules.supabase_storage`. `PurePath` já está importado (linha 4) e `optional_user` já está importado (linha 11).

O `try/except HTTPException` é deliberado: `_assert_project_edit_permission` levanta 401 quando não existe perfil para o `actor_id` e 403 quando a organização não bate. Nesta rota as duas situações são a mesma coisa do ponto de vista do chamador — "você não pode baixar" — e devem produzir o mesmo 403 com a mesma cópia.

3. Em `tests/test_certifier_workbench.py`, acrescentar:

```python
def test_certificate_download_requires_project_membership(monkeypatch) -> None:
    """D-13: referencia e hash sao publicos, o arquivo so sai para quem participa do projeto."""
    project = create_certifiable_project("cert")
    friendly_id = str(project["friendlyId"])

    async def fake_download(bucket: str, object_path: str) -> bytes:
        return PDF_BYTES

    monkeypatch.setattr(
        "backend_app.modules.projects.routes.download_storage_object", fake_download
    )

    anonymous = client.get(f"/api/v1/projects/{friendly_id}/certificate")
    assert anonymous.status_code == 403
    assert "produtor" in anonymous.json()["detail"]

    outsider = client.get(
        f"/api/v1/projects/{friendly_id}/certificate", headers=auth_headers(*COMPANY)
    )
    assert outsider.status_code == 403

    without_certificate = client.get(
        f"/api/v1/projects/{friendly_id}/certificate", headers=auth_headers(*PRODUCER)
    )
    assert without_certificate.status_code == 404

    approval = client.patch(
        f"/api/v1/certifier/projects/{friendly_id}/decision",
        data=approve_payload(),
        files={"certificate": ("certificado.pdf", PDF_BYTES, "application/pdf")},
        headers=auth_headers(*CERTIFIER),
    )
    assert approval.status_code == 200, approval.text

    owner = client.get(
        f"/api/v1/projects/{friendly_id}/certificate", headers=auth_headers(*PRODUCER)
    )
    assert owner.status_code == 200, owner.text
    assert owner.content == PDF_BYTES
    assert "attachment" in owner.headers["content-disposition"]
```
Se a aprovação exigir storage real (o `isolate_optional_storage_env` limpa as credenciais e o plano 04-03 pode falhar fechado no upload), monkeypatchar também `backend_app.modules.certifier.routes.upload_storage_object` para uma corrotina que devolve `True` — ler `04-03-SUMMARY.md` para o nome exato do símbolo importado lá antes de escrever essa parte.
  </action>
  <verify>
    <automated>uv run pytest tests/test_certifier_workbench.py::test_certificate_download_requires_project_membership -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def download_storage_object" backend_app/modules/supabase_storage.py` == 1
    - `grep -c "def download_object" backend_app/modules/supabase_storage.py` == 1
    - `grep -c "def _read" backend_app/modules/supabase_storage.py` == 1
    - `grep -c 'router.get("/projects/{project_id}/certificate"' backend_app/modules/projects/routes.py` == 1
    - `grep -c "CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL" backend_app/modules/projects/routes.py` == 3
    - `grep -c "Content-Disposition" backend_app/modules/projects/routes.py` == 1
    - `grep -c "public_object_url" backend_app/modules/projects/routes.py` == 0 (bucket privado: nada de URL pública para o PDF)
    - `grep -c "def test_certificate_download_requires_project_membership" tests/test_certifier_workbench.py` == 1
    - `uv run pytest tests/test_certifier_workbench.py -x` — todos os testes do arquivo passam
    - `uv run pytest -q` sem falhas novas
  </acceptance_criteria>
  <done>O PDF do certificado sai do bucket privado somente para produtor dono, certificadora do projeto ou admin; anônimo e terceiro recebem 403 com a cópia exata, e a ausência de credenciais falha fechada em 502.</done>
</task>

<task type="auto">
  <name>Task 3: Certificado e histórico público no dossiê (MrcaDetails + database.ts)</name>
  <files>src/services/database.ts, src/pages/Dashboard/MrcaDetails.tsx</files>
  <read_first>
    - src/services/database.ts (linhas 1-2 imports; 104-128 `ProjectPublicDossier`/`ProjectDossierDocument`; 292-298 `getProjectPublicDossier`)
    - src/services/api.ts (arquivo inteiro, 79 linhas — confirmar `API_BASE_URL`, e que `request()` faz `response.json()` e `clearAuthSession()` em 401)
    - src/pages/Dashboard/MrcaDetails.tsx (arquivo inteiro, 495 linhas — em especial 26-33 formatadores, 71-75 `EmptyState`, 250-262 linha do tempo, 339-361 bloco "Certificações")
    - .planning/phases/04-certification-workbench/04-UI-SPEC.md (linha 140 da Component Inventory: "Public dossier certificate section")
    - .planning/phases/04-certification-workbench/04-CONTEXT.md (D-13, D-20)
  </read_first>
  <action>
### 3.1 `src/services/database.ts`

1. Trocar o import da linha 2 para incluir `API_BASE_URL` (manter os símbolos já usados):
```typescript
import { API_BASE_URL, apiGet, apiPatch } from './api';
```

2. Adicionar os tipos, imediatamente acima de `ProjectPublicDossier` (linha ~104):
```typescript
export type ProjectCertificateReference = {
    documentId: string;
    sha256: string;
    mimeType: string;
    sizeBytes: number;
    uploadedAt: string;
    storagePath: string;
    storageBucket?: string;
    storageObjectPath?: string | null;
    filename: string | null;
    downloadAvailable: boolean;
};

export type ProjectPublicCertificationEvent = {
    id: string;
    action: string;
    label: string;
    createdAt: string;
};
```

3. Acrescentar dois campos a `ProjectPublicDossier` (sem remover nenhum dos existentes):
```typescript
    certificate: ProjectCertificateReference | null;
    certificationHistory: ProjectPublicCertificationEvent[];
```

4. Adicionar a função de download logo depois de `getProjectPublicDossier` (linha ~298):
```typescript
    downloadProjectCertificate: async (id: string): Promise<{ blob: Blob; filename: string }> => {
        // NAO usar apiGet: request() sempre faz response.json() e limpa a sessao em 401.
        // Aqui o corpo e binario e o 403 e um resultado legitimo para visitante anonimo.
        const token = localStorage.getItem('sinarca_token');
        const headers = new Headers();
        if (token) headers.set('Authorization', `Bearer ${token}`);

        let response: Response;
        try {
            response = await fetch(`${API_BASE_URL}/projects/${encodeURIComponent(id)}/certificate`, {
                method: 'GET',
                headers,
            });
        } catch {
            throw new Error('API indisponível. Verifique sua conexão ou se o serviço local está ativo.');
        }

        if (!response.ok) {
            let detail = '';
            try {
                detail = (await response.json())?.detail || '';
            } catch {
                detail = '';
            }
            throw new Error(detail || `Não foi possível baixar o certificado (HTTP ${response.status}).`);
        }

        const disposition = response.headers.get('Content-Disposition') || '';
        const match = /filename="?([^";]+)"?/i.exec(disposition);
        return { blob: await response.blob(), filename: match?.[1] || `certificado-${id}.pdf` };
    },
```
A mensagem de 403 vem do `detail` do servidor — é a mesma string de `CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL` (Task 2). Não duplicar a cópia no frontend.

### 3.2 `src/pages/Dashboard/MrcaDetails.tsx`

1. Imports: acrescentar `Download` e `FileCheck2` à lista de `lucide-react` (ordem alfabética, como o arquivo já faz) e `type ProjectPublicCertificationEvent` ao import de `../../services/database`.

2. Estado novo, junto aos existentes (linhas 80-83):
```typescript
const [certificateError, setCertificateError] = useState('');
const [certificateLoading, setCertificateLoading] = useState(false);
```

3. Handler, logo abaixo do `useEffect` de carga:
```typescript
const handleCertificateDownload = async () => {
    if (!id) return;
    setCertificateError('');
    setCertificateLoading(true);
    try {
        const { blob, filename } = await database.downloadProjectCertificate(id);
        const url = URL.createObjectURL(blob);
        const anchor = document.createElement('a');
        anchor.href = url;
        anchor.download = filename;
        document.body.appendChild(anchor);
        anchor.click();
        anchor.remove();
        URL.revokeObjectURL(url);
    } catch (err) {
        setCertificateError(err instanceof Error ? err.message : 'Não foi possível baixar o certificado.');
    } finally {
        setCertificateLoading(false);
    }
};
```

4. **Remover o código morto da linha 354**: apagar por completo `{cert.notes && <p className="text-sm text-gray-500 mt-4">{cert.notes}</p>}`. Após o plano 04-02 o serializador público não devolve mais `notes` (D-20/D-22); a linha nunca renderiza e sugere, falsamente, que o dossiê público exibiria notas internas.

5. **Estender o bloco "Certificações"** (aba `documents`, linhas 339-361). Dentro do mesmo `div.bg-white.p-8.rounded-2xl.border.border-gray-100.shadow-sm`, logo ABAIXO da lista de `dossier.certifications`, acrescentar o cartão do certificado. Reutilizar a casca `p-5 rounded-xl border border-gray-100 bg-gray-50` já usada pelos itens de certificação:
```jsx
{dossier.certificate && (
    <div className="mt-6 p-5 rounded-xl border border-gray-100 bg-gray-50">
        <div className="flex items-start justify-between gap-4">
            <div className="min-w-0">
                <p className="text-[10px] text-gray-400 font-bold uppercase tracking-widest">Certificado da certificação</p>
                <p className="text-sm font-black uppercase mt-1 truncate">
                    {dossier.certificate.filename || 'Certificado assinado'}
                </p>
                <p className="text-xs text-gray-500 mt-1">
                    {dossier.certificate.mimeType} - {Number(dossier.certificate.sizeBytes || 0).toLocaleString('pt-BR')} bytes - {formatDate(dossier.certificate.uploadedAt)}
                </p>
                <p className="text-[10px] text-gray-400 font-bold uppercase tracking-widest mt-4">Hash SHA-256</p>
                <p className="text-[10px] font-mono text-primary break-all">{dossier.certificate.sha256}</p>
                <p className="text-[10px] text-gray-400 font-bold uppercase tracking-widest mt-3">Referência de arquivo</p>
                <p className="text-[10px] font-mono text-gray-500 break-all">{dossier.certificate.storagePath}</p>
            </div>
            <FileCheck2 className="w-5 h-5 text-primary shrink-0" />
        </div>
        {dossier.certificate.downloadAvailable && (
            <button
                onClick={handleCertificateDownload}
                disabled={certificateLoading}
                className="mt-5 inline-flex items-center gap-2 px-4 py-3 rounded-xl bg-black text-white text-[10px] font-bold uppercase tracking-widest disabled:opacity-40"
            >
                <Download className="w-4 h-4" />
                {certificateLoading ? 'Preparando download...' : 'Baixar certificado'}
            </button>
        )}
        {certificateError && (
            <p className="mt-4 text-sm font-medium text-gray-500">{certificateError}</p>
        )}
    </div>
)}
```
Regras da D-13 que este bloco implementa: referência (`storagePath`) e hash (`sha256`) são SEMPRE públicos; o download é condicional — o botão só aparece quando `downloadAvailable`, e a permissão real é decidida no servidor (Task 2), com a recusa exibida como texto inline em vez de link quebrado.

6. **Adicionar o histórico público de certificação**, como um bloco novo na mesma aba `documents`, entre "Certificações" e "Auditorias e laudos". Copiar a casca do bloco "Eventos registrados" (linhas 247-267) e a linha do tempo (250-262):
```jsx
<div className="bg-white p-8 rounded-2xl border border-gray-100 shadow-sm">
    <h3 className="text-sm font-bold text-black uppercase tracking-widest mb-6">Histórico de certificação</h3>
    {(dossier.certificationHistory ?? []).length > 0 ? (
        <div className="space-y-6">
            {(dossier.certificationHistory ?? []).map((event: ProjectPublicCertificationEvent) => (
                <div key={event.id} className="flex gap-5">
                    <div className="w-9 h-9 rounded-full bg-primary/10 text-primary flex items-center justify-center shrink-0">
                        <CheckCircle2 className="w-5 h-5" />
                    </div>
                    <div>
                        <p className="text-[10px] font-bold text-primary uppercase tracking-[0.2em]">
                            {event.action} · {formatDate(event.createdAt)}
                        </p>
                        <h4 className="text-base font-bold text-black mt-1">{event.label}</h4>
                    </div>
                </div>
            ))}
        </div>
    ) : (
        <EmptyState text="Nenhuma decisão pública de certificação registrada para este projeto." />
    )}
</div>
```
D-20 é o limite deste bloco: renderizar APENAS `action`, `label` e `createdAt`. NÃO renderizar `metadata`, `beforeData`, `afterData`, `actorProfileId` nem `actorRole` — o backend já minimiza a lista (`PUBLIC_CERTIFICATION_HISTORY_ACTIONS`, `include_metadata=False` no plano 04-02), e o frontend não deve reexibir nada além disso mesmo que um campo extra apareça na resposta.

Manter o `text-primary`/`bg-primary` nesta tela: `MrcaDetails.tsx` é o dossiê PÚBLICO e legitimamente usa o token de marca (04-UI-SPEC.md, linha 133). A paleta emerald/amber/red é exclusiva das telas internas de operação.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cert.notes" src/pages/Dashboard/MrcaDetails.tsx` == 0
    - `grep -c "Certificado da certificação" src/pages/Dashboard/MrcaDetails.tsx` == 1
    - `grep -c "Histórico de certificação" src/pages/Dashboard/MrcaDetails.tsx` == 1
    - `grep -c "downloadProjectCertificate" src/pages/Dashboard/MrcaDetails.tsx` == 1
    - `grep -c "certificationHistory" src/pages/Dashboard/MrcaDetails.tsx` >= 2
    - `grep -c "event.metadata\|beforeData\|afterData\|actorProfileId" src/pages/Dashboard/MrcaDetails.tsx` == 0
    - `grep -c "Nenhuma decisão pública de certificação registrada para este projeto." src/pages/Dashboard/MrcaDetails.tsx` == 1
    - `grep -c "downloadProjectCertificate" src/services/database.ts` == 1
    - `grep -c "certificationHistory" src/services/database.ts` == 1
    - `grep -c "ProjectCertificateReference" src/services/database.ts` == 2
    - `grep -c "API_BASE_URL" src/services/database.ts` == 2
    - `grep -c "http://\|https://" src/services/database.ts` == 0
    - `grep -c "text-emerald-600\|bg-emerald-600" src/pages/Dashboard/MrcaDetails.tsx` == 0 (dossiê público mantém o token de marca)
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <done>Dossiê público exibe referência, hash e download condicional do certificado, além da linha do tempo de decisões finais — sem nenhuma nota interna e sem código morto de `cert.notes`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet anônima → `GET /projects/{id}/certificate` | Rota linkada a partir do dossiê público; entrega bytes de um bucket PRIVADO |
| produtor autenticado → `GET /projects/{id}/certification-history` | Papel `producer` é amplo; a trilha traz `notes` internas de UMA organização |
| backend (service role) → Supabase Storage | A chave service role ignora RLS; qualquer `object_path` construído a partir de entrada do usuário viraria leitura arbitrária |
| navegador → blob do certificado | Nome de arquivo vem de `metadata.filename`, controlado por quem fez o upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Information Disclosure | `GET /projects/{id}/certification-history` | mitigate | `require_role("producer","certifier","admin")` NÃO basta: `_assert_project_edit_permission` restringe à organização produtora/desenvolvedora ou à certificadora do projeto; teste cobre 403 para `company` e 401 para anônimo |
| T-04-26 | Information Disclosure | `GET /projects/{id}/certificate` | mitigate | `optional_user` + checagem org-scoped; anônimo e terceiro recebem 403 com `CERTIFICATE_DOWNLOAD_FORBIDDEN_DETAIL`; o bucket permanece privado e nenhuma URL pública é gerada para o PDF |
| T-04-27 | Elevation of Privilege | `download_storage_object` com service role | mitigate | `bucket`/`object_path` vêm exclusivamente de `certification_certificate(project)` (linha de `documents` já filtrada por `project_id` + `document_type`), nunca de parâmetro de rota — não há path traversal possível |
| T-04-28 | Tampering | `Content-Disposition` do download | mitigate | `PurePath(filename).name` remove diretórios e aspas são removidas antes de interpolar no header, impedindo header injection / response splitting via nome de arquivo |
| T-04-29 | Denial of Service | leitura síncrona do objeto em thread | accept | `asyncio.to_thread` com timeout de 30s espelha `upload_object`; o tamanho é limitado por `MAX_UPLOAD_BYTES` no momento do upload — nenhum arquivo maior pode existir |
| T-04-30 | Information Disclosure | histórico público em `MrcaDetails.tsx` | mitigate | Renderiza apenas `action`, `label` e `createdAt`; a lista já vem filtrada por `PUBLIC_CERTIFICATION_HISTORY_ACTIONS` e sem `metadata` (plano 04-02); acceptance criteria proíbe por grep qualquer referência a `metadata`/`beforeData`/`afterData`/`actorProfileId` no arquivo |
| T-04-31 | Repudiation | download de certificado sem rastro | accept | Leitura sem efeito colateral, já autorizada e escopada por organização; o vínculo certificado↔decisão já está auditado por `CERTIFICATION_CERTIFICATE_ATTACHED` (plano 04-03). Registrar audit event por download inflaria a tabela sem ganho de trilha |
</threat_model>

<verification>
### Automatizada

- `uv run pytest tests/test_certifier_workbench.py -x` — todos os testes do arquivo verdes, incluindo os dois novos
- `uv run pytest -q` — suíte completa sem falhas
- `npm run build` — código 0
- `npx tsc -b` pode continuar bloqueado por dívida legada fora do escopo (registrada em STATE.md); não regredir o número de erros
- Com a API local rodando:
  - `curl -s http://127.0.0.1:8000/api/v1/projects/PRC-2024-002/public-dossier | jq 'has("certificate") and has("certificationHistory")'` → `true`
  - `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/api/v1/projects/PRC-2024-002/certificate` → `403`
  - `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/api/v1/projects/PRC-2024-002/certification-history` → `401`

### Manual (absorve a verificação que o plano 04-06 remetia a um plano inexistente)

Não existe framework de teste de frontend neste repositório (ver `04-VALIDATION.md`), então as verificações abaixo são manuais e fecham a fase:

1. **Bancada da certificadora (D-01/D-02, plano 04-06):** `npm run dev`, entrar como certificadora, abrir `/painel/certificadora`, expandir um card da fila e percorrer as seis abas — Resumo, QTAGs / Geofence, Documentos, Cálculo, Decisão e Histórico. Todas renderizam sem erro no console.
2. **Duas filas e contador (D-10):** solicitar ajustes em um projeto e conferir que ele sai da "Fila de decisão", aparece em "Aguardando retorno do produtor" e o contador âmbar incrementa.
3. **Certificado no dossiê interno e público (D-13):** aprovar um projeto com upload de PDF; conferir a referência do certificado na aba Documentos de `CertifierReview.tsx` e, em `/projeto/{friendlyId}`, o cartão "Certificado da certificação" com hash e referência.
4. **Download condicional (D-13):** na rota pública `/projeto/{friendlyId}`, deslogado, clicar em "Baixar certificado" → aparece inline a mensagem "Download do certificado disponível apenas para o produtor, a certificadora e a administração do projeto."; logado como o produtor dono (rota `/painel/.../mrca/{friendlyId}`), o mesmo botão baixa o PDF.
5. **Minimização pública (D-20):** com uma certificação cujas `notes` internas existam, confirmar que a string não aparece em nenhum lugar de `/projeto/{friendlyId}` (busca no DOM) e que o bloco "Histórico de certificação" mostra apenas decisões finais.
</verification>

<success_criteria>
- Produtor dono, certificadora do projeto e admin (papel sob o qual a tesouraria opera hoje) leem a trilha interna completa com notas — D-22 fechada
- Produtor de outra organização e papel `company` recebem 403 na trilha interna e no download
- Dossiê público exibe referência e hash do certificado e a linha do tempo de decisões finais — D-13 e D-20 fechadas em `MrcaDetails.tsx`
- Download do certificado funciona a partir do bucket privado, com falha fechada em 502 quando o Storage não está configurado
- `cert.notes` não existe mais em `MrcaDetails.tsx`
- Nenhum plano da fase referencia um plano inexistente
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04-certification-workbench/04-07-SUMMARY.md`
</output>
</content>
</invoke>
