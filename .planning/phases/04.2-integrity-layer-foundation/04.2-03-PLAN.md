---
phase: 04.2-integrity-layer-foundation
plan: 03
type: execute
wave: 3
depends_on: ["04.2-02"]
files_modified:
  - backend_app/modules/integrity/service.py
  - backend_app/modules/integrity/schemas.py
  - backend_app/modules/integrity/routes.py
  - backend_app/modules/projects/service.py
  - tests/test_project_conflicts.py
autonomous: true
requirements: [INTG-03]
user_setup: []

must_haves:
  truths:
    - "Dois projetos com geometrias sobrepostas geram linhas de Conflict GEOSPATIAL_OVERLAP nos dois projetos, com severidade derivada do percentual"
    - "A severidade segue os limiares configuraveis (CLEAR/INFO/LOW/MEDIUM/HIGH/CRITICAL)"
    - "Editar a geometria de um projeto ate deixar de sobrepor marca o Conflict anterior como RESOLVED, sem duplicar linhas"
    - "DOUBLE_CLAIM so e avaliado entre pares que ja tem GEOSPATIAL_OVERLAP"
    - "GET /api/v1/projects/{id}/boundary-overlaps continua com o mesmo contrato de resposta da Phase 04.1"
  artifacts:
    - path: "backend_app/modules/integrity/service.py"
      provides: "overlap_severity + detect_and_persist_conflicts + list_conflicts"
      contains: "def overlap_severity"
    - path: "tests/test_project_conflicts.py"
      provides: "Integracao com PostGIS real cobrindo severidade e re-derivacao"
      min_lines: 120
  key_links:
    - from: "backend_app/modules/integrity/service.py"
      to: "ProjectsService.detect_boundary_overlaps"
      via: "chamada Python, sem reescrever o SQL de ST_Intersects"
      pattern: "detect_boundary_overlaps"
    - from: "backend_app/modules/projects/service.py"
      to: "IntegrityService.detect_and_persist_conflicts"
      via: "hook em create_project e no ramo de tags de update_project"
      pattern: "detect_and_persist_conflicts"
---

<objective>
Interpretar o overlap geoespacial que a Phase 04.1 deixou deliberadamente sem interpretacao: transformar cada sobreposicao em uma linha `Conflict` com severidade `CLEAR -> CRITICAL` (D-09/D-10), re-derivada a cada mudanca de `active_boundary` (D-11/D-20/D-23), e avaliar `DOUBLE_CLAIM` apenas entre pares ja sobrepostos (D-12).

Purpose: `Conflict` e o sinal de risco mais pesado do Risk Engine (Plan 04) e a unica deteccao automatica de fraude fundiaria que o Sinarca tem hoje.
Output: `overlap_severity` (funcao pura), `detect_and_persist_conflicts` (idempotente), rota `GET /projects/{id}/conflicts` e teste de integracao com PostGIS real.

**Nao faz parte desta plan:** risk score / Auto Hold (Plan 04), dossie publico (Plan 05). Nao chamar `recalculate_risk_score` aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-RESEARCH.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-PATTERNS.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-02-SUMMARY.md

<interfaces>
Contrato JA EXISTENTE a reutilizar sem reescrever (backend_app/modules/projects/service.py:1071-1127). O SQL usa pre-filtro GiST e cast ::geography; duplica-lo seria dobrar o custo e permitir divergencia:

```python
async def detect_boundary_overlaps(self, project_id: str) -> list[dict[str, Any]]:
    """... Esta fase NAO interpreta o overlap: nao cria Conflict, nao atribui
    severidade e nao bloqueia nada. Isso e Phase 04.2 (INTG-03)."""
    return [
        {
            "relatedProjectId": row["related_project_id"],          # uuid como str
            "relatedProjectFriendlyId": row["related_project_friendly_id"],
            "relatedProjectName": row["related_project_name"],
            "overlapAreaHa": round(float(row["overlap_area_ha"] or 0.0), 4),
            "overlapPercentage": round(float(row["overlap_percentage"] or 0.0), 4),          # % da area DESTE projeto
            "overlapPercentageOfRelated": round(float(row["overlap_percentage_of_related"] or 0.0), 4),  # % da area do OUTRO
        }
        for row in rows
    ]
```

Colunas de `Conflict` (Plan 01): `id, project_id, related_project_id, type, severity, overlap_percentage, overlap_area_ha, status, detected_at, resolved_at, metadata_, created_at, updated_at`.
Unique index `conflicts_project_related_type_idx (project_id, related_project_id, type)` — a chave logica de upsert.

Limiares configuraveis (backend_app/core/config.py, Plan 01):
`integrity_overlap_severity_info_pct = 1.0`, `_low_pct = 5.0`, `_medium_pct = 20.0`, `_high_pct = 50.0`, `_critical_pct = 50.0`, lidos via `get_settings()`.

Constantes: `CONFLICT_SEVERITIES = ("CLEAR","INFO","LOW","MEDIUM","HIGH","CRITICAL")`, `CONFLICT_STATUSES = ("OPEN","RESOLVED")`, `CONFLICT_TYPES` inclui `GEOSPATIAL_OVERLAP` e `DOUBLE_CLAIM`.

Pontos de gancho (linhas conferidas em 2026-08-16):
- `projects/service.py:879` — `await self.persist_project_boundary(project, payload.tags)` em `create_project` (a chamada de `create_origination_claims` da Plan 02 ja esta logo abaixo).
- `projects/service.py:970-971` — dentro de `if payload.tags is not None:` em `update_project`:
  ```python
              await self.session.flush()
              await self.persist_project_boundary(project, payload.tags)
  ```

Campos de `Project` usados na regra de DOUBLE_CLAIM: `methodology` (str) e `vintage` (str, ano).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Severidade de overlap e re-derivacao de Conflict no IntegrityService</name>
  <files>backend_app/modules/integrity/service.py, backend_app/modules/integrity/schemas.py, backend_app/modules/integrity/routes.py</files>
  <read_first>
    - backend_app/modules/projects/service.py linhas 1071-1128 (detect_boundary_overlaps — o contrato exato do dict retornado)
    - backend_app/modules/integrity/service.py (arquivo inteiro, escrito na Plan 02 — onde os metodos novos entram e como o audit event e emitido)
    - backend_app/modules/integrity/routes.py e schemas.py (Plan 02 — shape das rotas e DTOs)
    - backend_app/core/config.py (campos integrity_overlap_severity_*)
  </read_first>
  <action>
**1. Funcao pura de severidade** — adicionar no nivel de modulo de `backend_app/modules/integrity/service.py` (fora da classe, para ser testavel sem sessao):

```python
def overlap_severity(percentage: float, settings: Settings | None = None) -> str:
    """Severidade do overlap (D-10 / Bible secao 16), limiares configuraveis.

    0%           -> CLEAR
    >0% ate 1%   -> INFO
    >1% ate 5%   -> LOW
    >5% ate 20%  -> MEDIUM
    >20% ate 50% -> HIGH
    >50%         -> CRITICAL
    """
    config = settings or get_settings()
    if percentage <= 0:
        return "CLEAR"
    if percentage <= config.integrity_overlap_severity_info_pct:
        return "INFO"
    if percentage <= config.integrity_overlap_severity_low_pct:
        return "LOW"
    if percentage <= config.integrity_overlap_severity_medium_pct:
        return "MEDIUM"
    if percentage <= config.integrity_overlap_severity_high_pct:
        return "HIGH"
    return "CRITICAL"
```

**2. `async def detect_and_persist_conflicts(self, project: Project, *, actor_id: str | None = None, actor_role: str | None = None) -> list[Project]`** — retorna a lista de projetos relacionados afetados (a Plan 04 vai usar isso para recalcular o risco deles). Algoritmo exato:

a) Import local de `ProjectsService` dentro do metodo e `overlaps = await ProjectsService(self.session).detect_boundary_overlaps(str(project.id))`. **Proibido escrever qualquer `ST_Intersects` novo neste arquivo** (RESEARCH Pitfall 1).

b) Montar o conjunto desejado de pares, sempre nos DOIS sentidos, para que o projeto sobreposto tambem enxergue o conflito:
   - `(project.id, related_id, "GEOSPATIAL_OVERLAP")` com `overlap_percentage = item["overlapPercentage"]` e `overlap_area_ha = item["overlapAreaHa"]`;
   - `(related_id, project.id, "GEOSPATIAL_OVERLAP")` com `overlap_percentage = item["overlapPercentageOfRelated"]` e a mesma area.
   - severidade de cada linha = `overlap_severity(<o percentual daquela linha>)`.

c) DOUBLE_CLAIM (D-12): para cada par que ja esta na lista de overlap (nunca escanear o banco inteiro), carregar o `Project` relacionado e avaliar
   `same_attribute = related.methodology == project.methodology` e `same_period = related.vintage == project.vintage`.
   Se ambos verdadeiros, acrescentar ao conjunto desejado os dois sentidos com `type="DOUBLE_CLAIM"`, mesmos percentuais/area, `severity = overlap_severity(<percentual daquela linha>)` e `metadata_={"methodology": project.methodology, "vintage": project.vintage, "basis": "GEOSPATIAL_OVERLAP"}`.
   Comentario obrigatorio no codigo citando D-12 (evita comparacao O(n^2) global).

d) Carregar as linhas existentes: `select(Conflict).where(or_(Conflict.project_id == project.id, Conflict.related_project_id == project.id))`.

e) Reconciliar (D-20), sem nunca deletar:
   - par existente que NAO esta no conjunto desejado e esta `OPEN` -> `status="RESOLVED"`, `resolved_at=now`, `updated_at=now`, audit `CONFLICT_RESOLVED`;
   - par existente que ESTA no conjunto desejado -> atualizar `overlap_percentage`, `overlap_area_ha`, `severity`, `status="OPEN"`, `resolved_at=None`, `updated_at=now`; emitir audit `CONFLICT_SEVERITY_CHANGED` somente se a severidade mudou;
   - par desejado sem linha existente -> `session.add(Conflict(..., status="OPEN", detected_at=now))` + audit `CONFLICT_DETECTED`.
   - `metadata_` de GEOSPATIAL_OVERLAP: `{"related_friendly_id": item["relatedProjectFriendlyId"], "overlap_percentage_of_related": item["overlapPercentageOfRelated"]}`.
   - Sempre `await self.session.flush()` ao final; nunca `commit()` (a transacao pertence ao chamador).
   - Audit events com `entity_type="conflicts"`, `entity_id=conflict.id`, `actor_role=actor_role`, `metadata={"actor_external_id": actor_id, "project_id": str(<project_id da linha>), "related_project_id": str(<related>), "type": ..., "severity": ..., "overlap_percentage": ...}`.

f) Idempotencia obrigatoria: chamar o metodo duas vezes seguidas com a mesma geometria nao pode alterar a contagem de linhas nem os valores (nao usar `detected_at=now` no caminho de update).

**3. `async def list_conflicts(self, project: Project, *, status_filter: str | None = None) -> list[dict[str, Any]]`** — retorna `{"id", "type", "severity", "status", "overlapPercentage", "overlapAreaHa", "relatedProjectId", "relatedProjectFriendlyId", "detectedAt", "resolvedAt"}` das linhas com `project_id == project.id`, ordenadas por `severity` (CRITICAL primeiro, usando a ordem de `CONFLICT_SEVERITIES` invertida) e depois `detected_at desc`. `relatedProjectFriendlyId` sai de `metadata_.get("related_friendly_id")` (evita join extra).

**4. Rota** em `backend_app/modules/integrity/routes.py`: `GET /projects/{project_id}/conflicts` -> `ProjectConflictsResponse` (novo DTO em `schemas.py`, mesmo shape dos outros: `success`, `project_id`, `total`, `conflicts: list[dict[str, Any]]`), com o MESMO guard das rotas da Plan 02 (`require_role("producer","certifier","admin")` + `_assert_project_edit_permission`). Aceitar query param opcional `status` (`OPEN`/`RESOLVED`) validado contra `CONFLICT_STATUSES` com HTTP 400 em valor invalido. Nenhuma rota de escrita.

**Proibicoes:** nao alterar `detect_boundary_overlaps` nem o response model de `GET /projects/{id}/boundary-overlaps` (contrato congelado por testes da Phase 04.1); nao criar job/batch; nao chamar `recalculate_risk_score`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && python -c "
from backend_app.modules.integrity.service import overlap_severity as s
assert s(0) == 'CLEAR'
assert s(0.5) == 'INFO'
assert s(1.0) == 'INFO'
assert s(3.0) == 'LOW'
assert s(5.0) == 'LOW'
assert s(12.0) == 'MEDIUM'
assert s(20.0) == 'MEDIUM'
assert s(25.0) == 'HIGH'
assert s(50.0) == 'HIGH'
assert s(75.0) == 'CRITICAL'
assert s(100.0) == 'CRITICAL'
from backend_app.main import app
paths = {r.path for r in app.routes}
assert '/api/v1/projects/{project_id}/conflicts' in paths
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "st_intersects" backend_app/modules/integrity/service.py` retorna 0
    - `grep -c "detect_boundary_overlaps" backend_app/modules/integrity/service.py` retorna 1
    - `grep -c "session.delete\|delete(Conflict)" backend_app/modules/integrity/service.py` retorna 0 (D-20 resolve, nao deleta)
    - `grep -c "@router.post\|@router.put\|@router.patch\|@router.delete" backend_app/modules/integrity/routes.py` retorna 0
    - `git diff backend_app/modules/projects/service.py` nao mostra alteracao entre as linhas 1071-1128 (detect_boundary_overlaps intacto)
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Severidade e re-derivacao de Conflict implementadas sobre a query existente, expostas por rota org-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Ganchos de Conflict na criacao e na edicao de geometria</name>
  <files>backend_app/modules/projects/service.py</files>
  <read_first>
    - backend_app/modules/projects/service.py linhas 778-895 (create_project, com o hook de Claims da Plan 02 ja presente) e 895-996 (update_project, com o ramo `if payload.tags is not None:`)
    - backend_app/modules/integrity/service.py (Task 1 — assinatura de detect_and_persist_conflicts)
  </read_first>
  <action>
**Gancho 1 — `create_project`.** Logo apos a chamada `await self.integrity.create_origination_claims(project, actor_id=actor_id, actor_role=actor_role)` (inserida na Plan 02, na sequencia de `persist_project_boundary`), adicionar:

```python
        # Phase 04.2 / INTG-03 (D-09/D-11): interpreta o overlap ja calculado
        # pela Phase 04.1 e persiste Conflict com severidade, de forma sincrona.
        await self.integrity.detect_and_persist_conflicts(project, actor_id=actor_id, actor_role=actor_role)
```

**Gancho 2 — `update_project`, ramo de geometria (D-23).** Dentro de `if payload.tags is not None:`, imediatamente apos `await self.persist_project_boundary(project, payload.tags)` (linha 971):

```python
            # D-23: editar a geometria muda o overlap; Conflict e RE-DERIVADO
            # (resolve o que deixou de intersectar, atualiza o que continua).
            await self.integrity.detect_and_persist_conflicts(project, actor_id=actor_id, actor_role=actor_role)
```

Nao adicionar o gancho fora desse `if` — sem mudanca de tags nao ha mudanca de geometria.
Nao mexer em `persist_project_boundary` nem em nenhuma outra parte de `update_project`.
Nao chamar `recalculate_risk_score` (Plan 04).
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/test_project_boundaries.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "detect_and_persist_conflicts" backend_app/modules/projects/service.py` retorna 2
    - `grep -n "detect_and_persist_conflicts" backend_app/modules/projects/service.py` mostra as duas ocorrencias apos linhas contendo `persist_project_boundary`
    - `grep -c "recalculate_risk_score" backend_app/modules/projects/service.py` retorna 0
    - `pytest tests/test_project_boundaries.py -x -q` passa (inclusive `test_project_update_replaces_boundary_without_duplicating_row`)
  </acceptance_criteria>
  <done>Conflict sincronizado na criacao e em toda edicao de geometria, na mesma transacao.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de integracao de Conflict contra PostGIS real</name>
  <files>tests/test_project_conflicts.py</files>
  <read_first>
    - tests/test_project_boundaries.py linhas 170-232 (client TestClient, PRODUCER, auth_headers, project_payload) e 466-600 (uuid_hex, rectangle_tags, test_boundary_overlap_detection, test_boundary_overlap_excludes_disjoint_projects)
    - backend_app/modules/integrity/service.py (Task 1)
  </read_first>
  <action>
Criar `tests/test_project_conflicts.py`. `tests/` nao tem `__init__.py`, entao import estilo pacote nao resolve — copiar verbatim os helpers `client`, `PRODUCER`, `auth_headers`, `project_payload`, `uuid_hex`, `rectangle_tags` de `tests/test_project_boundaries.py` (mesmo fallback ja documentado na Phase 04.1), com um comentario no topo explicando a duplicacao.

Testes obrigatorios:

1. `test_overlap_generates_conflict_with_medium_severity`: dois retangulos deslocados meio span em cada eixo (overlap de ~25%, exatamente o fixture de `test_boundary_overlap_detection`); apos criar os dois projetos, `GET /api/v1/projects/{a}/conflicts` retorna 200 e contem uma linha com `relatedProjectFriendlyId == b`, `type == "GEOSPATIAL_OVERLAP"`, `status == "OPEN"`, `severity == "HIGH"` (25% cai na faixa >20-50) e `24.0 < overlapPercentage < 26.0`.

2. `test_conflict_is_mirrored_on_the_related_project`: no mesmo cenario, `GET /api/v1/projects/{b}/conflicts` tambem traz uma linha apontando para `a` (o projeto sobreposto enxerga o conflito).

3. `test_full_overlap_is_critical`: dois projetos com o MESMO retangulo -> `severity == "CRITICAL"` e `overlapPercentage > 99`.

4. `test_disjoint_projects_generate_no_conflict`: retangulos a ~550 km de distancia -> nenhuma linha com `relatedProjectFriendlyId` do outro em `/conflicts` (filtrar por id, nunca por `total`, porque outros projetos de teste compartilham o retangulo fixo de `project_payload`).

5. `test_conflict_is_resolved_when_geometry_moves_away`: cria A e B sobrepostos, confirma Conflict `OPEN`, depois `PUT /api/v1/projects/{a}` (mesma rota de update ja usada em `test_project_update_replaces_boundary_without_duplicating_row`) com `tags` de um retangulo distante; assere que a linha para B agora esta `status == "RESOLVED"` com `resolvedAt` nao nulo, e que NAO existe linha duplicada para o mesmo `relatedProjectFriendlyId` (contagem == 1) — prova de D-20.

6. `test_conflict_resync_is_idempotent`: repetir o mesmo `PUT` com as mesmas tags duas vezes e assere que a contagem total de conflitos do projeto nao muda entre as duas chamadas.

7. `test_double_claim_only_for_overlapping_pairs`: os dois projetos sobrepostos usam o mesmo `project_type`/metodologia e o mesmo vintage (padrao de `project_payload`) -> existe linha `type == "DOUBLE_CLAIM"` para o par; e o projeto disjunto do teste 4 NAO tem nenhuma linha `DOUBLE_CLAIM` (D-12).

8. `test_boundary_overlaps_contract_is_unchanged`: `GET /api/v1/projects/{a}/boundary-overlaps` continua retornando as chaves exatas `relatedProjectId, relatedProjectFriendlyId, relatedProjectName, overlapAreaHa, overlapPercentage, overlapPercentageOfRelated` e nao ganhou campo `severity` (a severidade e aditiva, em outro endpoint).

9. `test_conflicts_endpoint_requires_authentication`: sem header -> 401 (mesmo padrao de `test_boundary_overlaps_requires_authentication`).

Usar `base_lat = -10.0 - random.random()` / `base_lng = -48.0 - random.random()` em todos os cenarios para isolar de projetos de testes anteriores.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/test_project_conflicts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_project_conflicts.py -x -q` coleta 9 testes e todos passam
    - `grep -c "def test_" tests/test_project_conflicts.py` retorna 9
    - `pytest tests/test_project_boundaries.py -x -q` continua passando (contrato da Phase 04.1 intacto)
    - `pytest tests/ -x -q` passa (suite completa)
  </acceptance_criteria>
  <done>INTG-03 provado contra PostGIS real: severidade correta, espelhamento, resolucao e idempotencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizacao A -> GET /projects/{id}/conflicts | A linha de Conflict nomeia o projeto de terceiro (friendly_id) e o quanto ele sobrepoe — informacao competitivamente sensivel |
| edicao de geometria -> estado de Conflict | Um produtor poderia tentar "limpar" conflitos editando a geometria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-11 | Information Disclosure | GET /projects/{id}/conflicts | mitigate | Mesmo guard de `/boundary-overlaps`: `require_role("producer","certifier","admin")` + `_assert_project_edit_permission` org-scoped (precedente T-041-11); teste 9 cobre o 401 e a Plan 02 ja cobre o 403 cross-org do mesmo guard |
| T-04.2-12 | Tampering | apagar conflitos editando geometria | mitigate | Conflict nunca e deletado: vira `RESOLVED` com `resolved_at` e audit `CONFLICT_RESOLVED`; criterio grep proibe `session.delete`; o par continua inspecionavel e a Plan 04 mantem o historico em `risk_assessments` |
| T-04.2-13 | Denial of Service | deteccao de conflito em cada escrita de geometria | mitigate | Reuso do `ST_Intersects` com pre-filtro GiST (nenhuma query nova); DOUBLE_CLAIM avaliado apenas sobre os pares ja retornados pelo overlap (D-12), nunca varredura global O(n^2) |
| T-04.2-14 | Repudiation | mudanca de severidade sem trilha | mitigate | `create_audit_event` em `CONFLICT_DETECTED`, `CONFLICT_SEVERITY_CHANGED` e `CONFLICT_RESOLVED`, com `entity_type="conflicts"` |
</threat_model>

<verification>
1. `pytest tests/test_project_conflicts.py -x -q` — severidade, espelhamento, resolucao e DOUBLE_CLAIM verdes
2. `pytest tests/test_project_boundaries.py -x -q` — contrato de `/boundary-overlaps` intacto
3. `pytest tests/ -x -q` — suite completa verde
</verification>

<success_criteria>
- Conflict persistido nos dois sentidos com severidade correta para 25%, 100% e 0% de overlap
- Re-derivacao sem duplicar linha e sem deletar historico
- DOUBLE_CLAIM restrito a pares sobrepostos com mesma metodologia e mesmo vintage
- Endpoint `/boundary-overlaps` da Phase 04.1 inalterado
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04.2-integrity-layer-foundation/04.2-03-SUMMARY.md` com: assinatura final de `detect_and_persist_conflicts` (a Plan 04 usa o retorno para recalcular risco dos projetos afetados), shape do JSON de `/conflicts` e severidades observadas nos fixtures de teste.
</output>
