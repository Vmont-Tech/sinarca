---
phase: 04.2
slug: integrity-layer-foundation
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-16
---

# Phase 04.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (async tests via `asyncio.run(...)` wrapping, not `pytest-asyncio` markers — see `tests/test_project_boundaries.py`) |
| **Config file** | none found — tests run against a live local Supabase Postgres via `get_sessionmaker()` |
| **Quick run command** | `pytest tests/<new_or_touched_file>.py -x` |
| **Full suite command** | `pytest tests/` |
| **Estimated runtime** | ~90 seconds (mirrors Phase 04.1, requires local Supabase + PostGIS running) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/<new_or_touched_file>.py -x`
- **After every plan wave:** Run `pytest tests/`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.2-01-xx | 01 | 1 | INTG-01 | T-04.2-xx | Claim never receives client-supplied `status`/`confidenceScore` on creation | contract (HTTP) + schema | `pytest tests/contract/test_backend_app_api_v1.py -k claim -x` | ❌ W0 | ⬜ pending |
| 04.2-01-xx | 01 | 1 | INTG-02 | T-04.2-xx | `Evidence.hash` always equals `documents.sha256_hash`, never recomputed/spoofable | contract (HTTP) + db | `pytest tests/db/test_integrity_schema.py -k evidence -x` | ❌ W0 | ⬜ pending |
| 04.2-02-xx | 02 | 2 | INTG-03 | T-04.2-xx | Conflict/Risk endpoints reuse `_assert_project_edit_permission` org-scoping (no cross-org leak) | integration (live PostGIS) | `pytest tests/test_project_conflicts.py -x` | ❌ W0 | ⬜ pending |
| 04.2-02-xx | 02 | 2 | INTG-04 | T-04.2-xx | `risk_score` recompute is idempotent (pure function, no cumulative drift) | unit (pure `risk_engine.compute_signals`) + integration | `pytest tests/test_risk_engine.py -x` | ❌ W0 | ⬜ pending |
| 04.2-03-xx | 03 | 2/3 | INTG-05 | T-04.2-xx | Public dossier never serializes raw internal `risk_signals` verbatim (minimization allowlist) | contract (HTTP public-dossier) | `pytest tests/contract/test_frontend_project_links.py -k integrity -x` | ❌ W0 (extend existing) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Exact Task IDs are assigned by the planner; this table is the requirement→test contract the planner's tasks must satisfy, per `04.2-RESEARCH.md` "Phase Requirements → Test Map".*

---

## Wave 0 Requirements

- [ ] `tests/db/test_integrity_schema.py` — schema contract tests for `claims`/`evidence`/`conflicts`/`risk_assessments` migrations, mirroring `tests/db/test_project_boundaries_schema.py`'s static-SQL-assertion style
- [ ] `tests/test_project_conflicts.py` — live-Postgres integration test asserting severity thresholds against real `ST_Intersects` fixtures, mirroring `tests/test_project_boundaries.py`
- [ ] `tests/test_risk_engine.py` — pure unit tests for the signal-weighting function, no DB required
- [ ] Extend `tests/contract/test_backend_app_api_v1.py` with Claim/Evidence/Conflict/integrity endpoint contract tests, following the existing `auth_headers()`/`client.post("/api/v1/...")` pattern
- [ ] No new framework install needed — pytest + `TestClient` + `get_sessionmaker()` already fully wired

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Vocabulário de status público (Declared/Verified/Under Review/On Hold/Suspended/Revoked) legível e nunca "Certified" isolado no dossiê | INTG-05 | Julgamento visual/copy — não é uma asserção mecânica de string única, envolve revisão de UX do texto explicativo do risk score | Abrir `/projeto/:id` de um projeto seed com `integrity_status` variado e conferir que o texto nunca mostra só "Certified" e que a explicação do risk score é legível por humano |

*Any other behavior in this phase has automated verification per the Per-Task Verification Map above.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** verified 2026-08-18 via `04.2-VERIFICATION.md`; scoped integrity tests passed (`59 passed, 32 deselected`), frontend integrity contracts passed (`5 passed, 13 deselected`), and UAT/security artifacts are complete.
