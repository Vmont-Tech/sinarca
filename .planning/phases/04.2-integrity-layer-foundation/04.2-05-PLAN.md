---
phase: 04.2-integrity-layer-foundation
plan: 05
type: execute
wave: 5
depends_on: ["04.2-04"]
files_modified:
  - backend_app/modules/projects/service.py
  - backend_app/modules/projects/schemas.py
  - src/services/database.ts
  - src/pages/Dashboard/MrcaDetails.tsx
  - tests/contract/test_backend_app_api_v1.py
  - tests/contract/test_frontend_project_links.py
autonomous: false
requirements: [INTG-05]
user_setup: []

must_haves:
  truths:
    - "O dossie publico devolve um bloco integrity com o vocabulario publico da Bible secao 40"
    - "O dossie publico nunca expoe id, friendly_id ou metadata de projeto de terceiro vindos do Risk Engine"
    - "A pagina publica do projeto mostra o status de integridade ao lado do status operacional"
    - "A aba Integridade mostra o risk score com a lista de razoes legivel em PT-BR"
    - "Nenhum ponto da UI publica mostra um selo de confianca isolado sem o escopo do que foi verificado"
  artifacts:
    - path: "backend_app/modules/projects/service.py"
      provides: "public_integrity_item (allowlist de minimizacao)"
      contains: "def public_integrity_item"
    - path: "src/services/database.ts"
      provides: "Tipo ProjectIntegrityPayload no dossie publico"
      contains: "ProjectIntegrityPayload"
    - path: "src/pages/Dashboard/MrcaDetails.tsx"
      provides: "Pill de integridade + card Integridade e risco"
      contains: "integrityStatusLabel"
  key_links:
    - from: "backend_app/modules/projects/service.py"
      to: "IntegrityService.integrity_summary"
      via: "get_public_dossier monta o campo integrity minimizado"
      pattern: "public_integrity_item"
    - from: "src/pages/Dashboard/MrcaDetails.tsx"
      to: "dossier.integrity"
      via: "render do vocabulario D-16 e da explicacao de risco"
      pattern: "dossier.integrity"
---

<objective>
Fechar INTG-05: o status publico do projeto nunca aparece como um selo isolado. O dossie publico ganha um bloco `integrity` minimizado (vocabulario `Declared/Verified/Under Review/On Hold/Suspended/Revoked`, classe e score com as razoes) e `MrcaDetails.tsx` passa a exibi-lo ao lado do status operacional ja existente.

Purpose: e a superficie onde a mudanca de principio ("dado enviado = declaracao ate validacao") fica visivel para quem consome o dossie publico.
Output: `public_integrity_item` com allowlist explicita, campo `integrity` no `ProjectPublicDossierResponse`, tipo no `database.ts`, pill + card em `MrcaDetails.tsx`, testes de contrato backend e frontend, e um checkpoint humano de leitura do texto.

**Escopo travado (D-17):** NAO construir o Trust Badge completo dos 6 checks (Identity/Land Evidence/Geofence/Overlap/Rights/Independent Audit) — `Independent Audit` depende da Phase 05.1. Apenas texto de status + risco explicado. Nao criar tela nova nem rota nova no frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-PATTERNS.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-04-SUMMARY.md

<interfaces>
Padrao de minimizacao a copiar (backend_app/modules/projects/service.py:2134-2150):

```python
def public_boundary_item(boundary: dict[str, Any] | None) -> dict[str, Any] | None:
    """Visao publica da geometria (D-20/D-22)."""
    if boundary is None:
        return None
    return {
        "declared": boundary["declared"],
        "active": boundary["active"],
        "declaredAreaHa": boundary["declaredAreaHa"],
        "declaredVertexCount": boundary["declaredVertexCount"],
        "activeTier": boundary["activeTier"],
    }
```

Montagem do dossie publico (backend_app/modules/projects/service.py:334-349) — o campo novo entra como irmao de `boundary`:

```python
        return ProjectPublicDossierResponse(
            project=project_dto,
            tags=[tag_item(tag) for tag in tags],
            baseline=baseline_item(baseline),
            boundary=public_boundary_item(await self.boundary_item(str(project.id))),
            certifications=[public_certification_item(item) for item in certifications],
            ...
            certificationHistory=certification_history,
        )
```

DTO a estender (backend_app/modules/projects/schemas.py:96-109):

```python
class ProjectPublicDossierResponse(BaseModel):
    success: bool = True
    project: ProjectMRCA
    tags: list[dict[str, Any]]
    baseline: dict[str, Any] | None = None
    boundary: dict[str, Any] | None = None
    ...
```

`integrity_summary` (Plan 04) devolve a visao INTERNA:
`{"integrityStatus", "publicStatus", "riskScore", "riskClass", "autoHold", "assessedAt", "trigger", "conflictCount", "claimCount", "signals": [{"code","weight","reason","publicSafe"}]}`.

Frontend — tipo a estender (src/services/database.ts:145-160):

```typescript
export type ProjectPublicDossier = {
    success: boolean;
    project: ProjectMRCA;
    tags: Array<Record<string, any>>;
    baseline: Record<string, any> | null;
    boundary: ProjectBoundaryPayload | null;
    certifications: Array<Record<string, any>>;
    ...
};
```

Frontend — idioma de label PT-BR ja usado (src/pages/Dashboard/MrcaDetails.tsx:37-45):

```typescript
const statusLabel = (status: string) => ({
    ACTIVE: 'Ativo',
    AVAILABLE: 'Disponível',
    ...
}[status] || status);
```

Frontend — pills do hero (MrcaDetails.tsx:208-215):

```tsx
<div className="flex flex-wrap items-center gap-3 mb-6">
    <span className="px-4 py-1.5 bg-primary text-white rounded-full text-[10px] font-bold uppercase tracking-[0.2em] shadow-lg">
        {statusLabel(project.status)}
    </span>
    <span className="px-4 py-1.5 bg-white/20 backdrop-blur-md text-white rounded-full text-[10px] font-bold uppercase tracking-[0.2em] border border-white/20">
        Vintage {project.metrics.vintage}
    </span>
</div>
```

Frontend — card da aba Integridade a espelhar (MrcaDetails.tsx:331-363, "Baseline tecnico"), com `EmptyState` (MrcaDetails.tsx:73-77) como fallback.

Vocabulario publico (backend_app/modules/integrity/constants.py, Plan 01):
`PUBLIC_INTEGRITY_STATUSES = ("DECLARED","VERIFIED","UNDER_REVIEW","ON_HOLD","SUSPENDED","REVOKED")` e
`PUBLIC_INTEGRITY_STATUS_BY_INTERNAL` (mapa dos 11 estados internos para esses 6).
`PUBLIC_RISK_SIGNAL_CODES` — allowlist de sinais publicaveis.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco integrity minimizado no dossie publico</name>
  <files>backend_app/modules/projects/service.py, backend_app/modules/projects/schemas.py, tests/contract/test_backend_app_api_v1.py</files>
  <read_first>
    - backend_app/modules/projects/service.py linhas 275-350 (get_public_dossier) e 2121-2165 (public_certification_item/public_boundary_item/public_document_item)
    - backend_app/modules/projects/schemas.py linhas 96-110 (ProjectPublicDossierResponse)
    - backend_app/modules/integrity/service.py (integrity_summary, Plan 04)
    - backend_app/modules/integrity/constants.py (PUBLIC_INTEGRITY_STATUSES, PUBLIC_RISK_SIGNAL_CODES)
  </read_first>
  <action>
**1. `public_integrity_item`** — funcao de modulo em `backend_app/modules/projects/service.py`, ao lado de `public_boundary_item`:

```python
def public_integrity_item(summary: dict[str, Any] | None) -> dict[str, Any] | None:
    """Visao publica da integridade (D-16/D-17 / Bible secao 40 e 42).

    Allowlist explicita, mesmo principio de PUBLIC_DOCUMENT_TYPES: o dossie
    publico recebe o vocabulario de status, a classe/score de risco e as razoes
    em texto — NUNCA o metadata dos sinais (que carrega ids de projetos de
    terceiros), nunca relatedProjectId, nunca o trigger interno.
    """
    if summary is None:
        return None
    signals = [
        {
            "code": signal["code"],
            "weight": signal["weight"],
            "reason": signal["reason"],
        }
        for signal in summary.get("signals", [])
        if signal.get("publicSafe") and signal.get("code") in PUBLIC_RISK_SIGNAL_CODES
    ]
    return {
        "publicStatus": summary["publicStatus"],
        "riskScore": summary["riskScore"],
        "riskClass": summary["riskClass"],
        "conflictCount": summary["conflictCount"],
        "assessedAt": summary["assessedAt"],
        "signals": signals,
    }
```
Regras nao negociaveis: o retorno NAO pode conter `integrityStatus` interno (so o `publicStatus` dos 6 valores), `autoHold`, `trigger`, `claimCount`, nem qualquer `metadata` de sinal. `conflictCount` e apenas contagem — nunca a lista de projetos relacionados.

**2. `get_public_dossier`** — adicionar o campo como irmao de `boundary` (nao criar endpoint novo):
```python
            integrity=public_integrity_item(await self.integrity.integrity_summary(project)),
```

**3. `ProjectPublicDossierResponse`** — adicionar `integrity: dict[str, Any] | None = None` logo apos `boundary`.

**4. Testes de contrato** em `tests/contract/test_backend_app_api_v1.py` (nomes contendo `public_dossier_integrity`):
- `test_public_dossier_integrity_uses_public_vocabulary`: `GET /api/v1/projects/{id}/public-dossier` (SEM header de auth — visitante anonimo) retorna 200 com `integrity.publicStatus` em `{"DECLARED","VERIFIED","UNDER_REVIEW","ON_HOLD","SUSPENDED","REVOKED"}`.
- `test_public_dossier_integrity_is_minimized`: o dicionario `integrity` tem exatamente as chaves `{"publicStatus","riskScore","riskClass","conflictCount","assessedAt","signals"}`; cada item de `signals` tem exatamente `{"code","weight","reason"}`; e o JSON serializado do bloco `integrity` nao contem as substrings `"relatedProject"`, `"metadata"`, `"integrityStatus"`, `"autoHold"` nem `"trigger"`.
- `test_public_dossier_integrity_reasons_are_human_readable`: todo `reason` e string com mais de 10 caracteres e nenhum `reason` contem `"PRC-"` (nenhum friendly_id de terceiro vazou).
- `test_public_dossier_on_hold_project_does_not_show_bare_certified`: para o projeto em Auto Hold criado pelo cenario de geometria identica (mesmo fixture da Plan 04), `integrity.publicStatus == "ON_HOLD"` mesmo que `project.status` seja outro valor operacional.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/contract/test_backend_app_api_v1.py -k "public_dossier_integrity" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def public_integrity_item" backend_app/modules/projects/service.py` retorna 1
    - `grep -c "public_integrity_item" backend_app/modules/projects/service.py` retorna 2 (definicao + uso em get_public_dossier)
    - `grep -q "integrity: dict\[str, Any\] | None = None" backend_app/modules/projects/schemas.py` sai com codigo 0
    - `python -c "from backend_app.modules.projects.service import public_integrity_item as f; out=f({'integrityStatus':'ON_HOLD','publicStatus':'ON_HOLD','riskScore':100,'riskClass':'CRITICAL','autoHold':True,'assessedAt':'x','trigger':'T','conflictCount':2,'claimCount':2,'signals':[{'code':'DOUBLE_CLAIM','weight':60.0,'reason':'+60 x','publicSafe':True,'metadata':{'related':'PRC-1'}}]}); assert set(out)=={'publicStatus','riskScore','riskClass','conflictCount','assessedAt','signals'}, out; assert set(out['signals'][0])=={'code','weight','reason'}; print('ok')"` imprime `ok`
    - `pytest tests/contract/test_backend_app_api_v1.py -x -q` passa sem regressao
  </acceptance_criteria>
  <done>Dossie publico entrega integridade minimizada, sem vazar dados de terceiros nem estado interno.</done>
</task>

<task type="auto">
  <name>Task 2: Vocabulario de integridade e explicacao de risco na pagina publica do projeto</name>
  <files>src/services/database.ts, src/pages/Dashboard/MrcaDetails.tsx, tests/contract/test_frontend_project_links.py</files>
  <read_first>
    - src/services/database.ts linhas 125-175 (ProjectBoundaryPayload e ProjectPublicDossier)
    - src/pages/Dashboard/MrcaDetails.tsx linhas 28-78 (mapas de label e EmptyState), 200-245 (hero e nav de abas), 296-365 (aba Integridade completa)
    - tests/contract/test_frontend_project_links.py (estilo de teste por grep no fonte)
  </read_first>
  <action>
**1. `src/services/database.ts`** — adicionar o tipo e o campo, sem tocar em nada mais:

```typescript
/** Phase 04.2 / INTG-05: visao publica minimizada do Integrity Layer.
 *  publicStatus usa o vocabulario da Bible secao 40 (nunca "Certified" isolado). */
export type ProjectIntegrityPayload = {
    publicStatus: 'DECLARED' | 'VERIFIED' | 'UNDER_REVIEW' | 'ON_HOLD' | 'SUSPENDED' | 'REVOKED';
    riskScore: number | null;
    riskClass: string | null;
    conflictCount: number;
    assessedAt: string | null;
    signals: Array<{ code: string; weight: number; reason: string }>;
};
```
e em `ProjectPublicDossier`, logo apos `boundary: ProjectBoundaryPayload | null;`, adicionar `integrity: ProjectIntegrityPayload | null;`.

**2. `src/pages/Dashboard/MrcaDetails.tsx`:**

a) Ao lado de `statusLabel` (linha 37), adicionar dois mapas de label seguindo o mesmo idioma `{ CODE: 'Label' }[value] || value`:
```typescript
const integrityStatusLabel = (status?: string | null) => ({
    DECLARED: 'Declarado',
    VERIFIED: 'Verificado',
    UNDER_REVIEW: 'Em análise',
    ON_HOLD: 'Em retenção',
    SUSPENDED: 'Suspenso',
    REVOKED: 'Revogado',
}[status || ''] || 'Integridade não avaliada');

const riskClassLabel = (value?: string | null) => ({
    LOW: 'Risco baixo',
    MODERATE: 'Risco moderado',
    HIGH: 'Risco alto',
    VERY_HIGH: 'Risco muito alto',
    CRITICAL: 'Risco crítico',
}[value || ''] || 'Risco ainda não avaliado');
```

b) No bloco de pills do hero (linha 208-215), adicionar uma TERCEIRA pill depois da de Vintage, sem remover nenhuma existente:
```tsx
                                    <span className="px-4 py-1.5 bg-white/20 backdrop-blur-md text-white rounded-full text-[10px] font-bold uppercase tracking-[0.2em] border border-white/20">
                                        Integridade: {integrityStatusLabel(dossier.integrity?.publicStatus)}
                                    </span>
```
Regra INTG-05: a pill operacional continua existindo; nenhuma das duas pode ser exibida como selo de confianca isolado — as duas aparecem sempre juntas.

c) Na aba `activeTab === 'integrity'`, adicionar um card novo como PRIMEIRO filho da `<section>` (antes do card "Vértices / Georreferenciamento"), copiando o shape do card "Baseline tecnico":
```tsx
<div className="bg-white p-8 rounded-2xl border border-gray-100 shadow-sm">
    <h3 className="text-sm font-bold text-black uppercase tracking-widest mb-6">Integridade e risco</h3>
    {dossier.integrity ? (
        <div className="space-y-5">
            <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Status de integridade</p>
                    <p className="font-black text-xl mt-1">{integrityStatusLabel(dossier.integrity.publicStatus)}</p>
                </div>
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Score de risco</p>
                    <p className="font-black text-xl mt-1">
                        {dossier.integrity.riskScore ?? '—'}{dossier.integrity.riskScore !== null ? '/100' : ''}
                    </p>
                    <p className="text-xs text-gray-500 mt-1">{riskClassLabel(dossier.integrity.riskClass)}</p>
                </div>
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Sobreposições detectadas</p>
                    <p className="font-black text-xl mt-1">{dossier.integrity.conflictCount}</p>
                </div>
            </div>
            <p className="text-xs text-gray-500">
                O status de integridade descreve o que já foi verificado sobre este projeto. Ele é independente do
                status operacional e não substitui auditoria independente, ainda não disponível nesta fase.
            </p>
            {dossier.integrity.signals.length > 0 ? (
                <ul className="space-y-2">
                    {dossier.integrity.signals.map((signal) => (
                        <li key={signal.code} className="p-4 rounded-xl bg-amber-50 border border-amber-100 text-sm text-amber-900">
                            {signal.reason}
                        </li>
                    ))}
                </ul>
            ) : (
                <EmptyState text="Nenhum sinal de risco registrado para este projeto." />
            )}
        </div>
    ) : (
        <EmptyState text="Integridade ainda não avaliada para este projeto." />
    )}
</div>
```

Nao remover, reordenar ou renomear nenhum bloco existente da aba. Nao criar aba nova, rota nova ou componente novo. Nao renderizar `signal.code` cru para o usuario (o `reason` ja e a explicacao em PT-BR).

**3. `tests/contract/test_frontend_project_links.py`** — acrescentar, no estilo de grep sobre o fonte ja usado no arquivo:
- `test_public_dossier_renders_integrity_vocabulary`: o fonte de `MrcaDetails.tsx` contem `integrityStatusLabel`, e os seis rotulos `'Declarado'`, `'Verificado'`, `'Em análise'`, `'Em retenção'`, `'Suspenso'`, `'Revogado'`.
- `test_public_dossier_shows_integrity_next_to_operational_status`: o fonte contem `Integridade: {integrityStatusLabel(` e continua contendo `statusLabel(project.status)` (as duas pills coexistem — INTG-05).
- `test_public_dossier_never_renders_bare_certified_badge`: o fonte de `MrcaDetails.tsx` nao contem a string `"Certified"` nem `'SINARCA VERIFIED'` (o Trust Badge dos 6 checks e Phase 05.1 — D-17).
- `test_public_dossier_renders_risk_explanation`: o fonte contem `signal.reason` e `riskClassLabel`, provando que o score nunca aparece sozinho (D-15/Bible secao 42).
- `test_database_service_types_public_integrity`: o fonte de `src/services/database.ts` contem `ProjectIntegrityPayload` e `integrity: ProjectIntegrityPayload | null;`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && pytest tests/contract/test_frontend_project_links.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "integrityStatusLabel" src/pages/Dashboard/MrcaDetails.tsx` >= 3 (definicao + pill + card)
    - `grep -c "statusLabel(project.status)" src/pages/Dashboard/MrcaDetails.tsx` retorna 1 (a pill operacional continua existindo)
    - `grep -c "Certified" src/pages/Dashboard/MrcaDetails.tsx` retorna 0
    - `grep -q "integrity: ProjectIntegrityPayload | null;" src/services/database.ts` sai com codigo 0
    - `npm run build` conclui sem erro (nota: `npx tsc -b` segue bloqueado por divida legada fora do escopo — usar `npm run build`, o mesmo gate das Phases 2-04.1)
    - `pytest tests/contract/test_frontend_project_links.py -x -q` passa com os 5 testes novos
  </acceptance_criteria>
  <done>Dossie publico exibe o vocabulario de integridade junto do status operacional e explica o risco em PT-BR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferencia humana do texto publico de integridade</name>
  <what-built>
Backend: bloco `integrity` minimizado no `GET /api/v1/projects/{id}/public-dossier` (vocabulario publico, score, classe, contagem de sobreposicoes e razoes).
Frontend: terceira pill no hero de `MrcaDetails.tsx` com o status de integridade e card "Integridade e risco" na aba Integridade, com a lista de razoes.
Tudo automatizado e coberto por teste; falta apenas o julgamento de leitura (a unica verificacao marcada como manual no 04.2-VALIDATION.md).
  </what-built>
  <how-to-verify>
1. Com o Supabase local e o backend rodando, subir o frontend (`npm run dev`) e abrir `/projeto/PRC-2024-002` (projeto de seed, sem Claims: deve mostrar "Integridade: Declarado" e "Risco ainda não avaliado" / EmptyState — comportamento honesto esperado).
2. Criar um projeto novo pelo wizard (ou reaproveitar um criado pelos testes) e abrir o dossie publico dele: conferir que a pill de integridade aparece AO LADO da pill de status operacional, nunca sozinha.
3. Abrir a aba "Integridade" e ler o card "Integridade e risco": confirmar que (a) o score nunca aparece sem pelo menos uma razao ou o EmptyState, (b) as razoes estao em PT-BR e sao compreensiveis por um leigo, (c) nenhuma razao cita nome ou ID de outro projeto.
4. Para um projeto em Auto Hold (dois projetos com a mesma geometria), confirmar que o texto exibido e "Em retenção" e que o status operacional continua sendo mostrado separadamente.
5. Confirmar que em nenhum lugar da pagina aparece um selo do tipo "Certificado"/"Certified" isolado, sem explicar o escopo verificado.
  </how-to-verify>
  <action>
Pausar e pedir a conferencia humana descrita em <how-to-verify>. Nao alterar codigo antes da resposta.
Se o usuario pedir ajuste de texto, aplicar SOMENTE nos rotulos PT-BR de `integrityStatusLabel`/`riskClassLabel`
e no paragrafo explicativo do card "Integridade e risco" em `src/pages/Dashboard/MrcaDetails.tsx`, mantendo os
codigos (`DECLARED`, `VERIFIED`, `UNDER_REVIEW`, `ON_HOLD`, `SUSPENDED`, `REVOKED`) intactos e rodando
`pytest tests/contract/test_frontend_project_links.py -x -q` depois — se algum rotulo mudar, atualizar a
asserção correspondente do teste no mesmo commit.
  </action>
  <read_first>
    - src/pages/Dashboard/MrcaDetails.tsx (arquivo modificado na Task 2)
    - .planning/phases/04.2-integrity-layer-foundation/04.2-VALIDATION.md (secao Manual-Only Verifications)
  </read_first>
  <acceptance_criteria>
    - O usuario respondeu "aprovado" ou listou ajustes de texto especificos
    - Se houve ajuste de copy, ele foi aplicado e `pytest tests/contract/test_frontend_project_links.py -x -q` continua verde
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva os ajustes de texto necessarios</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante anonimo -> GET /projects/{id}/public-dossier | Endpoint sem autenticacao; tudo que entra ali e publico para sempre |
| Risk Engine interno -> dossie publico | Os sinais internos carregam metadata com ids de projetos de terceiros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-21 | Information Disclosure | bloco integrity do dossie publico | mitigate | `public_integrity_item` e allowlist explicita (6 chaves; sinal com 3 chaves), nunca serializacao verbatim de `integrity_summary`; teste `test_public_dossier_integrity_is_minimized` assere o conjunto exato de chaves e a ausencia de `relatedProject`/`metadata`/`autoHold`/`trigger` |
| T-04.2-22 | Information Disclosure | reason dos sinais publicados | mitigate | Reasons sao baseados em contagem (garantido na Plan 04) e o teste `test_public_dossier_integrity_reasons_are_human_readable` assere que nenhum contem `PRC-` |
| T-04.2-23 | Spoofing (percepcao de confianca) | UI publica | mitigate | INTG-05: pill de integridade sempre ao lado do status operacional; `grep -c "Certified"` deve ser 0 em MrcaDetails.tsx; checkpoint humano confirma a leitura |
| T-04.2-24 | Information Disclosure | conflictCount | accept | Contagem sem identificacao do terceiro; e exatamente o nivel de transparencia exigido pela Bible secao 40/41 (Overlap Check) e nao permite identificar o projeto vizinho |
</threat_model>

<verification>
1. `pytest tests/contract/test_backend_app_api_v1.py -x -q` — minimizacao do dossie publico verde
2. `pytest tests/contract/test_frontend_project_links.py -x -q` — vocabulario e ausencia de selo isolado verdes
3. `npm run build` — frontend compila
4. `pytest tests/ -x -q` — suite completa verde
5. Checkpoint humano aprovado
</verification>

<success_criteria>
- Dossie publico expoe exatamente 6 chaves em `integrity` e 3 por sinal
- Nenhum dado de projeto de terceiro no payload publico
- Pill de integridade sempre acompanhada do status operacional
- Score sempre acompanhado de razoes ou EmptyState honesto
- Nenhuma ocorrencia de "Certified" isolado na UI publica
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04.2-integrity-layer-foundation/04.2-05-SUMMARY.md` com: shape final do bloco `integrity` publico, textos PT-BR adotados e o resultado do checkpoint humano.
</output>
