---
phase: 04.2-integrity-layer-foundation
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - backend_app/modules/integrity/service.py
  - backend_app/modules/integrity/schemas.py
  - backend_app/modules/integrity/routes.py
  - backend_app/api/router.py
  - backend_app/modules/projects/service.py
  - backend_app/modules/projects/routes.py
  - backend_app/modules/certifier/service.py
  - tests/contract/test_backend_app_api_v1.py
autonomous: true
requirements: [INTG-01, INTG-02]
user_setup: []

must_haves:
  truths:
    - "Criar um projeto via POST /api/v1/projects gera 2 Claims (posse ou propriedade da terra + direito de operar) com status DECLARED e confidenceScore baixo"
    - "Nenhum Claim recebe status ou confidenceScore vindo do cliente"
    - "Todo documento de projeto enviado gera uma Evidence com hash igual a documents.sha256_hash"
    - "Enviar um documento LEGAL_OWNERSHIP reconcilia o Claim de terra de LAND_POSSESSION para LAND_OWNERSHIP"
    - "GET /api/v1/projects/{id}/claims responde 403 para organizacao que nao e dona do projeto"
    - "projects.status nao muda por causa de Claim/Evidence"
  artifacts:
    - path: "backend_app/modules/integrity/service.py"
      provides: "IntegrityService com create_origination_claims e create_evidence_for_document"
      contains: "class IntegrityService"
      min_lines: 220
    - path: "backend_app/modules/integrity/routes.py"
      provides: "GET /projects/{id}/claims e /projects/{id}/evidence org-scoped"
      contains: "/projects/{project_id}/claims"
    - path: "backend_app/modules/integrity/schemas.py"
      provides: "DTOs de resposta"
      contains: "class ProjectClaimsResponse"
  key_links:
    - from: "backend_app/modules/projects/service.py"
      to: "IntegrityService.create_origination_claims"
      via: "hook em create_project apos persist_project_boundary"
      pattern: "create_origination_claims"
    - from: "backend_app/modules/projects/routes.py"
      to: "IntegrityService.create_evidence_for_document"
      via: "hook em upload_project_document apos session.flush()"
      pattern: "create_evidence_for_document"
    - from: "backend_app/api/router.py"
      to: "integrity_router"
      via: "api_router.include_router"
      pattern: "include_router\\(integrity_router\\)"
---

<objective>
Implementar a camada Claim/Evidence: `IntegrityService` cria os Claims de originacao (D-01/D-03/D-18), cria Evidence a partir de cada `Document` reaproveitando `documents.sha256_hash` (D-07/D-08/D-22), e expoe duas rotas internas org-scoped de leitura.

Purpose: e o primeiro dos dois pilares de dados do Integrity Layer — muda o principio de "dado enviado = fato" para "dado enviado = declaracao ate validacao". Sem Claim/Evidence persistidos, o Risk Engine da Plan 04 nao tem sinais para computar.
Output: modulo `integrity` funcional (service + schemas + routes montadas), 4 pontos de gancho no codigo existente e testes de contrato HTTP.

**Nao faz parte desta plan:** Conflict (Plan 03), risk score / Auto Hold (Plan 04), dossie publico e frontend (Plan 05). Nao chamar `recalculate_risk_score` aqui — o metodo ainda nao existe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-RESEARCH.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-PATTERNS.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-01-SUMMARY.md

<interfaces>
Modelos criados na Plan 01 (backend_app/db/models.py) — usar exatamente estes nomes de coluna:

```python
Claim:    id, project_id, claimant_organization_id, claimant_profile_id, type, statement,
          status, confidence_score, valid_from, valid_to, metadata_, created_at, updated_at
Evidence: id, project_id, claim_id, document_id, type, source, source_type, hash,
          validation_method, validation_status, issued_at, expires_at, verified_at,
          metadata_, created_at, updated_at
```

Constantes (backend_app/modules/integrity/constants.py, Plan 01):
`CLAIM_TYPE_LAND_OWNERSHIP`, `CLAIM_TYPE_LAND_POSSESSION`, `CLAIM_TYPE_RIGHT_TO_OPERATE`,
`CLAIM_CONFIDENCE_DECLARED = 10`, `CLAIM_CONFIDENCE_EVIDENCE_PENDING = 30`, `CLAIM_CONFIDENCE_EVIDENCE_VERIFIED = 60`.

Helper de auditoria (backend_app/db/repositories.py:22-46) — assinatura completa:

```python
async def create_audit_event(
    session: AsyncSession, *, action: str, entity_type: str, entity_id: Any | None = None,
    actor_profile_id: Any | None = None, actor_role: str | None = None,
    before_data: dict[str, Any] | None = None, after_data: dict[str, Any] | None = None,
    metadata: dict[str, Any] | None = None,
) -> AuditEvent
```

Guard org-scoped a reutilizar (backend_app/modules/projects/service.py:1426-1445):

```python
async def _assert_project_edit_permission(self, project: Project, *, actor_id: str | None, actor_role: str | None) -> None:
    if actor_role == "admin":
        return
    profile = await self._actor_profile(actor_id)
    organization_id = profile.organization_id
    allowed = False
    if organization_id is not None and actor_role == "producer":
        allowed = organization_id in {project.producer_organization_id, project.developer_organization_id}
    elif organization_id is not None and actor_role == "certifier":
        allowed = organization_id == project.certifier_organization_id
    if not allowed:
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Seu perfil não pode editar este projeto")
```

Rota-analogo a copiar (backend_app/modules/projects/routes.py:363-384):

```python
@router.get("/projects/{project_id}/boundary-overlaps", response_model=ProjectBoundaryOverlapsResponse)
async def get_project_boundary_overlaps(
    project_id: str,
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> ProjectBoundaryOverlapsResponse:
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)
    overlaps = await service.detect_boundary_overlaps(project_id)
    return ProjectBoundaryOverlapsResponse(project_id=str(project.id), total=len(overlaps), overlaps=overlaps)
```

DTO-analogo (backend_app/modules/projects/schemas.py:137-141):

```python
class ProjectBoundaryOverlapsResponse(BaseModel):
    success: bool = True
    project_id: str
    total: int
    overlaps: list[dict[str, Any]] = []
```

Conjunto de documentos obrigatorios ja definido (backend_app/modules/projects/service.py:160-163 e 1770-1781):
`{"LEGAL_OWNERSHIP", "CAR"}` (pelo menos um) e `"FOREST_INVENTORY"` (obrigatorio).
Tipos de documento existentes no produto (src/services/projectDocuments.ts:3):
`LEGAL_OWNERSHIP | CAR | FOREST_INVENTORY | KML_OR_SHP | OTHER`, mais `CERTIFICATION_CERTIFICATE`
(`CERTIFICATION_CERTIFICATE_DOCUMENT_TYPE` em projects/service.py:165).

Pontos de gancho exatos (linhas conferidas em 2026-08-16):
- `projects/service.py:878-880`: `await self.session.flush()` / `await self.persist_project_boundary(project, payload.tags)` dentro de `create_project`, imediatamente antes do `create_audit_event(action="PROJECT_CREATED")`.
- `projects/service.py:716-755`: loop `for draft_document in draft_documents:` dentro de `submit_project_draft`, que faz `self.session.add(Document(...))` SEM flush por documento.
- `projects/routes.py:541-542`: `session.add(document)` + `await session.flush()` em `upload_project_document`, antes do `create_audit_event(action="PROJECT_DOCUMENT_UPLOADED")`.
- `certifier/service.py:136-138`: `self.session.add(document)` + `await self.session.flush()` + `return document` em `_persist_certificate`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IntegrityService com Claims e Evidence + DTOs + rotas montadas</name>
  <files>backend_app/modules/integrity/service.py, backend_app/modules/integrity/schemas.py, backend_app/modules/integrity/routes.py, backend_app/api/router.py</files>
  <read_first>
    - backend_app/modules/certifier/service.py linhas 1-70 e 483-508 (imports, shape da classe de servico, padrao read-or-create com flush)
    - backend_app/modules/projects/routes.py linhas 340-390 (rotas org-scoped analogas: pendencies e boundary-overlaps)
    - backend_app/modules/projects/schemas.py linhas 96-145 (estilo dos DTOs permissivos dict[str, Any])
    - backend_app/api/router.py (arquivo inteiro, 25 linhas)
    - backend_app/modules/integrity/constants.py (Plan 01)
  </read_first>
  <action>
**1. `backend_app/modules/integrity/service.py`** — criar `class IntegrityService` com `__init__(self, session: AsyncSession) -> None: self.session = session`. NAO importar `ProjectsService` no topo do modulo (import circular): quando precisar dele, usar import local dentro do metodo.

Metodos publicos desta plan:

`async def create_origination_claims(self, project: Project, *, actor_id: str | None, actor_role: str | None) -> list[Claim]`
- Determina o tipo de Claim de terra (D-18): `select(Document).where(Document.project_id == project.id, Document.document_type == "LEGAL_OWNERSHIP")` — se existir ao menos um, tipo = `LAND_OWNERSHIP`; caso contrario `LAND_POSSESSION`. (No fluxo de rascunho os documentos so sao criados DEPOIS de `create_project`, entao o padrao inicial sera `LAND_POSSESSION` e sera reconciliado depois por `_reconcile_land_claim_type`.)
- Cria/garante 2 Claims idempotentemente. Para cada `(tipo, statement)` abaixo, primeiro `select(Claim).where(Claim.project_id == project.id, Claim.type == tipo)`; se ja existir, nao inserir de novo (o unique index `claims_project_type_idx` protege no banco).
  - terra: `statement = f"Produtor declara {'propriedade' if tipo == 'LAND_OWNERSHIP' else 'posse'} da area de {float(project.area_hectares):.2f} ha em {project.city}/{project.state_id.upper()}."`
  - `RIGHT_TO_OPERATE`: `statement = f"Produtor declara direito de operar o projeto {project.friendly_id} sob a metodologia {project.methodology}."`
- Cada Claim nasce com `status="DECLARED"`, `confidence_score=CLAIM_CONFIDENCE_DECLARED` (10), `claimant_organization_id=project.producer_organization_id`, `valid_from=datetime.now(timezone.utc)`, `metadata_={"source": "origination", "friendly_id": project.friendly_id}`. **Nunca** aceitar `status`/`confidence_score` de payload do cliente — nao existe parametro para isso na assinatura.
- `await self.session.flush()` depois dos inserts.
- Para cada Claim criado, `create_audit_event(self.session, action="CLAIM_CREATED", entity_type="claims", entity_id=claim.id, actor_role=actor_role, metadata={"actor_external_id": actor_id, "friendly_id": project.friendly_id, "project_id": str(project.id), "claim_type": claim.type, "status": claim.status, "confidence_score": claim.confidence_score})`.
- Retorna a lista de Claims do projeto.

`async def create_evidence_for_document(self, document: Document, *, project: Project, actor_id: str | None, actor_role: str | None) -> Evidence | None`
- Se `document.project_id is None`, retorna `None` sem escrever nada (documentos de inventario nao pertencem a projeto).
- Reconciliacao de tipo de Claim (D-18): se `document.document_type.upper() == "LEGAL_OWNERSHIP"`, chamar `await self._reconcile_land_claim_type(project, actor_id=actor_id, actor_role=actor_role)` ANTES de resolver o Claim alvo.
- Resolve o Claim alvo por tipo de documento (D-22):
  - `LEGAL_OWNERSHIP`, `CAR` -> o Claim de terra do projeto (`LAND_OWNERSHIP` ou `LAND_POSSESSION`, o que existir);
  - `FOREST_INVENTORY`, `KML_OR_SHP`, `OTHER`, `CERTIFICATION_CERTIFICATE` e qualquer outro -> Claim `RIGHT_TO_OPERATE`.
  - Se o Claim alvo nao existir (projeto legado sem Claims), chamar `create_origination_claims(project, ...)` antes e resolver de novo. Se ainda assim nao existir, gravar a Evidence com `claim_id=None` (valvula de seguranca D-22) em vez de falhar.
- Idempotencia: `select(Evidence).where(Evidence.document_id == document.id, Evidence.validation_method == "HASH_INTEGRITY")` — se ja existir, retornar a linha existente sem inserir (o unique parcial `evidence_document_method_idx` protege no banco).
- Insere `Evidence(project_id=document.project_id, claim_id=<claim.id ou None>, document_id=document.id, type=document.document_type, source=document.document_type, source_type="SELF_DECLARED", hash=document.sha256_hash, validation_method="HASH_INTEGRITY", validation_status="VERIFIED", verified_at=datetime.now(timezone.utc), issued_at=document.uploaded_at, metadata_={"storage_path": document.storage_path, "mime_type": document.mime_type})`.
  - `hash` e SEMPRE `document.sha256_hash` (D-07). Proibido recomputar hash aqui — nao importar `hashlib` neste arquivo.
  - `validation_status="VERIFIED"` e legitimo porque o hash foi computado no servidor a partir dos bytes recebidos; e exatamente o que o metodo `HASH_INTEGRITY` afirma. Nenhum outro `validation_method` pode ser gravado nesta fase (D-08).
- `await self.session.flush()`, depois `await self._sync_structural_completeness_evidence(project, actor_id=actor_id, actor_role=actor_role)` e `await self._recompute_claim_states(project)`.
- `create_audit_event(..., action="EVIDENCE_CREATED", entity_type="evidence", entity_id=evidence.id, actor_role=actor_role, metadata={"actor_external_id": actor_id, "friendly_id": project.friendly_id, "document_id": str(document.id), "document_type": document.document_type, "sha256": document.sha256_hash, "claim_id": str(claim.id) if claim else None, "validation_method": "HASH_INTEGRITY"})`.

`async def _reconcile_land_claim_type(self, project, *, actor_id, actor_role) -> None`
- Se existir Claim `LAND_POSSESSION` do projeto e existir Document `LEGAL_OWNERSHIP` do projeto, mudar `claim.type = "LAND_OWNERSHIP"`, atualizar `claim.statement` para a forma "propriedade" (mesmo template acima), `claim.updated_at = now`, e gravar `create_audit_event(action="CLAIM_TYPE_RECONCILED", entity_type="claims", entity_id=claim.id, before_data={"type": "LAND_POSSESSION"}, after_data={"type": "LAND_OWNERSHIP"}, ...)`. Caso contrario, nao faz nada.

`async def _sync_structural_completeness_evidence(self, project, *, actor_id, actor_role) -> None`
- Le os `document_type` (upper) de todos os Documents do projeto.
- `complete = bool({"LEGAL_OWNERSHIP", "CAR"} & types) and "FOREST_INVENTORY" in types` — exatamente a mesma regra de `_validate_required_draft_documents` (projects/service.py:1770-1781). Nao inventar outra regra.
- Faz upsert logico de UMA Evidence por Claim `RIGHT_TO_OPERATE` com `validation_method="STRUCTURAL_COMPLETENESS"`, `document_id=None`, `hash=""` nao permitido — usar `hash=project.source_hash or project.friendly_id` como identificador estavel do dossie, `type="DOSSIER_STRUCTURE"`, `source="project_dossier"`, `source_type="SELF_DECLARED"`, `validation_status="VERIFIED" if complete else "PENDING"`, `verified_at=now if complete else None`, `metadata_={"document_types": sorted(types), "required": ["LEGAL_OWNERSHIP|CAR", "FOREST_INVENTORY"]}`.
- Se a linha ja existir, atualizar `validation_status`/`verified_at`/`metadata_`/`updated_at` em vez de inserir (unique parcial `evidence_claim_structural_idx`).

`async def _recompute_claim_states(self, project) -> None`
- Para cada Claim do projeto, recomputar de forma pura (idempotente, sem acumular):
  - tem >= 1 Evidence `HASH_INTEGRITY` com `validation_status="VERIFIED"` E a Evidence `STRUCTURAL_COMPLETENESS` do projeto esta `VERIFIED` -> `status="EVIDENCE_VERIFIED"`, `confidence_score=CLAIM_CONFIDENCE_EVIDENCE_VERIFIED` (60);
  - tem >= 1 Evidence qualquer -> `status="EVIDENCE_PENDING"`, `confidence_score=CLAIM_CONFIDENCE_EVIDENCE_PENDING` (30);
  - nenhuma Evidence -> `status="DECLARED"`, `confidence_score=CLAIM_CONFIDENCE_DECLARED` (10).
  - `status="VERIFIED"` NUNCA e atribuido nesta fase (D-08: exige verificacao independente, Phase 05.1). Deixar comentario explicito no codigo.
- So gravar `updated_at` e emitir `create_audit_event(action="CLAIM_STATE_RECOMPUTED", ...)` quando o valor realmente mudou (evita ruido de auditoria).

`async def list_claims(self, project: Project) -> list[dict[str, Any]]` — retorna, por Claim, `{"id", "type", "status", "confidenceScore", "statement", "validFrom", "createdAt", "evidence": [<evidence_item>...]}` ordenado por `created_at`.
`async def list_evidence(self, project: Project) -> list[dict[str, Any]]` — `{"id", "claimId", "documentId", "type", "source", "sourceType", "hash", "validationMethod", "validationStatus", "verifiedAt", "createdAt"}`.

**2. `backend_app/modules/integrity/schemas.py`** — no estilo permissivo do repo:

```python
from __future__ import annotations

from typing import Any

from pydantic import BaseModel


class ProjectClaimsResponse(BaseModel):
    success: bool = True
    project_id: str
    total: int
    claims: list[dict[str, Any]] = []


class ProjectEvidenceResponse(BaseModel):
    success: bool = True
    project_id: str
    total: int
    evidence: list[dict[str, Any]] = []
```

**3. `backend_app/modules/integrity/routes.py`** — `router = APIRouter(tags=["integrity"])` e duas rotas GET copiando o shape de `get_project_boundary_overlaps`:
- `GET /projects/{project_id}/claims` -> `ProjectClaimsResponse`
- `GET /projects/{project_id}/evidence` -> `ProjectEvidenceResponse`
Ambas com `current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin"))`, `session: AsyncSession = Depends(get_session)`, e no corpo: `service = ProjectsService(session)`, `project = await service._get_project_model(project_id)`, `await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)`, depois `IntegrityService(session).list_claims(project)` / `.list_evidence(project)`.
Nao criar nenhuma rota POST/PATCH/PUT para claims ou evidence nesta fase — toda criacao e server-side (T-04.2-06).
Docstring PT-BR em cada rota explicando o guard org-scoped (Conflict/Claim revelam dados de terceiros).

**4. `backend_app/api/router.py`** — adicionar `from backend_app.modules.integrity.routes import router as integrity_router` (ordem alfabetica entre `inventory` e... manter o bloco de imports ordenado como ja esta: `certifier`, depois `integrity`, depois `inventory`) e `api_router.include_router(integrity_router)` depois de `api_router.include_router(certifier_router)`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && python -c "
from backend_app.main import app
paths = {r.path for r in app.routes}
assert '/api/v1/projects/{project_id}/claims' in paths, sorted(p for p in paths if 'claims' in p)
assert '/api/v1/projects/{project_id}/evidence' in paths
methods = {r.path: r.methods for r in app.routes if getattr(r, 'path', '').endswith(('/claims', '/evidence'))}
for path, verbs in methods.items():
    assert verbs <= {'GET', 'HEAD'}, (path, verbs)
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import hashlib" backend_app/modules/integrity/service.py` retorna 0 (hash so vem de documents.sha256_hash)
    - `grep -q "from backend_app.modules.projects.service import ProjectsService" backend_app/modules/integrity/service.py` — se aparecer, DEVE estar indentado dentro de um metodo (import local), nunca na coluna 0
    - `grep -c "validation_method=\"HASH_INTEGRITY\"\|validation_method=\"STRUCTURAL_COMPLETENESS\"" backend_app/modules/integrity/service.py` >= 2 e nenhum outro literal de validation_method aparece no arquivo
    - `grep -c "@router.post\|@router.put\|@router.patch\|@router.delete" backend_app/modules/integrity/routes.py` retorna 0
    - `grep -c "_assert_project_edit_permission" backend_app/modules/integrity/routes.py` retorna 2
    - `grep -q "include_router(integrity_router)" backend_app/api/router.py` sai com codigo 0
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Modulo integrity montado no /api/v1 com duas rotas GET org-scoped e servico de Claim/Evidence completo.</done>
</task>

<task type="auto">
  <name>Task 2: Ganchos de Claim e Evidence nos 4 pontos de escrita existentes</name>
  <files>backend_app/modules/projects/service.py, backend_app/modules/projects/routes.py, backend_app/modules/certifier/service.py</files>
  <read_first>
    - backend_app/modules/projects/service.py linhas 212-215 (ProjectsService.__init__), 686-780 (submit_project_draft), 778-895 (create_project)
    - backend_app/modules/projects/routes.py linhas 483-560 (upload_project_document)
    - backend_app/modules/certifier/service.py linhas 101-138 (_persist_certificate) e 190-280 (record_decision)
    - backend_app/modules/integrity/service.py (escrito na Task 1)
  </read_first>
  <action>
**Gancho 0 — acesso preguicoso ao IntegrityService (evita import circular).** Em `backend_app/modules/projects/service.py`, alterar `ProjectsService.__init__` (linha 213-214) para:

```python
    def __init__(self, session: AsyncSession) -> None:
        self.session = session
        self._integrity: Any | None = None

    @property
    def integrity(self) -> "IntegrityService":
        # Import local: IntegrityService.detect_and_persist_conflicts (Plan 03)
        # importa ProjectsService de volta. Import no topo criaria ciclo.
        from backend_app.modules.integrity.service import IntegrityService

        if self._integrity is None:
            self._integrity = IntegrityService(self.session)
        return self._integrity
```

**Gancho 1 — Claims na criacao de projeto.** Em `create_project` (projects/service.py), imediatamente apos a linha `await self.persist_project_boundary(project, payload.tags)` (linha 879) e ANTES do `await create_audit_event(self.session, action="PROJECT_CREATED", ...)`:

```python
        # Phase 04.2 / INTG-01 (D-01): a submissao de originacao gera Claims
        # DECLARED. Nao promove projects.status nem depende de nada do payload
        # alem do que ja foi validado acima.
        await self.integrity.create_origination_claims(project, actor_id=actor_id, actor_role=actor_role)
```

**Gancho 2 — Evidence no envio de rascunho.** Em `submit_project_draft` (projects/service.py:716-756), o loop atual faz `self.session.add(Document(...))` sem guardar a referencia e sem flush. Refatorar minimamente: guardar cada Document em uma variavel local e acumular numa lista `created_documents: list[Document] = []`; apos o fim do loop, inserir:

```python
        if created_documents:
            await self.session.flush()
            for created_document in created_documents:
                await self.integrity.create_evidence_for_document(
                    created_document,
                    project=project,
                    actor_id=actor_id,
                    actor_role=actor_role,
                )
```
Colocar esse bloco entre o fim do loop `for draft_document in draft_documents:` e a linha `now = datetime.now(timezone.utc)` (linha 757). O `flush()` e obrigatorio: `Evidence.document_id` e FK e precisa do id real (RESEARCH Pitfall 3). Nao alterar a logica de `continue` do documento ja existente.

**Gancho 3 — Evidence no upload avulso de documento.** Em `backend_app/modules/projects/routes.py`, dentro de `upload_project_document`, entre `await session.flush()` (linha 542) e `await create_audit_event(session, action="PROJECT_DOCUMENT_UPLOADED", ...)` (linha 543):

```python
    # Phase 04.2 / INTG-02 (D-07): Evidence reaproveita documents.sha256_hash.
    await IntegrityService(session).create_evidence_for_document(
        document,
        project=project,
        actor_id=current_user.id,
        actor_role=current_user.role,
    )
```
com `from backend_app.modules.integrity.service import IntegrityService` no bloco de imports do arquivo.

**Gancho 4 — Evidence no certificado da certificadora (D-21).** Em `backend_app/modules/certifier/service.py`, dentro de `_persist_certificate`, entre `await self.session.flush()` (linha 137) e `return document` (linha 138):

```python
        # D-21: o certificado ganha Evidence pelo hook padrao de documento.
        # NAO existe validationMethod "CERTIFIER_REVIEW" nesta fase (D-08).
        await self.projects.integrity.create_evidence_for_document(
            document,
            project=project,
            actor_id=None,
            actor_role="certifier",
        )
```
Atencao: `_persist_certificate` tem um early-return quando o documento ja existe (linha 111-112) — nao adicionar a chamada nesse caminho (a Evidence ja existe e o metodo e idempotente de qualquer forma).

**Proibicoes desta task:**
- Nao adicionar nenhuma chamada a `recalculate_risk_score` (nao existe ate a Plan 04).
- Nao alterar nenhuma atribuicao a `project.status` em nenhum dos arquivos.
- Nao alterar a assinatura publica de `create_project`, `update_project`, `submit_project_draft`, `upload_project_document` ou `record_decision`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/test_certifier_workbench.py -x -q && pytest tests/test_project_boundaries.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create_origination_claims" backend_app/modules/projects/service.py` retorna 1
    - `grep -c "create_evidence_for_document" backend_app/modules/projects/service.py backend_app/modules/projects/routes.py backend_app/modules/certifier/service.py` soma 3 (um por arquivo)
    - `grep -c "recalculate_risk_score" backend_app/modules/projects/service.py backend_app/modules/projects/routes.py backend_app/modules/certifier/service.py` soma 0
    - `grep -n "def integrity" backend_app/modules/projects/service.py` mostra a property logo apos `__init__`
    - `git diff --stat backend_app/modules/certifier/service.py` mostra apenas linhas adicionadas em `_persist_certificate` (nenhuma linha de `record_decision` alterada)
    - `pytest tests/test_certifier_workbench.py -x -q` e `pytest tests/test_project_boundaries.py -x -q` passam sem regressao
  </acceptance_criteria>
  <done>Os 4 pontos de escrita existentes geram Claim/Evidence na mesma transacao, sem alterar contrato nem status operacional.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de contrato HTTP de Claim e Evidence</name>
  <files>tests/contract/test_backend_app_api_v1.py</files>
  <read_first>
    - tests/contract/test_backend_app_api_v1.py linhas 1-160 (fixtures auth_headers, project_payload, create_project_for_workflow e o padrao de teste do upload de documento)
    - tests/test_project_boundaries.py linhas 466-495 (helpers uuid_hex e rectangle_tags — usar retangulo aleatorio para nao colidir com projetos de teste anteriores)
    - backend_app/modules/integrity/service.py (Task 1)
  </read_first>
  <action>
Adicionar ao final de `tests/contract/test_backend_app_api_v1.py` (reutilizando as fixtures ja existentes no arquivo, sem criar client novo) os testes abaixo. Cada nome contem a palavra `claim`, `evidence` ou `integrity` para que `pytest -k claim` e `-k evidence` selecionem o subconjunto certo.

1. `test_project_create_generates_origination_claims`: cria projeto via `POST /api/v1/projects`; `GET /api/v1/projects/{friendly_id}/claims` com `auth_headers()` retorna 200; assere `total == 2`; assere que o conjunto de `type` e `{"LAND_POSSESSION", "RIGHT_TO_OPERATE"}` (sem documentos, D-18 cai em posse); assere que TODO claim tem `status == "DECLARED"` e `0 <= confidenceScore <= 20`; assere que `statement` e string nao vazia.

2. `test_claim_status_is_never_client_supplied`: repete a criacao passando no corpo do `POST /api/v1/projects` chaves extras `"integrity_status": "VERIFIED"`, `"risk_score": 0`, `"claims": [{"type": "LAND_OWNERSHIP", "status": "VERIFIED", "confidence_score": 100}]`; assere que a criacao ainda retorna 201 (campos extras ignorados pelo Pydantic) e que os Claims retornados continuam `DECLARED` com `confidenceScore == 10`, e que `GET /api/v1/projects/{id}/public-dossier` retorna `project.status` inalterado em relacao ao criado (nada de promocao por input do usuario — INTG-01).

3. `test_document_upload_creates_evidence_with_document_hash`: cria projeto, faz `POST /api/v1/projects/{id}/documents` com `document_type="FOREST_INVENTORY"` (mesmo padrao multipart do teste de upload ja existente no arquivo); guarda o `sha256` da resposta; `GET /api/v1/projects/{id}/evidence` retorna 200 com ao menos uma linha cujo `hash` e exatamente esse `sha256`, `validationMethod == "HASH_INTEGRITY"`, `sourceType == "SELF_DECLARED"` e `claimId` nao nulo.

4. `test_evidence_upload_is_idempotent`: repete o mesmo upload (mesmo arquivo, mesmo tipo) e assere que a contagem de Evidence com `validationMethod == "HASH_INTEGRITY"` para aquele documento continua 1.

5. `test_legal_ownership_document_reconciles_land_claim_type`: cria projeto (Claim de terra nasce `LAND_POSSESSION`), faz upload de documento `LEGAL_OWNERSHIP`, e assere que `GET .../claims` agora traz `LAND_OWNERSHIP` e nao traz mais `LAND_POSSESSION`, e que `total` continua 2 (reconciliou a linha, nao duplicou — D-18).

6. `test_structural_completeness_evidence_turns_verified_when_dossier_complete`: cria projeto; faz upload de `LEGAL_OWNERSHIP` e `FOREST_INVENTORY`; assere que existe Evidence com `validationMethod == "STRUCTURAL_COMPLETENESS"` e `validationStatus == "VERIFIED"`, e que os Claims passaram a `EVIDENCE_VERIFIED` com `confidenceScore == 60`.

7. `test_integrity_claims_endpoint_is_org_scoped`: com o token da certificadora de OUTRO projeto (ou de um papel sem vinculo — usar o mesmo padrao ja usado nos testes de `pendencies`/`boundary-overlaps` no repo), assere 403 em `GET /api/v1/projects/{id}/claims`; e sem header nenhum, assere 401.

8. `test_evidence_validation_method_is_capped`: assere que nenhum item de `GET .../evidence` tem `validationMethod` fora de `{"HASH_INTEGRITY", "STRUCTURAL_COMPLETENESS"}` (D-08).

Usar coordenadas de retangulo aleatorio (`rectangle_tags` copiado como helper local, com `base_lat = -10.0 - random.random()`) nos projetos criados por estes testes, para nao herdar as sobreposicoes de 100% dos fixtures fixos existentes.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/contract/test_backend_app_api_v1.py -k "claim or evidence or integrity" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/contract/test_backend_app_api_v1.py -k "claim or evidence or integrity" -x -q` coleta 8 testes e todos passam
    - `pytest tests/contract/test_backend_app_api_v1.py -x -q` (arquivo inteiro) passa sem regressao
    - `grep -c "def test_.*claim\|def test_.*evidence\|def test_.*integrity" tests/contract/test_backend_app_api_v1.py` retorna 8
    - `pytest tests/ -x -q` passa (suite completa, requer Supabase local ativo)
  </acceptance_criteria>
  <done>INTG-01 e INTG-02 provados por contrato HTTP, incluindo o guard org-scoped e o cap de validationMethod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado -> POST /api/v1/projects | Payload do produtor; nao pode influenciar status/confianca de Claim |
| organizacao A -> GET /api/v1/projects/{id}/claims,evidence | Claim/Evidence de projeto de terceiro sao dados internos |
| upload multipart -> Evidence.hash | O hash e a ancora de integridade; se vier do cliente, a Evidence perde valor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-06 | Spoofing / Elevation of Privilege | criacao de Claim | mitigate | `create_origination_claims` nao tem parametro de `status`/`confidence_score`; nenhuma rota POST/PATCH de claims existe; teste `test_claim_status_is_never_client_supplied` prova que campos extras no payload sao ignorados |
| T-04.2-07 | Tampering | Evidence.hash | mitigate | `hash = document.sha256_hash` sempre (D-07); `import hashlib` proibido em integrity/service.py (criterio de aceite grep); teste 3 compara o hash da Evidence com o sha256 devolvido pelo upload |
| T-04.2-08 | Information Disclosure | GET /projects/{id}/claims e /evidence | mitigate | `require_role("producer","certifier","admin")` + `_assert_project_edit_permission` org-scoped, identico a `/boundary-overlaps`; teste 7 prova 403 cross-org e 401 anonimo |
| T-04.2-09 | Elevation of Privilege | Evidence.validation_method | mitigate | Check constraint no banco (Plan 01) + apenas dois literais no servico (criterio grep) + teste 8 |
| T-04.2-10 | Repudiation | criacao/reconciliacao de Claim e Evidence | mitigate | `create_audit_event` em CLAIM_CREATED, CLAIM_TYPE_RECONCILED, CLAIM_STATE_RECOMPUTED e EVIDENCE_CREATED, com `entity_type` da tabela e `entity_id` da linha |
</threat_model>

<verification>
1. `pytest tests/contract/test_backend_app_api_v1.py -x -q` — contrato HTTP verde
2. `pytest tests/test_certifier_workbench.py -x -q` — fluxo da certificadora sem regressao (o gancho 4 roda dentro de `_persist_certificate`)
3. `pytest tests/ -x -q` — suite completa verde
4. `python -c "from backend_app.main import app; print(len(app.routes))"` — app importa (sem ciclo de import)
</verification>

<success_criteria>
- 2 Claims `DECLARED` por projeto criado, sem qualquer influencia do payload do cliente
- 1 Evidence `HASH_INTEGRITY` por documento de projeto, com hash identico a `documents.sha256_hash`
- Reconciliacao LAND_POSSESSION -> LAND_OWNERSHIP funcionando sem duplicar Claim
- Rotas `/claims` e `/evidence` org-scoped (403 cross-org, 401 anonimo)
- Zero mudanca em `projects.status` e zero regressao na suite existente
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04.2-integrity-layer-foundation/04.2-02-SUMMARY.md` com: assinaturas finais dos metodos de `IntegrityService` (as Plans 03-05 chamam esses metodos), shape exato do JSON de `/claims` e `/evidence`, e qualquer desvio nos pontos de gancho.
</output>
