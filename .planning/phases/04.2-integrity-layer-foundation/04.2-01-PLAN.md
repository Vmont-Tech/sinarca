---
phase: 04.2-integrity-layer-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql
  - supabase/migrations/202608170002_integrity_risk_assessments.sql
  - backend_app/db/models.py
  - backend_app/core/config.py
  - backend_app/modules/integrity/__init__.py
  - backend_app/modules/integrity/constants.py
  - tests/db/test_integrity_schema.py
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-04]
user_setup: []

must_haves:
  truths:
    - "As tabelas claims, evidence, conflicts, risk_assessments e risk_signals existem no Postgres local depois de npx -y supabase db reset"
    - "projects tem as colunas integrity_status (default DECLARED) e risk_score (nullable, 0-100)"
    - "Nenhuma das novas tabelas aceita DML de anon/authenticated nem tem policy de select"
    - "Limiares de severidade e pesos de risco sao configuraveis por env, com defaults da Bible secao 16/22"
    - "Nenhum tipo ENUM novo do Postgres foi criado"
  artifacts:
    - path: "supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql"
      provides: "Tabelas claims/evidence/conflicts + colunas integrity_status/risk_score + RLS"
      contains: "create table if not exists claims"
    - path: "supabase/migrations/202608170002_integrity_risk_assessments.sql"
      provides: "Tabelas append-only risk_assessments/risk_signals + RLS"
      contains: "create table if not exists risk_assessments"
    - path: "backend_app/modules/integrity/constants.py"
      provides: "Fonte unica de verdade dos vocabularios (espelha os check constraints)"
      contains: "CLAIM_TYPES"
    - path: "backend_app/db/models.py"
      provides: "Modelos Claim/Evidence/Conflict/ProjectRiskAssessment/RiskSignal"
      contains: "__tablename__ = \"claims\""
    - path: "backend_app/core/config.py"
      provides: "Settings com limiares de severidade e pesos de risco"
      contains: "integrity_overlap_severity_high_pct"
    - path: "tests/db/test_integrity_schema.py"
      provides: "Contrato estatico das migrations (RLS, checks, indices)"
      min_lines: 60
  key_links:
    - from: "backend_app/modules/integrity/constants.py"
      to: "supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql"
      via: "check (col in (...)) espelha as tuplas de constants.py"
      pattern: "check \\(type in \\('LAND_OWNERSHIP'"
    - from: "backend_app/db/models.py"
      to: "tabelas novas"
      via: "__tablename__ + colunas String (nunca Enum)"
      pattern: "__tablename__ = \"conflicts\""
---

<objective>
Criar a fundacao de schema do Integrity Layer: tabelas `claims`, `evidence`, `conflicts`, `risk_assessments`, `risk_signals`, colunas `projects.integrity_status`/`projects.risk_score`, modelos SQLAlchemy, vocabulario canonico em `backend_app/modules/integrity/constants.py` e limiares/pesos em `Settings`.

Purpose: nenhuma regra de negocio das Plans 02-05 pode existir sem essas tabelas no banco local. Esta plan e puramente aditiva: nao altera nenhuma tabela nem coluna existente, apenas adiciona duas colunas novas em `projects`.
Output: 2 migrations idempotentes aplicadas no Postgres local, 5 modelos novos, 1 modulo de constantes, 13 campos novos em `Settings`, 1 arquivo de teste de contrato de schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-CONTEXT.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-RESEARCH.md
@.planning/phases/04.2-integrity-layer-foundation/04.2-PATTERNS.md

<interfaces>
Helpers ja existentes em backend_app/db/models.py (linhas 80-89) — usar, nao recriar:

```python
def uuid_pk() -> Mapped[uuid.UUID]:
    return mapped_column(UUID(as_uuid=True), primary_key=True, server_default=text("uuid_generate_v4()"))

def created_at_column() -> Mapped[datetime]:
    return mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)

def updated_at_column() -> Mapped[datetime]:
    return mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)
```

Imports ja presentes no topo de models.py (linhas 1-9) — nada novo a importar:

```python
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Enum, ForeignKey, Index, Numeric, String, Text, UniqueConstraint, func, text
from sqlalchemy.dialects.postgresql import JSONB, UUID
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column
```

Modelo-analogo exato (models.py:277-296, CertificationPendency). `status` e String, NUNCA Enum:

```python
class CertificationPendency(Base):
    __tablename__ = "certification_pendencies"
    __table_args__ = (
        Index("certification_pendencies_project_status_idx", "project_id", "status", "created_at"),
    )

    id: Mapped[uuid.UUID] = uuid_pk()
    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    status: Mapped[str] = mapped_column(String, nullable=False, server_default=text("'OPEN'"))
    metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    created_at: Mapped[datetime] = created_at_column()
```

Settings existente (core/config.py:17-41) — estilo plano a estender:

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", enable_decoding=False, extra="ignore")
    app_env: str = "development"
    access_token_minutes: int = 60
    stellar_network: str = "testnet"
    supabase_jwt_secret: str | None = None
```

Guard idempotente de constraint (migrations/202608160001_scope_document_hash_uniqueness_by_type.sql:8-19):

```sql
do $$
begin
  if not exists (
    select 1 from pg_constraint
    where conname = 'documents_project_type_sha256_key'
      and conrelid = 'documents'::regclass
  ) then
    alter table documents
      add constraint documents_project_type_sha256_key unique (project_id, document_type, sha256_hash);
  end if;
end $$;
```

RLS de tabela operacional interna (migrations/202608150003_project_boundaries.sql:54-58):

```sql
alter table project_boundaries enable row level security;
revoke insert, update, delete on project_boundaries from anon, authenticated;
-- Sem policy de select: ver D-GEO-01 no cabecalho.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vocabulario canonico do Integrity Layer + limiares e pesos em Settings</name>
  <files>backend_app/modules/integrity/__init__.py, backend_app/modules/integrity/constants.py, backend_app/core/config.py</files>
  <read_first>
    - backend_app/core/config.py (76 linhas, arquivo inteiro — estilo plano dos campos e get_settings com lru_cache)
    - backend_app/modules/certifier/service.py linhas 31-59 (estilo do bloco de constantes: tuple[str, ...] + dict de labels)
    - backend_app/modules/certifier/__init__.py (1 linha — conteudo exato de todo __init__.py deste repo)
  </read_first>
  <action>
Criar `backend_app/modules/integrity/__init__.py` com exatamente uma linha: `from __future__ import annotations`

Criar `backend_app/modules/integrity/constants.py` com este conteudo exato (fonte unica de verdade; os check constraints da Task 2 espelham estas tuplas literalmente):

```python
from __future__ import annotations

# Vocabulario canonico do Sinarca Integrity Layer (Phase 04.2).
# Estas tuplas sao a fonte unica de verdade: os check constraints das migrations
# 202608170001 e 202608170002 espelham exatamente estas listas.
# Convencao do repo desde a Phase 04: text + check no SQL, String em models.py.
# NUNCA criar tipo ENUM novo no Postgres para estes campos.

CLAIM_TYPE_LAND_OWNERSHIP = "LAND_OWNERSHIP"
CLAIM_TYPE_LAND_POSSESSION = "LAND_POSSESSION"
CLAIM_TYPE_RIGHT_TO_OPERATE = "RIGHT_TO_OPERATE"

CLAIM_TYPES: tuple[str, ...] = (
    "LAND_OWNERSHIP",
    "LAND_POSSESSION",
    "RIGHT_TO_OPERATE",
)

# D-03: todo Claim nasce DECLARED. VERIFIED existe no vocabulario mas NUNCA e
# atribuido nesta fase (exige verificacao independente => Phase 05.1).
CLAIM_STATUSES: tuple[str, ...] = (
    "DECLARED",
    "EVIDENCE_PENDING",
    "EVIDENCE_VERIFIED",
    "VERIFIED",
    "REJECTED",
    "SUPERSEDED",
)

CLAIM_CONFIDENCE_DECLARED = 10
CLAIM_CONFIDENCE_EVIDENCE_PENDING = 30
CLAIM_CONFIDENCE_EVIDENCE_VERIFIED = 60

EVIDENCE_SOURCE_TYPES: tuple[str, ...] = (
    "SELF_DECLARED",
    "THIRD_PARTY",
    "EXTERNAL_REGISTRY",
    "SATELLITE",
    "FIELD_AUDIT",
)

# D-08: sem integracao externa, o sistema so consegue executar estes dois
# metodos. Nenhum outro pode ser gravado ate a Phase 05.1.
EVIDENCE_VALIDATION_METHODS: tuple[str, ...] = (
    "HASH_INTEGRITY",
    "STRUCTURAL_COMPLETENESS",
)

EVIDENCE_VALIDATION_STATUSES: tuple[str, ...] = (
    "PENDING",
    "VERIFIED",
    "FAILED",
    "EXPIRED",
)

# Bible secao 6. Nesta fase somente GEOSPATIAL_OVERLAP (D-09) e DOUBLE_CLAIM
# (D-12) sao emitidos; os demais ficam no check para nao exigir migration nova.
CONFLICT_TYPES: tuple[str, ...] = (
    "GEOSPATIAL_OVERLAP",
    "DUPLICATE_PROPERTY",
    "DOUBLE_CLAIM",
    "DUPLICATE_DOCUMENT",
    "IDENTITY_CONFLICT",
    "RIGHTS_CONFLICT",
    "EXTERNAL_REGISTRY_CONFLICT",
)

CONFLICT_SEVERITIES: tuple[str, ...] = (
    "CLEAR",
    "INFO",
    "LOW",
    "MEDIUM",
    "HIGH",
    "CRITICAL",
)

CONFLICT_STATUSES: tuple[str, ...] = ("OPEN", "RESOLVED")

# Bible secao 7, ordem canonica. D-04: eixo independente de ProjectStatusEnum.
INTEGRITY_STATUSES: tuple[str, ...] = (
    "DECLARED",
    "IDENTITY_VERIFIED",
    "EVIDENCE_PENDING",
    "EVIDENCE_VERIFIED",
    "UNDER_REVIEW",
    "INDEPENDENTLY_VERIFIED",
    "VERIFIED",
    "ON_HOLD",
    "SUSPENDED",
    "REVOKED",
    "REJECTED",
)

# Bible secao 23.
RISK_CLASSES: tuple[str, ...] = ("LOW", "MODERATE", "HIGH", "VERY_HIGH", "CRITICAL")
RISK_CLASS_BOUNDS: tuple[tuple[int, str], ...] = (
    (20, "LOW"),
    (40, "MODERATE"),
    (60, "HIGH"),
    (80, "VERY_HIGH"),
    (100, "CRITICAL"),
)
RISK_CLASS_AUTO_HOLD = "CRITICAL"

# Bible secao 22, restrita aos sinais computaveis sem registro externo.
RISK_SIGNAL_CODES: tuple[str, ...] = (
    "OVERLAP_CRITICAL",
    "OVERLAP_HIGH",
    "OVERLAP_MEDIUM",
    "OVERLAP_LOW",
    "DOUBLE_CLAIM",
    "LAND_CLAIM_UNVERIFIED",
    "CLAIM_EVIDENCE_PENDING",
    "POSSESSION_WITHOUT_TITLE",
)

# Allowlist de minimizacao (mesmo principio de PUBLIC_DOCUMENT_TYPES): apenas
# estes codigos aparecem no dossie publico, e apenas code/weight/reason —
# nunca o metadata, que carrega ids de projetos de terceiros.
PUBLIC_RISK_SIGNAL_CODES: frozenset[str] = frozenset(RISK_SIGNAL_CODES)

# D-16 / Bible secao 40: vocabulario publico obrigatorio.
PUBLIC_INTEGRITY_STATUSES: tuple[str, ...] = (
    "DECLARED",
    "VERIFIED",
    "UNDER_REVIEW",
    "ON_HOLD",
    "SUSPENDED",
    "REVOKED",
)

PUBLIC_INTEGRITY_STATUS_BY_INTERNAL: dict[str, str] = {
    "DECLARED": "DECLARED",
    "IDENTITY_VERIFIED": "UNDER_REVIEW",
    "EVIDENCE_PENDING": "UNDER_REVIEW",
    "EVIDENCE_VERIFIED": "UNDER_REVIEW",
    "UNDER_REVIEW": "UNDER_REVIEW",
    "INDEPENDENTLY_VERIFIED": "VERIFIED",
    "VERIFIED": "VERIFIED",
    "ON_HOLD": "ON_HOLD",
    "SUSPENDED": "SUSPENDED",
    "REVOKED": "REVOKED",
    "REJECTED": "REVOKED",
}
```

Em `backend_app/core/config.py`, adicionar estes 13 campos ao final da lista de campos da classe `Settings` (logo apos `supabase_jwt_secret: str | None = None`, ANTES do primeiro `@field_validator`), sem tocar em campo existente e sem criar validator novo:

```python

    # Phase 04.2 / D-10: limiares de severidade de overlap (Bible secao 16).
    # Interpretacao: pct <= 0 -> CLEAR; <= info -> INFO; <= low -> LOW;
    # <= medium -> MEDIUM; <= high -> HIGH; > high -> CRITICAL.
    integrity_overlap_severity_info_pct: float = 1.0
    integrity_overlap_severity_low_pct: float = 5.0
    integrity_overlap_severity_medium_pct: float = 20.0
    integrity_overlap_severity_high_pct: float = 50.0
    integrity_overlap_severity_critical_pct: float = 50.0

    # Phase 04.2 / D-13: pesos dos sinais de risco (Bible secao 22).
    integrity_risk_weight_overlap_critical: float = 50.0
    integrity_risk_weight_overlap_high: float = 30.0
    integrity_risk_weight_overlap_medium: float = 15.0
    integrity_risk_weight_overlap_low: float = 5.0
    integrity_risk_weight_double_claim: float = 60.0
    integrity_risk_weight_land_claim_unverified: float = 20.0
    integrity_risk_weight_claim_evidence_pending: float = 10.0
    integrity_risk_weight_possession_without_title: float = 10.0
```
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && python -c "from backend_app.core.config import get_settings; from backend_app.modules.integrity.constants import CLAIM_TYPES, INTEGRITY_STATUSES, RISK_SIGNAL_CODES; s=get_settings(); assert s.integrity_overlap_severity_high_pct==50.0; assert s.integrity_risk_weight_double_claim==60.0; assert len(INTEGRITY_STATUSES)==11; assert len(RISK_SIGNAL_CODES)==8; assert len(CLAIM_TYPES)==3; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "integrity_risk_weight_" backend_app/core/config.py` retorna 8
    - `grep -c "integrity_overlap_severity_" backend_app/core/config.py` retorna 5
    - `grep -q "PUBLIC_INTEGRITY_STATUS_BY_INTERNAL" backend_app/modules/integrity/constants.py` sai com codigo 0
    - `grep -q "RISK_CLASS_AUTO_HOLD = \"CRITICAL\"" backend_app/modules/integrity/constants.py` sai com codigo 0
    - backend_app/modules/integrity/__init__.py contem exatamente `from __future__ import annotations`
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Constantes e Settings importaveis, defaults iguais a Bible secao 16/22/23.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations idempotentes e modelos SQLAlchemy das 5 tabelas novas</name>
  <files>supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql, supabase/migrations/202608170002_integrity_risk_assessments.sql, backend_app/db/models.py</files>
  <read_first>
    - supabase/migrations/202608150001_certification_workbench.sql (arquivo inteiro — create table + check + indice + RLS/revoke sem policy)
    - supabase/migrations/202608150003_project_boundaries.sql (arquivo inteiro — RLS de tabela interna e cabecalho de comentario PT-BR)
    - supabase/migrations/202608160001_scope_document_hash_uniqueness_by_type.sql (guard do $$ ... pg_constraint)
    - backend_app/db/models.py linhas 143-192 (classe Project — onde as 2 colunas entram) e 260-318 (Certification/CertificationPendency/TreasuryAuthorization)
    - backend_app/modules/integrity/constants.py (criado na Task 1 — listas dos check constraints)
  </read_first>
  <action>
Criar `supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql` com exatamente este conteudo:

```sql
-- Phase 04.2 / INTG-01..03: fundacao do Sinarca Integrity Layer.
-- claims/evidence/conflicts sao tabelas OPERACIONAIS INTERNAS: RLS habilitado,
-- DML revogado de anon/authenticated e SEM policy de select. Todo acesso passa
-- pelo backend_app com auth propria e guard org-scoped.
-- D-04: projects.integrity_status NAO substitui projects.status (enum
-- operacional de 16 estados). Sao dois eixos paralelos.

create table if not exists claims (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  claimant_organization_id uuid references organizations(id),
  claimant_profile_id uuid references profiles(id),
  type text not null check (type in ('LAND_OWNERSHIP', 'LAND_POSSESSION', 'RIGHT_TO_OPERATE')),
  statement text not null,
  status text not null default 'DECLARED' check (status in ('DECLARED', 'EVIDENCE_PENDING', 'EVIDENCE_VERIFIED', 'VERIFIED', 'REJECTED', 'SUPERSEDED')),
  confidence_score integer not null default 10 check (confidence_score >= 0 and confidence_score <= 100),
  valid_from timestamptz,
  valid_to timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Um Claim por (projeto, tipo): torna a criacao de Claim na originacao
-- idempotente e permite reconciliar LAND_POSSESSION -> LAND_OWNERSHIP (D-18)
-- sem duplicar linha.
create unique index if not exists claims_project_type_idx on claims (project_id, type);
create index if not exists claims_project_status_idx on claims (project_id, status, created_at desc);

create table if not exists evidence (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  claim_id uuid references claims(id),
  document_id uuid references documents(id),
  type text not null,
  source text not null,
  source_type text not null default 'SELF_DECLARED' check (source_type in ('SELF_DECLARED', 'THIRD_PARTY', 'EXTERNAL_REGISTRY', 'SATELLITE', 'FIELD_AUDIT')),
  hash text not null,
  validation_method text not null check (validation_method in ('HASH_INTEGRITY', 'STRUCTURAL_COMPLETENESS')),
  validation_status text not null default 'PENDING' check (validation_status in ('PENDING', 'VERIFIED', 'FAILED', 'EXPIRED')),
  issued_at timestamptz,
  expires_at timestamptz,
  verified_at timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- D-07: hash sempre vem de documents.sha256_hash, nunca recomputado.
create unique index if not exists evidence_document_method_idx
  on evidence (document_id, validation_method)
  where document_id is not null;
-- Uma unica Evidence de completude estrutural por Claim (upsert logico).
create unique index if not exists evidence_claim_structural_idx
  on evidence (claim_id)
  where validation_method = 'STRUCTURAL_COMPLETENESS';
create index if not exists evidence_project_created_idx on evidence (project_id, created_at desc);
create index if not exists evidence_claim_created_idx on evidence (claim_id, created_at desc);

create table if not exists conflicts (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  related_project_id uuid references projects(id),
  type text not null check (type in ('GEOSPATIAL_OVERLAP', 'DUPLICATE_PROPERTY', 'DOUBLE_CLAIM', 'DUPLICATE_DOCUMENT', 'IDENTITY_CONFLICT', 'RIGHTS_CONFLICT', 'EXTERNAL_REGISTRY_CONFLICT')),
  severity text not null check (severity in ('CLEAR', 'INFO', 'LOW', 'MEDIUM', 'HIGH', 'CRITICAL')),
  overlap_percentage numeric(9, 4),
  overlap_area_ha numeric(14, 4),
  status text not null default 'OPEN' check (status in ('OPEN', 'RESOLVED')),
  detected_at timestamptz not null default now(),
  resolved_at timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- D-20: Conflict e RE-DERIVADO a cada mudanca de active_boundary. O unique
-- abaixo garante upsert por par ordenado (nao duplica apos edicao de geometria).
create unique index if not exists conflicts_project_related_type_idx
  on conflicts (project_id, related_project_id, type);
create index if not exists conflicts_project_status_idx on conflicts (project_id, status, severity);

alter table projects add column if not exists integrity_status text not null default 'DECLARED';
alter table projects add column if not exists risk_score integer;

do $$
begin
  if not exists (
    select 1 from pg_constraint
    where conname = 'projects_integrity_status_check'
      and conrelid = 'projects'::regclass
  ) then
    alter table projects
      add constraint projects_integrity_status_check
      check (integrity_status in ('DECLARED', 'IDENTITY_VERIFIED', 'EVIDENCE_PENDING', 'EVIDENCE_VERIFIED', 'UNDER_REVIEW', 'INDEPENDENTLY_VERIFIED', 'VERIFIED', 'ON_HOLD', 'SUSPENDED', 'REVOKED', 'REJECTED'));
  end if;
  if not exists (
    select 1 from pg_constraint
    where conname = 'projects_risk_score_check'
      and conrelid = 'projects'::regclass
  ) then
    alter table projects
      add constraint projects_risk_score_check
      check (risk_score is null or (risk_score >= 0 and risk_score <= 100));
  end if;
end $$;

alter table claims enable row level security;
alter table evidence enable row level security;
alter table conflicts enable row level security;

revoke insert, update, delete on claims, evidence, conflicts from anon, authenticated;

-- Sem policy de select: Conflict revela existencia e proximidade geometrica de
-- projetos de terceiros (mesma classe de sensibilidade de project_boundaries,
-- D-GEO-01 / T-041-11). Exposicao apenas via /api/v1 com guard org-scoped.
```

Criar `supabase/migrations/202608170002_integrity_risk_assessments.sql` com exatamente este conteudo:

```sql
-- Phase 04.2 / INTG-04: Risk Engine append-only (D-19).
-- Uma linha nova de risk_assessments por recalculo (mesmo padrao de
-- certifications); "risco atual" e sempre a ultima linha por created_at.
-- risk_signals carrega a explicabilidade obrigatoria (D-15 / Bible secao 42).

create table if not exists risk_assessments (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  risk_score integer not null check (risk_score >= 0 and risk_score <= 100),
  risk_class text not null check (risk_class in ('LOW', 'MODERATE', 'HIGH', 'VERY_HIGH', 'CRITICAL')),
  integrity_status text not null check (integrity_status in ('DECLARED', 'IDENTITY_VERIFIED', 'EVIDENCE_PENDING', 'EVIDENCE_VERIFIED', 'UNDER_REVIEW', 'INDEPENDENTLY_VERIFIED', 'VERIFIED', 'ON_HOLD', 'SUSPENDED', 'REVOKED', 'REJECTED')),
  auto_hold boolean not null default false,
  trigger text not null,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index if not exists risk_assessments_project_created_idx
  on risk_assessments (project_id, created_at desc);

create table if not exists risk_signals (
  id uuid primary key default uuid_generate_v4(),
  risk_assessment_id uuid not null references risk_assessments(id) on delete cascade,
  code text not null,
  weight numeric(6, 2) not null,
  reason text not null,
  public_safe boolean not null default false,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index if not exists risk_signals_assessment_idx on risk_signals (risk_assessment_id, created_at);

alter table risk_assessments enable row level security;
alter table risk_signals enable row level security;

revoke insert, update, delete on risk_assessments, risk_signals from anon, authenticated;

-- Sem policy de select: score e sinais internos so saem por /api/v1 (visao
-- interna org-scoped) ou pelo dossie publico ja minimizado.
```

Em `backend_app/db/models.py`, adicionar na classe `Project` logo apos a linha `status: Mapped[str] = mapped_column(ProjectStatusEnum, nullable=False, server_default=text("'REGISTERED'"))` (linha 163):

```python
    # Phase 04.2 / D-04: eixo de confianca, INDEPENDENTE de ProjectStatusEnum.
    # Pitfall RESEARCH #4: NUNCA escrever "ON_HOLD" em project.status.
    integrity_status: Mapped[str] = mapped_column(String, nullable=False, server_default=text("'DECLARED'"))
    risk_score: Mapped[int | None] = mapped_column()
```

E acrescentar ao final de `backend_app/db/models.py` as 5 classes abaixo. Todas usam `uuid_pk()`, `created_at_column()`, `metadata_` mapeado para a coluna `"metadata"`, e `String` (NUNCA `Enum`) nos campos de status/tipo/severidade:

- `class Claim(Base)`, `__tablename__ = "claims"`, `__table_args__ = (Index("claims_project_status_idx", "project_id", "status", "created_at"),)`. Campos: `id`; `project_id` (UUID FK projects.id, nullable=False); `claimant_organization_id` (UUID FK organizations.id, nullable); `claimant_profile_id` (UUID FK profiles.id, nullable); `type` (String, nullable=False); `statement` (Text, nullable=False); `status` (String, nullable=False, server_default `text("'DECLARED'")`); `confidence_score` (Mapped[int], nullable=False, server_default `text("10")`); `valid_from` e `valid_to` (DateTime(timezone=True), nullable); `metadata_`; `created_at`; `updated_at = updated_at_column()`.
- `class Evidence(Base)`, `__tablename__ = "evidence"`, `__table_args__ = (Index("evidence_project_created_idx", "project_id", "created_at"),)`. Campos: `id`; `project_id` (FK projects.id, nullable=False); `claim_id` (FK claims.id, nullable — valvula de seguranca D-22); `document_id` (FK documents.id, nullable); `type` (String, nullable=False); `source` (String, nullable=False); `source_type` (String, nullable=False, server_default `text("'SELF_DECLARED'")`); `hash` (String, nullable=False); `validation_method` (String, nullable=False); `validation_status` (String, nullable=False, server_default `text("'PENDING'")`); `issued_at`, `expires_at`, `verified_at` (DateTime(timezone=True), nullable); `metadata_`; `created_at`; `updated_at = updated_at_column()`.
- `class Conflict(Base)`, `__tablename__ = "conflicts"`, `__table_args__ = (Index("conflicts_project_status_idx", "project_id", "status", "severity"),)`. Campos: `id`; `project_id` (FK projects.id, nullable=False); `related_project_id` (FK projects.id, nullable); `type` (String, nullable=False); `severity` (String, nullable=False); `overlap_percentage` (Numeric(9, 4), nullable); `overlap_area_ha` (Numeric(14, 4), nullable); `status` (String, nullable=False, server_default `text("'OPEN'")`); `detected_at` (DateTime(timezone=True), nullable=False, server_default `func.now()`); `resolved_at` (DateTime(timezone=True), nullable); `metadata_`; `created_at`; `updated_at = updated_at_column()`.
- `class ProjectRiskAssessment(Base)`, `__tablename__ = "risk_assessments"`, `__table_args__ = (Index("risk_assessments_project_created_idx", "project_id", "created_at"),)`. Campos: `id`; `project_id` (FK projects.id, nullable=False); `risk_score` (Mapped[int], nullable=False); `risk_class` (String, nullable=False); `integrity_status` (String, nullable=False); `auto_hold` (Boolean, nullable=False, server_default `text("false")`); `trigger` (String, nullable=False); `metadata_`; `created_at`.
- `class RiskSignal(Base)`, `__tablename__ = "risk_signals"`, `__table_args__ = (Index("risk_signals_assessment_idx", "risk_assessment_id", "created_at"),)`. Campos: `id`; `risk_assessment_id` (UUID FK risk_assessments.id, nullable=False); `code` (String, nullable=False); `weight` (Numeric(6, 2), nullable=False); `reason` (Text, nullable=False); `public_safe` (Boolean, nullable=False, server_default `text("false")`); `metadata_`; `created_at`.

Nao alterar nenhuma outra classe. Nao criar nenhum `Enum(...)` novo. Nao tocar em `supabase/seed.sql` (projetos de seed herdam `integrity_status = 'DECLARED'` e `risk_score = null` pelos defaults — comportamento honesto e intencional).
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && python -c "from backend_app.db.models import Claim, Evidence, Conflict, ProjectRiskAssessment, RiskSignal, Project; assert Claim.__tablename__=='claims'; assert Evidence.__tablename__=='evidence'; assert Conflict.__tablename__=='conflicts'; assert ProjectRiskAssessment.__tablename__=='risk_assessments'; assert RiskSignal.__tablename__=='risk_signals'; assert hasattr(Project,'integrity_status') and hasattr(Project,'risk_score'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "create type" supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql` retorna 0
    - `grep -ci "create type" supabase/migrations/202608170002_integrity_risk_assessments.sql` retorna 0
    - `grep -c "enable row level security" supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql` retorna 3
    - `grep -ci "create policy" supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql` retorna 0 e o mesmo para a migration 202608170002
    - `grep -c "Enum(" backend_app/db/models.py` permanece igual ao valor antes da edicao (nenhum Enum novo)
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Migrations escritas e modelos importaveis; nenhum ENUM novo; RLS sem policy de select nas 5 tabelas.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar as migrations no banco local e travar o contrato de schema em teste</name>
  <files>tests/db/test_integrity_schema.py</files>
  <read_first>
    - tests/db/test_project_boundaries_schema.py (arquivo inteiro, 70 linhas — estilo de asserção estática sobre o SQL, helpers read() e sql_without_comments())
    - supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql (escrita na Task 2)
    - supabase/migrations/202608170002_integrity_risk_assessments.sql (escrita na Task 2)
    - .planning/phases/04.1-geospatial-foundation/04.1-01-PLAN.md linhas 456-490 (o comando de reset local usado por este repo)
  </read_first>
  <action>
**Esta task e BLOCKING.** As duas migrations da Task 2 precisam existir antes. Type-check e import de models passam SEM o banco estar atualizado — verificar sem aplicar produziria falso positivo. As Plans 02-05 nao rodam sem estas tabelas no Postgres local.

Passo 1 — aplicar no banco local (o comando canonico deste repo, ja usado na Phase 04.1):

```
npx -y supabase db reset
```

Se o reset falhar, PARAR e reportar como blocker (nao seguir para as Plans 02-05).

Passo 2 — provar que as tabelas existem de fato no banco (nao apenas no arquivo):

```
python -c "
import asyncio
from sqlalchemy import text
from backend_app.db.session import get_sessionmaker

async def run():
    async with get_sessionmaker()() as s:
        rows = (await s.execute(text(\"select table_name from information_schema.tables where table_schema='public' and table_name in ('claims','evidence','conflicts','risk_assessments','risk_signals')\"))).scalars().all()
        cols = (await s.execute(text(\"select column_name from information_schema.columns where table_name='projects' and column_name in ('integrity_status','risk_score')\"))).scalars().all()
        print(sorted(rows), sorted(cols))
        assert len(rows) == 5, rows
        assert len(cols) == 2, cols

asyncio.run(run())
"
```

Passo 3 — criar `tests/db/test_integrity_schema.py` no estilo estatico de `tests/db/test_project_boundaries_schema.py` (sem conexao com banco; apenas leitura dos arquivos SQL), com estes testes:

- `ROOT = Path(__file__).resolve().parents[2]`, `CORE_SQL = ROOT / "supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql"`, `RISK_SQL = ROOT / "supabase/migrations/202608170002_integrity_risk_assessments.sql"`, mais os helpers `read()` e `sql_without_comments()` copiados verbatim do arquivo analogo.
- `test_integrity_core_tables_exist`: `create table if not exists claims`, `create table if not exists evidence`, `create table if not exists conflicts` em `sql_without_comments(CORE_SQL)`.
- `test_risk_tables_exist`: `create table if not exists risk_assessments` e `create table if not exists risk_signals` em `sql_without_comments(RISK_SQL)`.
- `test_no_new_postgres_enum_types`: `"create type" not in sql` para os dois arquivos.
- `test_check_constraints_mirror_constants`: importar `CLAIM_TYPES`, `CLAIM_STATUSES`, `EVIDENCE_SOURCE_TYPES`, `EVIDENCE_VALIDATION_METHODS`, `EVIDENCE_VALIDATION_STATUSES`, `CONFLICT_TYPES`, `CONFLICT_SEVERITIES`, `CONFLICT_STATUSES`, `INTEGRITY_STATUSES`, `RISK_CLASSES` de `backend_app.modules.integrity.constants` e assertar que cada valor de cada tupla aparece como `'valor'` (minusculado, entre aspas simples) no SQL correspondente — garante que constants.py e o check nunca divergem.
- `test_evidence_validation_method_is_capped_to_two_methods`: `"check (validation_method in ('hash_integrity', 'structural_completeness'))"` presente em `sql_without_comments(CORE_SQL)` (D-08).
- `test_integrity_tables_are_backend_only`: para `CORE_SQL`, `sql.count("enable row level security") == 3` e `"revoke insert, update, delete on claims, evidence, conflicts from anon, authenticated"` presente e `"create policy" not in sql`; para `RISK_SQL`, `sql.count("enable row level security") == 2` e `"revoke insert, update, delete on risk_assessments, risk_signals from anon, authenticated"` presente e `"create policy" not in sql`.
- `test_conflicts_have_unique_pair_index_for_resync`: `"create unique index if not exists conflicts_project_related_type_idx"` presente (D-20, evita conflito duplicado apos edicao de geometria).
- `test_claims_have_unique_project_type_index`: `"create unique index if not exists claims_project_type_idx"` presente.
- `test_risk_assessments_are_append_only`: `"risk_assessments_project_created_idx"` presente e `"unique"` NAO aparece em nenhuma linha que contenha `risk_assessments` (D-19: sem unique por project_id).
- `test_projects_columns_are_additive_only`: em `CORE_SQL`, `"alter table projects add column if not exists integrity_status"` e `"...risk_score"` presentes; `"drop column"` ausente; `"alter table projects alter column status"` ausente (D-04: nao mexe no enum operacional).
- `test_existing_tables_are_not_dropped`: `"drop table"` ausente nos dois arquivos.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && pytest tests/db/test_integrity_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `npx -y supabase db reset` concluiu sem erro (registrar as ultimas linhas da saida no SUMMARY)
    - O snippet do Passo 2 imprime as 5 tabelas e as 2 colunas e nao dispara AssertionError
    - `pytest tests/db/test_integrity_schema.py -x -q` passa com 11 ou mais testes coletados
    - `pytest tests/db/ -x -q` continua passando (test_project_boundaries_schema.py sem regressao)
    - `pytest tests/test_project_boundaries.py -x -q` continua passando (colunas novas em projects nao quebram a Phase 04.1)
  </acceptance_criteria>
  <done>Schema do Integrity Layer vivo no Postgres local e travado por teste de contrato estatico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente Supabase (anon/authenticated) -> Postgres | O frontend tem chave anon do Supabase; qualquer tabela sem RLS correta e legivel/gravavel direto, sem passar pelo backend_app |
| migration -> schema existente | Uma migration mal escrita pode alterar semantica de `projects.status` (enum operacional de 16 estados) e quebrar certificacao/tesouraria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Tampering | tabelas claims/evidence/conflicts/risk_assessments/risk_signals | mitigate | `enable row level security` + `revoke insert, update, delete ... from anon, authenticated` nas 5 tabelas; teste `test_integrity_tables_are_backend_only` trava o contrato |
| T-04.2-02 | Information Disclosure | conflicts (revela existencia/proximidade de projeto de terceiro) | mitigate | Nenhuma `create policy` de select nas novas tabelas; leitura so via `/api/v1` com guard org-scoped (Plans 02-04); teste assere `"create policy" not in sql` |
| T-04.2-03 | Tampering | projects.status | mitigate | Migration so faz `add column if not exists`; teste `test_projects_columns_are_additive_only` assere ausencia de `drop column` e de `alter column status`; `integrity_status` e coluna nova e independente |
| T-04.2-04 | Elevation of Privilege | evidence.validation_method | mitigate | Check constraint restringe a `HASH_INTEGRITY`/`STRUCTURAL_COMPLETENESS` (D-08) no nivel do banco — nem um bug de servico consegue gravar metodo de validacao externo nesta fase |
| T-04.2-05 | Tampering | projects.risk_score | mitigate | Check `risk_score is null or between 0 and 100` no banco; escrita apenas pelo backend_app (Plan 04) |
</threat_model>

<verification>
1. `pytest tests/db/ -x -q` — contrato estatico das migrations verde
2. `pytest tests/test_project_boundaries.py -x -q` — Phase 04.1 sem regressao com as colunas novas em `projects`
3. `python -c "from backend_app.main import app; print('import ok')"` — app importa com os modelos novos
4. Passo 2 da Task 3 — 5 tabelas + 2 colunas confirmadas no banco vivo
</verification>

<success_criteria>
- 2 migrations idempotentes aplicadas com `npx -y supabase db reset` sem erro
- 5 modelos SQLAlchemy novos + 2 colunas em `Project`, sem nenhum `Enum` novo
- 13 campos novos em `Settings` com defaults da Bible secao 16/22
- `tests/db/test_integrity_schema.py` verde, cobrindo RLS, checks, indices e a paridade constants.py <-> SQL
- Nenhum teste existente quebrado
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/04.2-integrity-layer-foundation/04.2-01-SUMMARY.md` registrando: nomes finais de tabelas/colunas, saida do `db reset`, contagem de linhas em cada tabela nova apos reset, e quaisquer desvios dos nomes propostos (as Plans 02-05 dependem deles).
</output>
