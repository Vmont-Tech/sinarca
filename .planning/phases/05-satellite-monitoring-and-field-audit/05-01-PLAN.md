---
phase: 05-satellite-monitoring-and-field-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - backend_app/core/config.py
  - backend_app/modules/satellite/__init__.py
  - backend_app/modules/satellite/constants.py
  - backend_app/db/models.py
  - supabase/migrations/202608180001_satellite_observations_anomalies_events.sql
  - supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql
  - supabase/migrations/202608180003_credit_adjustment_pendencies.sql
  - tests/db/test_satellite_schema.py
autonomous: true
requirements: [SATM-05, SATM-06, SATM-09, SATM-10]
user_setup:
  - service: copernicus
    why: "Reconstrução histórica e monitoramento contínuo Sentinel-2 via Copernicus Data Space Ecosystem (D-08/D-10)"
    env_vars:
      - name: COPERNICUS_CLIENT_ID
        source: "dataspace.copernicus.eu -> conta gratuita -> User Settings -> OAuth clients -> Create new client"
      - name: COPERNICUS_CLIENT_SECRET
        source: "dataspace.copernicus.eu -> mesma tela, exibido apenas uma vez na criação do client"
    dashboard_config:
      - task: "Criar conta gratuita no Copernicus Data Space Ecosystem (quota: 10.000 PU/mês, 300 req/min, 2 requests concorrentes)"
        location: "https://dataspace.copernicus.eu"

must_haves:
  truths:
    - "As tabelas satellite_observations, satellite_anomalies, project_events, satellite_evidence, satellite_jobs, copernicus_api_usage e credit_adjustment_pendencies existem no Postgres local depois de npx -y supabase db reset"
    - "satellite_observations rejeita duas linhas com o mesmo (project_id, satellite, scene_id, processing_version)"
    - "Nenhuma das tabelas novas tem policy de SELECT no RLS"
    - "project_events.type so aceita VEGETATION_LOSS, VEGETATION_RECOVERY ou POSSIBLE_FIRE — DEFORESTATION e rejeitado pelo banco"
    - "httpx e dependencia de producao e APScheduler esta no manifesto"
    - "Os thresholds de satelite sao lidos de Settings, nunca hardcoded"
  artifacts:
    - path: "supabase/migrations/202608180001_satellite_observations_anomalies_events.sql"
      provides: "satellite_observations, satellite_anomalies, project_events, satellite_evidence"
      contains: "create table if not exists satellite_observations"
      min_lines: 120
    - path: "supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql"
      provides: "satellite_jobs, copernicus_api_usage"
      contains: "create table if not exists satellite_jobs"
    - path: "supabase/migrations/202608180003_credit_adjustment_pendencies.sql"
      provides: "credit_adjustment_pendencies (analoga a certification_pendencies)"
      contains: "create table if not exists credit_adjustment_pendencies"
    - path: "backend_app/modules/satellite/constants.py"
      provides: "Vocabulario canonico espelhado pelos check constraints"
      contains: "PROJECT_EVENT_TYPES"
      min_lines: 60
    - path: "tests/db/test_satellite_schema.py"
      provides: "Contrato de schema travado em teste"
      min_lines: 60
  key_links:
    - from: "backend_app/modules/satellite/constants.py"
      to: "supabase/migrations/202608180001_satellite_observations_anomalies_events.sql"
      via: "check (... in (...)) espelha exatamente as tuplas"
      pattern: "PROJECT_EVENT_TYPES"
    - from: "backend_app/db/models.py"
      to: "satellite_observations"
      via: "SatelliteObservation ORM model"
      pattern: "satellite_observations"
    - from: "backend_app/core/config.py"
      to: "AnomalyDetector / HistoricalReconstruction"
      via: "satellite_* settings injetados"
      pattern: "satellite_ndvi_drop_threshold"
---

<objective>
Criar toda a fundacao de dados e configuracao da Phase 05: as duas dependencias novas (`httpx` promovido a producao, `APScheduler`), os thresholds configuraveis em `Settings`, o vocabulario canonico do dominio satelital, as sete tabelas operacionais internas (com RLS e idempotencia D-15) e os modelos ORM correspondentes — aplicados no Postgres local por um passo **[BLOCKING]** de `db reset`.

Purpose: nenhum outro plano desta fase roda sem estas tabelas e sem `httpx`/`APScheduler` no manifesto. Este e o unico plano que toca schema; os demais so leem/escrevem atraves dos modelos criados aqui.
Output: 3 migrations idempotentes, 7 modelos ORM, `satellite/constants.py`, bloco `satellite_*` em `config.py`, contrato de schema travado em `tests/db/test_satellite_schema.py`.

**Nao faz parte deste plano:** adapter Copernicus (Plan 03), qualquer logica de negocio, rotas ou frontend. Nenhuma linha de dado real e gravada aqui.

**Nota de Wave 0 (VALIDATION.md):** os cinco arquivos de teste listados como Wave 0 sao criados dentro da mesma task que escreve o codigo de producao correspondente (padrao `tdd="true"` ja usado em `04.2-04-PLAN.md` Task 1), e nao centralizados aqui. Este plano cria o unico teste que so depende do schema (`tests/db/test_satellite_schema.py`) e o diretorio `tests/modules/satellite/` nasce com `test_anomaly_detector.py` no Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-VALIDATION.md

<interfaces>
Padrao de migration idempotente ja estabelecido (`supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql`):
- `create table if not exists`
- `text not null check (col in (...))` para todo campo de vocabulario — NUNCA `CREATE TYPE`/ENUM
- `create index if not exists` / `create unique index if not exists`
- rodape RLS: `alter table X enable row level security;` + `revoke insert, update, delete on X from anon, authenticated;` + comentario justificando a ausencia de policy de SELECT
- `uuid_generate_v4()` como default de PK (extensao ja habilitada desde `202605220001_initial_schema.sql`)
- guard `do $$ begin if not exists (select 1 from pg_constraint where conname = ... and conrelid = 'X'::regclass) then ... end if; end $$;` quando altera tabela existente

Padrao de pendencia estruturada ja existente (`202608150001_certification_workbench.sql`, `certification_pendencies`): `project_id`, `category`, `description`, `status text not null default 'OPEN' check (status in ('OPEN','RESOLVED','CANCELLED'))`, `producer_response`, `responded_by_profile_id`, `responded_at`, `resolved_at`, `metadata jsonb`, `created_at`.

Helpers ORM ja disponiveis em `backend_app/db/models.py`: `uuid_pk()`, `created_at_column()`, `Mapped[...]`/`mapped_column`, `JSONB`, `Numeric`, `UUID(as_uuid=True)`, `text("...")` para server_default. Coluna JSONB de metadados e sempre mapeada como `metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))`.

Bloco de settings ja existente (`backend_app/core/config.py:43-59`) a imitar em estilo (campo flat + comentario PT-BR citando a decisao):
```python
    # Phase 04.2 / D-13: pesos dos sinais de risco (Bible secao 22).
    integrity_risk_weight_overlap_critical: float = 50.0
```

Vocabulario canonico ja existente a imitar (`backend_app/modules/integrity/constants.py:1-8`): comentario de cabecalho declarando que as tuplas sao a fonte unica de verdade espelhada pelos check constraints da migration.

Ultima migration existente: `202608170002_integrity_risk_assessments.sql`. As novas usam o prefixo `202608180001/2/3`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencias novas, thresholds em Settings e vocabulario canonico do dominio satelital</name>
  <files>pyproject.toml, .env.example, backend_app/core/config.py, backend_app/modules/satellite/__init__.py, backend_app/modules/satellite/constants.py</files>
  <read_first>
    - pyproject.toml (arquivo inteiro — `[project] dependencies` e `[dependency-groups] dev`)
    - backend_app/core/config.py (arquivo inteiro, 94 linhas — bloco `integrity_*` linhas 43-59 e o modelo de estilo)
    - backend_app/modules/integrity/constants.py (arquivo inteiro, 145 linhas — cabecalho e formato de tupla a copiar)
    - .env.example (bloco STELLAR_/ETHERFUSE_ linhas 26-37 — formato de declaracao de credencial de provider)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md secoes "Standard Stack" e "Common Pitfalls / Pitfall 1"
  </read_first>
  <action>
**1. `pyproject.toml` — mover `httpx` para producao e adicionar APScheduler (RESEARCH Pitfall 1).**

Em `[project] dependencies`, inserir em ordem alfabetica:
```toml
    "APScheduler>=3.11,<4",
    "httpx>=0.28.1",
```
Em `[dependency-groups] dev`, REMOVER a linha `"httpx>=0.28.1",` (fica so `"pytest>=9.0.3",`). Nao adotar `sentinelhub-py` — RESEARCH "Alternatives Considered" rejeita explicitamente (traz numpy/shapely/pyproj/pillow/tifffile/requests como deps de producao e e sincrona).

Rodar `uv sync` ao final e confirmar que `uv.lock` foi atualizado com `apscheduler`.

**2. `backend_app/core/config.py` — novo bloco de settings**, inserido logo apos `integrity_risk_weight_possession_without_title` (linha 59), antes do `@field_validator`:

```python
    # Phase 05 / D-20: peso do sinal de anomalia satelital confirmada
    # (mesmo padrao configuravel dos demais sinais de D-13/Phase 04.2).
    integrity_risk_weight_satellite_anomaly_critical: float = 50.0
    integrity_risk_weight_satellite_anomaly_high: float = 30.0

    # Phase 05 / D-12: janela da reconstrucao historica (anos anteriores a
    # projects.created_at), granularidade mensal (~60 pontos em 5 anos).
    satellite_historical_years: int = 5

    # Phase 05 / D-13: maxCloudCoverage bloqueia observacoes acima do limite;
    # preferencia documentada por cenas abaixo de satellite_preferred_cloud_coverage_pct.
    satellite_max_cloud_coverage_pct: float = 20.0
    satellite_preferred_cloud_coverage_pct: float = 10.0

    # Phase 05 / D-17: queda relativa de NDVI entre observacoes mensais
    # consecutivas que dispara SatelliteAnomaly (0.15 = queda de 15%).
    satellite_ndvi_drop_threshold: float = 0.15
    satellite_ndvi_recovery_threshold: float = 0.15
    # Queda relativa de NBR que caracteriza assinatura de fogo (POSSIBLE_FIRE).
    satellite_nbr_fire_threshold: float = 0.27

    # Phase 05 / D-14: scheduler in-process (APScheduler). O poller consome a
    # fila persistida em satellite_jobs; o job periodico enfileira monitoramento.
    satellite_scheduler_enabled: bool = True
    satellite_job_poll_seconds: int = 30
    satellite_monitoring_interval_hours: int = 24
    # D-11: quota gratuita CDSE permite 2 requests concorrentes; o lote de
    # projetos por ciclo nunca dispara mais que isso em paralelo.
    satellite_monitoring_batch_size: int = 5
    copernicus_max_concurrent_requests: int = 2
    copernicus_request_timeout_seconds: float = 30.0

    # COPERNICUS_CLIENT_ID/COPERNICUS_CLIENT_SECRET NAO ficam em Settings:
    # sao lidos por os.getenv em CopernicusAdapterConfig.from_env() (Plan 03),
    # mesmo padrao fail-closed de backend_app/adapters/stellar.py.
```

**3. `.env.example` — acrescentar ao final do bloco de providers:**
```
# Copernicus Data Space Ecosystem (Phase 5 / D-10) — conta gratuita em dataspace.copernicus.eu
COPERNICUS_CLIENT_ID=replace-with-copernicus-client-id
COPERNICUS_CLIENT_SECRET=replace-with-copernicus-client-secret
```

**4. `backend_app/modules/satellite/__init__.py`** — arquivo vazio (mesmo padrao de `backend_app/modules/integrity/__init__.py`).

**5. `backend_app/modules/satellite/constants.py`** — vocabulario canonico, cabecalho no mesmo estilo de `integrity/constants.py`:

```python
from __future__ import annotations

# Vocabulario canonico do monitoramento satelital Sinarca (Phase 05, Bible secao 40).
# Estas tuplas sao a fonte unica de verdade: os check constraints das migrations
# 202608180001, 202608180002 e 202608180003 espelham exatamente estas listas.
# Convencao do repo desde a Phase 04: text + check no SQL, String em models.py.
# NUNCA criar tipo ENUM novo no Postgres para estes campos.

SATELLITE_PROVIDERS: tuple[str, ...] = ("COPERNICUS",)
SATELLITE_PLATFORMS: tuple[str, ...] = ("SENTINEL_2",)
SATELLITE_PRODUCTS: tuple[str, ...] = ("L2A",)
SATELLITE_INDEX_NAMES: tuple[str, ...] = ("NDVI", "NDMI", "NBR")

# D-17: SatelliteAnomaly nasce em PENDING_ANALYSIS. LINKED = ja vinculada a um
# ProjectEvent pelo Correlation Engine.
SATELLITE_ANOMALY_STATUSES: tuple[str, ...] = (
    "PENDING_ANALYSIS",
    "ANALYZED",
    "LINKED",
    "DISMISSED",
)

# D-17: nunca DEFORESTATION automatico. Vocabulario inicial FECHADO.
PROJECT_EVENT_TYPES: tuple[str, ...] = (
    "VEGETATION_LOSS",
    "VEGETATION_RECOVERY",
    "POSSIBLE_FIRE",
)

# D-18: DETECTED -> ANALYZED -> CONFIRMED/DISMISSED.
# Nunca existe transicao automatica direta DETECTED -> CONFIRMED.
PROJECT_EVENT_STATUSES: tuple[str, ...] = (
    "DETECTED",
    "ANALYZED",
    "CONFIRMED",
    "DISMISSED",
)
PROJECT_EVENT_TRANSITIONS: dict[str, tuple[str, ...]] = {
    "DETECTED": ("ANALYZED", "DISMISSED"),
    "ANALYZED": ("CONFIRMED", "DISMISSED"),
    "CONFIRMED": (),
    "DISMISSED": (),
}

PROJECT_EVENT_SEVERITIES: tuple[str, ...] = ("LOW", "MEDIUM", "HIGH", "CRITICAL")
# D-20: so severidade HIGH/CRITICAL confirmada vira sinal de risco.
PROJECT_EVENT_RISK_SEVERITIES: tuple[str, ...] = ("HIGH", "CRITICAL")

# D-17: faixas de severidade por queda relativa de NDVI. Mesmo formato de
# RISK_CLASS_BOUNDS (integrity/constants.py:96-103): primeiro limite >= valor.
NDVI_DROP_SEVERITY_BOUNDS: tuple[tuple[float, str], ...] = (
    (0.25, "LOW"),
    (0.35, "MEDIUM"),
    (0.50, "HIGH"),
    (1.00, "CRITICAL"),
)

SATELLITE_JOB_TYPES: tuple[str, ...] = ("HISTORICAL_RECONSTRUCTION", "CONTINUOUS_MONITORING")
# D-14: estados persistidos do job assincrono.
SATELLITE_JOB_STATUSES: tuple[str, ...] = ("PENDING", "PROCESSING", "COMPLETED", "FAILED")
SATELLITE_JOB_ACTIVE_STATUSES: tuple[str, ...] = ("PENDING", "PROCESSING")

# D-19: evidencia visual so e buscada quando a anomalia chega a ANALYZED.
SATELLITE_EVIDENCE_KINDS: tuple[str, ...] = ("BEFORE_IMAGE", "AFTER_IMAGE", "STATISTICS_SNAPSHOT")

# D-26: observabilidade de consumo como linhas estruturadas (sem Prometheus nesta fase).
COPERNICUS_USAGE_ENDPOINTS: tuple[str, ...] = ("TOKEN", "STAC_SEARCH", "STATISTICS", "PROCESS")
COPERNICUS_USAGE_OUTCOMES: tuple[str, ...] = ("SUCCESS", "ERROR")

# D-23: pendencia de revisao manual de credito, analoga a certification_pendencies.
CREDIT_ADJUSTMENT_PENDENCY_STATUSES: tuple[str, ...] = ("OPEN", "RESOLVED", "CANCELLED")
CREDIT_ADJUSTMENT_PENDENCY_CATEGORY = "SATELLITE_INCIDENT"

# D-07/D-25: valor gravado em projects.metadata.baseline_source quando o
# baseline exibido passa a vir de observacao Sentinel-2 real.
BASELINE_SOURCE_COPERNICUS = "COPERNICUS"
BASELINE_SOURCE_DETERMINISTIC = "deterministic_baseline"
SENTINEL_STATUS_BLOCKED = "BLOCKED_MISSING_PROVIDER_CREDENTIALS"
SENTINEL_STATUS_ACTIVE = "ACTIVE_COPERNICUS_SENTINEL_2"

# Guarda explicita da regra de aceite SATM-06: este rotulo NUNCA pode ser
# produzido automaticamente por nenhum caminho de codigo desta fase.
FORBIDDEN_AUTOMATIC_EVENT_TYPES: frozenset[str] = frozenset({"DEFORESTATION"})
```
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv sync && uv run python -c "import httpx, apscheduler; from apscheduler.schedulers.asyncio import AsyncIOScheduler; from backend_app.core.config import Settings; from backend_app.modules.satellite.constants import PROJECT_EVENT_TYPES, PROJECT_EVENT_STATUSES, FORBIDDEN_AUTOMATIC_EVENT_TYPES; s=Settings(); assert s.satellite_historical_years==5; assert s.satellite_max_cloud_coverage_pct==20.0; assert s.satellite_ndvi_drop_threshold==0.15; assert s.copernicus_max_concurrent_requests==2; assert PROJECT_EVENT_TYPES==('VEGETATION_LOSS','VEGETATION_RECOVERY','POSSIBLE_FIRE'); assert 'DEFORESTATION' not in PROJECT_EVENT_TYPES; assert 'DEFORESTATION' in FORBIDDEN_AUTOMATIC_EVENT_TYPES; assert PROJECT_EVENT_STATUSES==('DETECTED','ANALYZED','CONFIRMED','DISMISSED'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^\[project\]/,/^\[tool/p' pyproject.toml | grep -c httpx` retorna 1
    - `sed -n '/^\[dependency-groups\]/,$p' pyproject.toml | grep -c httpx` retorna 0
    - `grep -c 'APScheduler' pyproject.toml` retorna 1
    - `grep -ic 'sentinelhub' pyproject.toml` retorna 0
    - `grep -c 'apscheduler' uv.lock` maior ou igual a 1
    - `grep -v '^ *#' backend_app/core/config.py | grep -c 'satellite_'` maior ou igual a 12
    - `grep -v '^ *#' backend_app/core/config.py | grep -c 'copernicus_client_id'` retorna 0
    - `grep -c 'COPERNICUS_CLIENT_ID' .env.example` retorna 1
    - `grep -v '^#' backend_app/modules/satellite/constants.py | grep -ic 'create type'` retorna 0
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Dependencias de producao corretas, thresholds configuraveis e vocabulario fechado disponiveis para todos os planos seguintes.</done>
</task>

<task type="auto">
  <name>Task 2: Tres migrations idempotentes (7 tabelas) e os modelos ORM correspondentes</name>
  <files>supabase/migrations/202608180001_satellite_observations_anomalies_events.sql, supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql, supabase/migrations/202608180003_credit_adjustment_pendencies.sql, backend_app/db/models.py</files>
  <read_first>
    - supabase/migrations/202608170001_integrity_claims_evidence_conflicts.sql (arquivo inteiro, 116 linhas — padrao exato de tabela + indice + rodape RLS)
    - supabase/migrations/202608150001_certification_workbench.sql linhas 13-65 (certification_pendencies + rodape RLS)
    - backend_app/db/models.py linhas 1-100 (helpers uuid_pk/created_at_column) e linhas 588-680 (Claim/Evidence/Conflict/ProjectRiskAssessment/RiskSignal — estilo a copiar)
    - backend_app/modules/satellite/constants.py (Task 1 — as tuplas que os checks devem espelhar)
  </read_first>
  <action>
**Migration A — `supabase/migrations/202608180001_satellite_observations_anomalies_events.sql`**

Cabecalho em comentario PT-BR: `-- Phase 05 (SATM-05/06) — observacoes, anomalias, eventos e evidencia satelital (Bible secao 40).`

```sql
create table if not exists satellite_observations (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  provider text not null default 'COPERNICUS' check (provider in ('COPERNICUS')),
  satellite text not null default 'SENTINEL_2' check (satellite in ('SENTINEL_2')),
  product text not null default 'L2A' check (product in ('L2A')),
  scene_id text not null,
  processing_version text not null default 'v1',
  observed_at timestamptz not null,
  cloud_coverage numeric(5, 2),
  ndvi_mean numeric(6, 4),
  ndvi_min numeric(6, 4),
  ndvi_max numeric(6, 4),
  ndmi_mean numeric(6, 4),
  nbr_mean numeric(6, 4),
  valid_pixel_percentage numeric(5, 2),
  source text not null default 'STATISTICAL_API',
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

-- D-15: idempotencia exata exigida pelo CONTEXT.md e por SATM-10.
create unique index if not exists satellite_observations_idempotency_idx
  on satellite_observations (project_id, satellite, scene_id, processing_version);
create index if not exists satellite_observations_project_observed_idx
  on satellite_observations (project_id, observed_at desc);

create table if not exists satellite_anomalies (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  observation_id uuid not null references satellite_observations(id),
  previous_observation_id uuid references satellite_observations(id),
  index_name text not null default 'NDVI' check (index_name in ('NDVI', 'NDMI', 'NBR')),
  value_before numeric(6, 4),
  value_after numeric(6, 4),
  drop_ratio numeric(6, 4),
  severity text not null check (severity in ('LOW', 'MEDIUM', 'HIGH', 'CRITICAL')),
  confidence numeric(5, 2),
  affected_area_ha numeric(14, 4),
  status text not null default 'PENDING_ANALYSIS'
    check (status in ('PENDING_ANALYSIS', 'ANALYZED', 'LINKED', 'DISMISSED')),
  reason text,
  detected_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

-- Idempotencia do detector: uma anomalia por (projeto, observacao, indice).
create unique index if not exists satellite_anomalies_idempotency_idx
  on satellite_anomalies (project_id, observation_id, index_name);
create index if not exists satellite_anomalies_project_status_idx
  on satellite_anomalies (project_id, status, detected_at desc);

-- D-17: vocabulario FECHADO. Classificacao juridica de desmatamento esta
-- fora de escopo (Bible), por isso o rotulo correspondente nao existe no check.
-- ATENCAO: project_events e uma tabela de DOMINIO, distinta de audit_events
-- (trilha generica write-once). Cada transicao aqui gera TAMBEM um audit_event.
create table if not exists project_events (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  anomaly_id uuid references satellite_anomalies(id),
  type text not null check (type in ('VEGETATION_LOSS', 'VEGETATION_RECOVERY', 'POSSIBLE_FIRE')),
  status text not null default 'DETECTED'
    check (status in ('DETECTED', 'ANALYZED', 'CONFIRMED', 'DISMISSED')),
  severity text not null check (severity in ('LOW', 'MEDIUM', 'HIGH', 'CRITICAL')),
  confidence numeric(5, 2),
  affected_area_ha numeric(14, 4),
  ndvi_before numeric(6, 4),
  ndvi_after numeric(6, 4),
  summary text,
  detected_at timestamptz not null default now(),
  analyzed_at timestamptz,
  decided_at timestamptz,
  decided_by_profile_id uuid references profiles(id),
  decision_notes text,
  -- D-22: desbloqueio auditavel. cleared_at nunca e preenchido por timeout;
  -- so por decisao humana explicita (ANOMALY_REVIEW_CLEARED).
  cleared_at timestamptz,
  cleared_by_profile_id uuid references profiles(id),
  clearance_notes text,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create unique index if not exists project_events_anomaly_idx
  on project_events (anomaly_id) where anomaly_id is not null;
create index if not exists project_events_project_status_idx
  on project_events (project_id, status, detected_at desc);

create table if not exists satellite_evidence (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  project_event_id uuid references project_events(id),
  anomaly_id uuid references satellite_anomalies(id),
  kind text not null check (kind in ('BEFORE_IMAGE', 'AFTER_IMAGE', 'STATISTICS_SNAPSHOT')),
  storage_bucket text not null default 'projects',
  storage_object_path text,
  storage_path text,
  sha256_hash text not null,
  mime_type text not null default 'image/png',
  size_bytes bigint,
  captured_at timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

-- D-19: no maximo um before e um after por evento.
create unique index if not exists satellite_evidence_event_kind_idx
  on satellite_evidence (project_event_id, kind) where project_event_id is not null;
create index if not exists satellite_evidence_project_idx
  on satellite_evidence (project_id, created_at desc);

alter table satellite_observations enable row level security;
alter table satellite_anomalies enable row level security;
alter table project_events enable row level security;
alter table satellite_evidence enable row level security;
revoke insert, update, delete on satellite_observations, satellite_anomalies, project_events, satellite_evidence from anon, authenticated;
-- D-16: sem policy de select. Observacao/anomalia/evento revelam estado
-- operacional e geometria de projeto de terceiro (mesma classe de
-- sensibilidade de claims/evidence/conflicts) — leitura apenas via /api/v1
-- com guard org-scoped.
```

**Migration B — `supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql`**

```sql
-- D-14: fila persistida do scheduler in-process. "Enfileirar" = inserir uma
-- linha PENDING; o poller do APScheduler consome. Sobrevive a restart.
create table if not exists satellite_jobs (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  job_type text not null check (job_type in ('HISTORICAL_RECONSTRUCTION', 'CONTINUOUS_MONITORING')),
  status text not null default 'PENDING'
    check (status in ('PENDING', 'PROCESSING', 'COMPLETED', 'FAILED')),
  attempts integer not null default 0,
  observations_persisted integer not null default 0,
  anomalies_detected integer not null default 0,
  window_start timestamptz,
  window_end timestamptz,
  started_at timestamptz,
  finished_at timestamptz,
  error_message text,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Idempotencia do enfileiramento: no maximo um job ativo por (projeto, tipo).
create unique index if not exists satellite_jobs_active_idx
  on satellite_jobs (project_id, job_type) where status in ('PENDING', 'PROCESSING');
create index if not exists satellite_jobs_status_created_idx
  on satellite_jobs (status, created_at asc);

-- D-26: consumo Copernicus como linhas estruturadas (sem Prometheus nesta fase).
create table if not exists copernicus_api_usage (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid references projects(id),
  satellite_job_id uuid references satellite_jobs(id),
  endpoint text not null check (endpoint in ('TOKEN', 'STAC_SEARCH', 'STATISTICS', 'PROCESS')),
  outcome text not null check (outcome in ('SUCCESS', 'ERROR')),
  http_status integer,
  processing_units numeric(10, 4),
  duration_ms integer,
  error_code text,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index if not exists copernicus_api_usage_created_idx
  on copernicus_api_usage (created_at desc);
create index if not exists copernicus_api_usage_endpoint_idx
  on copernicus_api_usage (endpoint, outcome, created_at desc);

alter table satellite_jobs enable row level security;
alter table copernicus_api_usage enable row level security;
revoke insert, update, delete on satellite_jobs, copernicus_api_usage from anon, authenticated;
-- D-16/D-26: tabelas operacionais internas; consumo de quota e estado de job
-- nao sao dado publico — leitura apenas via /api/v1 interno.
```

**Migration C — `supabase/migrations/202608180003_credit_adjustment_pendencies.sql`**

Espelha `certification_pendencies` (`202608150001_certification_workbench.sql:13-34`), trocando `certification_id`/`certifier_organization_id` por `project_event_id` e adicionando `affected_area_ha` (D-23):

```sql
-- D-23: "recalculo de creditos apos incidente" NAO calcula toneladas de
-- carbono a partir de NDVI (fora de escopo na Bible). Cria uma pendencia
-- estruturada de revisao MANUAL, analoga a certification_pendencies, e o
-- projeto fica indisponivel para venda/mint enquanto o Auto Hold durar.
create table if not exists credit_adjustment_pendencies (
  id uuid primary key default uuid_generate_v4(),
  project_id uuid not null references projects(id),
  project_event_id uuid references project_events(id),
  raised_by_profile_id uuid references profiles(id),
  category text not null default 'SATELLITE_INCIDENT',
  description text not null,
  affected_area_ha numeric(14, 4),
  status text not null default 'OPEN' check (status in ('OPEN', 'RESOLVED', 'CANCELLED')),
  producer_response text,
  responded_by_profile_id uuid references profiles(id),
  responded_at timestamptz,
  resolved_at timestamptz,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index if not exists credit_adjustment_pendencies_project_status_idx
  on credit_adjustment_pendencies (project_id, status, created_at desc);
create unique index if not exists credit_adjustment_pendencies_event_idx
  on credit_adjustment_pendencies (project_event_id) where project_event_id is not null;

alter table credit_adjustment_pendencies enable row level security;
revoke insert, update, delete on credit_adjustment_pendencies from anon, authenticated;
-- Mesma classe de sensibilidade de certification_pendencies
-- (202608150001_certification_workbench.sql:58-65): sem policy de select.
```

**Modelos ORM — `backend_app/db/models.py`**, acrescentados ao final do arquivo, apos `RiskSignal`, seguindo exatamente o estilo dos modelos da Phase 04.2:

`SatelliteObservation` (`satellite_observations`), `SatelliteAnomaly` (`satellite_anomalies`), `ProjectEvent` (`project_events`), `SatelliteEvidence` (`satellite_evidence`), `SatelliteJob` (`satellite_jobs`), `CopernicusApiUsage` (`copernicus_api_usage`), `CreditAdjustmentPendency` (`credit_adjustment_pendencies`).

Regras de mapeamento:
- Toda coluna de vocabulario e `Mapped[str] = mapped_column(String, nullable=False, server_default=text("'X'"))` — nunca `Enum`.
- Toda coluna `numeric(p, s)` vira `Mapped[Decimal | None] = mapped_column(Numeric(p, s))`.
- `metadata_: Mapped[dict] = mapped_column("metadata", JSONB, nullable=False, server_default=text("'{}'::jsonb"))`.
- `created_at: Mapped[datetime] = created_at_column()`.
- `SatelliteObservation.__table_args__ = (UniqueConstraint("project_id", "satellite", "scene_id", "processing_version", name="satellite_observations_idempotency_idx"),)` — declara a chave D-15 tambem no ORM, para que `on_conflict_do_nothing(index_elements=[...])` funcione no Plan 05.
- `SatelliteJob.updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=text("now()"))`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
from backend_app.db.models import (SatelliteObservation, SatelliteAnomaly, ProjectEvent, SatelliteEvidence, SatelliteJob, CopernicusApiUsage, CreditAdjustmentPendency)
expected = {SatelliteObservation:'satellite_observations', SatelliteAnomaly:'satellite_anomalies', ProjectEvent:'project_events', SatelliteEvidence:'satellite_evidence', SatelliteJob:'satellite_jobs', CopernicusApiUsage:'copernicus_api_usage', CreditAdjustmentPendency:'credit_adjustment_pendencies'}
for model, table in expected.items():
    assert model.__tablename__ == table, (model, model.__tablename__)
cols = {c.name for c in SatelliteObservation.__table__.columns}
for required in ('project_id','satellite','scene_id','processing_version','observed_at','cloud_coverage','ndvi_mean','ndmi_mean','nbr_mean'):
    assert required in cols, required
ev = {c.name for c in ProjectEvent.__table__.columns}
for required in ('type','status','severity','cleared_at','cleared_by_profile_id','decided_by_profile_id','affected_area_ha'):
    assert required in ev, required
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'create table if not exists' supabase/migrations/202608180001_satellite_observations_anomalies_events.sql` retorna 4
    - `grep -c 'create table if not exists' supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql` retorna 2
    - `grep -c 'create table if not exists' supabase/migrations/202608180003_credit_adjustment_pendencies.sql` retorna 1
    - `cat supabase/migrations/2026081800*.sql | grep -v '^ *--' | grep -ic 'create type'` retorna 0
    - `cat supabase/migrations/2026081800*.sql | grep -ic 'deforestation'` retorna 0
    - `cat supabase/migrations/2026081800*.sql | grep -v '^ *--' | grep -c 'enable row level security'` retorna 7
    - `cat supabase/migrations/2026081800*.sql | grep -v '^ *--' | grep -c 'revoke insert, update, delete'` retorna 3
    - `cat supabase/migrations/2026081800*.sql | grep -v '^ *--' | grep -ic 'create policy'` retorna 0
    - `grep -c 'satellite_observations_idempotency_idx' supabase/migrations/202608180001_satellite_observations_anomalies_events.sql` retorna 1
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Schema completo escrito de forma idempotente e espelhado por modelos ORM importaveis.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar as migrations no Postgres local e travar o contrato de schema em teste</name>
  <files>tests/db/test_satellite_schema.py</files>
  <read_first>
    - tests/db/test_integrity_schema.py (arquivo inteiro — padrao exato de teste de contrato de schema por leitura do SQL)
    - supabase/migrations/202608180001_satellite_observations_anomalies_events.sql (Task 2)
    - supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql (Task 2)
    - supabase/migrations/202608180003_credit_adjustment_pendencies.sql (Task 2)
    - backend_app/modules/satellite/constants.py (Task 1)
    - .planning/phases/04.2-integrity-layer-foundation/04.2-01-SUMMARY.md secao "Rule 3" (armadilha conhecida: seed.sql desalinhado quebra o db reset)
  </read_first>
  <action>
**Esta task e BLOCKING.** As tres migrations da Task 2 e os modelos ORM precisam existir antes. Import de modelos e type-check passam SEM o banco estar atualizado — verificar sem aplicar produziria um falso positivo. Os Plans 02-09 nao rodam sem estas tabelas no Postgres local.

**1. Aplicar o schema (comando canonico deste repo, ja usado em `04.1-01-PLAN.md` e `04.2-01-PLAN.md`):**
```bash
npx -y supabase db reset
```
Registrar a saida (ultimas linhas) no SUMMARY. Se falhar por causa de `supabase/seed.sql` (armadilha ja ocorrida na Phase 04.2, Rule 3), corrigir o seed no mesmo commit e registrar como desvio — o reset e obrigatorio.

Observacao: o script `npm run db:reset` roda `npx supabase db reset` seguido de `scripts/backfill_project_integrity.py`. Usar `npm run db:reset` se o backfill de integridade tambem for desejado; para esta task o `npx -y supabase db reset` puro basta.

**2. Confirmar a existencia real das 7 tabelas:**
```bash
PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "
select tablename from pg_tables
where schemaname='public'
  and tablename in ('satellite_observations','satellite_anomalies','project_events','satellite_evidence','satellite_jobs','copernicus_api_usage','credit_adjustment_pendencies')
order by tablename;"
```
Deve listar exatamente 7 nomes.

**3. Provar a idempotencia D-15 e a ausencia de policy (D-16) contra o banco vivo** (registrar as saidas no SUMMARY):
```bash
PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "
select indexdef from pg_indexes where indexname='satellite_observations_idempotency_idx';"

PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "
select count(*) from pg_policies where schemaname='public'
  and tablename in ('satellite_observations','satellite_anomalies','project_events','satellite_evidence','satellite_jobs','copernicus_api_usage','credit_adjustment_pendencies');"
```
O segundo comando deve retornar `0`.

**4. Provar que o rotulo proibido e rejeitado pelo proprio banco (SATM-06):**
```bash
PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "
insert into project_events (project_id, type, severity)
select id, 'DEFORESTATION', 'HIGH' from projects limit 1;"
```
Deve falhar com `violates check constraint "project_events_type_check"`. Registrar a mensagem exata no SUMMARY.

**5. Criar `tests/db/test_satellite_schema.py`** no mesmo formato de `tests/db/test_integrity_schema.py` (leitura do texto das migrations, sem banco — roda em qualquer ambiente):

```python
from __future__ import annotations

from pathlib import Path

from backend_app.modules.satellite.constants import (
    COPERNICUS_USAGE_ENDPOINTS,
    CREDIT_ADJUSTMENT_PENDENCY_STATUSES,
    PROJECT_EVENT_SEVERITIES,
    PROJECT_EVENT_STATUSES,
    PROJECT_EVENT_TYPES,
    SATELLITE_ANOMALY_STATUSES,
    SATELLITE_EVIDENCE_KINDS,
    SATELLITE_JOB_STATUSES,
    SATELLITE_JOB_TYPES,
)

ROOT = Path(__file__).resolve().parents[2]
CORE_SQL = ROOT / "supabase/migrations/202608180001_satellite_observations_anomalies_events.sql"
JOBS_SQL = ROOT / "supabase/migrations/202608180002_satellite_jobs_copernicus_usage.sql"
CREDIT_SQL = ROOT / "supabase/migrations/202608180003_credit_adjustment_pendencies.sql"


def sql_without_comments(path: Path) -> str:
    return "\n".join(
        line for line in path.read_text(encoding="utf-8").splitlines()
        if not line.strip().startswith("--")
    ).lower()
```

Testes obrigatorios (todos operando sobre `sql_without_comments`, nunca sobre o texto cru — comentario nao pode satisfazer um gate):
1. `test_satellite_core_tables_exist` — as 4 tabelas da migration A.
2. `test_satellite_jobs_and_usage_tables_exist` — as 2 tabelas da migration B.
3. `test_credit_adjustment_pendencies_table_exists` — a tabela da migration C.
4. `test_observation_idempotency_index_matches_d15` — o SQL contem `create unique index if not exists satellite_observations_idempotency_idx` e `(project_id, satellite, scene_id, processing_version)`.
5. `test_project_event_vocabulary_mirrors_constants` — para cada valor em `PROJECT_EVENT_TYPES`, `PROJECT_EVENT_STATUSES` e `PROJECT_EVENT_SEVERITIES`, `f"'{value.lower()}'"` esta presente no SQL da migration A.
6. `test_anomaly_and_evidence_vocabulary_mirrors_constants` — idem para `SATELLITE_ANOMALY_STATUSES` e `SATELLITE_EVIDENCE_KINDS`.
7. `test_job_and_usage_vocabulary_mirrors_constants` — idem para `SATELLITE_JOB_TYPES`, `SATELLITE_JOB_STATUSES`, `COPERNICUS_USAGE_ENDPOINTS` (migration B) e `CREDIT_ADJUSTMENT_PENDENCY_STATUSES` (migration C).
8. `test_no_enum_type_is_created` — `"create type"` nao aparece em nenhuma das tres.
9. `test_deforestation_is_never_a_valid_event_type` — a substring `"deforestation"` nao aparece em nenhuma das tres migrations (SATM-06 travado no schema).
10. `test_rls_enabled_without_select_policy` — cada migration contem `enable row level security` e `revoke insert, update, delete`, e nenhuma contem `create policy`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/db/test_satellite_schema.py -x && PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "select count(*) from pg_tables where schemaname='public' and tablename in ('satellite_observations','satellite_anomalies','project_events','satellite_evidence','satellite_jobs','copernicus_api_usage','credit_adjustment_pendencies');" | tr -d ' ' | grep -qx 7 && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx -y supabase db reset` concluiu sem erro (registrar o tail da saida no SUMMARY)
    - A query de `pg_tables` retorna exatamente `7`
    - A query de `pg_policies` sobre as 7 tabelas retorna `0`
    - O `insert ... 'DEFORESTATION'` falha com `violates check constraint "project_events_type_check"` (mensagem no SUMMARY)
    - `uv run pytest -q tests/db/test_satellite_schema.py` coleta 10 ou mais testes e todos passam
    - `uv run pytest -q` (suite completa) passa sem regressao
  </acceptance_criteria>
  <done>Schema aplicado no Postgres local, idempotencia e vocabulario fechado provados contra o banco vivo, contrato travado em teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated (PostgREST) -> tabelas novas | Supabase expoe qualquer tabela sem RLS diretamente ao cliente; 7 tabelas novas cruzam essa fronteira |
| codigo da aplicacao -> vocabulario de dominio | Um valor fora do vocabulario (ex. `DEFORESTATION`) violaria uma regra de aceite se o banco nao o rejeitasse |
| ambiente de producao -> manifesto de dependencias | `httpx` em grupo dev quebraria producao em runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | satellite_observations / satellite_anomalies / project_events / satellite_evidence | mitigate | RLS habilitado e nenhuma policy de SELECT criada (D-16); DML revogado de `anon, authenticated`; teste 10 e a query de `pg_policies` (=0) provam. Mesma classe de sensibilidade de `claims`/`conflicts` da Phase 04.2 |
| T-05-02 | Information Disclosure | copernicus_api_usage | mitigate | Mesma RLS sem policy; a tabela nunca armazena `COPERNICUS_CLIENT_SECRET` nem corpo de resposta do token endpoint — apenas endpoint/outcome/status/duracao/PU (colunas fixas, sem campo livre para payload) |
| T-05-03 | Tampering | project_events.type | mitigate | `check (type in ('VEGETATION_LOSS','VEGETATION_RECOVERY','POSSIBLE_FIRE'))` — o banco rejeita `DEFORESTATION` mesmo se o codigo tentar; provado por insert real na Task 3 |
| T-05-04 | Tampering | duplicidade de observacao satelital | mitigate | `unique index satellite_observations_idempotency_idx (project_id, satellite, scene_id, processing_version)` (D-15); o Postgres garante, nao lock de aplicacao |
| T-05-05 | Denial of Service | quota Copernicus estourada por job duplicado | accept (mitigado parcialmente) | `satellite_jobs_active_idx` (unique parcial em PENDING/PROCESSING) impede enfileiramento duplicado por projeto; a coordenacao entre multiplos workers Uvicorn e tratada no Plan 05 (RESEARCH Pitfall 2). Nesta task apenas a garantia de banco existe |
| T-05-06 | Elevation of Privilege | producao sem `httpx` instalado | mitigate | `httpx` movido para `[project] dependencies`; gate de aceite verifica os dois grupos separadamente (RESEARCH Pitfall 1) |
| T-05-07 | Repudiation | schema aplicado apenas em codigo, nao no banco | mitigate | Task 3 e BLOCKING e exige prova por `psql` contra o banco vivo, nao por import de modelo |
</threat_model>

<verification>
1. `uv run python -c "import httpx, apscheduler"` — dependencias instaladas
2. `uv run pytest -q tests/db/test_satellite_schema.py` — contrato de schema verde
3. `npx -y supabase db reset` — migrations aplicadas sem erro
4. Query `pg_tables` retorna 7; query `pg_policies` retorna 0
5. `uv run pytest -q` — suite completa sem regressao
</verification>

<success_criteria>
- `httpx` em `[project] dependencies` e `APScheduler>=3.11,<4` no manifesto; `sentinelhub` ausente
- 7 tabelas criadas com `text + check`, RLS habilitado, DML revogado e zero policies
- `satellite_observations` idempotente por `(project_id, satellite, scene_id, processing_version)`
- `DEFORESTATION` rejeitado pelo banco
- Thresholds de satelite disponiveis em `Settings` com os defaults de D-12/D-13/D-17
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-01-SUMMARY.md` registrando: nomes finais de tabelas/colunas (os Plans 02-09 dependem deles), saida do `db reset`, mensagem exata da rejeicao do `DEFORESTATION`, contagem de linhas em cada tabela nova apos reset (esperado 0) e quaisquer desvios dos nomes propostos.
</output>
