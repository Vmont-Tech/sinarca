---
phase: 05-satellite-monitoring-and-field-audit
plan: 08
type: execute
wave: 6
depends_on: ["05-02", "05-07"]
files_modified:
  - src/services/auditEvidence.ts
  - src/pages/Dashboard/AuditorReview.tsx
  - src/pages/Dashboard/MrcaDetails.tsx
autonomous: false
requirements: [SATM-01, SATM-02, SATM-03, SATM-04, SATM-07]

must_haves:
  truths:
    - "O auditor seleciona um arquivo e ele e enviado de verdade ao backend, com estado visivel de enviando/enviado/erro por linha"
    - "Nenhuma string local:// e construida ou exibida em nenhum ponto da tela de auditoria"
    - "A assinatura deixa de ser um input de texto livre e vira um badge somente leitura rotulado como stub SHA-256"
    - "O auditor ve um bloqueio explicito quando o dispositivo nao tem leitor NFC, com confirmacao manual obrigatoria e nunca auto-marcada"
    - "O verify envia Document.id reais dos uploads da mesma sessao"
    - "O dossie publico mostra baseline Sentinel real sem tile ambar quando ha observacao Copernicus, e mantem o ambar apenas no estado bloqueado"
    - "O dossie publico mostra existencia, data e conclusao da auditoria sem laudo interno, coordenadas ou assinatura completa"
  artifacts:
    - path: "src/services/auditEvidence.ts"
      provides: "uploadAuditEvidence via FormData (D-01)"
      contains: "FormData"
      min_lines: 45
    - path: "src/pages/Dashboard/AuditorReview.tsx"
      provides: "Upload real, badge de assinatura, banner NFC fail-closed"
      contains: "Assinatura verificável (stub SHA-256)"
    - path: "src/pages/Dashboard/MrcaDetails.tsx"
      provides: "Tiles de baseline condicionais e bloco de auditoria minimizado"
      contains: "Copernicus Sentinel-2 (observação real)"
  key_links:
    - from: "src/services/auditEvidence.ts"
      to: "POST /api/v1/audit/{project_id}/evidence"
      via: "apiPost com FormData (multipart)"
      pattern: "audit/.*evidence"
    - from: "src/pages/Dashboard/AuditorReview.tsx"
      to: "PATCH /api/v1/audit/verify/{project_id}"
      via: "evidencias_url com Document.id reais"
      pattern: "evidencias_url"
    - from: "src/pages/Dashboard/MrcaDetails.tsx"
      to: "dossier.satellite / dossier.baseline"
      via: "tiles condicionais por baseline_source"
      pattern: "baseline_source"
---

<objective>
Trocar o rascunho local da tela de auditoria de campo por uma experiencia real: upload multipart com estado por arquivo (D-01/D-06), badge de assinatura verificavel somente leitura (D-03), releitura de QTAG/NFC fail-closed com confirmacao manual (D-04), e o reflexo disso no dossie publico — tiles de baseline condicionais (D-25/SATM-07) e bloco de auditoria minimizado (D-05/SATM-03).

Purpose: fecha a metade de UI de SATM-01/02/03/04 e a Surface C de SATM-07. Sem este plano, os endpoints reais dos Plans 02/07 existem mas o auditor continua gerando `local://`.
Output: `src/services/auditEvidence.ts`, `AuditorReview.tsx` evoluido (nunca reescrito), `MrcaDetails.tsx` com tiles condicionais e bloco de auditoria publico.

**Nao faz parte deste plano:** o dashboard satelital `MonitoringNDVI.tsx` (Plan 09) e qualquer alteracao de backend. As duas superficies tocadas aqui sao a **A (Field Audit, tema claro/azul)** e a **C (Public Dossier, claro sobre branco)** do UI-SPEC — nao misturar com a paleta escura da Surface B.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-02-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md

<interfaces>
Padrao de upload real ja existente a copiar (`src/services/projectDocuments.ts:33-60`):
```ts
export const uploadProjectDocument = async (projectId, documentType, file): Promise<UploadedProjectDocument> => {
    const body = new FormData();
    body.append('document_type', documentType);
    body.append('file', file);
    const response = await apiPost<ProjectDocumentUploadResponse>(`/projects/${projectId}/documents`, body);
    if (!response) throw new Error('Upload de documento sem resposta da API.');
    return { id: response.id, sha256: response.sha256, ... };
};
```
Transporte multipart ja resolvido (`src/services/api.ts:34-40`): `apiPost(path, formData)` detecta `FormData` e nao seta `Content-Type` — o browser define o boundary. Nenhuma mudanca necessaria em `api.ts`.

Endpoint criado no Plan 02: `POST /api/v1/audit/{project_id}/evidence`, multipart com campo `file`, resposta 201:
`{success, id, project_id, document_type: "AUDIT_EVIDENCE", filename, sha256, mime_type, size_bytes, storage_bucket, storage_object_path, storage_path, status: "UPLOADED"}`.
Extensoes aceitas: `.pdf .png .jpg .jpeg .mp4`; limite 50 MiB. Reenvio do mesmo arquivo devolve o mesmo `id` (dedup por hash).

Endpoint evoluido no Plan 02: `PATCH /api/v1/audit/verify/{project_id}` — `evidencias_url` agora precisa conter `Document.id` (UUID) de evidencias `AUDIT_EVIDENCE` do mesmo projeto; qualquer outra coisa retorna 400. O `assinatura_digital` enviado e IGNORADO. A resposta traz `assinatura_digital` (hash de 64 hex), `assinatura_tipo: "STUB_SHA256"`, `assinatura_verificavel_em` (ISO) e `evidencias_url` (lista de ids).

Estado atual a substituir (`AuditorReview.tsx`):
- linha 44-50: `type AuditEvidenceFile = { id; name; size; type; localUrl }`
- linhas 226-249: `addEvidenceFiles` sincrona, monta `localUrl: 'local://auditoria/...'`
- linhas 275-296: `verify()` envia `draft.evidenceFiles.map((f) => f.localUrl)` e `assinatura_digital: draft.signature`
- linhas 517-531: dropzone (`label.cursor-pointer.rounded-xl.border-2.border-dashed.border-gray-200.bg-gray-50`, `accept="image/*,.pdf,.doc,.docx,.heic"`, subtexto "Evidência local até o envio definitivo")
- linhas 536-551: linha de arquivo (`rounded-xl border border-gray-100 bg-white px-3 py-2`) exibindo `file.localUrl`
- linhas 585-587: `<input value={draft.signature} onChange=...>` rotulado "Assinatura digital"
- linhas 298-310: `copyReportPreview` com `navigator.clipboard.writeText` + `copiedProjectId` com reset em 1800ms
- linhas 67-69: `MAX_AUDIT_EVIDENCE_FILE_SIZE_BYTES = 10 * 1024 * 1024`, `MAX_AUDIT_EVIDENCE_FILE_LIMIT_TEXT`, `MAX_AUDIT_EVIDENCE_FILE_REJECTION_PREFIX`
- linha 13: `Fingerprint` ja importado de `lucide-react`; `Loader2`, `CheckCircle2`, `AlertTriangle`, `Copy`, `Trash2` tambem ja importados

Fail-closed de NFC ja pronto (`src/services/fieldCapture.ts`, consumir tal como esta, sem alterar o arquivo):
```ts
export const detectFieldCapabilities = (): FieldCapability   // { nfc: 'available' | 'unsupported', geolocation, secureContext }
export const getNfcCaptureStatus = (): NfcCaptureStatus       // 'unsupported' | 'blocked_missing_credentials' | 'manual_entry_required'
```
`getNfcCaptureStatus()` NUNCA devolve sucesso — nao existe estado `available` no tipo de retorno. Esse e exatamente o comportamento fail-closed de D-04.

Estado atual de `MrcaDetails.tsx`:
- linha 131: mapa `technicalStatusLabel` com a entrada `deterministic_baseline: 'Baseline determinístico local'`
- linha 148: `const [activeTab, setActiveTab] = useState<'overview' | 'integrity' | 'documents' | 'transactions'>('overview')`
- linha 364: bloco `activeTab === 'integrity'`
- linhas 444-461: tiles `bg-gray-50 border-gray-100` com `dossier.baseline.sentinelSceneId`, `.baselineHash`, `.ndviMean`, `.pointsAnalyzed`
- linhas 462-470: os DOIS tiles `bg-amber-50 border-amber-100` ("Fonte do baseline" / "Status Sentinel")
- linhas 555-598: bloco `dossier.audits.map(...)` dentro da aba `documents`

Novo no dossie publico (Plan 07): `dossier.satellite = { baselineSource, sentinelStatus, blocked, ndviMean, pointsAnalyzed, referenceHash, sentinelSceneId, lastObservedAt }`.
Novo shape de `dossier.audits[]` (Plan 02, `public_audit_item`): `{ id, status, conclusion, auditedAt, createdAt, evidenceCount, signatureKind, signaturePreview }` — **`reportText`, `latitude`, `longitude` e `evidenceUrls` nao existem mais neste bloco**.

Contrato visual travado (UI-SPEC): Surface A usa acento `blue-700`/`blue-600`, sucesso `emerald-600`/`emerald-50`, aviso `amber-600`/`amber-50`, destrutivo `red-600`; botoes de acao `px-4 py-3`; eyebrow `text-[10px] uppercase font-bold tracking-widest`; card padding `p-6`. Surface C mantem `bg-gray-50 border-gray-100` como tile normal e reserva `bg-amber-50`/`text-amber-900` exclusivamente para o estado bloqueado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auditEvidence.ts e upload real com estado por arquivo em AuditorReview (D-01/D-06/SATM-01)</name>
  <files>src/services/auditEvidence.ts, src/pages/Dashboard/AuditorReview.tsx</files>
  <read_first>
    - src/services/projectDocuments.ts (arquivo inteiro, 60 linhas — shape exato a copiar)
    - src/services/api.ts linhas 20-60 (`apiPost`, deteccao de FormData)
    - src/pages/Dashboard/AuditorReview.tsx linhas 1-70 (imports e constantes) e 220-300 (addEvidenceFiles, removeEvidenceFile, verify)
    - src/pages/Dashboard/AuditorReview.tsx linhas 505-560 (dropzone e linha de arquivo)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md "Component Inventory" linhas 164-167 (classes exatas dos tres estados de linha)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-02-SUMMARY.md (shape real da resposta do endpoint)
  </read_first>
  <action>
**1. Criar `src/services/auditEvidence.ts`** com o shape exato de `projectDocuments.ts`:

```ts
import { apiPost } from './api';

export const AUDIT_EVIDENCE_MAX_BYTES = 50 * 1024 * 1024;
export const AUDIT_EVIDENCE_ACCEPT = '.pdf,.png,.jpg,.jpeg,.mp4,image/png,image/jpeg,application/pdf,video/mp4';

export type UploadedAuditEvidence = {
    id: string;
    projectId: string;
    documentType: string;
    filename: string;
    mimeType: string;
    sizeBytes: number;
    sha256: string;
};

type AuditEvidenceUploadResponse = {
    success: boolean;
    id: string;
    project_id: string;
    document_type: string;
    filename?: string;
    mime_type: string;
    size_bytes: number;
    sha256: string;
    status: string;
};

export const uploadAuditEvidence = async (projectId: string, file: File): Promise<UploadedAuditEvidence> => {
    const body = new FormData();
    body.append('file', file);
    const response = await apiPost<AuditEvidenceUploadResponse>(
        `/audit/${encodeURIComponent(projectId)}/evidence`,
        body,
    );
    if (!response) throw new Error('Upload de evidência sem resposta da API.');
    return {
        id: response.id,
        projectId: response.project_id,
        documentType: response.document_type,
        filename: response.filename || file.name,
        mimeType: response.mime_type,
        sizeBytes: response.size_bytes,
        sha256: response.sha256,
    };
};
```
Nenhum outro helper. **Nao** criar wrapper de progresso com `XMLHttpRequest`: `apiPost` ja e a via unica de rede do app e o estado de progresso desta tela e binario (enviando/concluido), conforme UI-SPEC.

**2. `AuditorReview.tsx` — tipo e estado de arquivo.** Substituir `AuditEvidenceFile` (linhas 44-50):
```ts
type AuditEvidenceUploadState = 'uploading' | 'success' | 'error';

type AuditEvidenceFile = {
    id: string;              // chave local estavel (nao e o Document.id)
    name: string;
    size: number;
    type: string;
    state: AuditEvidenceUploadState;
    documentId?: string;     // Document.id devolvido pelo backend (D-02)
    sha256?: string;
    error?: string;
    file?: File;             // guardado apenas para permitir "Tentar novamente"
};
```
`localUrl` deixa de existir no tipo — essa remocao e o que impede qualquer reintroducao acidental de `local://`.

**3. Substituir `addEvidenceFiles` (linhas 226-249) por versao assincrona:**
```ts
const addEvidenceFiles = async (project: AuditItem, files: FileList | null) => {
    if (!files || files.length === 0) return;
    const projectKey = project.friendlyId || project.id;
    const selected = Array.from(files);
    const accepted = selected.filter((file) => file.size <= MAX_AUDIT_EVIDENCE_FILE_SIZE_BYTES);
    const rejected = selected.filter((file) => file.size > MAX_AUDIT_EVIDENCE_FILE_SIZE_BYTES);
    setEvidenceError(rejected.length > 0
        ? `${MAX_AUDIT_EVIDENCE_FILE_REJECTION_PREFIX}: ${rejected.map((file) => file.name).join(', ')}.`
        : '');

    const pending = accepted.map((file, index) => ({
        id: `${Date.now()}-${index}-${file.name}`,
        name: file.name,
        size: file.size,
        type: file.type || 'arquivo',
        state: 'uploading' as const,
        file,
    }));
    setDraft((current) => ({ ...current, evidenceFiles: [...current.evidenceFiles, ...pending] }));

    for (const row of pending) {
        await uploadOneEvidence(project, row.id, row.file as File);
    }
};

const uploadOneEvidence = async (project: AuditItem, rowId: string, file: File) => {
    const patch = (next: Partial<AuditEvidenceFile>) =>
        setDraft((current) => ({
            ...current,
            evidenceFiles: current.evidenceFiles.map((row) => (row.id === rowId ? { ...row, ...next } : row)),
        }));
    patch({ state: 'uploading', error: undefined });
    try {
        const uploaded = await uploadAuditEvidence(project.id, file);
        patch({ state: 'success', documentId: uploaded.id, sha256: uploaded.sha256 });
    } catch (error) {
        patch({
            state: 'error',
            error: error instanceof Error
                ? error.message
                : 'Não foi possível enviar esta evidência. O arquivo não foi salvo — verifique a conexão e tente novamente.',
        });
    }
};
```
Os uploads sao sequenciais de proposito (o backend valida magic bytes e faz IO de Storage por arquivo; disparar N em paralelo de um dispositivo de campo com rede ruim piora a taxa de sucesso). **Nunca** remover a linha da lista em caso de erro — UI-SPEC: "Never silently drop a failed file from the list".

Atualizar `MAX_AUDIT_EVIDENCE_FILE_SIZE_BYTES` para `AUDIT_EVIDENCE_MAX_BYTES` importado do servico (50 MiB), e os dois textos:
```ts
const MAX_AUDIT_EVIDENCE_FILE_LIMIT_TEXT = 'Limite máximo: 50 MB por arquivo (foto, vídeo ou PDF)';
const MAX_AUDIT_EVIDENCE_FILE_REJECTION_PREFIX = 'Arquivos acima de 50 MB não foram anexados';
```

`removeEvidenceFile` ganha guarda: nao remove linha com `state === 'uploading'` (UI-SPEC: "remove button stays but only enabled once not mid-upload").

**4. Dropzone (linhas 517-531)** — manter a shell exatamente como esta (`label.cursor-pointer.flex.flex-col.items-center.justify-center.rounded-xl.border-2.border-dashed.border-gray-200.bg-gray-50.px-4.py-5`, icone `FileUp` `text-blue-600`, titulo "Selecionar arquivos"). Trocar apenas:
- subtexto: `Envio direto e seguro — hash SHA-256 é gerado automaticamente ao concluir o envio.` (copy travada no UI-SPEC)
- `accept={AUDIT_EVIDENCE_ACCEPT}` e acrescentar `capture="environment"` ao `<input type="file">` (D-06: captura de foto/video pelo proprio navegador, sem SDK nativo)
- o handler passa a ser `void addEvidenceFiles(project, event.currentTarget.files)`

**5. Linha de arquivo (linhas 536-551)** — os tres estados do UI-SPEC, mantendo a shell `rounded-xl border border-gray-100 bg-white px-3 py-2`:
- `uploading`: acrescentar `<Loader2 className="h-3 w-3 animate-spin text-blue-600" />` ao lado do nome e uma barra `<div className="h-1 rounded-full bg-blue-100"><div className="h-1 w-1/2 rounded-full bg-blue-600 animate-pulse" /></div>`; texto `Enviando…`
- `success`: `<CheckCircle2 className="h-4 w-4 text-emerald-600" />` + `<p className="text-[10px] font-mono text-emerald-700 truncate">{row.sha256?.slice(0, 16)}…</p>` e o rotulo `Enviado`
- `error`: shell vira `rounded-xl border border-red-200 bg-red-50 px-3 py-2`, exibe `row.error` e um botao `Tentar novamente` que chama `uploadOneEvidence(project, row.id, row.file!)`
Em nenhum estado renderizar caminho de storage — o `sha256` truncado e a unica prova tecnica exibida.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'local://' src/pages/Dashboard/AuditorReview.tsx` retorna 0
    - `grep -c 'localUrl' src/pages/Dashboard/AuditorReview.tsx` retorna 0
    - `grep -c 'uploadAuditEvidence' src/pages/Dashboard/AuditorReview.tsx` retorna 2 (import + chamada)
    - `grep -c 'FormData' src/services/auditEvidence.ts` retorna 1
    - `grep -c 'storage_object_path\|storage_path\|supabase://' src/pages/Dashboard/AuditorReview.tsx` retorna 0
    - `grep -c "capture=" src/pages/Dashboard/AuditorReview.tsx` retorna 1
    - `grep -c "state === 'uploading'\|state === 'success'\|state === 'error'" src/pages/Dashboard/AuditorReview.tsx` maior ou igual a 3
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Evidencia de campo enviada de verdade, com estado visivel por arquivo e zero rastro de `local://`.</done>
</task>

<task type="auto">
  <name>Task 2: Badge de assinatura verificavel e releitura QTAG/NFC fail-closed (D-02/D-03/D-04, SATM-02/SATM-04)</name>
  <files>src/pages/Dashboard/AuditorReview.tsx</files>
  <read_first>
    - src/pages/Dashboard/AuditorReview.tsx (apos a Task 1; especialmente `verify`, `copyReportPreview` linhas 298-310, o bloco de checks linhas 495-515 e o campo de assinatura linhas 585-587)
    - src/services/fieldCapture.ts (arquivo inteiro, 57 linhas — consumir sem alterar)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md linhas 141-142 e 168-169 (copy e classes travadas)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-02-SUMMARY.md (campos `assinatura_tipo`/`assinatura_verificavel_em` da resposta)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-03, D-04
  </read_first>
  <action>
**1. `verify()` (linhas 275-296) — enviar Document.id reais (D-02):**
```ts
const uploadedIds = draft.evidenceFiles
    .filter((row) => row.state === 'success' && row.documentId)
    .map((row) => row.documentId as string);

const pendingUploads = draft.evidenceFiles.some((row) => row.state === 'uploading');
if (pendingUploads) {
    setEvidenceError('Aguarde a conclusão do envio das evidências antes de registrar a auditoria.');
    return;
}
if (!nfcManuallyConfirmed && nfcStatus !== 'available') {
    setEvidenceError('Confirme manualmente a verificação física das QTAGs antes de registrar a auditoria.');
    return;
}

const response = await apiPatch<AuditVerifyResponse>(`/audit/verify/${encodeURIComponent(project.id)}`, {
    status,
    laudo_texto: buildAuditReport(project, monitoring, draft, status),
    latitude,
    longitude,
    evidencias_url: uploadedIds,
    auditor_id: user?.id ?? 'aud-005',
});
setSignatureResult(response ? {
    hash: response.assinatura_digital,
    kind: response.assinatura_tipo,
    signedAt: response.assinatura_verificavel_em,
} : null);
```
**Remover** o campo `assinatura_digital` do payload: o backend recalcula e o valor do cliente e descartado (D-03). Remover tambem a linha que injetava `monitoring?.baseline.evidenceUri` na lista — `evidencias_url` agora so aceita `Document.id` e um URI de baseline retornaria 400.

Remover `signature` de `AuditDraft` e de `createDefaultDraft`, e trocar a linha correspondente em `buildAuditReport` (`Assinado: ${draft.signature}`) por `Assinatura: gerada pelo servidor ao registrar a auditoria (stub SHA-256).`

Tipo da resposta:
```ts
type AuditVerifyResponse = {
    success: boolean;
    project_id: string;
    new_status: string;
    assinatura_digital: string;
    assinatura_tipo: string;
    assinatura_verificavel_em: string;
    evidencias_url: string[];
};
```

**2. Badge de assinatura (substitui o `<input>` das linhas 585-587):**
```tsx
<div className="space-y-1">
    <span className="text-xs font-bold uppercase text-gray-400">Assinatura verificável (stub SHA-256)</span>
    {signatureResult ? (
        <div className="flex items-center gap-2">
            <span className="inline-flex items-center gap-2 rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">
                <Fingerprint className="h-3.5 w-3.5" />
                <span className="font-mono">{signatureResult.hash.slice(0, 16)}…</span>
            </span>
            <button
                type="button"
                onClick={() => copySignature(signatureResult.hash)}
                className="inline-flex h-8 items-center gap-1 rounded-lg border border-gray-200 px-2 text-xs font-bold text-gray-600 transition hover:bg-gray-50"
                aria-label="Copiar assinatura verificável"
            >
                <Copy className="h-3.5 w-3.5" /> {copiedSignature ? 'Copiado' : 'Copiar'}
            </button>
        </div>
    ) : (
        <p className="rounded-xl border border-gray-100 bg-gray-50 px-3 py-2 text-sm text-gray-500">
            Gerada automaticamente pelo servidor ao registrar a auditoria.
        </p>
    )}
    <p className="text-xs text-gray-500">
        Gerada pelo servidor a partir do laudo e das evidências desta sessão de auditoria — não é uma assinatura biométrica.
    </p>
</div>
```
`copySignature` reaproveita **exatamente** a mecanica de `copyReportPreview` (linhas 298-310): `navigator.clipboard.writeText` dentro de `try/catch`, estado booleano com reset por `window.setTimeout(..., 1800)`. Nao criar uma segunda abordagem de clipboard.

O campo **nunca** e editavel: nao existe `onChange`, nao existe `input` de assinatura em nenhum lugar do arquivo depois desta task.

**3. Banner de releitura QTAG/NFC fail-closed (D-04/SATM-04)** — inserido acima do grupo de checks (linhas 495-515), dentro do card expandido:
```tsx
const [nfcManuallyConfirmed, setNfcManuallyConfirmed] = React.useState(false);
const nfcStatus = React.useMemo(() => getNfcCaptureStatus(), []);
const fieldCapabilities = React.useMemo(() => detectFieldCapabilities(), []);
```
(importados de `../../services/fieldCapture`, arquivo **nao** alterado.)

```tsx
<div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-semibold text-amber-700">
    <div className="flex items-center gap-2">
        <AlertTriangle className="h-4 w-4" />
        <span>Leitor NFC indisponível neste dispositivo.</span>
    </div>
    <p className="mt-1 text-xs font-medium text-amber-700">
        Confirme manualmente que a tag foi localizada e está fisicamente íntegra antes de prosseguir.
    </p>
    <label className="mt-3 flex items-center gap-2 text-xs font-bold text-amber-800">
        <input
            type="checkbox"
            checked={nfcManuallyConfirmed}
            onChange={(event) => setNfcManuallyConfirmed(event.target.checked)}
            className="h-4 w-4 rounded border-amber-300 text-amber-600 focus:ring-amber-500"
        />
        Confirmo a verificação física das QTAGs em campo
    </label>
</div>
```
Regras nao negociaveis:
- O checkbox nasce **sempre** `false` e e resetado junto com o draft em `openEvidenceReview` — nunca pre-marcado.
- O banner e renderizado sempre que `nfcStatus !== 'available'`. Como `getNfcCaptureStatus()` **nunca** devolve `'available'` (o tipo de retorno nao tem esse valor — `fieldCapture.ts:53-57`), na pratica o banner aparece em 100% dos dispositivos hoje. Isso e o comportamento correto de D-04: o sistema nunca finge que a releitura aconteceu. Registrar esse raciocinio em um comentario de 2 linhas acima do bloco.
- Quando `fieldCapabilities.nfc === 'unsupported'`, acrescentar a frase `Este navegador não expõe a Web NFC API.` como terceira linha do banner.
- O resultado (`nfcManuallyConfirmed`) e incluido em `buildAuditReport` como uma linha explicita: `QTAG/NFC: releitura automática indisponível; verificação física confirmada manualmente pelo auditor.` — para que o laudo registre a natureza da confirmacao, nunca sugerindo leitura real.
- Os checks existentes `tagsLocated`/`tagsIntact` continuam como estao; o banner e um gate adicional, nao um substituto.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Assinatura verificável (stub SHA-256)' src/pages/Dashboard/AuditorReview.tsx` retorna 1
    - `grep -ic 'assinatura biométrica\|assinatura digital verificada' src/pages/Dashboard/AuditorReview.tsx` retorna 1 (apenas na frase de negacao "não é uma assinatura biométrica")
    - `grep -c 'draft.signature' src/pages/Dashboard/AuditorReview.tsx` retorna 0
    - `grep -c 'assinatura_digital:' src/pages/Dashboard/AuditorReview.tsx` retorna 0 (nao envia mais no payload)
    - `grep -c 'getNfcCaptureStatus\|detectFieldCapabilities' src/pages/Dashboard/AuditorReview.tsx` maior ou igual a 3
    - `grep -c 'Leitor NFC indisponível neste dispositivo.' src/pages/Dashboard/AuditorReview.tsx` retorna 1
    - `grep -c 'nfcManuallyConfirmed' src/pages/Dashboard/AuditorReview.tsx` maior ou igual a 4
    - `grep -c 'checked={true}\|defaultChecked' src/pages/Dashboard/AuditorReview.tsx` retorna 0 (checkbox nunca pre-marcado)
    - `grep -c 'documentId' src/pages/Dashboard/AuditorReview.tsx` maior ou igual a 2
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Assinatura exibida como stub somente leitura e releitura de QTAG bloqueada com confirmacao manual explicita.</done>
</task>

<task type="auto">
  <name>Task 3: Dossie publico — tiles de baseline condicionais e bloco de auditoria minimizado (D-05/D-25, SATM-03/SATM-07)</name>
  <files>src/pages/Dashboard/MrcaDetails.tsx</files>
  <read_first>
    - src/pages/Dashboard/MrcaDetails.tsx linhas 120-160 (mapa `technicalStatusLabel` e estado `activeTab`)
    - src/pages/Dashboard/MrcaDetails.tsx linhas 364-478 (aba `integrity`, incluindo o bloco "Baseline técnico" 440-475)
    - src/pages/Dashboard/MrcaDetails.tsx linhas 550-600 (bloco `dossier.audits.map`)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md secao "Surface C" e linha 153 (copy travada)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-02-SUMMARY.md (novo shape de `public_audit_item`)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md (shape de `public_satellite_item`)
  </read_first>
  <action>
**1. Mapa de rotulos (linha 131)** — acrescentar as entradas novas ao objeto `technicalStatusLabel`, sem remover as existentes:
```ts
    COPERNICUS: 'Copernicus Sentinel-2 (observação real)',
    ACTIVE_COPERNICUS_SENTINEL_2: 'Monitoramento Sentinel-2 ativo',
```
A entrada `deterministic_baseline: 'Baseline determinístico local'` e `BLOCKED_MISSING_PROVIDER_CREDENTIALS` **permanecem** — sao o rotulo correto do estado fail-closed.

**2. Tiles condicionais (linhas 462-470)** — a regra de cor mais importante da Surface C: *ambar significa "bloqueado", nao "dado de satelite"*.
```tsx
const satellite = dossier.satellite;
const baselineIsReal = (satellite?.baselineSource ?? metadata.baseline_source) === 'COPERNICUS';
const tileClass = baselineIsReal
    ? 'p-5 rounded-xl bg-gray-50 border border-gray-100'
    : 'p-5 rounded-xl bg-amber-50 border border-amber-100';
const tileLabelClass = baselineIsReal
    ? 'text-[10px] uppercase font-bold text-gray-400'
    : 'text-[10px] uppercase font-bold text-amber-700';
const tileValueClass = baselineIsReal ? 'font-bold text-gray-900 mt-1' : 'font-bold text-amber-900 mt-1';
```
Aplicar essas tres classes aos dois tiles existentes ("Fonte do baseline" e "Status Sentinel"), preservando o conteudo (`technicalStatusLabel(metadata.baseline_source || metadata.baseline_adapter)` e `technicalStatusLabel(metadata.sentinel_status)`). Nenhuma outra classe do grid muda.

**3. Bloco "Monitoramento por satélite" (novo, inserido logo apos o grid de "Baseline técnico", ainda na aba `integrity`)** — versao minimizada de D-25, reaproveitando a shell de tile ja usada nas linhas 446-461:
```tsx
{satellite && (
    <div className="mt-6">
        <h4 className="text-[10px] uppercase font-bold tracking-widest text-gray-400 mb-3">Monitoramento por satélite</h4>
        {satellite.blocked ? (
            <div className="p-5 rounded-xl bg-amber-50 border border-amber-100">
                <p className="font-bold text-amber-900">Monitoramento satelital bloqueado.</p>
                <p className="text-sm text-amber-800 mt-1">
                    Faltam credenciais do provedor Copernicus neste ambiente. Nenhum dado simulado é exibido.
                </p>
            </div>
        ) : (
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">NDVI médio</p>
                    <p className="font-black text-xl mt-1">{satellite.ndviMean?.toFixed(3) ?? '—'}</p>
                </div>
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Pontos analisados</p>
                    <p className="font-black text-xl mt-1">{satellite.pointsAnalyzed?.toLocaleString('pt-BR') ?? '—'}</p>
                </div>
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Hash de referência</p>
                    <p className="font-mono break-all mt-1 text-primary">{satellite.referenceHash ?? '—'}</p>
                </div>
                <div className="p-5 rounded-xl bg-gray-50 border border-gray-100">
                    <p className="text-[10px] uppercase font-bold text-gray-400">Última observação</p>
                    <p className="font-bold mt-1">{satellite.lastObservedAt ? formatDate(satellite.lastObservedAt) : '—'}</p>
                </div>
            </div>
        )}
    </div>
)}
```
**Proibido** neste bloco: grafico de serie temporal, camadas de mapa, lista de eventos/anomalias, cloud coverage por cena e qualquer coordenada — tudo isso e Surface B (interna, Plan 09). D-25 e explicito: o dossie publico e a versao minimizada.

**4. Bloco de auditoria (linhas 555-598)** — adequar ao novo `public_audit_item`, que **nao tem mais** `reportText`, `latitude`, `longitude` nem `evidenceUrls`:
- Exibir por auditoria: `conclusion` (titulo), `auditedAt` formatado, chip `{evidenceCount} evidência(s) anexada(s)` e, quando `signatureKind` existir, um chip `<span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700"><Fingerprint className="h-3 w-3" /> Assinatura verificável (stub SHA-256)</span>` seguido de `<span className="font-mono text-xs text-gray-500">{audit.signaturePreview}</span>`.
- **Remover** qualquer JSX que leia `audit.reportText`, `audit.latitude`, `audit.longitude` ou `audit.evidenceUrls` — esses campos deixaram de existir na resposta e continuariam renderizando `undefined`.
- Atualizar o tipo TypeScript local do dossie (onde `audits` estiver tipado) para o novo shape; se estiver com `any`, declarar o tipo explicito agora.
- Estado vazio: manter o comportamento existente (`dossier.audits.length > 0 ? ... : ...`), com o texto `Nenhuma auditoria de campo registrada até o momento.`

**5. Tipagem** — acrescentar ao tipo do dossie:
```ts
satellite?: {
    baselineSource: string | null;
    sentinelStatus: string | null;
    blocked: boolean;
    ndviMean: number | null;
    pointsAnalyzed: number | null;
    referenceHash: string | null;
    sentinelSceneId: string | null;
    lastObservedAt: string | null;
} | null;
```
Se o tipo do dossie vier de `src/services/database.ts` ou `src/data/mrca_db.ts`, estender la — mas **nao** alterar nenhum outro campo desses arquivos neste plano; se o campo puder ser adicionado apenas localmente em `MrcaDetails.tsx`, preferir o local para nao ampliar o escopo.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && grep -c 'audit.reportText\|audit.latitude\|audit.evidenceUrls' src/pages/Dashboard/MrcaDetails.tsx | grep -qx 0 && echo PUBLIC_DOSSIER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Copernicus Sentinel-2 (observação real)' src/pages/Dashboard/MrcaDetails.tsx` retorna 1
    - `grep -c 'baselineIsReal' src/pages/Dashboard/MrcaDetails.tsx` maior ou igual a 4
    - `grep -c 'bg-amber-50' src/pages/Dashboard/MrcaDetails.tsx` maior ou igual a 1 e nenhuma dessas ocorrencias esta em classe fixa dos dois tiles de baseline (todas passam por `tileClass` ou pelo ramo `satellite.blocked`)
    - `grep -c 'Monitoramento satelital bloqueado.' src/pages/Dashboard/MrcaDetails.tsx` retorna 1
    - `grep -c 'Hash de referência' src/pages/Dashboard/MrcaDetails.tsx` retorna 1
    - `grep -c 'audit.reportText\|audit.latitude\|audit.longitude\|audit.evidenceUrls' src/pages/Dashboard/MrcaDetails.tsx` retorna 0
    - `grep -ic 'recharts\|chart.js\|leaflet' src/pages/Dashboard/MrcaDetails.tsx` retorna 0 (nenhuma camada de mapa/grafico no dossie publico)
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Dossie publico com baseline Sentinel real sem falso alarme ambar e auditoria visivel apenas no nivel de existencia/data/conclusao.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificacao visual da auditoria de campo e do dossie publico</name>
  <what-built>
    Upload real de evidencia com estados por arquivo, badge de assinatura verificavel (stub SHA-256) somente leitura, banner NFC fail-closed com confirmacao manual obrigatoria, tiles de baseline condicionais no dossie publico e bloco satelital minimizado. Todos os testes de backend dos Plans 02/07 estao verdes e `npm run build` passa.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e `uv run uvicorn backend_app.main:app --reload --port 5680` com o Supabase local ativo.
    2. Entrar como auditor e abrir `/painel/auditoria`. Expandir um projeto da fila com "Revisar evidências".
    3. Selecionar uma foto (JPG/PNG) e um PDF. Confirmar que cada linha passa por: spinner azul + "Enviando…" -> icone verde + hash SHA-256 truncado em `font-mono` verde. Nenhuma linha deve exibir `local://` nem caminho `supabase://`.
    4. Selecionar um arquivo acima de 50 MB: deve aparecer a mensagem de rejeicao sem quebrar a lista. Desligar a rede e tentar um upload: a linha deve ficar vermelha com "Tentar novamente" funcional, e o arquivo NAO pode sumir da lista.
    5. Confirmar que o banner ambar "Leitor NFC indisponível neste dispositivo." aparece com o checkbox **desmarcado** e que tentar registrar a auditoria sem marca-lo mostra o aviso de bloqueio.
    6. Marcar o checkbox, preencher observacoes/conclusao e clicar em "Aprovar". Conferir que o badge verde "Assinatura verificável (stub SHA-256)" aparece com hash truncado e que o botao "Copiar" cola o hash completo.
    7. Confirmar que NAO existe mais nenhum campo de texto editavel de assinatura na tela.
    8. Abrir o dossie publico do mesmo projeto (`/dossie/{friendlyId}`), aba "Integridade". Com credenciais Copernicus ausentes, os tiles "Fonte do baseline"/"Status Sentinel" devem continuar ambar e o bloco "Monitoramento por satélite" deve mostrar "Monitoramento satelital bloqueado.". Se houver observacao Copernicus real no ambiente, os dois tiles devem ficar cinza (`bg-gray-50`) e o bloco deve exibir NDVI medio, pontos analisados e hash de referencia.
    9. Ainda no dossie publico, conferir o bloco de auditoria: deve mostrar conclusao, data, contagem de evidencias e o chip de assinatura — e **nenhum** texto de laudo, coordenada do auditor ou hash completo.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que divergiu (tela, passo e comportamento observado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispositivo de campo -> API | Arquivo binario e coordenadas saem de um dispositivo nao confiavel |
| cliente -> assinatura | Um campo editavel de assinatura permitiria ao auditor forjar o valor |
| hardware NFC -> laudo | Um estado de "leitura bem-sucedida" inventado no cliente falsificaria a verificacao fisica |
| dossie interno -> dossie publico | Laudo, coordenadas e hash completo nao podem ser renderizados publicamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-50 | Spoofing | assinatura de auditoria forjada no cliente | mitigate | O campo editavel e removido; `assinatura_digital` sai do payload; o badge so renderiza o valor devolvido pelo servidor; gate `grep -c 'draft.signature'` = 0 |
| T-05-51 | Spoofing | releitura de QTAG simulada | mitigate | `getNfcCaptureStatus()` (Phase 3) nao possui estado de sucesso no tipo de retorno; o checkbox nasce sempre desmarcado e o `verify` bloqueia sem ele; o laudo grava explicitamente que a confirmacao foi manual (D-04) |
| T-05-52 | Tampering | evidencia referenciada sem existir no servidor | mitigate | Apenas linhas com `state === 'success'` e `documentId` entram em `evidencias_url`; o backend (Plan 02) ainda revalida cada id contra o projeto e retorna 400 |
| T-05-53 | Information Disclosure | caminho de storage exibido na tela de campo | mitigate | O tipo `AuditEvidenceFile` nao tem campo de caminho; a UI so exibe nome, tamanho e SHA-256 truncado; gate `grep -c 'supabase://'` = 0 |
| T-05-54 | Information Disclosure | laudo/coordenadas no dossie publico | mitigate | O backend ja removeu os campos (`public_audit_item`, Plan 02) e a UI deixa de referencia-los; gate `grep -c 'audit.reportText...'` = 0 |
| T-05-55 | Repudiation | usuario nao percebe que um upload falhou | mitigate | Linha em erro permanece na lista com estilo `border-red-200 bg-red-50`, mensagem e botao de retentativa; o registro da auditoria e bloqueado enquanto houver upload em andamento |
| T-05-56 | Tampering | falso "tudo certo" visual quando o provider esta bloqueado | mitigate | Tiles ambar reservados ao estado bloqueado e bloco satelital com empty state explicito "Nenhum dado simulado é exibido"; a cor so muda quando `baselineSource === 'COPERNICUS'` |
</threat_model>

<verification>
1. `npm run build` — compilacao limpa das tres superficies tocadas
2. `npm run lint` — sem novos erros de lint
3. Gates de grep de anti-regressao (`local://`, `draft.signature`, `audit.reportText`) todos em 0
4. Checkpoint humano da Task 4 aprovado
</verification>

<success_criteria>
- Upload real com tres estados visiveis por arquivo e zero `local://` na tela de auditoria
- Assinatura exibida como badge somente leitura rotulado "Assinatura verificável (stub SHA-256)"
- Banner NFC fail-closed com confirmacao manual obrigatoria e nunca pre-marcada
- `evidencias_url` enviado com `Document.id` reais
- Dossie publico com tiles condicionais, bloco satelital minimizado e auditoria sem laudo/coordenada
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-08-SUMMARY.md` registrando: o shape final de `AuditEvidenceFile`, o comportamento observado do `getNfcCaptureStatus()` no ambiente de teste, o resultado do checkpoint humano e qualquer divergencia entre o UI-SPEC e o que foi implementado.
</output>
