---
phase: 05-satellite-monitoring-and-field-audit
plan: 09
type: execute
wave: 6
depends_on: ["05-07"]
files_modified:
  - src/services/satelliteMonitoring.ts
  - src/pages/Dashboard/MonitoringNDVI.tsx
  - src/App.tsx
autonomous: false
requirements: [SATM-06, SATM-07, SATM-08, SATM-09, SATM-10]

must_haves:
  truths:
    - "A pagina de monitoramento deixa de ser hardcoded em PRC-2024-002 e passa a operar sobre o projeto da rota"
    - "Nenhum campo exibido vem de deterministic_baseline quando existe observacao Copernicus real"
    - "O mapa e um Leaflet real com a geometria persistida do projeto, nao uma imagem com filtro CSS"
    - "As camadas base (RGB/NDVI/NDMI/NBR) sao exclusivas entre si e as camadas de sobreposicao (Boundary/Anomalies/Events) sao combinaveis"
    - "O grafico de serie temporal e SVG inline, sem nenhuma biblioteca de grafico nova"
    - "A lista de anomalias mostra severidade e estado, e a decisao humana so aparece para eventos em ANALYZED"
    - "Confirmar uma anomalia exige justificativa e mostra imediatamente o novo estado de integridade do projeto"
    - "Sem credenciais Copernicus a pagina mostra o empty state de bloqueio, nunca dado simulado"
  artifacts:
    - path: "src/services/satelliteMonitoring.ts"
      provides: "Cliente tipado das rotas satelitais do Plan 07"
      contains: "environmental-events"
      min_lines: 130
    - path: "src/pages/Dashboard/MonitoringNDVI.tsx"
      provides: "Dashboard real: mapa Leaflet, timeline SVG, lista de eventos, slider before/after"
      contains: "L.map"
      min_lines: 450
  key_links:
    - from: "src/pages/Dashboard/MonitoringNDVI.tsx"
      to: "GET /api/v1/projects/{id}/satellite/observations"
      via: "serie temporal NDVI/NDMI/NBR"
      pattern: "satellite/observations"
    - from: "src/pages/Dashboard/MonitoringNDVI.tsx"
      to: "PATCH /api/v1/projects/{id}/environmental-events/{event_id}/decision"
      via: "Confirmar/Descartar anomalia (D-18)"
      pattern: "decision"
    - from: "src/pages/Dashboard/MonitoringNDVI.tsx"
      to: "src/components/ProjectGeofencePreview.tsx"
      via: "logica de montagem do Leaflet portada para o card escuro"
      pattern: "leaflet"
---

<objective>
Transformar `MonitoringNDVI.tsx` — hoje inteiramente simulado (projeto fixo `PRC-2024-002`, "mapa" que e um `<img>` com `hue-rotate()`, metricas vindas de `deterministic_baseline()`) — no dashboard real de monitoramento satelital descrito por D-24: mapa Leaflet com camadas ligaveis, serie temporal NDVI/NDMI/NBR em SVG inline, cartao de condicao atual, lista/detalhe de anomalias com decisao humana e comparacao before/after.

Purpose: e a Surface B do UI-SPEC e a ultima peca de UI de SATM-06/07/08/09/10. O UI-SPEC e explicito: **a casca visual escura permanece, o dado falso e o mapa falso saem.** Nao construir uma pagina nova.
Output: `src/services/satelliteMonitoring.ts`, `MonitoringNDVI.tsx` evoluido, rota com parametro de projeto em `App.tsx`.

**Nao faz parte deste plano:** `AuditorReview.tsx`/`MrcaDetails.tsx` (Plan 08) e qualquer alteracao de backend. Nenhuma biblioteca nova pode ser adicionada ao `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md

<interfaces>
Rotas disponiveis (Plan 07), todas org-scoped:
- `GET /api/v1/projects/{project_id}/satellite/summary` -> `{success, project_id, summary: {lastObservation, cloudCoverage, ndviMean, observationCount, baselineSource, sentinelStatus, blocked, blockedReason, lastJob: {type, status, error, finishedAt}, eventCounts: {DETECTED, ANALYZED, CONFIRMED, DISMISSED}}}`
- `GET /api/v1/projects/{project_id}/satellite/observations?date_from=&date_to=&limit=` -> `{success, project_id, total, observations: [{id, sceneId, satellite, product, processingVersion, observedAt, cloudCoverage, ndviMean, ndviMin, ndviMax, ndmiMean, nbrMean, validPixelPercentage}]}`
- `GET /api/v1/projects/{project_id}/environmental-events?status=` -> `{success, project_id, total, events: [...]}`
- `GET /api/v1/projects/{project_id}/environmental-events/{event_id}` -> `{success, project_id, event}`
- `GET /api/v1/projects/{project_id}/environmental-events/{event_id}/evidence/{evidence_id}/image` -> bytes PNG
- `PATCH /api/v1/projects/{project_id}/environmental-events/{event_id}/decision` body `{decision: "CONFIRMED" | "DISMISSED", notes}` -> `{success, project_id, event, integrity}`
- `PATCH /api/v1/projects/{project_id}/environmental-events/{event_id}/clear` body `{notes}` -> idem
- `GET /api/v1/projects/{project_id}/credit-adjustment-pendencies?status=` -> `{success, project_id, total, pendencies}`

`event_item` (Plan 07): `{id, type, status, severity, confidence, affectedAreaHa, ndviBefore, ndviAfter, summary, detectedAt, analyzedAt, decidedAt, decisionNotes, clearedAt, clearanceNotes, correlation, evidence: [{id, kind, sha256, capturedAt, mimeType}]}`. `kind` esta em `BEFORE_IMAGE | AFTER_IMAGE | STATISTICS_SNAPSHOT`.

Geometria ja disponivel: `GET /api/v1/projects/{id}/public-dossier` devolve `boundary` (GeoJSON, `[lon, lat]`) — o mesmo consumido hoje por `ProjectGeofencePreview`. Confirmar em `src/services/database.ts` se ja existe helper de dossie antes de criar outro.

Transporte: `apiGet`/`apiPatch` de `src/services/api.ts` (bearer token e `VITE_API_URL` ja resolvidos). Para a imagem PNG, usar `${API_BASE_URL}` + o path, dentro de um `<img>` — se a rota exigir bearer, buscar com `fetch` autenticado e converter para `URL.createObjectURL(blob)`, revogando no cleanup do `useEffect`.

Mecanica de mapa a portar (`src/components/ProjectGeofencePreview.tsx`, ler antes de escrever):
```tsx
import L, { type LatLngExpression } from 'leaflet';
import 'leaflet/dist/leaflet.css';
// :194-222 — efeito de mount/unmount, L.map(container, {attributionControl, scrollWheelZoom, zoomControl}),
//            L.layerGroup().addTo(map), window.setTimeout(() => map.invalidateSize(), 0), cleanup com map.remove()
// :36-47 — PREVIEW_BASE_LAYERS.satellite = ArcGIS World_Imagery (esta e a camada "RGB")
// :244-284 — L.polygon(points, {...}).addTo(layer) + map.fitBounds(bounds.pad(0.25), {maxZoom: 15})
```
`ST_AsGeoJSON` emite `[lon, lat]`; o Leaflet espera `[lat, lng]` — **inverter ao converter**, exatamente como o preview ja faz.

Casca visual a PRESERVAR (`MonitoringNDVI.tsx`): header/status card (97-127), grid `lg:grid-cols-12` com mapa em `lg:col-span-8` e sidebar em `lg:col-span-4` (130-246), legenda NDVI `bg-black/60 backdrop-blur-xl` (172-183), badge de canto `absolute top-6 right-6` (185-189), lista "Atividades Recentes" com conector `before:absolute before:left-[11px]` (225-244), grid de metricas (249-264).
Casca a REMOVER: `MONITORED_PROJECT_ID` (18), o `<img>` com `hue-rotate`/`invert sepia` (134-148), o `viewMode` de 3 botoes (32, 151-170), e toda leitura de `database.getMonitoringProject`.

Paleta travada (UI-SPEC Surface B): fundo `bg-sinarca-deep`/`bg-[#00120b]`, borda `border-sinarca-border`, acento `#00ff94` (`text-sinarca-neon`/`primary`), headings `font-serif font-bold` (700, **nunca** `font-black`), rotulos `text-[10px] uppercase font-bold tracking-widest`, chip `px-4 py-2 rounded-lg text-[10px] font-bold uppercase tracking-widest border`, botao de acao `px-4 py-3`.
Cores de serie: NDVI `#00ff94`, NDMI `#38BDF8`, NBR `#FB923C`.
Cores de estado de evento: `DETECTED` `#60A5FA`, `ANALYZED` `#FB923C`, `CONFIRMED` `#F87171`, `DISMISSED` `#9CBA9C`, revisado/liberado `#00ff94`.
Copy travada: "Confirmar anomalia", "Descartar anomalia", "Registrar revisão e liberar bloqueio", "Reconstrução histórica em andamento.", "Monitoramento satelital bloqueado.", "Nenhuma anomalia detectada.", "Imagem antes/depois ainda não disponível — só é gerada quando a anomalia avança para análise.", chip "Pendência de recálculo de crédito".

Restricao dura: `package.json` tem apenas `@svg-maps/brazil`, `@types/leaflet`, `leaflet`, `lucide-react`, `react`, `react-dom`, `react-router-dom`. **Nenhuma dependencia nova** — o grafico e SVG inline (`<svg viewBox="0 0 100 100">` com `<path>`/`<circle>`), como o UI-SPEC determina.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: satelliteMonitoring.ts, rota project-scoped e cartao de condicao atual com dados reais (SATM-07/SATM-10)</name>
  <files>src/services/satelliteMonitoring.ts, src/App.tsx, src/pages/Dashboard/MonitoringNDVI.tsx</files>
  <read_first>
    - src/services/projectDocuments.ts e src/services/api.ts (padrao de servico tipado e transporte)
    - src/pages/Dashboard/MonitoringNDVI.tsx (arquivo inteiro, 268 linhas)
    - src/App.tsx linhas 120-150 (definicao das rotas do painel)
    - src/layouts/DashboardLayout.tsx linhas 130-170 (itens de sidebar que apontam para /painel/monitoramento)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md (shape real de cada resposta)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md secao "Surface B" e "Critical finding on Surface B"
  </read_first>
  <action>
**1. Criar `src/services/satelliteMonitoring.ts`** no padrao de `projectDocuments.ts` (tipos explicitos, mapeamento snake_case -> camelCase apenas onde a API ainda usa snake, funcoes finas sem estado):

```ts
export type SatelliteObservation = {
    id: string; sceneId: string; observedAt: string;
    cloudCoverage: number | null; ndviMean: number | null; ndviMin: number | null; ndviMax: number | null;
    ndmiMean: number | null; nbrMean: number | null; validPixelPercentage: number | null;
    processingVersion: string;
};

export type SatelliteEventEvidence = { id: string; kind: 'BEFORE_IMAGE' | 'AFTER_IMAGE' | 'STATISTICS_SNAPSHOT'; sha256: string; capturedAt: string | null; mimeType: string };

export type EnvironmentalEventStatus = 'DETECTED' | 'ANALYZED' | 'CONFIRMED' | 'DISMISSED';
export type EnvironmentalEventType = 'VEGETATION_LOSS' | 'VEGETATION_RECOVERY' | 'POSSIBLE_FIRE';

export type EnvironmentalEvent = {
    id: string; type: EnvironmentalEventType; status: EnvironmentalEventStatus;
    severity: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
    confidence: number | null; affectedAreaHa: number | null;
    ndviBefore: number | null; ndviAfter: number | null; summary: string | null;
    detectedAt: string; analyzedAt: string | null; decidedAt: string | null; decisionNotes: string | null;
    clearedAt: string | null; clearanceNotes: string | null;
    correlation: Record<string, unknown> | null;
    evidence: SatelliteEventEvidence[];
};

export type SatelliteSummary = {
    blocked: boolean; blockedReason: string | null;
    baselineSource: string | null; sentinelStatus: string | null;
    observationCount: number; ndviMean: number | null; cloudCoverage: number | null;
    lastObservation: SatelliteObservation | null;
    lastJob: { type: string; status: string; error: string | null; finishedAt: string | null } | null;
    eventCounts: Record<EnvironmentalEventStatus, number>;
};
```
Funcoes: `fetchSatelliteSummary(projectId)`, `fetchSatelliteObservations(projectId, params?)`, `fetchEnvironmentalEvents(projectId, status?)`, `decideEnvironmentalEvent(projectId, eventId, decision, notes)`, `clearEnvironmentalEvent(projectId, eventId, notes)`, `fetchCreditAdjustmentPendencies(projectId)`, e `satelliteEvidenceImageUrl(projectId, eventId, evidenceId)` devolvendo o path relativo (o componente resolve o blob autenticado).

Tipos e nomes de rota vem do `05-07-SUMMARY.md` — **conferir cada campo contra o SUMMARY antes de escrever**; qualquer divergencia deve ser corrigida no cliente, nunca "adaptada" com `any`.

**2. Rota project-scoped (`src/App.tsx` linha 134):**
```tsx
<Route path="monitoramento" element={<MonitoringNDVI />} />
<Route path="monitoramento/:projectId" element={<MonitoringNDVI />} />
```
Manter a rota sem parametro para nao quebrar os links existentes na sidebar (`DashboardLayout.tsx:140` e `:164`). Em `MonitoringNDVI.tsx`, resolver o projeto assim:
```tsx
const { projectId: routeProjectId } = useParams<{ projectId: string }>();
```
Quando `routeProjectId` for `undefined`, buscar a lista de projetos visiveis ao usuario (`apiGet('/projects')`, ja usado em outras telas) e selecionar o primeiro; renderizar um seletor `<select>` no header (chip escuro, `bg-black/50 border-white/10 text-[10px] uppercase font-bold tracking-widest`) que navega para `/painel/monitoramento/{friendlyId}`. **Proibido** manter qualquer constante com `PRC-2024-002` no arquivo.

**3. Estado e carregamento** — substituir `database.getMonitoringProject` por:
```tsx
const [summary, setSummary] = useState<SatelliteSummary | null>(null);
const [observations, setObservations] = useState<SatelliteObservation[]>([]);
const [events, setEvents] = useState<EnvironmentalEvent[]>([]);
const [pendencies, setPendencies] = useState<CreditAdjustmentPendency[]>([]);
```
Um `loadAll(projectId)` que dispara as quatro chamadas com `Promise.all` e alimenta os quatro estados; `handleRefresh` reusa `loadAll` (o botao `RefreshCw` das linhas 120-125 permanece igual). Remover o import de `database`/`MonitoringProjectResponse` se ele deixar de ser usado.

**4. Empty states (copy travada, antes de qualquer render de dado):**
- `summary.blocked === true` -> card `bg-sinarca-deep border border-sinarca-border rounded-3xl p-6` com `h2` `font-serif font-bold` "Monitoramento satelital bloqueado." e corpo "Faltam credenciais do provedor Copernicus neste ambiente. Nenhum dado simulado é exibido — configure `COPERNICUS_CLIENT_ID`/`COPERNICUS_CLIENT_SECRET` para habilitar." A pagina **nao** renderiza mapa, grafico nem metricas nesse estado.
- `summary.lastJob?.type === 'HISTORICAL_RECONSTRUCTION' && summary.lastJob.status !== 'COMPLETED' && observations.length === 0` -> "Reconstrução histórica em andamento." + "Estamos processando até 5 anos de observações Sentinel-2 para este projeto. A página atualiza automaticamente quando novos pontos chegam." (implementar o "atualiza automaticamente" com um `setInterval` de 30s que chama `loadAll`, limpo no cleanup do efeito — sem websocket).

**5. Cartao de condicao atual e grid de metricas (linhas 87-92 e 249-264)** — manter as classes exatamente como estao e trocar apenas a fonte dos valores:
- "NDVI Atual" -> `summary.lastObservation?.ndviMean` (3 casas)
- "Cobertura de nuvem" (substitui "Pontos Analisados" no primeiro card) -> `summary.lastObservation?.cloudCoverage` em `%`
- "Pontos Analisados" -> `summary.observationCount`
- "Última observação" -> `formatDateTime(summary.lastObservation?.observedAt)` com `sceneId` truncado no chip de `trend`
- O card "Status da Reserva" (113-119) passa a refletir integridade: `Protegido` em `#00ff94` quando nao ha evento CONFIRMED nao revisado, `Bloqueado` em `text-orange-400` quando `summary.eventCounts.CONFIRMED > 0` ou o projeto esta em `ON_HOLD`.
Nenhum valor pode vir de `deterministic_baseline` — se `summary.baselineSource !== 'COPERNICUS'`, o valor exibido e `—` com o rotulo de bloqueio, nunca um numero.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && grep -c 'PRC-2024-002\|getMonitoringProject' src/pages/Dashboard/MonitoringNDVI.tsx | grep -qx 0 && echo SCOPED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PRC-2024-002' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c 'MONITORED_PROJECT_ID' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c 'getMonitoringProject' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c 'useParams' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 2 (import + uso)
    - `grep -c 'monitoramento/:projectId' src/App.tsx` retorna 1
    - `grep -c 'Monitoramento satelital bloqueado.' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'Reconstrução histórica em andamento.' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'fetchSatelliteSummary\|fetchSatelliteObservations\|fetchEnvironmentalEvents' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 3
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Pagina operando sobre o projeto da rota com dados reais e empty states fail-closed corretos.</done>
</task>

<task type="auto">
  <name>Task 2: Mapa Leaflet real com camadas ligaveis e grafico de serie temporal SVG (D-24)</name>
  <files>src/pages/Dashboard/MonitoringNDVI.tsx</files>
  <read_first>
    - src/components/ProjectGeofencePreview.tsx (arquivo inteiro, 368 linhas — mount/unmount, base layers, polygon, fitBounds, e o renderer SVG `technicalPreview`)
    - src/pages/Dashboard/MonitoringNDVI.tsx (apos a Task 1; especialmente o card de mapa 130-190)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md linhas 170-175 (Component Inventory: grupo de camadas, mapa, legenda, grafico)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-24
  </read_first>
  <action>
**1. Substituir o `<img>` com filtro CSS (linhas 134-148) por um Leaflet real**, portando a mecanica de `ProjectGeofencePreview.tsx:194-284` para dentro do card escuro existente (`relative rounded-3xl overflow-hidden border border-sinarca-border bg-[#00120b] h-[500px] lg:h-[650px]` — classe preservada):
```tsx
import L, { type LatLngExpression } from 'leaflet';
import 'leaflet/dist/leaflet.css';

const mapContainerRef = useRef<HTMLDivElement | null>(null);
const mapRef = useRef<L.Map | null>(null);
const baseLayerRef = useRef<L.TileLayer | null>(null);
const overlayRef = useRef<L.LayerGroup | null>(null);
```
Efeito de mount/unmount identico ao do preview: cria `L.map(container, { attributionControl: true, scrollWheelZoom: true, zoomControl: true })`, cria `L.layerGroup().addTo(map)`, chama `window.setTimeout(() => map.invalidateSize(), 0)` e no cleanup faz `map.remove()` zerando os refs.

Camada base RGB: a mesma URL ja usada pelo preview (`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}`, atribuicao Esri).

**2. Dois grupos de chips independentes** (UI-SPEC: "do not merge them into one 7-way exclusive group"), ambos com a classe de chip ja existente nas linhas 151-170 (`px-4 py-2 rounded-lg text-[10px] font-bold uppercase tracking-widest transition-all border`, ativo `bg-sinarca-neon text-sinarca-forest border-sinarca-neon`, inativo `bg-black/50 text-white border-white/10 backdrop-blur-md`):

- **Grupo A — camada base, selecao unica (semantica de radio):** `RGB`, `NDVI`, `NDMI`, `NBR`. Estado `const [baseLayer, setBaseLayer] = useState<'RGB' | 'NDVI' | 'NDMI' | 'NBR'>('RGB')`. Cada chip usa `aria-checked` e `role="radio"` dentro de um `role="radiogroup"` rotulado "Camada base".
  - `RGB` = tile Esri.
  - `NDVI`/`NDMI`/`NBR`: nao existe endpoint de tile de indice nesta fase (o backend usa Statistical API, nao WMS — D-09). Renderizar a AOI como **polígono coroplético**: o poligono do boundary preenchido com a cor derivada do valor do indice da observacao mais recente, usando a mesma rampa da legenda (`from-red-500 via-yellow-400 to-green-500`). Escrever um comentario explicito:
    ```tsx
    // D-09: a Statistical API devolve estatisticas agregadas por AOI, nao tiles.
    // A camada de indice colore a propria AOI pelo valor agregado da observacao
    // mais recente — nunca renderiza um raster que o backend nao possui.
    ```
    Legenda (bloco 172-183, preservado): trocar apenas os rotulos e o valor exibido conforme o indice ativo — nao criar uma segunda legenda.
- **Grupo B — sobreposicoes, multi-selecao (toggles independentes):** `Boundary`, `Anomalies`, `Events`. Estado `const [overlays, setOverlays] = useState<Record<'BOUNDARY' | 'ANOMALIES' | 'EVENTS', boolean>>({ BOUNDARY: true, ANOMALIES: true, EVENTS: false })`. Cada chip usa `aria-pressed`.
  - `Boundary`: `L.polygon(points, { color: '#00ff94', fillColor: '#00ff94', fillOpacity: 0.15, opacity: 0.95, weight: 3 })` — cores da Surface B, nao as verdes do preview claro.
  - `Anomalies`/`Events`: `L.circleMarker` no centroide da AOI com a cor de estado do UI-SPEC (`DETECTED` `#60A5FA`, `ANALYZED` `#FB923C`, `CONFIRMED` `#F87171`, `DISMISSED` `#9CBA9C`) e `bindTooltip` com tipo, severidade e data. Comentario: os eventos desta fase nao tem geometria propria (`project_events` nao guarda geometry nesta fase) — o marcador representa o evento sobre a AOI, nao um poligono de dano.

Um efeito separado, dependente de `[baseLayer, overlays, boundary, events]`, faz `overlayRef.current?.clearLayers()` e redesenha — mesma disciplina do preview (nunca acumular camadas).

Conversao de coordenadas obrigatoria: o GeoJSON vem `[lon, lat]` e o Leaflet quer `[lat, lng]` — inverter no `map` da lista, como o preview ja faz.

**3. Grafico de serie temporal — SVG inline, sem biblioteca** (UI-SPEC: "never a chart library"). Card novo abaixo do grid principal, com `bg-sinarca-deep border border-sinarca-border rounded-3xl p-6`:
- Titulo `h3` `font-serif font-bold` (700, nunca `font-black`) "Série temporal de índices".
- Chips de indice **multi-selecao** (mesma classe de chip; o usuario compara 1 a 3 linhas): `NDVI` `#00ff94`, `NDMI` `#38BDF8`, `NBR` `#FB923C`.
- Render: `<svg viewBox="0 0 100 100" preserveAspectRatio="none" className="w-full h-48">` com um `<path>` por indice ativo, `stroke={cor}` `fill="none"` `strokeWidth="0.8"` `vectorEffect="non-scaling-stroke"`, e um `<circle r="0.9">` por ponto.
- Normalizacao pura (funcao de modulo, testavel por leitura): `x = (index / (points.length - 1)) * 100`, `y = 100 - ((value - min) / (max - min)) * 100`, com `min`/`max` calculados sobre **todos** os indices ativos juntos para que as linhas sejam comparaveis. Pontos com valor `null` interrompem o path (gerar um novo segmento `M`, nunca interpolar por cima de dado ausente).
- Eixo: apenas 3 rotulos de data (`primeiro`, `meio`, `ultimo`) em `text-[10px] text-text-muted uppercase font-bold tracking-widest` abaixo do SVG, e o par min/max a esquerda. Sem grid pesado.
- Com menos de 2 observacoes, mostrar "Ainda não há pontos suficientes para a série temporal." em vez de um SVG vazio.
- Marcar no eixo os meses com anomalia: `<circle r="1.6" fill="#F87171" fillOpacity="0.35">` atras do ponto correspondente, quando existir evento com `detectedAt` no mesmo mes.

**4. Não** adicionar nada ao `package.json`. Nenhum `import` de `recharts`, `chart.js`, `d3`, `victory` ou similar.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && node -e "const p=require('./package.json');const banned=['recharts','chart.js','chartjs','d3','victory','apexcharts','nivo'];const found=Object.keys({...p.dependencies,...p.devDependencies}).filter(k=>banned.some(b=>k.includes(b)));if(found.length){console.error('dependencia de grafico proibida:',found);process.exit(1);}console.log('NO_CHART_LIB')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hue-rotate\|invert sepia' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c "L.map(" src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c "leaflet/dist/leaflet.css" src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'invalidateSize' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'clearLayers' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1
    - `grep -c 'role="radiogroup"' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1 e `grep -c 'aria-pressed' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 3
    - `grep -c 'viewBox="0 0 100 100"' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1
    - `grep -c '#00ff94' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1 e `grep -c '#38BDF8' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1 e `grep -c '#FB923C' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1
    - `grep -ic 'recharts\|chart.js\|from .d3' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c 'font-black' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0 (Surface B usa 700, nunca 900)
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Mapa real com camadas base exclusivas e sobreposicoes combinaveis, e serie temporal comparavel em SVG inline.</done>
</task>

<task type="auto">
  <name>Task 3: Lista/detalhe de anomalias, decisao humana, slider before/after e pendencia de credito (SATM-06/08/09)</name>
  <files>src/pages/Dashboard/MonitoringNDVI.tsx</files>
  <read_first>
    - src/pages/Dashboard/MonitoringNDVI.tsx (apos as Tasks 1 e 2; em especial a lista "Atividades Recentes" 225-244 e o botao ghost 219-221)
    - src/pages/Dashboard/AuditorReview.tsx linhas 195-215 (modelo de interacao de expandir linha por `activeProjectId`, a ser replicado)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md linhas 143-155 e 176-179 (copy das confirmacoes destrutivas e classes do detalhe expandido)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md (contrato de `PATCH .../decision` e `.../clear`, e o bloco `integrity` devolvido)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-18, D-20, D-22, D-23
  </read_first>
  <action>
**1. Lista de eventos** — card `bg-sinarca-deep border border-sinarca-border rounded-3xl p-6`, reaproveitando **exatamente** o padrao de icone-ponto-conector de "Atividades Recentes" (`space-y-6 relative before:absolute before:left-[11px] before:top-2 before:bottom-2 before:w-px before:bg-white/10`). O card "Atividades Recentes" original permanece: o novo card e "Anomalias e eventos ambientais".
- Cada linha: ponto colorido pela cor de estado do UI-SPEC, titulo com o rotulo PT-BR do tipo (`Perda de vegetação` / `Recuperação de vegetação` / `Possível incêndio`), `summary` como descricao, `detectedAt` em `font-mono text-[9px]`, e um badge de severidade (`LOW/MEDIUM` em `text-text-muted`, `HIGH/CRITICAL` em `text-red-400`).
- Quando `clearedAt` estiver preenchido, o ponto usa `#00ff94` e um chip `Revisado` aparece ao lado do estado — e a UI de D-22.
- Empty state: "Nenhuma anomalia detectada." + "O monitoramento contínuo compara observações mensais de NDVI; anomalias aparecem aqui automaticamente quando detectadas."
- Interacao: a linha expande **no lugar** ao clicar, com o mesmo modelo `activeEventId` de `AuditorReview.tsx`/`CertifierReview.tsx`. **Nao introduzir modal** — nao existe nenhum no app.

**2. Detalhe expandido** contendo, nesta ordem: badge de severidade, `affectedAreaHa` formatada em ha, `ndviBefore` -> `ndviAfter`, `confidence` em `%`, os itens de `correlation` (renderizados como lista de rotulos PT-BR, ignorando chaves desconhecidas), o slider before/after e os botoes de decisao.

**3. Slider before/after** — card `rounded-3xl border border-sinarca-border bg-[#00120b]`:
```tsx
<div className="relative aspect-video overflow-hidden rounded-2xl">
    <img src={afterUrl} alt="Imagem depois do evento" className="absolute inset-0 h-full w-full object-cover" />
    <div className="absolute inset-0" style={{ clipPath: `inset(0 ${100 - sliderValue}% 0 0)` }}>
        <img src={beforeUrl} alt="Imagem antes do evento" className="h-full w-full object-cover" />
    </div>
    <div className="absolute inset-y-0 w-px bg-sinarca-neon" style={{ left: `${sliderValue}%` }} />
    <span className="absolute top-6 left-6 text-[10px] text-white font-bold uppercase tracking-widest">Antes</span>
    <span className="absolute top-6 right-6 text-[10px] text-white font-bold uppercase tracking-widest">Depois</span>
    <input
        type="range" min={0} max={100} value={sliderValue}
        onChange={(event) => setSliderValue(Number(event.target.value))}
        aria-label="Comparar imagem antes e depois"
        className="absolute inset-x-0 bottom-0 w-full cursor-ew-resize opacity-0 h-full"
    />
</div>
```
O `<input type="range">` fica visualmente oculto mas **continua no DOM e operavel por teclado** (UI-SPEC: "not a mouse-only drag") — nunca substituir por `onMouseMove`.
As imagens sao buscadas com `fetch` autenticado a partir de `satelliteEvidenceImageUrl(...)`, convertidas com `URL.createObjectURL(blob)` e revogadas com `URL.revokeObjectURL` no cleanup do `useEffect` (evita vazamento de blob ao expandir varios eventos).
Quando faltar `BEFORE_IMAGE` ou `AFTER_IMAGE`, renderizar no lugar do slider: "Imagem antes/depois ainda não disponível — só é gerada quando a anomalia avança para análise."

**4. Decisao humana (D-18)** — os dois botoes só sao renderizados quando `event.status === 'ANALYZED'`:
```tsx
<button className="inline-flex items-center justify-center gap-2 rounded-xl bg-red-600 px-4 py-3 text-sm font-bold text-white transition hover:bg-red-700">Confirmar anomalia</button>
<button className="inline-flex items-center justify-center gap-2 rounded-xl border border-white/10 px-4 py-3 text-sm font-bold text-white transition hover:bg-white/5">Descartar anomalia</button>
```
Para `DETECTED` mostrar o texto "Aguardando correlação automática." e nenhum botao; para `CONFIRMED`/`DISMISSED` mostrar a decisao registrada (`decidedAt` + `decisionNotes`) sem botoes de decisao.
Cada acao abre um painel de confirmacao **inline** (nao modal) com a copy travada:
- Confirmar: "Esta decisão fica registrada permanentemente na trilha de auditoria e pode bloquear o projeto automaticamente (Auto Hold) se o risco calculado for crítico. Descreva a justificativa da confirmação."
- Descartar: "A anomalia será marcada como descartada e não afeta o score de risco do projeto. Descreva por que ela não procede."
Ambos com `<textarea>` obrigatorio (botao desabilitado com justificativa vazia — o backend retorna 400 nesse caso e a UI nao deve provocar o erro).
Ao concluir, usar o `integrity` devolvido pela resposta para atualizar imediatamente o card "Status da Reserva" e recarregar `events`/`pendencies` — sem `window.location.reload()`.

**5. Desbloqueio auditavel (D-22)** — quando `event.status === 'CONFIRMED' && !event.clearedAt`, renderizar o botao `Registrar revisão e liberar bloqueio` (estilo ghost, `border border-white/10 px-4 py-3`) com o mesmo painel inline de justificativa obrigatoria, chamando `clearEnvironmentalEvent`. Nunca oferecer esse botao para eventos nao confirmados.

**6. Pendencia de recalculo de credito (D-23)** — quando `pendencies` tiver alguma com `status === 'OPEN'`, exibir no topo da pagina (abaixo do header) o chip travado no UI-SPEC:
```tsx
<span className="rounded-full bg-amber-100 px-2 py-1 text-xs font-bold text-amber-700">Pendência de recálculo de crédito</span>
```
seguido da `description` da pendencia e da `affectedAreaHa`. Este e o unico uso de ambar na Surface B, herdado verbatim da Phase 4 — nao criar cor nova.

**7. Botao "Validar Presença Física" (linha 219-221)** — manter como esta; nao esta no escopo desta fase.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Confirmar anomalia' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 2 (botao + confirmacao)
    - `grep -c 'Descartar anomalia' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 2
    - `grep -c 'Registrar revisão e liberar bloqueio' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c "status === 'ANALYZED'" src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1 (botoes de decisao so nesse estado — D-18)
    - `grep -c 'Nenhuma anomalia detectada.' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'Imagem antes/depois ainda não disponível' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'type="range"' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1 e `grep -c 'aria-label="Comparar imagem antes e depois"' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -c 'revokeObjectURL' src/pages/Dashboard/MonitoringNDVI.tsx` maior ou igual a 1
    - `grep -c 'Pendência de recálculo de crédito' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 1
    - `grep -ic 'modal\|Dialog' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `grep -c 'window.location.reload' src/pages/Dashboard/MonitoringNDVI.tsx` retorna 0
    - `npm run build` e `npm run lint` concluem sem erro
  </acceptance_criteria>
  <done>Ciclo humano completo na tela: anomalia visivel, decisao com justificativa, comparacao visual acessivel e pendencia de credito sinalizada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificacao visual do dashboard satelital</name>
  <what-built>
    `MonitoringNDVI.tsx` deixou de ser simulado: rota project-scoped, dados reais das rotas do Plan 07, mapa Leaflet com 4 camadas base exclusivas e 3 sobreposicoes combinaveis, serie temporal NDVI/NDMI/NBR em SVG inline, lista/detalhe de anomalias com decisao humana, slider before/after acessivel e chip de pendencia de credito. `npm run build` e `npm run lint` passam e nenhuma dependencia nova foi adicionada.
  </what-built>
  <how-to-verify>
    1. Com o Supabase local e a API rodando, abrir `/painel/monitoramento` e conferir que a pagina carrega um projeto real (nao `PRC-2024-002` hardcoded) e que o seletor de projeto navega para `/painel/monitoramento/{friendlyId}`.
    2. Sem credenciais Copernicus configuradas, confirmar o empty state "Monitoramento satelital bloqueado." com a frase "Nenhum dado simulado é exibido" e a ausencia total de mapa/grafico/metricas numericas.
    3. Com observacoes semeadas no banco (ou credenciais reais), conferir: o mapa e um Leaflet de verdade (arrastavel, com zoom e atribuicao Esri), nao uma imagem estatica.
    4. Alternar as 4 camadas base (RGB/NDVI/NDMI/NBR): apenas uma fica ativa por vez e a legenda inferior esquerda troca de rotulo/valor.
    5. Alternar as 3 sobreposicoes (Boundary/Anomalies/Events): elas combinam entre si e com qualquer camada base; desligar `Boundary` remove o poligono verde neon sem afetar as demais.
    6. No grafico, ativar NDVI, depois NDMI e NBR juntos: as tres linhas usam `#00ff94`, `#38BDF8` e `#FB923C` e ficam na mesma escala comparavel. Meses com anomalia mostram o halo vermelho.
    7. Expandir uma anomalia em `ANALYZED`: conferir severidade, area afetada, NDVI antes/depois, confianca e o slider before/after. Operar o slider **pelo teclado** (Tab ate ele, setas esquerda/direita) e confirmar que a divisoria se move.
    8. Clicar em "Confirmar anomalia" sem preencher a justificativa: o botao de envio deve ficar desabilitado. Preencher e confirmar: o card "Status da Reserva" deve atualizar na hora e, se o risco virar critico, indicar bloqueio.
    9. Conferir que o chip ambar "Pendência de recálculo de crédito" aparece apos a confirmacao e que o botao "Registrar revisão e liberar bloqueio" so existe para eventos `CONFIRMED` ainda nao revisados.
    10. Conferir que um evento em `DETECTED` nao oferece nenhum botao de decisao (apenas "Aguardando correlação automática.").
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que divergiu (passo, comportamento esperado e observado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente -> decisao de incidente | Confirmar uma anomalia pode bloquear o projeto e suspender vendas |
| API -> renderizacao | Dado ausente ou provider bloqueado nao pode virar numero plausivel na tela |
| projeto da rota -> dados exibidos | `projectId` vem da URL e e manipulavel |
| imagem de evidencia -> browser | Bytes vindos do Storage sao renderizados como imagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-57 | Spoofing | dado simulado apresentado como observacao real | mitigate | Empty state dedicado quando `summary.blocked`; qualquer metrica sem `baselineSource === 'COPERNICUS'` renderiza `—`; gates de grep proibindo `PRC-2024-002`, `getMonitoringProject` e `hue-rotate` |
| T-05-58 | Elevation of Privilege | usuario acessando projeto de outra organizacao pela URL | mitigate | Todas as rotas consumidas sao org-scoped no backend (Plan 07, `_assert_project_edit_permission`); a UI trata 403 mostrando erro explicito e nao renderiza dado parcial. A UI **nao** e a fronteira de autorizacao |
| T-05-59 | Elevation of Privilege | decisao disparada por ator sem papel | mitigate | O backend (Plan 07) exige `require_role("auditor","certifier","admin")`; a UI esconde os botoes para outros papeis apenas como conveniencia, e um 403 e exibido como erro sem alterar o estado local |
| T-05-60 | Tampering | confirmacao acidental de anomalia | mitigate | Painel inline de confirmacao com justificativa obrigatoria e copy destrutiva travada pelo UI-SPEC; botao desabilitado com texto vazio |
| T-05-61 | Repudiation | usuario nao percebe que a decisao mudou o estado do projeto | mitigate | A resposta do `PATCH` traz o bloco `integrity`, aplicado imediatamente ao card "Status da Reserva", e a lista/pendencias sao recarregadas |
| T-05-62 | Denial of Service | vazamento de memoria por blobs de imagem | mitigate | `URL.createObjectURL` sempre pareado com `URL.revokeObjectURL` no cleanup do efeito; gate de grep |
| T-05-63 | Denial of Service | polling agressivo durante reconstrucao | accept | `setInterval` de 30s apenas enquanto o job esta em andamento e a lista de observacoes esta vazia, sempre limpo no cleanup; carga desprezivel para o volume atual |
| T-05-64 | Information Disclosure | caminho de storage da evidencia exposto no cliente | mitigate | O backend (Plan 07) nao serializa `storage_object_path`; a imagem e servida por rota dedicada por id, e o cliente so conhece `evidenceId` |
</threat_model>

<verification>
1. `npm run build` — compilacao limpa
2. `npm run lint` — sem novos erros
3. Verificacao de `package.json` — nenhuma biblioteca de grafico adicionada
4. Gates de grep de anti-regressao (`PRC-2024-002`, `getMonitoringProject`, `hue-rotate`, `font-black`) todos em 0
5. Checkpoint humano da Task 4 aprovado
</verification>

<success_criteria>
- Pagina project-scoped, sem nenhum identificador hardcoded e sem dado de `deterministic_baseline`
- Mapa Leaflet real com 4 camadas base exclusivas e 3 sobreposicoes combinaveis
- Serie temporal NDVI/NDMI/NBR em SVG inline, sem dependencia nova
- Decisao humana disponivel apenas em `ANALYZED`, com justificativa obrigatoria e feedback imediato de integridade
- Slider before/after operavel por teclado e com empty state correto
- Chip de pendencia de recalculo de credito visivel apos confirmacao
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-09-SUMMARY.md` registrando: os tipos finais de `satelliteMonitoring.ts`, a decisao tomada para as camadas NDVI/NDMI/NBR (coropletico da AOI, por ausencia de tiles), o comportamento observado do polling de reconstrucao, o resultado do checkpoint humano e qualquer divergencia em relacao ao UI-SPEC.
</output>
