---
phase: 05-satellite-monitoring-and-field-audit
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - backend_app/modules/satellite/service.py
  - backend_app/modules/satellite/historical_reconstruction.py
  - backend_app/modules/satellite/scheduler.py
  - backend_app/main.py
  - backend_app/modules/projects/service.py
  - tests/conftest.py
  - tests/modules/satellite/test_historical_reconstruction.py
autonomous: true
requirements: [SATM-05, SATM-07, SATM-10]

must_haves:
  truths:
    - "Criar um projeto enfileira um job HISTORICAL_RECONSTRUCTION e responde o HTTP imediatamente, sem esperar nenhuma chamada ao Copernicus"
    - "A reconstrucao persiste uma observacao mensal por mes com cena disponivel abaixo do maxCloudCoverage, cobrindo 5 anos anteriores a created_at"
    - "Rodar a mesma reconstrucao duas vezes nao cria observacao duplicada (idempotencia por project_id+satellite+scene_id+processing_version)"
    - "Meses cuja melhor cena esta acima do maxCloudCoverage nao geram observacao nenhuma — nunca um valor estimado"
    - "Ao concluir com sucesso, projects.metadata.baseline_source vira COPERNICUS e sentinel_status deixa de ser BLOCKED_MISSING_PROVIDER_CREDENTIALS"
    - "Sem credenciais Copernicus o job termina FAILED com a mensagem explicita do adapter, e nenhum dado simulado e persistido"
    - "O scheduler inicia no lifespan do FastAPI e e encerrado no shutdown, sem deixar job zumbi"
    - "Todo I/O dentro do job e assincrono — nenhuma chamada bloqueante trava o event loop do servidor"
  artifacts:
    - path: "backend_app/modules/satellite/service.py"
      provides: "Persistencia idempotente de observacao, ciclo de vida de satellite_jobs, usage recorder"
      contains: "on_conflict_do_nothing"
      min_lines: 200
    - path: "backend_app/modules/satellite/historical_reconstruction.py"
      provides: "Reconstrucao de 5 anos com composicao mensal por menor cloud cover"
      contains: "class HistoricalReconstructionService"
      min_lines: 150
    - path: "backend_app/modules/satellite/scheduler.py"
      provides: "Registro dos jobs APScheduler chamados pelo lifespan"
      contains: "AsyncIOScheduler"
      min_lines: 80
    - path: "backend_app/main.py"
      provides: "Primeiro lifespan do repositorio"
      contains: "asynccontextmanager"
  key_links:
    - from: "backend_app/modules/projects/service.py"
      to: "satellite_jobs"
      via: "enqueue_historical_reconstruction dentro de create_project (D-14)"
      pattern: "enqueue_historical_reconstruction"
    - from: "backend_app/main.py"
      to: "backend_app/modules/satellite/scheduler.py"
      via: "lifespan inicia e encerra o AsyncIOScheduler"
      pattern: "register_satellite_jobs"
    - from: "backend_app/modules/satellite/service.py"
      to: "copernicus_api_usage"
      via: "usage_recorder persistindo CopernicusUsageRecord (D-26)"
      pattern: "CopernicusApiUsage"
---

<objective>
Construir a primeira infraestrutura assincrona do backend (D-14): a camada de persistencia idempotente das observacoes, o servico de reconstrucao historica de 5 anos com composicao mensal (D-12/D-13), o scheduler APScheduler acoplado a um lifespan do FastAPI, e o gancho em `create_project` que enfileira o job sem bloquear o request HTTP.

Purpose: fecha SATM-05 (reconstrucao real persistida) e a metade "infra" de SATM-10 (job fora do ciclo request/response). E tambem o ponto em que `deterministic_baseline()` deixa de ser a fonte do baseline exibido (SATM-07): a funcao continua existindo como fallback de seed/teste, mas os metadados que a UI le passam a apontar para o Copernicus assim que a reconstrucao conclui.
Output: `satellite/service.py`, `satellite/historical_reconstruction.py`, `satellite/scheduler.py`, lifespan em `main.py`, hook em `create_project`, testes de reconstrucao com provider falso.

**Nao faz parte deste plano:** deteccao/persistencia de anomalia e evento (Plan 06), rotas HTTP (Plan 07), frontend (Plans 08/09). Este plano nao cria nenhuma rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-01-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-03-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-04-SUMMARY.md

<interfaces>
Contrato do provider (Plan 03, `backend_app/adapters/satellite.py`):
```python
class SatelliteProvider(Protocol):
    async def search_scenes(self, *, aoi, date_from, date_to, max_cloud_coverage, limit=500) -> list[SceneDTO]: ...
    async def get_statistics(self, *, aoi, date_from, date_to, aggregation_interval="P1M", max_cloud_coverage=20.0) -> list[IndexStatisticsDTO]: ...
    async def get_image(self, *, aoi, date_from, date_to, max_cloud_coverage=20.0, width=512, height=512) -> SatelliteImageDTO: ...
    async def aclose(self) -> None: ...
```
`SceneDTO(scene_id, observed_at, cloud_coverage, processing_version, platform, product, metadata)`;
`IndexStatisticsDTO(interval_from, interval_to, ndvi_mean, ndvi_min, ndvi_max, ndmi_mean, nbr_mean, valid_pixel_percentage, metadata)`;
`CopernicusUsageRecord(endpoint, outcome, http_status, duration_ms, processing_units, error_code, metadata)`.
Factory: `build_copernicus_provider(usage_recorder=None) -> CopernicusProvider`.

AOI (Phase 04.1): `ProjectsService.boundary_item(project_id)` (`projects/service.py:1189-1238`) devolve `{"active": <GeoJSON>, "declared": ..., "declaredAreaHa": ..., "activeTier": ...}` ou `None`. `ST_AsGeoJSON` ja emite `[lon, lat]` — convencao correta para o Sentinel Hub, **nao inverter**.

Estado atual de `create_project` (`projects/service.py:817-881`):
```python
friendly_id = await self._next_friendly_id()
baseline = deterministic_baseline(payload)
...
metadata_={..., "baseline_source": "deterministic_baseline",
           "sentinel_status": "BLOCKED_MISSING_PROVIDER_CREDENTIALS"},
...
self.session.add(ProjectBaseline(project_id=project.id, sentinel_scene_id=f"SINARCA_SENTINEL_{friendly_id}", baseline_hash=..., points_analyzed=..., vegetation_cover_pct=..., ndvi_mean=..., captured_at=..., evidence_uri=...))
```
`await self.persist_project_boundary(project, payload.tags)` acontece na linha 903 (AOI ja existe quando o job rodar). A linha 1015 tem a chamada equivalente em `update_project`.

`main.py` hoje (37 linhas) e `create_app()` puro, **sem nenhum hook de lifespan/startup** — este e o primeiro do repositorio.

Sessao fora do request: `backend_app/db/session.py` expoe `get_sessionmaker()` (usar `async with get_sessionmaker()() as session:` dentro do job — `get_session` e um dependency generator do FastAPI e nao serve aqui).

`tests/` cria `client = TestClient(app)` no nivel de modulo, **sem** `with`, entao o lifespan NAO roda nos testes existentes (comportamento do Starlette TestClient). Ainda assim, o conftest deve desligar o scheduler explicitamente para blindar contra qualquer teste futuro que use o context manager.

Modelos disponiveis (Plan 01): `SatelliteObservation`, `SatelliteJob`, `CopernicusApiUsage` (+ `SatelliteAnomaly`, `ProjectEvent`, `SatelliteEvidence`, `CreditAdjustmentPendency`, usados pelos Plans 06/07).
Constantes (Plan 01): `SATELLITE_JOB_TYPES`, `SATELLITE_JOB_STATUSES`, `SATELLITE_JOB_ACTIVE_STATUSES`, `BASELINE_SOURCE_COPERNICUS`, `SENTINEL_STATUS_ACTIVE`, `SENTINEL_STATUS_BLOCKED`.
Settings (Plan 01): `satellite_historical_years`, `satellite_max_cloud_coverage_pct`, `satellite_preferred_cloud_coverage_pct`, `satellite_scheduler_enabled`, `satellite_job_poll_seconds`, `satellite_monitoring_interval_hours`, `satellite_monitoring_batch_size`.

Upsert idempotente com SQLAlchemy async: `from sqlalchemy.dialects.postgresql import insert as pg_insert` + `.on_conflict_do_nothing(index_elements=["project_id", "satellite", "scene_id", "processing_version"])`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SatelliteService — persistencia idempotente de observacao, ciclo de vida de satellite_jobs e usage recorder</name>
  <files>backend_app/modules/satellite/service.py</files>
  <read_first>
    - backend_app/modules/integrity/service.py linhas 1-80 e 707-852 (estilo de servico: `__init__(self, session)`, `flush()` nunca `commit()` dentro do metodo, `create_audit_event` por transicao)
    - backend_app/db/models.py (modelos SatelliteObservation/SatelliteJob/CopernicusApiUsage do Plan 01)
    - backend_app/db/repositories.py (assinatura de `create_audit_event`)
    - backend_app/adapters/satellite.py (DTOs do Plan 03)
    - backend_app/modules/satellite/constants.py (Plan 01)
  </read_first>
  <action>
Criar `backend_app/modules/satellite/service.py` com a classe `SatelliteService`, no mesmo formato de `IntegrityService`:

```python
class SatelliteService:
    def __init__(self, session: AsyncSession) -> None:
        self.session = session
```

**Metodos de job (D-14):**

`async def enqueue_job(self, project: Project, *, job_type: str, window_start=None, window_end=None, metadata=None) -> SatelliteJob | None`
- Valida `job_type in SATELLITE_JOB_TYPES`.
- Usa `pg_insert(SatelliteJob).values(...).on_conflict_do_nothing()` **sem** `index_elements` explicito nao funciona para indice parcial; em vez disso, checar antes com `select(SatelliteJob).where(SatelliteJob.project_id == project.id, SatelliteJob.job_type == job_type, SatelliteJob.status.in_(SATELLITE_JOB_ACTIVE_STATUSES))` e, se ja existir, devolver `None` (nao enfileira duplicado). O indice unico parcial `satellite_jobs_active_idx` do Plan 01 e a rede de seguranca do banco; esta checagem e a rede de seguranca da aplicacao.
- Emite `create_audit_event(action="SATELLITE_JOB_ENQUEUED", entity_type="projects", entity_id=project.id, metadata={"job_type": ..., "friendly_id": project.friendly_id})`.
- `await self.session.flush()`; **nunca** `commit()` (a transacao pertence ao chamador — mesma disciplina de `IntegrityService`).

`async def claim_next_jobs(self, *, limit: int) -> list[SatelliteJob]`
- `select(SatelliteJob).where(SatelliteJob.status == "PENDING").order_by(SatelliteJob.created_at.asc()).limit(limit).with_for_update(skip_locked=True)` — `skip_locked` evita que dois workers peguem o mesmo job (mitigacao parcial de RESEARCH Pitfall 2).
- Marca cada job como `PROCESSING`, incrementa `attempts`, seta `started_at` e `updated_at`, `await self.session.commit()` (aqui o commit e proposital: o job precisa ficar reservado antes do trabalho longo).

`async def finish_job(self, job: SatelliteJob, *, status: str, observations_persisted: int = 0, anomalies_detected: int = 0, error_message: str | None = None) -> None`
- Seta `status` (`COMPLETED`/`FAILED`), contadores, `finished_at`, `updated_at`, `error_message` (truncado em 500 caracteres).
- `create_audit_event(action=f"SATELLITE_JOB_{status}", entity_type="projects", entity_id=job.project_id, metadata={"job_type": job.job_type, "observations_persisted": ..., "anomalies_detected": ..., "error": error_message})`.
- `await self.session.commit()`.

**Persistencia idempotente de observacao (D-15/SATM-10):**

`async def persist_observations(self, project: Project, rows: Sequence[dict[str, Any]]) -> int`
- Cada `row` traz `scene_id`, `processing_version`, `observed_at`, `cloud_coverage`, `ndvi_mean`, `ndvi_min`, `ndvi_max`, `ndmi_mean`, `nbr_mean`, `valid_pixel_percentage`, `metadata`.
- Usa `pg_insert(SatelliteObservation).values([...]).on_conflict_do_nothing(index_elements=["project_id", "satellite", "scene_id", "processing_version"])` com `.returning(SatelliteObservation.id)` para contar quantas foram realmente inseridas.
- Comentario obrigatorio: `# D-15/SATM-10: idempotencia garantida pelo Postgres (unique index), nao por lock de aplicacao (RESEARCH "Don't Hand-Roll").`
- Devolve o numero de linhas efetivamente inseridas. Lista vazia -> `0` sem tocar o banco.
- `await self.session.flush()`.

`async def latest_observation(self, project_id) -> SatelliteObservation | None` e
`async def list_observations(self, project_id, *, date_from=None, date_to=None, limit=500) -> list[SatelliteObservation]` — ordenadas por `observed_at`.

**Baseline exibido (D-07/SATM-07):**

`async def apply_baseline_from_observations(self, project: Project) -> dict[str, Any] | None`
- Le todas as observacoes do projeto. Se nao houver nenhuma, devolve `None` sem alterar nada.
- Calcula: `ndvi_mean` = media aritmetica dos `ndvi_mean` nao nulos (arredondada em 4 casas), `points_analyzed` = quantidade de observacoes, `latest` = observacao mais recente.
- Atualiza `project.metadata_` **preservando as demais chaves**:
```python
        metadata = dict(project.metadata_ or {})
        metadata["baseline_source"] = BASELINE_SOURCE_COPERNICUS
        metadata["baseline_adapter"] = BASELINE_SOURCE_COPERNICUS
        metadata["sentinel_status"] = SENTINEL_STATUS_ACTIVE
        metadata["sentinel_scene_id"] = latest.scene_id
        metadata["sentinel_observed_at"] = latest.observed_at.isoformat()
        metadata["sentinel_observation_count"] = points_analyzed
        project.metadata_ = metadata
```
  Comentario obrigatorio: `# D-07/SATM-07: a partir daqui nenhum campo de baseline EXIBIDO deriva de deterministic_baseline(). A funcao continua existindo em projects/service.py apenas como fallback de seed/teste quando o provider nunca rodou.`
- Cria uma `ProjectBaseline` nova (append, nunca update — o historico de baseline e informacao auditavel) com:
  - `sentinel_scene_id = latest.scene_id`
  - `baseline_hash = hashlib.sha256(f"{project.id}|{latest.scene_id}|{latest.processing_version}|{ndvi_mean}".encode()).hexdigest()` — hash de REFERENCIA da observacao real (D-25 exige "hash de referencia" no dossie publico), nunca hash do nome do projeto
  - `points_analyzed`, `ndvi_mean`, `vegetation_cover_pct = round(min(100.0, max(0.0, ndvi_mean * 100)), 3)` com comentario: `# Aproximacao de cobertura para a UI existente. NAO e estimativa de carbono (fora de escopo, D-23).`
  - `captured_at = latest.observed_at`
  - `evidence_uri = None` (nao inventar URI; a evidencia real vive em satellite_evidence/satellite_observations)
  - Colisao de `baseline_hash` (coluna `unique`): se ja existir uma `ProjectBaseline` com esse hash, nao inserir de novo (reconstrucao repetida e idempotente).
- Emite `create_audit_event(action="SATELLITE_BASELINE_APPLIED", entity_type="projects", entity_id=project.id, before_data={"baseline_source": <valor anterior>}, after_data={"baseline_source": BASELINE_SOURCE_COPERNICUS, "ndvi_mean": ndvi_mean, "points_analyzed": points_analyzed})`.
- Devolve o dicionario com os valores aplicados.

**Usage recorder (D-26):**

`def usage_recorder_for(self, *, project_id=None, satellite_job_id=None) -> Callable[[CopernicusUsageRecord], Awaitable[None]]`
- Devolve uma corrotina que insere uma linha em `CopernicusApiUsage` com `endpoint`, `outcome`, `http_status`, `processing_units`, `duration_ms`, `error_code`, `metadata` do record.
- Envolvida em `try/except Exception` que apenas registra em log: falha de observabilidade **nunca** pode derrubar o job (comentario obrigatorio).
- Comentario: `# D-26: sem Prometheus nesta fase. Linhas estruturadas consultaveis, reaproveitaveis na Phase 9.`

`async def usage_summary(self, *, since: datetime | None = None) -> dict[str, Any]` — agregado por `endpoint`/`outcome` com `count`, `sum(processing_units)`, `avg(duration_ms)`; usado pela rota interna do Plan 07.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
import inspect
from backend_app.modules.satellite.service import SatelliteService
for name in ('enqueue_job','claim_next_jobs','finish_job','persist_observations','latest_observation','list_observations','apply_baseline_from_observations','usage_summary'):
    assert inspect.iscoroutinefunction(getattr(SatelliteService, name)), name
assert not inspect.iscoroutinefunction(SatelliteService.usage_recorder_for)
src = inspect.getsource(SatelliteService)
assert 'on_conflict_do_nothing' in src
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'on_conflict_do_nothing' backend_app/modules/satellite/service.py` retorna 1
    - `grep -c 'skip_locked' backend_app/modules/satellite/service.py` retorna 1
    - `grep -c 'BASELINE_SOURCE_COPERNICUS' backend_app/modules/satellite/service.py` maior ou igual a 2
    - `grep -c 'deterministic_baseline' backend_app/modules/satellite/service.py` retorna 0
    - `grep -c 'requests\.\|urllib' backend_app/modules/satellite/service.py` retorna 0
    - `grep -c 'CopernicusApiUsage' backend_app/modules/satellite/service.py` maior ou igual a 1
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Camada de persistencia idempotente, ciclo de vida de job e observabilidade de consumo prontos para os workers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HistoricalReconstructionService — 5 anos, composicao mensal por menor cloud cover (D-12/D-13/SATM-05)</name>
  <files>backend_app/modules/satellite/historical_reconstruction.py, tests/modules/satellite/test_historical_reconstruction.py</files>
  <read_first>
    - backend_app/modules/satellite/service.py (Task 1)
    - backend_app/adapters/satellite.py (Plan 03 — SceneDTO/IndexStatisticsDTO)
    - backend_app/modules/projects/service.py linhas 1189-1240 (`boundary_item`, de onde vem a AOI)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-12, D-13, D-15
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Pitfall 3" (nenhum I/O bloqueante dentro do job)
  </read_first>
  <behavior>
    - `select_monthly_scenes` sobre 3 cenas do mesmo mes devolve apenas a de menor `cloud_coverage`
    - Cenas com `cloud_coverage` acima do limite sao descartadas antes da selecao
    - Cena sem `cloud_coverage` (`None`) e tratada como pior candidata do mes (so entra se for a unica)
    - Meses sem nenhuma cena elegivel simplesmente nao aparecem no resultado — nunca um ponto interpolado
    - `join_statistics_to_scenes` casa cada `IndexStatisticsDTO` mensal com a cena selecionada do mesmo (ano, mes); estatistica sem cena correspondente e descartada e vice-versa
    - `reconstruction_window` devolve `(created_at - satellite_historical_years anos, created_at)` e respeita o valor de `Settings`
    - Reconstrucao com provider falso de 60 meses persiste 60 observacoes; rodar de novo persiste 0 novas (idempotencia)
    - Projeto sem `active_boundary` termina o job em FAILED com mensagem citando a ausencia da geometria, sem chamar o provider
    - `RuntimeError` do provider (credenciais ausentes) termina o job em FAILED com a mensagem do adapter e persiste 0 observacoes
    - Ao concluir com sucesso, `projects.metadata.baseline_source == "COPERNICUS"` e `sentinel_status != "BLOCKED_MISSING_PROVIDER_CREDENTIALS"`
  </behavior>
  <action>
Criar `backend_app/modules/satellite/historical_reconstruction.py`.

**Funcoes puras no nivel de modulo** (testaveis sem banco, sem provider):

```python
def reconstruction_window(reference: datetime, settings: Settings | None = None) -> tuple[datetime, datetime]:
    """D-12: 5 anos anteriores a projects.created_at, configuravel."""
    config = settings or get_settings()
    years = max(1, int(config.satellite_historical_years))
    # timedelta(days=365*anos) em vez de dateutil.relativedelta: o repo nao
    # depende de dateutil e a diferenca de dias bissextos e irrelevante para
    # uma janela de composicao mensal.
    return (reference - timedelta(days=365 * years), reference)


def select_monthly_scenes(scenes: Sequence[SceneDTO], *, max_cloud_coverage: float) -> dict[tuple[int, int], SceneDTO]:
    """D-13: melhor observacao do mes = menor cloudCoverage abaixo do limite.

    Cena sem cloud_coverage conhecida e tratada como a pior candidata
    (float('inf')) -- so vence se for a unica do mes. Mes sem cena elegivel
    simplesmente nao existe no resultado: NUNCA interpolar ou estimar.
    """


def join_statistics_to_scenes(
    statistics: Sequence[IndexStatisticsDTO],
    scenes_by_month: dict[tuple[int, int], SceneDTO],
) -> list[dict[str, Any]]:
    """Casa a estatistica mensal com a cena escolhida do mesmo (ano, mes).

    A Statistical API agrega por intervalo e nao devolve sceneId; a chave de
    idempotencia D-15 exige um scene_id real, por isso o STAC e consultado em
    paralelo e o join acontece aqui. Intervalo sem cena (ou cena sem
    intervalo) e descartado -- nunca inventar scene_id sintetico.
    """
```
O `join` devolve dicionarios prontos para `SatelliteService.persist_observations`, com `metadata={"interval_from": ..., "interval_to": ..., "platform": scene.platform, "scene_cloud_cover": scene.cloud_coverage}`.

**Servico:**

```python
class HistoricalReconstructionService:
    def __init__(self, session: AsyncSession, provider: SatelliteProvider, settings: Settings | None = None) -> None:
```
`async def run(self, job: SatelliteJob) -> None`, na ordem:
1. Carregar o `Project` do job. Carregar a AOI com `ProjectsService(self.session).boundary_item(str(project.id))`; se `None` ou `boundary["active"] is None`, chamar `SatelliteService.finish_job(job, status="FAILED", error_message="Projeto sem active_boundary (Phase 04.1): reconstrução histórica não pode definir a AOI")` e **retornar sem chamar o provider**.
2. `date_from, date_to = reconstruction_window(project.created_at, self.settings)`; gravar em `job.window_start`/`job.window_end`.
3. Chamar o provider **duas vezes apenas** (economia de PU, D-11):
   - `scenes = await provider.search_scenes(aoi=aoi, date_from=date_from, date_to=date_to, max_cloud_coverage=config.satellite_max_cloud_coverage_pct)`
   - `statistics = await provider.get_statistics(aoi=aoi, date_from=date_from, date_to=date_to, aggregation_interval="P1M", max_cloud_coverage=config.satellite_max_cloud_coverage_pct)`
   Comentario obrigatorio explicando a estrategia: `# Uma unica janela de 5 anos por chamada (STAC + Statistical com aggregationInterval P1M) em vez de 60 chamadas mensais: mesma cobertura, ~2 requests em vez de ~120 (D-11).`
4. `scenes_by_month = select_monthly_scenes(...)`; `rows = join_statistics_to_scenes(...)`.
5. `persisted = await SatelliteService(self.session).persist_observations(project, rows)`.
6. `await SatelliteService(self.session).apply_baseline_from_observations(project)`.
7. `await SatelliteService(self.session).finish_job(job, status="COMPLETED", observations_persisted=persisted)`.
8. Qualquer `Exception` (inclusive o `RuntimeError` fail-closed do adapter) e capturada, faz `await self.session.rollback()`, e finaliza o job em `FAILED` com `error_message=str(exc)[:500]`. **Nunca** persistir dado simulado no ramo de erro. Re-lancar apos finalizar o job **nao** deve acontecer: o poller precisa continuar com os proximos jobs.

Todo I/O e `await` (provider `httpx.AsyncClient`, sessao `AsyncSession`). Proibido `requests`, `urllib`, `time.sleep` ou query SQLAlchemy sincrona neste arquivo (RESEARCH Pitfall 3).

**Testes** — criar `tests/modules/satellite/test_historical_reconstruction.py`. Provider falso implementando o Protocol, sem HTTP:
```python
class FakeProvider:
    def __init__(self, scenes, statistics, *, raises: Exception | None = None) -> None: ...
    async def search_scenes(self, **kwargs): ...
    async def get_statistics(self, **kwargs): ...
    async def get_image(self, **kwargs): ...
    async def aclose(self) -> None: ...
```
Os testes das funcoes puras (`select_monthly_scenes`, `join_statistics_to_scenes`, `reconstruction_window`) rodam sem banco. Os testes de `run` usam `asyncio.run` com `get_sessionmaker()` contra o Postgres local (mesmo padrao dos demais testes de integracao do repo) e criam o projeto pelo endpoint HTTP ja existente (`POST /api/v1/projects`, com as fixtures copiadas de `tests/test_project_boundaries.py`, que ja constroi projeto com boundary valida).

Cobrir cada item de `<behavior>`, incluindo por nome:
- `test_reconstruction_persists_sixty_monthly_observations`
- `test_reconstruction_is_idempotent_on_second_run`
- `test_month_without_eligible_scene_is_never_interpolated`
- `test_reconstruction_fails_closed_without_provider_credentials`
- `test_reconstruction_without_active_boundary_never_calls_provider`
- `test_reconstruction_marks_baseline_source_as_copernicus`
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/modules/satellite/test_historical_reconstruction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def select_monthly_scenes' backend_app/modules/satellite/historical_reconstruction.py` retorna 1
    - `grep -c 'def join_statistics_to_scenes' backend_app/modules/satellite/historical_reconstruction.py` retorna 1
    - `grep -c 'def reconstruction_window' backend_app/modules/satellite/historical_reconstruction.py` retorna 1
    - `grep -c 'import requests\|import urllib\|time.sleep' backend_app/modules/satellite/historical_reconstruction.py` retorna 0
    - `grep -c 'search_scenes' backend_app/modules/satellite/historical_reconstruction.py` retorna 1 e `grep -c 'get_statistics' backend_app/modules/satellite/historical_reconstruction.py` retorna 1 (duas chamadas por reconstrucao, nao um loop mensal)
    - `grep -c 'satellite_historical_years' backend_app/modules/satellite/historical_reconstruction.py` retorna 1
    - `uv run pytest -q tests/modules/satellite/test_historical_reconstruction.py` coleta 10 ou mais testes e todos passam
  </acceptance_criteria>
  <done>Reconstrucao de 5 anos real, mensal, idempotente e fail-closed, com baseline exibido apontando para o Copernicus.</done>
</task>

<task type="auto">
  <name>Task 3: Scheduler APScheduler no lifespan do FastAPI e enfileiramento em create_project (D-14/SATM-10)</name>
  <files>backend_app/modules/satellite/scheduler.py, backend_app/main.py, backend_app/modules/projects/service.py, tests/conftest.py</files>
  <read_first>
    - backend_app/main.py (arquivo inteiro, 37 linhas — nao existe lifespan hoje)
    - backend_app/db/session.py (arquivo inteiro — `get_sessionmaker()` vs `get_session()`)
    - backend_app/modules/projects/service.py linhas 802-910 (`create_project` completo, incluindo `persist_project_boundary` na linha 903)
    - backend_app/modules/satellite/service.py e historical_reconstruction.py (Tasks 1 e 2)
    - tests/conftest.py (arquivo inteiro, 30 linhas)
    - Dockerfile.api e docker-compose.dokploy.yml (confirmar o numero de workers Uvicorn de producao — RESEARCH Open Question 2 / Assumption A1)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Pattern 3" e "Pitfall 2"
  </read_first>
  <action>
**1. `backend_app/modules/satellite/scheduler.py`:**

```python
from __future__ import annotations

# Phase 05 / D-14 -- Primeira infraestrutura assincrona do backend.
#
# Desenho: "enfileirar" = inserir uma linha PENDING em satellite_jobs (feito
# dentro do request, em transacao); "executar" = um poller do APScheduler que
# consome a fila fora do ciclo request/response. Isso mantem SATM-10
# ("nao bloquear requests HTTP") satisfeito e faz o trabalho sobreviver a
# restart -- diferente de BackgroundTasks, que morre com o processo.
```

`async def run_pending_satellite_jobs(settings: Settings | None = None) -> None`
- Abre `async with get_sessionmaker()() as session:` (nunca `get_session`, que e dependency do FastAPI).
- `jobs = await SatelliteService(session).claim_next_jobs(limit=config.satellite_monitoring_batch_size)`.
- Constroi UM provider por ciclo (`build_copernicus_provider(usage_recorder=...)`) e o reutiliza para todos os jobs do lote — o token cacheado e o semaforo de 2 requests so funcionam se a instancia for compartilhada (D-08/D-11).
- Processa os jobs **em serie** (`for job in jobs:`), nunca com `asyncio.gather` — comentario obrigatorio: `# D-11: quota gratuita permite 2 requests concorrentes. Processar projetos em paralelo estouraria a quota mesmo com o semaforo do adapter, porque cada projeto ja faz 2 chamadas.`
- Despacha por `job.job_type`: `HISTORICAL_RECONSTRUCTION` -> `HistoricalReconstructionService(...).run(job)`; `CONTINUOUS_MONITORING` -> importar e chamar `SatelliteMonitoringService(...).run(job)` **se o modulo existir** (o Plan 06 cria `monitoring.py`); ate la, o dispatch registra `FAILED` com `"Tipo de job ainda não implementado"`. Escrever o dispatch como um `dict[str, Callable]` para que o Plan 06 apenas registre a entrada nova.
- `finally: await provider.aclose()`.
- Envolver o ciclo inteiro em `try/except Exception` que loga e retorna: uma excecao nao tratada dentro de um job do APScheduler mata o agendamento daquele job.

`async def enqueue_due_monitoring_jobs(settings: Settings | None = None) -> None`
- Seleciona projetos elegiveis a monitoramento continuo: projetos com pelo menos uma `SatelliteObservation` e cuja observacao mais recente seja mais antiga que `satellite_monitoring_interval_hours`, limitado a `satellite_monitoring_batch_size`.
- Para cada um, `SatelliteService(session).enqueue_job(project, job_type="CONTINUOUS_MONITORING", ...)`; `await session.commit()` ao final.

`def register_satellite_jobs(scheduler: AsyncIOScheduler, settings: Settings) -> None`
- Se `not settings.satellite_scheduler_enabled`: loga `"Scheduler satelital desabilitado por configuração"` e retorna sem registrar nada.
- `scheduler.add_job(run_pending_satellite_jobs, "interval", seconds=settings.satellite_job_poll_seconds, id="satellite_pending_jobs", replace_existing=True, max_instances=1, coalesce=True, misfire_grace_time=60)`
- `scheduler.add_job(enqueue_due_monitoring_jobs, "interval", hours=settings.satellite_monitoring_interval_hours, id="satellite_monitoring_enqueue", replace_existing=True, max_instances=1, coalesce=True, misfire_grace_time=3600)`
- `max_instances=1` + `coalesce=True` sao o que impede sobreposicao de ciclos dentro do MESMO processo.
- Comentario obrigatorio sobre RESEARCH Pitfall 2, com a conclusao registrada apos ler `Dockerfile.api`/`docker-compose.dokploy.yml`:
```python
    # RESEARCH Pitfall 2 / Assumption A1: APScheduler in-process nao coordena
    # entre processos. Registrar aqui o numero de workers Uvicorn confirmado
    # na leitura do Dockerfile.api/docker-compose.dokploy.yml. Com 1 worker
    # (default do uvicorn sem --workers) nao ha duplicacao. Com N > 1, o
    # `skip_locked` de claim_next_jobs evita processar o MESMO job duas vezes,
    # mas N pollers concorrentes ainda podem estourar a quota de 2 requests
    # concorrentes -- nesse caso, SATELLITE_SCHEDULER_ENABLED deve ficar
    # ligado em apenas uma replica, ou adotar SQLAlchemyJobStore (deferido).
```
Se a leitura revelar `--workers` maior que 1, **PARAR e reportar** como blocker antes de prosseguir, registrando no SUMMARY.

**2. `backend_app/main.py` — primeiro lifespan do repositorio** (RESEARCH Pattern 3):
```python
@asynccontextmanager
async def lifespan(application: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    scheduler = AsyncIOScheduler()
    register_satellite_jobs(scheduler, settings)
    if settings.satellite_scheduler_enabled:
        scheduler.start()
    application.state.scheduler = scheduler
    try:
        yield
    finally:
        if scheduler.running:
            scheduler.shutdown(wait=False)
```
`create_app` passa `lifespan=lifespan` ao construir o `FastAPI(...)`. Nao alterar mais nada em `main.py` (CORS, handlers, routers ficam iguais).

**3. `backend_app/modules/projects/service.py` — gancho em `create_project`:**
Logo APOS `await self.persist_project_boundary(project, payload.tags)` (linha ~903, garantindo que a AOI ja existe) e antes do commit/retorno:
```python
        # D-14/SATM-10: enfileira a reconstrucao historica e responde o HTTP
        # imediatamente. NENHUMA chamada ao Copernicus acontece dentro deste
        # request -- o poller do APScheduler consome satellite_jobs fora do
        # ciclo request/response.
        await SatelliteService(self.session).enqueue_job(
            project, job_type="HISTORICAL_RECONSTRUCTION"
        )
```
`deterministic_baseline(payload)` e o `ProjectBaseline` inicial (linhas 817 e 870-881) **permanecem intocados**: sao o placeholder de criacao ate a reconstrucao concluir, e os metadados `baseline_source`/`sentinel_status` ja marcam esse estado como bloqueado/deterministico — exatamente o que `apply_baseline_from_observations` sobrescreve depois. Acrescentar um comentario de uma linha acima da chamada de `deterministic_baseline` apontando para esse contrato.

Importar `SatelliteService` no topo com import local dentro do metodo se houver risco de ciclo (`satellite/service.py` importa `ProjectsService` para `boundary_item` no Plan 05 Task 2 — se o ciclo aparecer, mover o import de `ProjectsService` para dentro do metodo em `historical_reconstruction.py`, nunca criar um segundo `boundary_item`).

**4. `tests/conftest.py`** — acrescentar ao fixture autouse existente:
```python
    # Phase 05: nenhum teste deve iniciar o scheduler in-process. Os testes
    # atuais usam TestClient(app) no nivel de modulo (sem context manager),
    # entao o lifespan nao roda; esta linha e a blindagem para qualquer teste
    # futuro que use `with TestClient(app)`.
    monkeypatch.setenv("SATELLITE_SCHEDULER_ENABLED", "false")
```
(colocado antes do `get_settings.cache_clear()` ja existente).

**5. Teste de nao-bloqueio (SATM-10)** — acrescentar a `tests/modules/satellite/test_historical_reconstruction.py`:
- `test_create_project_enqueues_job_without_calling_provider`: cria um projeto via `POST /api/v1/projects` com um provider global que levantaria excecao se chamado (monkeypatch em `build_copernicus_provider`), assere HTTP 201 e que existe exatamente 1 linha `satellite_jobs` `PENDING` do tipo `HISTORICAL_RECONSTRUCTION` para aquele projeto.
- `test_create_project_response_time_is_not_blocked_by_reconstruction`: mede `time.perf_counter()` em torno do `POST /api/v1/projects` e assere que ficou abaixo de 5 segundos (o teste falharia se a reconstrucao rodasse sincrona).
- `test_enqueue_is_idempotent_for_active_job`: chamar `enqueue_job` duas vezes para o mesmo projeto/tipo cria apenas uma linha ativa.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
from backend_app.main import app, create_app
assert app.router.lifespan_context is not None
from backend_app.modules.satellite.scheduler import register_satellite_jobs, run_pending_satellite_jobs, enqueue_due_monitoring_jobs
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from backend_app.core.config import Settings
s = AsyncIOScheduler(); register_satellite_jobs(s, Settings(satellite_scheduler_enabled=True))
ids = sorted(j.id for j in s.get_jobs())
assert ids == ['satellite_monitoring_enqueue','satellite_pending_jobs'], ids
s2 = AsyncIOScheduler(); register_satellite_jobs(s2, Settings(satellite_scheduler_enabled=False))
assert s2.get_jobs() == []
print('ok')
" && uv run pytest -q tests/modules/satellite/test_historical_reconstruction.py -x && uv run pytest -q tests/test_api_integration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'asynccontextmanager' backend_app/main.py` retorna 1
    - `grep -c 'lifespan=lifespan' backend_app/main.py` retorna 1
    - `grep -c 'scheduler.shutdown' backend_app/main.py` retorna 1
    - `grep -c 'max_instances=1' backend_app/modules/satellite/scheduler.py` retorna 2
    - `grep -c 'asyncio.gather' backend_app/modules/satellite/scheduler.py` retorna 0 (jobs em serie — D-11)
    - `grep -c 'get_sessionmaker' backend_app/modules/satellite/scheduler.py` maior ou igual a 1 e `grep -c 'Depends(get_session)' backend_app/modules/satellite/scheduler.py` retorna 0
    - `grep -c 'enqueue_historical_reconstruction\|enqueue_job' backend_app/modules/projects/service.py` retorna 1
    - `grep -c 'deterministic_baseline' backend_app/modules/projects/service.py` retorna 2 (definicao + a chamada existente em create_project, preservada como fallback)
    - `grep -c 'SATELLITE_SCHEDULER_ENABLED' tests/conftest.py` retorna 1
    - O numero de workers Uvicorn de producao esta registrado em comentario em `scheduler.py` e no SUMMARY
    - `uv run pytest -q` (suite completa) passa sem regressao
  </acceptance_criteria>
  <done>Scheduler in-process ativo no lifespan, criacao de projeto enfileira sem bloquear e nenhum teste inicia jobs reais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request HTTP -> trabalho de longa duracao | Uma chamada sincrona ao Copernicus dentro de `create_project` travaria a API |
| processo da API -> quota compartilhada CDSE | Multiplos workers/pollers concorrem pela mesma quota |
| job assincrono -> event loop do servidor | Um `await` esquecido bloquearia todas as requests |
| dado ausente -> baseline exibido | Falha do provider nao pode virar numero plausivel na tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Denial of Service | `create_project` bloqueado por I/O externo | mitigate | "Enfileirar" e apenas um INSERT na transacao ja aberta; teste `test_create_project_enqueues_job_without_calling_provider` monkeypatcha o provider para explodir se chamado, e o teste de tempo trava o comportamento |
| T-05-28 | Denial of Service | job duplicado por multiplos workers estourando quota | accept (mitigado) | `claim_next_jobs` usa `with_for_update(skip_locked=True)`; `satellite_jobs_active_idx` impede fila duplicada; `max_instances=1`/`coalesce=True` impedem sobreposicao no mesmo processo. Coordenacao entre processos (SQLAlchemyJobStore) fica deferida — o numero de workers de producao e confirmado por leitura e registrado; se for maior que 1, e blocker reportado |
| T-05-29 | Denial of Service | job bloqueante travando o event loop | mitigate | Todo I/O e `await` (`httpx.AsyncClient` + `AsyncSession`); gate de aceite proibe `requests`/`urllib`/`time.sleep` nos modulos do job (RESEARCH Pitfall 3) |
| T-05-30 | Spoofing | baseline simulado apresentado como observacao real | mitigate | `apply_baseline_from_observations` so roda quando ha observacao persistida; no ramo de erro o job termina `FAILED` e `baseline_source` permanece `deterministic_baseline` com `sentinel_status = BLOCKED_MISSING_PROVIDER_CREDENTIALS`; teste `test_reconstruction_fails_closed_without_provider_credentials` |
| T-05-31 | Tampering | observacao duplicada inflando `points_analyzed` | mitigate | `on_conflict_do_nothing` sobre o unique index D-15; teste de segunda execucao persistindo 0 novas linhas |
| T-05-32 | Information Disclosure | mensagem de erro do provider vazando segredo no `satellite_jobs.error_message` | mitigate | O adapter (Plan 03) ja converte falha de token em `RuntimeError("Falha ao autenticar no Copernicus Data Space Ecosystem")` sem encadear a request; `error_message` e truncado em 500 caracteres e a tabela nao tem policy de SELECT |
| T-05-33 | Repudiation | mudanca de fonte do baseline sem trilha | mitigate | `SATELLITE_BASELINE_APPLIED` em `audit_events` com `before_data`/`after_data` de `baseline_source`; `SATELLITE_JOB_ENQUEUED`/`_COMPLETED`/`_FAILED` por transicao de job |
</threat_model>

<verification>
1. `uv run pytest -q tests/modules/satellite/test_historical_reconstruction.py` — SATM-05 verde
2. `uv run python -c "..."` (registro de jobs do scheduler) — lifespan e registro corretos
3. `uv run pytest -q tests/test_api_integration.py tests/test_project_boundaries.py` — criacao de projeto sem regressao
4. `uv run pytest -q` — suite completa verde, sem nenhum teste iniciando scheduler
</verification>

<success_criteria>
- `create_project` enfileira e responde de imediato; nenhuma chamada externa no request
- Reconstrucao de 5 anos com composicao mensal por menor cloud cover, idempotente e sem interpolar meses vazios
- `baseline_source` vira `COPERNICUS` apenas quando ha observacao real persistida
- Scheduler inicia/encerra no lifespan, jobs em serie, desligavel por configuracao
- Numero de workers Uvicorn de producao confirmado e documentado
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-05-SUMMARY.md` registrando: assinatura de `SatelliteService` (o Plan 06/07 consome), o dicionario de dispatch do scheduler (onde o Plan 06 registra `CONTINUOUS_MONITORING`), o numero de workers Uvicorn confirmado em producao, quantas observacoes a reconstrucao gerou nos fixtures de teste, e a formula final do `baseline_hash` de referencia.
</output>
