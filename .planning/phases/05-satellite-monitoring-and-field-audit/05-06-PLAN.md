---
phase: 05-satellite-monitoring-and-field-audit
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - backend_app/modules/satellite/monitoring.py
  - backend_app/modules/satellite/evidence.py
  - backend_app/modules/satellite/scheduler.py
  - backend_app/modules/storage_paths.py
  - tests/modules/satellite/test_monitoring_job.py
autonomous: true
requirements: [SATM-06, SATM-10]

must_haves:
  truths:
    - "O job periodico busca apenas cenas posteriores a ultima observacao do projeto e persiste as novas de forma idempotente"
    - "Observacao com cloud coverage acima de satellite_max_cloud_coverage_pct nunca e persistida"
    - "Rodar o job duas vezes seguidas sobre os mesmos dados nao cria observacao, anomalia nem evento duplicado"
    - "Uma queda de NDVI acima do threshold cria SatelliteAnomaly e um ProjectEvent"
    - "O ProjectEvent avanca automaticamente ate ANALYZED e para ali — nunca chega a CONFIRMED sem acao humana"
    - "Nenhum ProjectEvent criado automaticamente tem tipo DEFORESTATION"
    - "Toda transicao de ProjectEvent gera entrada em audit_events e na timeline do projeto"
    - "before.png/after.png so sao buscados quando o evento chega a ANALYZED, nunca por observacao de rotina"
    - "Falha ao gerar a imagem before/after nao derruba o evento nem o job"
  artifacts:
    - path: "backend_app/modules/satellite/monitoring.py"
      provides: "SatelliteMonitoringService: janela incremental, anomalia, evento, correlacao"
      contains: "class SatelliteMonitoringService"
      min_lines: 220
    - path: "backend_app/modules/satellite/evidence.py"
      provides: "SatelliteEvidenceService: before/after PNG com hash SHA-256 (D-19)"
      contains: "BEFORE_IMAGE"
      min_lines: 100
    - path: "tests/modules/satellite/test_monitoring_job.py"
      provides: "Cobertura SATM-10 e da metade de persistencia de SATM-06"
      min_lines: 180
  key_links:
    - from: "backend_app/modules/satellite/monitoring.py"
      to: "backend_app/modules/satellite/anomaly_detector.py"
      via: "detect_anomalies puro sobre pares consecutivos"
      pattern: "detect_anomalies"
    - from: "backend_app/modules/satellite/monitoring.py"
      to: "project_events"
      via: "DETECTED -> ANALYZED pelo Correlation Engine (D-18)"
      pattern: "ANALYZED"
    - from: "backend_app/modules/satellite/evidence.py"
      to: "satellite_evidence"
      via: "hash SHA-256 do PNG + upload_storage_object (D-19)"
      pattern: "sha256"
---

<objective>
Implementar o monitoramento continuo (SATM-10) e a metade de persistencia de SATM-06: janela incremental respeitando `maxCloudCoverage`, deteccao de anomalia usando o modulo puro do Plan 04, criacao de `SatelliteAnomaly` e `ProjectEvent`, avanco automatico `DETECTED -> ANALYZED` pelo Correlation Engine (D-18) e busca de evidencia visual before/after apenas nesse momento (D-19).

Purpose: e o que transforma observacao em incidente rastreavel. O limite duro desta fase esta aqui: o pipeline automatico PARA em `ANALYZED`. `CONFIRMED`/`DISMISSED` sao decisao humana e vivem no Plan 07.
Output: `satellite/monitoring.py`, `satellite/evidence.py`, registro do dispatch `CONTINUOUS_MONITORING` no scheduler, testes de idempotencia e de vocabulario fechado.

**Nao faz parte deste plano:** rotas HTTP, decisao humana, pendencia de credito, Auto Hold (todos no Plan 07) e frontend (Plans 08/09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-04-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-05-SUMMARY.md

<interfaces>
Do Plan 04 (`backend_app/modules/satellite/anomaly_detector.py`, modulo puro):
```python
@dataclass(frozen=True)
class ObservationSnapshot:
    observed_at: datetime
    ndvi_mean: float | None = None
    ndmi_mean: float | None = None
    nbr_mean: float | None = None
    cloud_coverage: float | None = None
    valid_pixel_percentage: float | None = None

@dataclass(frozen=True)
class AnomalySignal:
    index_name: str; event_type: str; severity: str
    value_before: float; value_after: float; drop_ratio: float
    confidence: float; reason: str; metadata: dict

def detect_anomaly(previous, current, settings=None) -> AnomalySignal | None
def detect_anomalies(observations, settings=None) -> list[tuple[int, AnomalySignal]]
```

Do Plan 05 (`backend_app/modules/satellite/service.py`):
`SatelliteService.persist_observations(project, rows) -> int` (idempotente por D-15), `latest_observation`, `list_observations`, `finish_job`, `usage_recorder_for`, `apply_baseline_from_observations`.
Do Plan 05 (`historical_reconstruction.py`): funcoes puras `select_monthly_scenes`, `join_statistics_to_scenes` — REAPROVEITAR, nao reescrever.
Do Plan 05 (`scheduler.py`): dicionario de dispatch por `job_type` onde este plano registra `CONTINUOUS_MONITORING`.

Do Plan 03 (`backend_app/adapters/satellite.py`): `SatelliteProvider`, `SceneDTO`, `IndexStatisticsDTO`, `SatelliteImageDTO`.

Pipeline de hash + Storage a reaproveitar (`backend_app/modules/projects/routes.py:519-530` e `backend_app/modules/storage_paths.py`):
```python
location = project_document_location(friendly_id, document_type, sha256, extension)
await upload_storage_object(location.bucket, location.object_path, content, mime_type)
```
`storage_paths.py` tem `PROJECTS_BUCKET`, `storage_uri(bucket, object_path)`, `normalize_document_type` e as fabricas `project_document_location`/`project_image_location`.

Trilha auditavel (`backend_app/db/repositories.py`): `create_audit_event(session, action=..., entity_type=..., entity_id=..., actor_role=None, before_data=None, after_data=None, metadata=None)`.
Timeline do projeto (`backend_app/modules/audit/routes.py:77-85`):
```python
project.timeline = [*(project.timeline or []), {"title": ..., "date": <iso date>, "status": "active"|"completed", "desc": ...}]
```
ATENCAO (Phase 04 / STATE.md): `project.timeline` e serializado tanto no dossie publico quanto na revisao interna pelo mesmo `project_to_mrca()`. A descricao gravada aqui precisa ser publica e neutra — nunca nota interna.

Constantes (Plan 01): `PROJECT_EVENT_TYPES`, `PROJECT_EVENT_STATUSES`, `PROJECT_EVENT_TRANSITIONS`, `SATELLITE_ANOMALY_STATUSES`, `SATELLITE_EVIDENCE_KINDS`, `FORBIDDEN_AUTOMATIC_EVENT_TYPES`.
Settings (Plan 01): `satellite_max_cloud_coverage_pct`, `satellite_ndvi_drop_threshold`, `satellite_monitoring_interval_hours`.

Modelos (Plan 01): `SatelliteObservation`, `SatelliteAnomaly`, `ProjectEvent`, `SatelliteEvidence`, `SatelliteJob`.
Idempotencia ja garantida no banco: `satellite_anomalies_idempotency_idx (project_id, observation_id, index_name)`, `project_events_anomaly_idx (anomaly_id)`, `satellite_evidence_event_kind_idx (project_event_id, kind)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SatelliteMonitoringService — janela incremental, maxCloudCoverage e idempotencia (SATM-10)</name>
  <files>backend_app/modules/satellite/monitoring.py, tests/modules/satellite/test_monitoring_job.py</files>
  <read_first>
    - backend_app/modules/satellite/historical_reconstruction.py (Plan 05 — `select_monthly_scenes`/`join_statistics_to_scenes` a reaproveitar e a estrutura de `run(job)` a espelhar)
    - backend_app/modules/satellite/service.py (Plan 05 — persist_observations, finish_job, list_observations)
    - backend_app/modules/satellite/anomaly_detector.py (Plan 04)
    - backend_app/modules/satellite/scheduler.py (Plan 05 — o dicionario de dispatch)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-11, D-13, D-15, D-17
  </read_first>
  <behavior>
    - Projeto sem nenhuma observacao anterior: a janela comeca em `now - satellite_monitoring_interval_hours * 2` (nunca refaz os 5 anos)
    - Projeto com observacao anterior: a janela comeca exatamente em `ultima_observacao.observed_at` e termina em `now`
    - Cena com `cloud_coverage` acima de `satellite_max_cloud_coverage_pct` nao vira observacao
    - Duas execucoes consecutivas sobre a mesma resposta do provider persistem N e depois 0 observacoes
    - Duas execucoes consecutivas nao criam anomalia duplicada para a mesma combinacao (project, observation, index)
    - Duas execucoes consecutivas nao criam um segundo ProjectEvent para a mesma anomalia
    - `satellite_jobs` do tipo CONTINUOUS_MONITORING termina COMPLETED com `observations_persisted` e `anomalies_detected` preenchidos
    - Provider sem credenciais -> job FAILED com a mensagem do adapter, 0 observacoes, 0 anomalias
    - Projeto sem `active_boundary` -> job FAILED sem chamar o provider
    - O job nunca usa `asyncio.gather` para paralelizar chamadas ao provider (D-11)
  </behavior>
  <action>
Criar `backend_app/modules/satellite/monitoring.py` com `SatelliteMonitoringService`, espelhando a estrutura de `HistoricalReconstructionService` (Plan 05 Task 2):

```python
class SatelliteMonitoringService:
    def __init__(self, session: AsyncSession, provider: SatelliteProvider, settings: Settings | None = None) -> None:
```

**Funcao pura no nivel de modulo:**
```python
def monitoring_window(last_observed_at: datetime | None, now: datetime, settings: Settings | None = None) -> tuple[datetime, datetime]:
    """Janela incremental do monitoramento continuo (SATM-10).

    Com observacao anterior, comeca exatamente onde a ultima parou -- a
    idempotencia D-15 cobre a sobreposicao de borda. Sem observacao anterior,
    usa uma janela curta (2 ciclos) em vez dos 5 anos: reconstrucao historica
    e responsabilidade do HISTORICAL_RECONSTRUCTION, nunca deste job.
    """
```

**`async def run(self, job: SatelliteJob) -> None`**, na ordem:
1. Carregar `Project`; obter AOI via `ProjectsService(self.session).boundary_item(str(project.id))`. Sem `active`, finalizar FAILED com `"Projeto sem active_boundary (Phase 04.1): monitoramento não pode definir a AOI"` e retornar SEM chamar o provider.
2. `last = await SatelliteService(self.session).latest_observation(project.id)`; `date_from, date_to = monitoring_window(last.observed_at if last else None, datetime.now(timezone.utc), self.settings)`. Gravar em `job.window_start`/`job.window_end`.
3. Duas chamadas ao provider (mesma economia de PU do Plan 05): `search_scenes(...)` e `get_statistics(..., aggregation_interval="P1M")`, ambas com `max_cloud_coverage=config.satellite_max_cloud_coverage_pct`. Comentario obrigatorio: `# D-11: duas chamadas por projeto, em serie. O scheduler ja processa projetos em serie; nada aqui usa asyncio.gather.`
4. `scenes_by_month = select_monthly_scenes(scenes, max_cloud_coverage=config.satellite_max_cloud_coverage_pct)` e `rows = join_statistics_to_scenes(statistics, scenes_by_month)` — IMPORTADOS de `historical_reconstruction.py`, nunca reimplementados.
5. Guarda extra de D-13 antes de persistir: descartar qualquer `row` cujo `cloud_coverage` seja `None` ou maior que o limite. Comentario: `# D-13: maxCloudCoverage BLOQUEIA a observacao. Nao existe observacao "com ressalva".`
6. `persisted = await SatelliteService(self.session).persist_observations(project, rows)`.
7. `anomalies = await self.detect_and_persist_anomalies(project)` (Task 2).
8. `await SatelliteService(self.session).apply_baseline_from_observations(project)` — mantem o baseline exibido sempre na observacao mais recente.
9. `finish_job(job, status="COMPLETED", observations_persisted=persisted, anomalies_detected=len(anomalies))`.
10. `except Exception as exc:` -> `rollback()` + `finish_job(job, status="FAILED", error_message=str(exc)[:500])`, sem re-lancar (o poller continua com os proximos jobs) e SEM persistir nada simulado.

**Registrar o dispatch** em `backend_app/modules/satellite/scheduler.py`: acrescentar `"CONTINUOUS_MONITORING": SatelliteMonitoringService` ao dicionario criado no Plan 05, substituindo o ramo temporario que marcava FAILED com "Tipo de job ainda nao implementado".

**Testes** — criar `tests/modules/satellite/test_monitoring_job.py` com o mesmo `FakeProvider` do Plan 05 (copiado, nao importado — `tests/` nao tem `__init__.py`). Testes puros para `monitoring_window`, e testes de integracao com `asyncio.run` + `get_sessionmaker()` para `run`. Um teste por item de `<behavior>`, incluindo por nome:
- `test_monitoring_window_starts_at_last_observation`
- `test_monitoring_window_without_history_is_short_not_five_years`
- `test_cloudy_scene_is_never_persisted`
- `test_monitoring_job_is_idempotent_across_runs`
- `test_monitoring_job_fails_closed_without_credentials`
- `test_monitoring_job_without_active_boundary_never_calls_provider`
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/modules/satellite/test_monitoring_job.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def monitoring_window' backend_app/modules/satellite/monitoring.py` retorna 1
    - `grep -c 'from backend_app.modules.satellite.historical_reconstruction import' backend_app/modules/satellite/monitoring.py` retorna 1 (reaproveitamento, nao duplicacao)
    - `grep -c 'def select_monthly_scenes\|def join_statistics_to_scenes' backend_app/modules/satellite/monitoring.py` retorna 0
    - `grep -c 'asyncio.gather' backend_app/modules/satellite/monitoring.py` retorna 0
    - `grep -c 'satellite_max_cloud_coverage_pct' backend_app/modules/satellite/monitoring.py` maior ou igual a 2
    - `grep -c 'CONTINUOUS_MONITORING' backend_app/modules/satellite/scheduler.py` maior ou igual a 1
    - `grep -c 'ainda não implementado' backend_app/modules/satellite/scheduler.py` retorna 0
    - `uv run pytest -q tests/modules/satellite/test_monitoring_job.py` coleta 10 ou mais testes e todos passam
  </acceptance_criteria>
  <done>Job periodico incremental, idempotente, fail-closed e respeitando o limite de nuvem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anomalia persistida, ProjectEvent DETECTED para ANALYZED e trilha auditavel (SATM-06, D-17/D-18/D-21)</name>
  <files>backend_app/modules/satellite/monitoring.py, tests/modules/satellite/test_monitoring_job.py</files>
  <read_first>
    - backend_app/modules/satellite/monitoring.py (Task 1)
    - backend_app/modules/satellite/anomaly_detector.py (Plan 04 — a saida AnomalySignal a mapear)
    - backend_app/modules/integrity/service.py linhas 772-850 (padrao append-only + create_audit_event por transicao)
    - backend_app/modules/audit/routes.py linhas 77-95 (padrao de append em project.timeline + create_audit_event)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-17, D-18, D-21, D-23
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Pitfall 5" (project_events nao e audit_events)
  </read_first>
  <behavior>
    - Serie com queda de NDVI de 0.30 entre dois meses gera 1 SatelliteAnomaly com status LINKED e 1 ProjectEvent
    - O ProjectEvent criado tem status ANALYZED ao fim do job (passou por DETECTED e foi correlacionado no mesmo ciclo)
    - Nenhum ProjectEvent criado automaticamente tem status CONFIRMED — nem no caminho de severidade CRITICAL
    - `ProjectEvent.type` sempre em PROJECT_EVENT_TYPES; nenhuma linha com DEFORESTATION e criada em nenhum cenario
    - `affected_area_ha` e derivada da area do boundary ponderada pela queda relativa, nunca de estimativa de carbono
    - Cada anomalia detectada gera dois audit_events: SATELLITE_ANOMALY_DETECTED e SATELLITE_EVENT_ANALYZED
    - Cada evento acrescenta exatamente uma entrada na project.timeline, com descricao publica e neutra
    - Rodar o job de novo sobre os mesmos dados nao cria anomalia nem evento novo, e nao acrescenta entrada na timeline
    - Serie com recuperacao de NDVI gera VEGETATION_RECOVERY com severidade LOW
  </behavior>
  <action>
Acrescentar a `SatelliteMonitoringService`:

**`async def detect_and_persist_anomalies(self, project: Project) -> list[SatelliteAnomaly]`**
1. Ler todas as observacoes do projeto ordenadas por `observed_at` (`SatelliteService.list_observations`).
2. Montar `ObservationSnapshot` para cada uma e chamar `detect_anomalies(snapshots, self.settings)` — o modulo puro do Plan 04 faz toda a regra; NENHUMA comparacao de NDVI acontece neste arquivo.
3. Para cada par `(indice, signal)` devolvido, resolver a `SatelliteObservation` correspondente (`observations[indice]`) e a anterior (`observations[indice - 1]`), e persistir com idempotencia:
```python
        stmt = (
            pg_insert(SatelliteAnomaly)
            .values(
                project_id=project.id,
                observation_id=current.id,
                previous_observation_id=previous.id,
                index_name=signal.index_name,
                value_before=Decimal(str(signal.value_before)),
                value_after=Decimal(str(signal.value_after)),
                drop_ratio=Decimal(str(signal.drop_ratio)),
                severity=signal.severity,
                confidence=Decimal(str(signal.confidence)),
                affected_area_ha=affected_area,
                status="PENDING_ANALYSIS",
                reason=signal.reason,
                detected_at=current.observed_at,
                metadata_={"event_type": signal.event_type, **signal.metadata},
            )
            .on_conflict_do_nothing(index_elements=["project_id", "observation_id", "index_name"])
            .returning(SatelliteAnomaly.id)
        )
```
   Anomalia ja existente (conflito) e simplesmente pulada — sem update, sem evento novo.
4. `affected_area` (D-23, sem estimativa de carbono):
```python
        # Area afetada = area ativa do projeto ponderada pela queda relativa
        # do indice. NAO e estimativa de carbono nem de toneladas (fora de
        # escopo por D-23/Bible): e uma ordem de grandeza para a revisao
        # humana dimensionar o incidente.
        affected_area = round(active_area_ha * max(0.0, signal.drop_ratio), 4) if active_area_ha else None
```
   `active_area_ha` vem de `boundary_item(...)["declaredAreaHa"]` (Phase 04.1).
5. `create_audit_event(action="SATELLITE_ANOMALY_DETECTED", entity_type="projects", entity_id=project.id, metadata={"anomaly_id": ..., "index": signal.index_name, "severity": signal.severity, "drop_ratio": signal.drop_ratio, "observed_at": current.observed_at.isoformat()})`.
6. Chamar `await self.correlate_and_create_event(project, anomaly, signal)` para cada anomalia recem-criada.

**`async def correlate_and_create_event(self, project, anomaly, signal) -> ProjectEvent | None`** — Correlation Engine (D-18). Cabecalho obrigatorio no metodo:
```python
        # D-18: o pipeline automatico vai ate ANALYZED e PARA. As transicoes
        # para CONFIRMED e DISMISSED exigem decisao humana (Plan 07). Nao
        # existe, em nenhum ramo deste metodo, atribuicao desses estados.
```
1. Guarda nao-discricionaria antes de qualquer escrita:
```python
        if signal.event_type not in PROJECT_EVENT_TYPES or signal.event_type in FORBIDDEN_AUTOMATIC_EVENT_TYPES:
            raise ValueError(f"Tipo de evento não permitido automaticamente: {signal.event_type}")
```
2. Inserir `ProjectEvent` com `status="DETECTED"`, `type=signal.event_type`, `severity=signal.severity`, `confidence`, `affected_area_ha=anomaly.affected_area_ha`, `ndvi_before`/`ndvi_after` de `signal.value_before`/`value_after`, `summary=signal.reason`, `detected_at=anomaly.detected_at`, `anomaly_id=anomaly.id`, usando `on_conflict_do_nothing(index_elements=["anomaly_id"])`. Se nada foi inserido (evento ja existe), retornar `None`.
3. **Correlacao** (leve, sem intervencao humana — o que D-18 chama de "cruza com historico e QTAGs"):
   - Contar eventos ja CONFIRMED do projeto (`select count(*) from project_events where project_id = ... and status = 'CONFIRMED'`).
   - Ler `ProjectTag` com `status == "ACTIVE"`; registrar em `metadata["stale_qtags"]` as que tem `last_seen_at` anterior a `detected_at` por mais de 180 dias.
   - Contar quantas observacoes consecutivas anteriores ja estavam em queda (persistencia do sinal).
   - Guardar tudo em `event.metadata_["correlation"]`. Comentario obrigatorio: `# A correlacao enriquece a explicacao mostrada ao revisor humano; ela NAO altera severidade nem status. Elevar severidade automaticamente seria decidir sem humano (D-18).`
4. Avancar o estado validando contra o vocabulario: `assert "ANALYZED" in PROJECT_EVENT_TRANSITIONS[event.status]`, entao `event.status = "ANALYZED"` e `event.analyzed_at = datetime.now(timezone.utc)`.
5. `create_audit_event(action="SATELLITE_EVENT_DETECTED", ...)` e `create_audit_event(action="SATELLITE_EVENT_ANALYZED", entity_type="projects", entity_id=project.id, before_data={"status": "DETECTED"}, after_data={"status": "ANALYZED"}, metadata={"event_id": ..., "type": ..., "severity": ..., "correlation": ...})`.
6. **Timeline (D-21)** — a "notificacao de incidente" desta fase e o registro persistido e visivel:
```python
        # D-21: sem provedor de push/e-mail nesta fase. A notificacao e o
        # registro persistido e visivel: audit_events + timeline do projeto,
        # que produtor, certificadora e auditor ja consultam hoje.
        # ATENCAO: project.timeline aparece TAMBEM no dossie publico
        # (project_to_mrca), entao a descricao e publica e neutra.
        project.timeline = [
            *(project.timeline or []),
            {
                "title": SATELLITE_EVENT_TIMELINE_TITLES[event.type],
                "date": event.detected_at.date().isoformat(),
                "status": "active",
                "desc": (
                    "Monitoramento Sentinel-2 registrou variação relevante de vegetação; "
                    f"evento em análise técnica (severidade {event.severity.lower()})."
                ),
            },
        ]
```
   Com o mapa de titulos declarado no topo de `monitoring.py` (nao em `constants.py`, que pertence ao Plan 01 e nao esta em `files_modified` deste plano):
```python
SATELLITE_EVENT_TIMELINE_TITLES: dict[str, str] = {
    "VEGETATION_LOSS": "Perda de vegetação detectada por satélite",
    "VEGETATION_RECOVERY": "Recuperação de vegetação detectada por satélite",
    "POSSIBLE_FIRE": "Possível incêndio detectado por satélite",
}
```
7. Marcar `anomaly.status = "LINKED"` (a anomalia agora tem evento).
8. Chamar `SatelliteEvidenceService(...).capture_before_after(project, event)` (Task 3), envolto em `try/except Exception` que apenas loga e segue: falha de imagem NAO pode derrubar o evento nem o job.

**Testes** — acrescentar a `tests/modules/satellite/test_monitoring_job.py` um teste por item de `<behavior>`, incluindo por nome:
- `test_ndvi_drop_creates_anomaly_and_analyzed_event`
- `test_automatic_pipeline_never_reaches_confirmed`
- `test_no_project_event_is_ever_deforestation` — apos rodar o job em varios cenarios, uma query direta em `project_events` filtrando `type = 'DEFORESTATION'` retorna 0 linhas
- `test_event_appends_single_public_timeline_entry` — a `desc` gravada nao contem nota interna nem friendly_id de outro projeto
- `test_second_run_does_not_duplicate_anomaly_event_or_timeline`
- `test_vegetation_recovery_is_low_severity`
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/modules/satellite/test_monitoring_job.py -x && PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -tAc "select count(*) from project_events where type = 'DEFORESTATION';" | tr -d ' ' | grep -qx 0 && echo NO_DEFORESTATION</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'detect_anomalies' backend_app/modules/satellite/monitoring.py` maior ou igual a 1
    - `grep -v '^ *#' backend_app/modules/satellite/monitoring.py | grep -c '"CONFIRMED"'` retorna no maximo 1 e, se retornar 1, a unica ocorrencia esta dentro da query de contagem da correlacao (nunca em atribuicao de `event.status`)
    - `grep -v '^ *#' backend_app/modules/satellite/monitoring.py | grep -c 'event.status = "CONFIRMED"'` retorna 0
    - `grep -c 'FORBIDDEN_AUTOMATIC_EVENT_TYPES' backend_app/modules/satellite/monitoring.py` retorna 1
    - `grep -c 'PROJECT_EVENT_TRANSITIONS' backend_app/modules/satellite/monitoring.py` retorna 1
    - `grep -c 'SATELLITE_ANOMALY_DETECTED' backend_app/modules/satellite/monitoring.py` retorna 1 e `grep -c 'SATELLITE_EVENT_ANALYZED' backend_app/modules/satellite/monitoring.py` retorna 1
    - `grep -ic 'carbono\|tonelada\|carbon_stock' backend_app/modules/satellite/monitoring.py` retorna 0 (D-23)
    - A query `select count(*) from project_events where type = 'DEFORESTATION'` retorna 0 apos a suite
    - `uv run pytest -q tests/modules/satellite/test_monitoring_job.py` coleta 16 ou mais testes e todos passam
  </acceptance_criteria>
  <done>Anomalia e evento persistidos com vocabulario fechado, ciclo parando em ANALYZED e notificacao por registro persistido.</done>
</task>

<task type="auto">
  <name>Task 3: SatelliteEvidenceService — before/after PNG com hash SHA-256, apenas em ANALYZED (D-19)</name>
  <files>backend_app/modules/satellite/evidence.py, backend_app/modules/storage_paths.py, tests/modules/satellite/test_monitoring_job.py</files>
  <read_first>
    - backend_app/modules/storage_paths.py (arquivo inteiro, 51 linhas — fabricas de StorageLocation a imitar)
    - backend_app/modules/projects/routes.py linhas 519-535 (hash -> location -> upload_storage_object, pipeline a reaproveitar)
    - backend_app/modules/supabase_storage.py (assinaturas de upload_storage_object/download_storage_object)
    - backend_app/adapters/satellite.py (Plan 03 — SatelliteImageDTO)
    - backend_app/modules/satellite/monitoring.py (Task 2 — o ponto de chamada)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-19
  </read_first>
  <action>
**1. `backend_app/modules/storage_paths.py`** — acrescentar uma fabrica no mesmo formato das existentes:
```python
def satellite_evidence_location(project_friendly_id: str, kind: str, sha256: str, extension: str = ".png") -> StorageLocation:
    normalized_kind = normalize_document_type(kind)
    object_path = f"projects/{project_friendly_id}/satellite/{normalized_kind}/{sha256}{extension}"
    return StorageLocation(PROJECTS_BUCKET, object_path, storage_uri(PROJECTS_BUCKET, object_path))
```

**2. `backend_app/modules/satellite/evidence.py`** com `SatelliteEvidenceService`:

```python
class SatelliteEvidenceService:
    """Evidencia visual de anomalia (D-19).

    A Process API custa MUITO mais Processing Units que a Statistical API,
    entao before.png/after.png sao gerados APENAS quando um ProjectEvent
    chega a ANALYZED -- nunca por observacao de rotina. Cada imagem e
    hasheada em SHA-256 e persistida com o mesmo pipeline de
    documents.sha256_hash (Phase 3/04.2).
    """
    def __init__(self, session: AsyncSession, provider: SatelliteProvider, settings: Settings | None = None) -> None:
```

`async def capture_before_after(self, project: Project, event: ProjectEvent) -> list[SatelliteEvidence]`
1. Guarda de estado: `if event.status != "ANALYZED": return []` — comentario `# D-19: gate de custo. Qualquer outro estado retorna sem gastar PU.`
2. Guarda de idempotencia: se ja existem evidencias BEFORE_IMAGE e AFTER_IMAGE para `event.id`, retornar as existentes sem chamar o provider.
3. Resolver as duas janelas a partir da anomalia vinculada: `before` = janela de mais ou menos 15 dias em torno de `anomaly.previous_observation.observed_at`; `after` = janela de mais ou menos 15 dias em torno de `anomaly.observation.observed_at`. Sem anomalia vinculada, retornar `[]`.
4. Para cada janela, na ordem (nunca em paralelo — D-11):
   - `image = await self.provider.get_image(aoi=aoi, date_from=..., date_to=..., max_cloud_coverage=config.satellite_max_cloud_coverage_pct, width=512, height=512)`
   - `sha256 = hashlib.sha256(image.content).hexdigest()`
   - `location = satellite_evidence_location(project.friendly_id, kind, sha256, ".png")`
   - `await upload_storage_object(location.bucket, location.object_path, image.content, image.mime_type)`
   - `pg_insert(SatelliteEvidence).values(project_id=..., project_event_id=event.id, anomaly_id=event.anomaly_id, kind=kind, storage_bucket=location.bucket, storage_object_path=location.object_path, storage_path=location.uri, sha256_hash=sha256, mime_type=image.mime_type, size_bytes=len(image.content), captured_at=..., metadata_={"width": image.width, "height": image.height, "window_from": ..., "window_to": ...}).on_conflict_do_nothing(index_elements=["project_event_id", "kind"])`
5. `create_audit_event(action="SATELLITE_EVIDENCE_CAPTURED", entity_type="projects", entity_id=project.id, metadata={"event_id": str(event.id), "kinds": [...], "sha256": {kind: hash}})`.
6. Qualquer excecao do provider (inclusive o `RuntimeError` fail-closed) e PROPAGADA para o chamador — e o `try/except` do `correlate_and_create_event` (Task 2) que decide seguir sem imagem. Comentario obrigatorio: `# Este servico nao engole erro: quem decide que "sem imagem o evento continua valido" e o monitoring, nao a camada de evidencia.`

`async def list_evidence(self, event_id) -> list[SatelliteEvidence]` e
`async def load_evidence_bytes(self, evidence: SatelliteEvidence) -> bytes | None` (via `download_storage_object`), consumidos pela rota do Plan 07.

**3. Testes** — acrescentar a `tests/modules/satellite/test_monitoring_job.py` (o comportamento e exercitado pelo caminho ANALYZED com `FakeProvider` devolvendo um PNG minimo `b"\x89PNG\r\n\x1a\n" + b"\x00" * 64`):
- `test_before_after_is_only_captured_when_event_reaches_analyzed` — `FakeProvider` conta chamadas de `get_image`: 0 quando nenhum evento chega a ANALYZED, 2 quando um chega
- `test_before_after_capture_is_idempotent` — segunda execucao nao chama `get_image` de novo
- `test_event_survives_image_capture_failure` — `FakeProvider.get_image` levantando excecao ainda deixa o ProjectEvent em ANALYZED e o job em COMPLETED
- `test_satellite_evidence_hash_matches_image_bytes` — o `sha256_hash` persistido e igual ao SHA-256 dos bytes devolvidos pelo provider
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
import inspect
from backend_app.modules.storage_paths import satellite_evidence_location
loc = satellite_evidence_location('PRC-2024-002', 'BEFORE_IMAGE', 'a'*64)
assert loc.bucket == 'projects', loc
assert loc.object_path == 'projects/PRC-2024-002/satellite/before_image/' + 'a'*64 + '.png', loc.object_path
assert loc.uri.startswith('supabase://projects/'), loc.uri
from backend_app.modules.satellite.evidence import SatelliteEvidenceService
for name in ('capture_before_after','list_evidence','load_evidence_bytes'):
    assert inspect.iscoroutinefunction(getattr(SatelliteEvidenceService, name)), name
print('ok')
" && uv run pytest -q tests/modules/satellite/test_monitoring_job.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def satellite_evidence_location' backend_app/modules/storage_paths.py` retorna 1
    - `grep -c 'event.status != "ANALYZED"' backend_app/modules/satellite/evidence.py` retorna 1
    - `grep -c 'hashlib.sha256' backend_app/modules/satellite/evidence.py` retorna 1
    - `grep -c 'upload_storage_object' backend_app/modules/satellite/evidence.py` retorna 1
    - `grep -c 'on_conflict_do_nothing' backend_app/modules/satellite/evidence.py` retorna 1
    - `grep -c 'get_image' backend_app/modules/satellite/monitoring.py` retorna 0 (a Process API so e chamada pela camada de evidencia)
    - `uv run pytest -q tests/modules/satellite/test_monitoring_job.py` passa por inteiro
    - `uv run pytest -q` (suite completa) passa sem regressao
  </acceptance_criteria>
  <done>Evidencia visual gerada apenas no gate de custo correto, com hash SHA-256 real e sem derrubar o evento em caso de falha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado satelital -> incidente persistido | Um evento errado bloqueia um projeto legitimo; um evento omitido esconde um incidente real |
| pipeline automatico -> decisao humana | A fronteira dura de D-18: nada automatico pode confirmar |
| timeline do projeto -> dossie publico | `project.timeline` e serializado publicamente pelo mesmo `project_to_mrca` |
| job -> quota Copernicus | Process API e a chamada cara; disparo indevido esgota a quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-34 | Elevation of Privilege | evento confirmado sem humano | mitigate | `PROJECT_EVENT_TRANSITIONS` valida a transicao; gate de aceite `grep -c 'event.status = "CONFIRMED"'` = 0 em `monitoring.py`; teste `test_automatic_pipeline_never_reaches_confirmed` |
| T-05-35 | Tampering | rotulo juridico automatico | mitigate | `ValueError` explicito quando `event_type` esta em `FORBIDDEN_AUTOMATIC_EVENT_TYPES` + check constraint do banco (Plan 01); query direta em `project_events` provando 0 linhas `DEFORESTATION` |
| T-05-36 | Information Disclosure | nota interna vazando pela timeline publica | mitigate | Descricao fixa, neutra e sem identificador de terceiro, gerada de um mapa de titulos constante; teste `test_event_appends_single_public_timeline_entry` (mesma disciplina ja adotada na Phase 04 para as notes do certificador) |
| T-05-37 | Denial of Service | Process API disparada por observacao de rotina | mitigate | Gate `event.status != "ANALYZED"` + idempotencia por `(project_event_id, kind)`; `get_image` proibido em `monitoring.py`; teste contando chamadas |
| T-05-38 | Tampering | anomalia/evento duplicado inflando o historico | mitigate | `on_conflict_do_nothing` sobre os tres indices unicos criados no Plan 01; teste de segunda execucao sem duplicacao (incluindo a timeline) |
| T-05-39 | Denial of Service | falha de Storage/Process derrubando o job inteiro | mitigate | `capture_before_after` propaga, mas o chamador em `correlate_and_create_event` captura e segue; teste `test_event_survives_image_capture_failure` |
| T-05-40 | Repudiation | incidente sem trilha | mitigate | `SATELLITE_ANOMALY_DETECTED`, `SATELLITE_EVENT_DETECTED`, `SATELLITE_EVENT_ANALYZED` e `SATELLITE_EVIDENCE_CAPTURED` em `audit_events`, mais a entrada na timeline (D-21) |
</threat_model>

<verification>
1. `uv run pytest -q tests/modules/satellite/test_monitoring_job.py` — SATM-06 (persistencia) e SATM-10 verdes
2. Query `select count(*) from project_events where type = 'DEFORESTATION'` retorna 0
3. `uv run pytest -q tests/modules/satellite/` — reconstrucao e monitoramento juntos sem regressao
4. `uv run pytest -q` — suite completa verde
</verification>

<success_criteria>
- Job incremental idempotente que respeita `maxCloudCoverage` e nunca paraleliza chamadas ao provider
- Anomalia e evento persistidos com vocabulario fechado e severidade derivada do modulo puro
- Ciclo automatico terminando em ANALYZED, com trilha em `audit_events` e timeline publica neutra
- before/after apenas em ANALYZED, com hash SHA-256 real, sem derrubar o evento em caso de falha
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-06-SUMMARY.md` registrando: shape final de `SatelliteAnomaly`/`ProjectEvent` populados (o Plan 07 serializa), o conteudo exato de `metadata.correlation`, a formula final de `affected_area_ha`, os textos de timeline gravados e a contagem de chamadas ao provider por ciclo de monitoramento.
</output>
