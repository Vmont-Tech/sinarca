---
phase: 05-satellite-monitoring-and-field-audit
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend_app/adapters/satellite.py
  - backend_app/adapters/copernicus.py
  - tests/adapters/test_copernicus.py
  - .planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md
autonomous: true
requirements: [SATM-05, SATM-10]

must_haves:
  truths:
    - "Sem COPERNICUS_CLIENT_ID/COPERNICUS_CLIENT_SECRET todo metodo publico do provider levanta RuntimeError explicito antes de qualquer request HTTP"
    - "Nenhum caminho de codigo do adapter devolve NDVI/NDMI/NBR simulado ou derivado de hash"
    - "O token OAuth2 e buscado uma vez e reutilizado ate 30s antes de expirar, protegido por asyncio.Lock"
    - "Nunca ha mais de 2 requests concorrentes contra o Sentinel Hub (semaforo interno do adapter)"
    - "Toda chamada externa emite um registro de consumo (endpoint, outcome, http_status, duracao, processing units) sem nunca conter o client secret"
    - "As URLs de saida sao constantes de configuracao, nunca parametro vindo de request"
  artifacts:
    - path: "backend_app/adapters/satellite.py"
      provides: "Protocol SatelliteProvider + DTOs (SceneDTO, IndexStatisticsDTO, SatelliteImageDTO, CopernicusUsageRecord)"
      contains: "class SatelliteProvider"
      min_lines: 70
    - path: "backend_app/adapters/copernicus.py"
      provides: "CopernicusProvider fail-closed com httpx.AsyncClient, token cacheado e semaforo"
      contains: "def assert_ready"
      min_lines: 220
    - path: "tests/adapters/test_copernicus.py"
      provides: "Cobertura SATM-05 com httpx.MockTransport, sem credenciais reais"
      min_lines: 150
    - path: ".planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md"
      provides: "Pre-requisito operacional de credenciais (D-10)"
      min_lines: 40
  key_links:
    - from: "backend_app/adapters/copernicus.py"
      to: "https://identity.dataspace.copernicus.eu/.../token"
      via: "OAuth2 client credentials cacheado (D-08)"
      pattern: "client_credentials"
    - from: "backend_app/adapters/copernicus.py"
      to: "https://sh.dataspace.copernicus.eu/api/v1/statistics"
      via: "Statistical API como via preferencial (D-09)"
      pattern: "api/v1/statistics"
    - from: "backend_app/adapters/copernicus.py"
      to: "CopernicusUsageRecord"
      via: "usage_recorder chamado a cada request (D-26)"
      pattern: "usage_recorder"
---

<objective>
Implementar a integracao real com o Copernicus Data Space Ecosystem: o Protocol `SatelliteProvider` com seus DTOs e a implementacao `CopernicusProvider` sobre `httpx.AsyncClient` — fail-closed como `stellar.py`, com token OAuth2 cacheado, semaforo de 2 requests concorrentes (D-11) e registro de consumo por chamada (D-26). E o primeiro adapter assincrono do repositorio.

Purpose: e a peca que substitui `deterministic_baseline()` como fonte de dado real. Sem ela, SATM-05/SATM-07 sao impossiveis. Nenhum plano posterior fala HTTP com o Copernicus — todos passam por aqui.
Output: `adapters/satellite.py` (contrato), `adapters/copernicus.py` (implementacao), testes com `httpx.MockTransport` (sem credenciais reais), e o documento de pre-requisito operacional de credenciais.

**Nao faz parte deste plano:** persistencia (o adapter nao importa `AsyncSession` nem modelo ORM), scheduler, deteccao de anomalia, rotas ou frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-01-SUMMARY.md

<interfaces>
Padrao fail-closed a replicar (`backend_app/adapters/stellar.py:36-60`, lido integralmente):
```python
@dataclass(frozen=True)
class StellarAdapterConfig:
    ...
    @classmethod
    def from_env(cls) -> "StellarAdapterConfig": ...
    def assert_ready(self) -> None:
        missing: list[str] = []
        if not self.issuer_secret_key: missing.append("STELLAR_ISSUER_SECRET_KEY")
        if missing: raise RuntimeError("Configuração Stellar incompleta")
```
Todo metodo publico abre com `self.config.assert_ready()` (`stellar.py:56-57`, `133-134`).

Estilo de teste de adapter ja existente (`tests/adapters/test_blockchain_financial_adapters.py`):
```python
def test_stellar_testnet_mode_fails_closed_without_required_secrets() -> None:
    sponsor = StellarReserveSponsor(StellarAdapterConfig(mode="testnet", network="testnet"))
    with pytest.raises(RuntimeError, match="Configuração Stellar incompleta"):
        sponsor.sponsor_account_and_trustline("PRC-2026-001", "GPRODUCER", "SINARCA")
```

Endpoints confirmados (D-08/D-09, CONTEXT.md):
- Token: `POST https://identity.dataspace.copernicus.eu/auth/realms/CDSE/protocol/openid-connect/token`, form `grant_type=client_credentials&client_id=...&client_secret=...`, resposta `{"access_token": "...", "expires_in": 600}`
- STAC: `POST https://stac.dataspace.copernicus.eu/v1/search`, colecao `sentinel-2-l2a`
- Statistical: `POST https://sh.dataspace.copernicus.eu/api/v1/statistics`
- Process: `POST https://sh.dataspace.copernicus.eu/api/v1/process`

Quotas (D-11): 10.000 PU/mes, 300 PU/min, 300 req/min, **2 requests concorrentes**, 12 TB/mes.

Settings ja disponiveis (Plan 01): `copernicus_max_concurrent_requests: int = 2`, `copernicus_request_timeout_seconds: float = 30.0`, `satellite_max_cloud_coverage_pct: float = 20.0`, `satellite_preferred_cloud_coverage_pct: float = 10.0`.

Constantes ja disponiveis (Plan 01, `backend_app/modules/satellite/constants.py`): `COPERNICUS_USAGE_ENDPOINTS`, `COPERNICUS_USAGE_OUTCOMES`, `SATELLITE_INDEX_NAMES`.

Anti-padrao explicito (RESEARCH): `etherfuse.py`/`polygon.py` usam `urllib.request` sincrono deliberadamente; `copernicus.py` e o primeiro adapter ASSINCRONO e deve usar `httpx.AsyncClient` de ponta a ponta. Nenhum `requests`/`urllib` neste arquivo.

Rejeicao registrada (RESEARCH "Alternatives Considered"): `sentinelhub-py` NAO deve ser adotado (8+ deps de producao novas, biblioteca sincrona).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato SatelliteProvider, config fail-closed, token cacheado e semaforo de concorrencia</name>
  <files>backend_app/adapters/satellite.py, backend_app/adapters/copernicus.py</files>
  <read_first>
    - backend_app/adapters/stellar.py (arquivo inteiro, 169 linhas — padrao fail-closed a replicar)
    - backend_app/adapters/etherfuse.py (para confirmar o que NAO copiar: cliente sincrono urllib)
    - backend_app/core/config.py (bloco `copernicus_*`/`satellite_*` criado no Plan 01)
    - backend_app/modules/satellite/constants.py (COPERNICUS_USAGE_ENDPOINTS, COPERNICUS_USAGE_OUTCOMES)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Architecture Patterns" Pattern 1 e Pattern 2 (codigo a escrever verbatim)
  </read_first>
  <action>
**1. `backend_app/adapters/satellite.py`** — contrato desacoplado (D-07), analogo em desenho ao que a Phase 05.1 fara para `ExternalRegistryProvider`:

```python
from __future__ import annotations

# Phase 05 / D-07 -- Contrato SatelliteProvider.
#
# Interface desacoplada do provedor concreto: o codigo de dominio
# (backend_app/modules/satellite/*) depende apenas deste Protocol, nunca de
# httpx nem de detalhes do Copernicus. Modulo PURO: sem banco, sem ORM, sem
# cliente HTTP -- apenas dataclasses e o Protocol.

from dataclasses import dataclass, field
from datetime import datetime
from typing import Any, Protocol, Sequence


@dataclass(frozen=True)
class SceneDTO:
    """Cena Sentinel-2 descoberta via STAC."""
    scene_id: str
    observed_at: datetime
    cloud_coverage: float | None
    processing_version: str
    platform: str = "SENTINEL_2"
    product: str = "L2A"
    metadata: dict[str, Any] = field(default_factory=dict)


@dataclass(frozen=True)
class IndexStatisticsDTO:
    """Estatisticas agregadas de um intervalo (Statistical API)."""
    interval_from: datetime
    interval_to: datetime
    ndvi_mean: float | None = None
    ndvi_min: float | None = None
    ndvi_max: float | None = None
    ndmi_mean: float | None = None
    nbr_mean: float | None = None
    valid_pixel_percentage: float | None = None
    metadata: dict[str, Any] = field(default_factory=dict)


@dataclass(frozen=True)
class SatelliteImageDTO:
    """Imagem PNG bruta (Process API), usada apenas em before/after (D-19)."""
    content: bytes
    mime_type: str
    captured_from: datetime
    captured_to: datetime
    width: int
    height: int


@dataclass(frozen=True)
class CopernicusUsageRecord:
    """D-26: uma linha de consumo por chamada externa. NUNCA contem segredo."""
    endpoint: str            # COPERNICUS_USAGE_ENDPOINTS
    outcome: str             # COPERNICUS_USAGE_OUTCOMES
    http_status: int | None
    duration_ms: int
    processing_units: float | None = None
    error_code: str | None = None
    metadata: dict[str, Any] = field(default_factory=dict)


class SatelliteProvider(Protocol):
    async def search_scenes(
        self, *, aoi: dict[str, Any], date_from: datetime, date_to: datetime,
        max_cloud_coverage: float, limit: int = 500,
    ) -> list[SceneDTO]: ...

    async def get_statistics(
        self, *, aoi: dict[str, Any], date_from: datetime, date_to: datetime,
        aggregation_interval: str = "P1M", max_cloud_coverage: float = 20.0,
    ) -> list[IndexStatisticsDTO]: ...

    async def get_image(
        self, *, aoi: dict[str, Any], date_from: datetime, date_to: datetime,
        max_cloud_coverage: float = 20.0, width: int = 512, height: int = 512,
    ) -> SatelliteImageDTO: ...

    async def aclose(self) -> None: ...
```

**2. `backend_app/adapters/copernicus.py` — parte 1 (config + token + concorrencia + usage).**

Config, escrita verbatim conforme RESEARCH Pattern 1 (sem modo `local`: Copernicus so tem "configurado ou nao"):
```python
@dataclass(frozen=True)
class CopernicusAdapterConfig:
    client_id: str | None = None
    client_secret: str | None = None
    base_url: str = "https://sh.dataspace.copernicus.eu"
    stac_url: str = "https://stac.dataspace.copernicus.eu/v1"
    token_url: str = "https://identity.dataspace.copernicus.eu/auth/realms/CDSE/protocol/openid-connect/token"
    timeout_seconds: float = 30.0
    max_concurrent_requests: int = 2

    @classmethod
    def from_env(cls, settings: Settings | None = None) -> "CopernicusAdapterConfig":
        config = settings or get_settings()
        return cls(
            client_id=os.getenv("COPERNICUS_CLIENT_ID"),
            client_secret=os.getenv("COPERNICUS_CLIENT_SECRET"),
            timeout_seconds=config.copernicus_request_timeout_seconds,
            max_concurrent_requests=config.copernicus_max_concurrent_requests,
        )

    def assert_ready(self) -> None:
        missing: list[str] = []
        if not self.client_id:
            missing.append("COPERNICUS_CLIENT_ID")
        if not self.client_secret:
            missing.append("COPERNICUS_CLIENT_SECRET")
        if missing:
            raise RuntimeError(f"Configuração Copernicus incompleta: {', '.join(missing)}")
```
As tres URLs sao **constantes de config, nunca parametro de rota/request** (RESEARCH Security V10/SSRF). Escrever esse motivo em comentario acima do dataclass.

Provider — construtor conforme RESEARCH Pattern 2:
```python
UsageRecorder = Callable[[CopernicusUsageRecord], Awaitable[None]]


class CopernicusProvider:
    """SatelliteProvider real (D-07/D-08/D-09/D-11).

    Fail-closed: sem credenciais, todo metodo publico levanta RuntimeError
    ANTES de qualquer request. NUNCA devolve indice simulado -- e o mesmo
    principio de backend_app/adapters/stellar.py.
    """

    def __init__(
        self,
        config: CopernicusAdapterConfig | None = None,
        *,
        transport: httpx.AsyncBaseTransport | None = None,
        usage_recorder: UsageRecorder | None = None,
    ) -> None:
        self.config = config or CopernicusAdapterConfig.from_env()
        self._client = httpx.AsyncClient(timeout=self.config.timeout_seconds, transport=transport)
        self._token: str | None = None
        self._token_expires_at: datetime | None = None
        self._token_lock = asyncio.Lock()
        # D-11: quota gratuita CDSE permite apenas 2 requests concorrentes.
        # O semaforo fica AQUI, no adapter, nunca espalhado pelos call sites.
        self._concurrency = asyncio.Semaphore(self.config.max_concurrent_requests)
        self._usage_recorder = usage_recorder
```
O parametro `transport` existe exclusivamente para permitir `httpx.MockTransport` nos testes (Task 3) sem introduzir `respx` como dependencia nova.

`_get_token` verbatim conforme RESEARCH Pattern 2, com margem de 30s:
```python
    async def _get_token(self) -> str:
        self.config.assert_ready()
        async with self._token_lock:
            now = datetime.now(timezone.utc)
            if self._token and self._token_expires_at and now < self._token_expires_at:
                return self._token
            started = time.perf_counter()
            try:
                resp = await self._client.post(
                    self.config.token_url,
                    data={
                        "grant_type": "client_credentials",
                        "client_id": self.config.client_id,
                        "client_secret": self.config.client_secret,
                    },
                )
                resp.raise_for_status()
            except httpx.HTTPError as exc:
                # NUNCA logar/propagar o corpo da resposta nem o payload da
                # request de token: ambos podem conter o client secret.
                await self._record_usage("TOKEN", "ERROR", _status_of(exc), started, error_code=type(exc).__name__)
                raise RuntimeError("Falha ao autenticar no Copernicus Data Space Ecosystem") from None
            body = resp.json()
            self._token = body["access_token"]
            self._token_expires_at = now + timedelta(seconds=int(body["expires_in"]) - 30)
            await self._record_usage("TOKEN", "SUCCESS", resp.status_code, started)
            return self._token
```
Notar o `raise ... from None`: encadear a excecao original exporia a request (com o secret) no traceback.

Helpers privados obrigatorios:
- `async def _record_usage(self, endpoint: str, outcome: str, http_status: int | None, started: float, *, processing_units: float | None = None, error_code: str | None = None, metadata: dict | None = None) -> None` — monta `CopernicusUsageRecord` com `duration_ms=int((time.perf_counter() - started) * 1000)` e chama `self._usage_recorder(record)` se existir. Nunca levanta: falha de observabilidade nao pode derrubar o job (`try/except Exception: pass` com comentario explicito).
- `async def _authorized_post(self, url: str, *, endpoint: str, json_body: dict, expect_json: bool = True) -> tuple[int, Any]` — obtem token, entra em `async with self._concurrency:`, cronometra, faz `POST` com `Authorization: Bearer <token>`, chama `_record_usage` em sucesso e erro, e extrai `processing_units` do header `x-processingunits-spent` quando presente (converter para `float`, ignorar se ausente/invalido). Em `HTTPStatusError` 401 uma unica vez: invalidar o token (`self._token = None`) e repetir a chamada — o token pode ter expirado entre a checagem e o envio.
- `async def aclose(self) -> None` — `await self._client.aclose()`.
- Modulo deve expor `def build_copernicus_provider(usage_recorder=None) -> CopernicusProvider` como factory (usada pelo Plan 05).

**Proibido neste arquivo:** `import requests`, `import urllib`, qualquer `from backend_app.db` ou `AsyncSession`, e qualquer fallback que devolva valor simulado quando o provider nao esta configurado.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
import asyncio, inspect
from backend_app.adapters.satellite import SatelliteProvider, SceneDTO, IndexStatisticsDTO, SatelliteImageDTO, CopernicusUsageRecord
from backend_app.adapters.copernicus import CopernicusAdapterConfig, CopernicusProvider, build_copernicus_provider
cfg = CopernicusAdapterConfig(client_id=None, client_secret=None)
try:
    cfg.assert_ready()
    raise SystemExit('assert_ready deveria falhar sem credenciais')
except RuntimeError as exc:
    assert 'COPERNICUS_CLIENT_ID' in str(exc) and 'COPERNICUS_CLIENT_SECRET' in str(exc), exc
p = CopernicusProvider(cfg)
assert p._concurrency._value == 2, p._concurrency._value
for name in ('search_scenes','get_statistics','get_image','aclose'):
    assert inspect.iscoroutinefunction(getattr(CopernicusProvider, name)), name
asyncio.run(p.aclose())
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vc '^ *#' backend_app/adapters/copernicus.py` maior que 150
    - `grep -c 'import requests\|import urllib' backend_app/adapters/copernicus.py` retorna 0
    - `grep -c 'AsyncSession\|backend_app.db' backend_app/adapters/copernicus.py` retorna 0
    - `grep -c 'asyncio.Semaphore' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'asyncio.Lock' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'assert_ready' backend_app/adapters/copernicus.py` maior ou igual a 4 (definicao + os 3 metodos publicos, via `_get_token`)
    - `grep -c 'AsyncSession\|httpx' backend_app/adapters/satellite.py` retorna 0
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Contrato desacoplado publicado e infraestrutura de autenticacao/concorrencia/observabilidade do adapter pronta e fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: search_scenes (STAC), get_statistics (Statistical API) e get_image (Process API)</name>
  <files>backend_app/adapters/copernicus.py</files>
  <read_first>
    - backend_app/adapters/copernicus.py (apos a Task 1)
    - backend_app/adapters/satellite.py (Task 1 — os DTOs que cada metodo deve devolver)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-09, D-11, D-13, D-19
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Don't Hand-Roll" (NDVI/NDMI/NBR sao calculados pelo Sentinel Hub, nunca no backend)
  </read_first>
  <action>
Implementar os tres metodos publicos em `CopernicusProvider`. Todos abrem com `self.config.assert_ready()` (mesmo que `_get_token` tambem chame — o gate precisa ser visivel no proprio metodo, como em `stellar.py:56`) e usam `_authorized_post`.

**Evalscript unico para os tres indices** (constante de modulo `SENTINEL2_INDICES_EVALSCRIPT`). O calculo acontece no Copernicus; o backend so faz parsing de JSON (nunca numpy/rasterio):
```javascript
//VERSION=3
function setup() {
  return {
    input: [{ bands: ["B04", "B08", "B11", "B12", "SCL", "dataMask"] }],
    output: [
      { id: "ndvi", bands: 1, sampleType: "FLOAT32" },
      { id: "ndmi", bands: 1, sampleType: "FLOAT32" },
      { id: "nbr", bands: 1, sampleType: "FLOAT32" },
      { id: "dataMask", bands: 1 }
    ]
  };
}
function evaluatePixel(s) {
  // SCL 3=sombra de nuvem, 8/9=nuvem media/alta, 10=cirrus
  let valid = (s.dataMask === 1 && s.SCL !== 3 && s.SCL !== 8 && s.SCL !== 9 && s.SCL !== 10) ? 1 : 0;
  return {
    ndvi: [index(s.B08, s.B04)],
    ndmi: [index(s.B08, s.B11)],
    nbr:  [index(s.B08, s.B12)],
    dataMask: [valid]
  };
}
```
E `SENTINEL2_TRUE_COLOR_EVALSCRIPT` para before/after (Process API):
```javascript
//VERSION=3
function setup() {
  return { input: ["B02", "B03", "B04"], output: { bands: 3 } };
}
function evaluatePixel(s) {
  return [2.5 * s.B04, 2.5 * s.B03, 2.5 * s.B02];
}
```

**`search_scenes`** — `POST {stac_url}/search`, endpoint de usage `"STAC_SEARCH"`:
```python
body = {
    "collections": ["sentinel-2-l2a"],
    "datetime": f"{_iso_z(date_from)}/{_iso_z(date_to)}",
    "intersects": aoi,
    "limit": min(limit, 500),
    "filter-lang": "cql2-json",
    "filter": {"op": "<=", "args": [{"property": "eo:cloud_cover"}, float(max_cloud_coverage)]},
}
```
`aoi` e a geometria GeoJSON crua devolvida por `ST_AsGeoJSON(active_boundary)` (coordenadas `[lon, lat]`, ja na convencao correta — nao inverter). Parsing da resposta: para cada `feature` de `body["features"]`, construir `SceneDTO(scene_id=feature["id"], observed_at=<parse de properties["datetime"]>, cloud_coverage=<properties.get("eo:cloud_cover")>, processing_version=<properties.get("processing:version") or properties.get("s2:processing_baseline") or "v1">, metadata={"collection": ..., "platform": properties.get("platform")})`. Descartar features sem `id` ou sem `datetime`. Ordenar por `observed_at` ascendente. Se a resposta trouxer link de paginacao (`links` com `rel == "next"`), seguir no maximo 5 paginas e parar — a janela de 5 anos com filtro de nuvem cabe folgadamente nisso; registrar o limite em comentario.

**`get_statistics`** — `POST {base_url}/api/v1/statistics`, endpoint de usage `"STATISTICS"`:
```python
body = {
    "input": {
        "bounds": {"geometry": aoi, "properties": {"crs": "http://www.opengis.net/def/crs/OGC/1.3/CRS84"}},
        "data": [{
            "type": "sentinel-2-l2a",
            "dataFilter": {"maxCloudCoverage": float(max_cloud_coverage), "mosaickingOrder": "leastCC"},
        }],
    },
    "aggregation": {
        "timeRange": {"from": _iso_z(date_from), "to": _iso_z(date_to)},
        "aggregationInterval": {"of": aggregation_interval},
        "evalscript": SENTINEL2_INDICES_EVALSCRIPT,
        "resx": 10,
        "resy": 10,
    },
    "calculations": {"default": {"statistics": {"default": {"stats": ["mean", "min", "max"]}}}},
}
```
Parsing: a resposta traz `data: [{interval: {from, to}, outputs: {ndvi: {bands: {B0: {stats: {mean, min, max, sampleCount, noDataCount}}}}, ndmi: {...}, nbr: {...}}}]`. Para cada item de `data`:
- Pular intervalos com chave `error` (a Statistical API devolve `{"interval": ..., "error": {...}}` quando nao ha dado no periodo) — nunca inventar valor.
- Extrair `mean/min/max` de `ndvi`, `mean` de `ndmi` e `nbr`, tolerando ausencia de banda (usar `None`).
- `valid_pixel_percentage` calculado a partir de `sampleCount`/`noDataCount` do NDVI quando presentes: `round(100.0 * sampleCount / (sampleCount + noDataCount), 2)`; `None` se nao houver os dois campos.
- Nunca substituir `None` por 0.0 — indice ausente e ausencia de dado, nao zero.
Devolver ordenado por `interval_from`.

Escrever um helper puro e testavel `def parse_statistics_response(payload: dict) -> list[IndexStatisticsDTO]` no nivel de modulo (fora da classe) para que os testes cubram o parsing sem HTTP.
Idem `def parse_stac_response(payload: dict) -> list[SceneDTO]`.

**`get_image`** — `POST {base_url}/api/v1/process`, endpoint de usage `"PROCESS"`, resposta binaria (`expect_json=False`):
```python
body = {
    "input": {
        "bounds": {"geometry": aoi, "properties": {"crs": "http://www.opengis.net/def/crs/OGC/1.3/CRS84"}},
        "data": [{
            "type": "sentinel-2-l2a",
            "dataFilter": {
                "timeRange": {"from": _iso_z(date_from), "to": _iso_z(date_to)},
                "maxCloudCoverage": float(max_cloud_coverage),
                "mosaickingOrder": "leastCC",
            },
        }],
    },
    "output": {"width": width, "height": height,
               "responses": [{"identifier": "default", "format": {"type": "image/png"}}]},
    "evalscript": SENTINEL2_TRUE_COLOR_EVALSCRIPT,
}
```
Devolver `SatelliteImageDTO(content=<bytes>, mime_type="image/png", captured_from=date_from, captured_to=date_to, width=width, height=height)`. Comentario obrigatorio no metodo: `# D-19: chamado APENAS quando uma anomalia chega a ANALYZED. Nunca por observacao de rotina -- Process API custa muito mais PU que a Statistical.`

Helper `_iso_z(value: datetime) -> str` que normaliza para UTC e emite `...Z` (o Sentinel Hub rejeita offsets com `+00:00`).
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
from datetime import datetime, timezone
from backend_app.adapters.copernicus import parse_stac_response, parse_statistics_response, SENTINEL2_INDICES_EVALSCRIPT, SENTINEL2_TRUE_COLOR_EVALSCRIPT
scenes = parse_stac_response({'features':[
  {'id':'S2B_T22KEV_20240315','properties':{'datetime':'2024-03-15T13:20:00Z','eo:cloud_cover':4.2,'processing:version':'05.11'}},
  {'id':'S2A_T22KEV_20240220','properties':{'datetime':'2024-02-20T13:20:00Z','eo:cloud_cover':18.0}},
  {'id':'sem-data','properties':{}},
]})
assert [s.scene_id for s in scenes] == ['S2A_T22KEV_20240220','S2B_T22KEV_20240315'], scenes
assert scenes[1].processing_version == '05.11'
assert scenes[0].processing_version == 'v1'
stats = parse_statistics_response({'data':[
  {'interval':{'from':'2024-01-01T00:00:00Z','to':'2024-02-01T00:00:00Z'},'outputs':{
     'ndvi':{'bands':{'B0':{'stats':{'mean':0.81,'min':0.4,'max':0.93,'sampleCount':1000,'noDataCount':50}}}},
     'ndmi':{'bands':{'B0':{'stats':{'mean':0.31}}}},
     'nbr':{'bands':{'B0':{'stats':{'mean':0.55}}}}}},
  {'interval':{'from':'2024-02-01T00:00:00Z','to':'2024-03-01T00:00:00Z'},'error':{'type':'EXECUTION_ERROR'}},
]})
assert len(stats) == 1, stats
assert stats[0].ndvi_mean == 0.81 and stats[0].ndmi_mean == 0.31 and stats[0].nbr_mean == 0.55
assert stats[0].valid_pixel_percentage == 95.24, stats[0].valid_pixel_percentage
assert 'index(s.B08, s.B04)' in SENTINEL2_INDICES_EVALSCRIPT
assert 'B02' in SENTINEL2_TRUE_COLOR_EVALSCRIPT
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'api/v1/statistics' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'api/v1/process' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'sentinel-2-l2a' backend_app/adapters/copernicus.py` maior ou igual a 2
    - `grep -c 'cql2-json' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'def parse_stac_response' backend_app/adapters/copernicus.py` retorna 1
    - `grep -c 'def parse_statistics_response' backend_app/adapters/copernicus.py` retorna 1
    - `grep -ic 'numpy\|rasterio\|gdal\|sentinelhub' backend_app/adapters/copernicus.py` retorna 0
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Os tres metodos do contrato implementados sobre os endpoints reais, com parsers puros testaveis sem rede.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes do adapter com httpx.MockTransport e documento de pre-requisito de credenciais (D-10)</name>
  <files>tests/adapters/test_copernicus.py, .planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md</files>
  <read_first>
    - tests/adapters/test_blockchain_financial_adapters.py (arquivo inteiro — estilo de assercao fail-closed a copiar)
    - backend_app/adapters/copernicus.py (Tasks 1 e 2)
    - .planning/docs/providers/PHASE1-PROVIDER-SMOKE.md (formato do documento de provider a replicar)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-VALIDATION.md (comando esperado: `uv run pytest -q tests/adapters/test_copernicus.py -x`)
  </read_first>
  <behavior>
    - `search_scenes`/`get_statistics`/`get_image` sem credenciais levantam `RuntimeError` com a mensagem `Configuração Copernicus incompleta` ANTES de qualquer request (o MockTransport nao registra nenhuma chamada)
    - Com credenciais e MockTransport, `search_scenes` devolve `SceneDTO`s ordenados e o transport recebeu exatamente 2 requests (token + STAC)
    - Uma segunda chamada consecutiva NAO refaz o token (total de 3 requests, nao 4) — token cacheado (D-08)
    - Um token com `expires_in: 20` e considerado expirado imediatamente (margem de 30s) e provoca nova busca de token
    - `get_statistics` devolve os DTOs parseados e ignora intervalos com `error`
    - `get_image` devolve `SatelliteImageDTO` com `mime_type == "image/png"` e os bytes exatos do mock
    - Um erro 500 na Statistical API vira `RuntimeError`/`httpx.HTTPStatusError` propagado — nunca uma lista vazia silenciosa nem valores simulados
    - Toda chamada emite um `CopernicusUsageRecord` no `usage_recorder`, e nenhum record contem o valor do client secret em nenhum campo
    - Com `max_concurrent_requests=2`, 5 chamadas simultaneas nunca tem mais de 2 requests em voo ao mesmo tempo
  </behavior>
  <action>
**1. `tests/adapters/test_copernicus.py`** — sem `respx` (dependencia nova evitada, mesma postura da rejeicao do `sentinelhub-py`): usar `httpx.MockTransport`, que ja vem com o `httpx`.

Estrutura base:
```python
from __future__ import annotations

# Phase 05 / SATM-05 -- testes do CopernicusProvider sem credenciais reais e
# sem rede: httpx.MockTransport (parte do proprio httpx, zero dependencia nova).

import asyncio
import json
import httpx
import pytest

from backend_app.adapters.copernicus import CopernicusAdapterConfig, CopernicusProvider

CLIENT_SECRET = "segredo-que-nunca-pode-vazar"

CONFIGURED = CopernicusAdapterConfig(client_id="cli-123", client_secret=CLIENT_SECRET)
UNCONFIGURED = CopernicusAdapterConfig(client_id=None, client_secret=None)

AOI = {"type": "Polygon", "coordinates": [[[-47.9, -15.8], [-47.8, -15.8], [-47.8, -15.7], [-47.9, -15.7], [-47.9, -15.8]]]}


def make_transport(handlers: dict[str, httpx.Response], calls: list[httpx.Request]) -> httpx.MockTransport:
    def handler(request: httpx.Request) -> httpx.Response:
        calls.append(request)
        for fragment, response in handlers.items():
            if fragment in str(request.url):
                return response
        return httpx.Response(404, json={"detail": f"sem mock para {request.url}"})
    return httpx.MockTransport(handler)
```
Usar `asyncio.run(...)` diretamente nos testes (o repo nao tem `pytest-asyncio` — confirmar com `grep -c pytest-asyncio pyproject.toml` antes; se ausente, todos os testes async devem ser embrulhados em `asyncio.run`).

Testes obrigatorios, um por item de `<behavior>`, incluindo por nome:
- `test_copernicus_search_scenes_fails_closed_without_credentials`
- `test_copernicus_get_statistics_fails_closed_without_credentials`
- `test_copernicus_get_image_fails_closed_without_credentials`
- `test_token_is_cached_between_calls`
- `test_token_is_refreshed_when_close_to_expiry`
- `test_search_scenes_parses_and_sorts_scenes`
- `test_get_statistics_skips_intervals_with_error`
- `test_get_image_returns_png_bytes`
- `test_statistics_http_error_never_returns_simulated_data`
- `test_usage_records_never_contain_client_secret` — coleta todos os `CopernicusUsageRecord` emitidos e assere `CLIENT_SECRET not in json.dumps(record.__dict__, default=str)` para cada um
- `test_concurrency_never_exceeds_two_in_flight` — handler que incrementa/decrementa um contador compartilhado e registra o maximo observado; dispara 5 `get_statistics` com `asyncio.gather` e assere `max_in_flight <= 2`

**2. `.planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md`** — mesmo formato de `PHASE1-PROVIDER-SMOKE.md`, em PT-BR, contendo:
- **Status atual:** `BLOCKED_MISSING_PROVIDER_CREDENTIALS` — nenhuma credencial provisionada neste ambiente (D-10).
- **Pre-requisito operacional (fora do codigo):** criar conta gratuita em `https://dataspace.copernicus.eu`, gerar OAuth client em User Settings, copiar `client_id`/`client_secret`.
- **Variaveis de ambiente:** `COPERNICUS_CLIENT_ID`, `COPERNICUS_CLIENT_SECRET` (mesmas do `.env.example`).
- **Endpoints usados** (os 4 confirmados em D-08/D-09) e **quotas** (D-11: 10.000 PU/mes, 300 PU/min, 300 req/min, 2 concorrentes, 12 TB/mes).
- **Custo em PU:** Statistical >= 0,01 PU/request; STAC/Catalog >= 0,01 PU (ate 1 PU); Process e a chamada cara — por isso so roda em anomalia `ANALYZED` (D-19).
- **Comportamento sem credenciais:** todo metodo levanta `RuntimeError: Configuração Copernicus incompleta: ...`; o job de monitoramento marca `satellite_jobs.status = FAILED` com essa mensagem e a UI mostra o empty state "Monitoramento satelital bloqueado" (UI-SPEC). Nenhum dado simulado e exibido.
- **Smoke real quando as credenciais existirem** (checklist manual, herdado de VALIDATION.md "Manual-Only Verifications"): rodar reconstrucao historica de 1 projeto e conferir `copernicus_api_usage` populado + quota respeitada.
- **Observabilidade:** tabela `copernicus_api_usage` (D-26), sem Prometheus nesta fase.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/adapters/test_copernicus.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/adapters/test_copernicus.py` coleta 11 ou mais testes e todos passam
    - `grep -c 'respx' tests/adapters/test_copernicus.py pyproject.toml` retorna 0 em ambos
    - `grep -c 'MockTransport' tests/adapters/test_copernicus.py` maior ou igual a 1
    - `grep -c 'COPERNICUS_CLIENT_ID' .planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md` maior ou igual a 1
    - `grep -c 'BLOCKED_MISSING_PROVIDER_CREDENTIALS' .planning/docs/providers/PHASE5-COPERNICUS-PROVIDER.md` maior ou igual a 1
    - `uv run pytest -q tests/adapters/` passa sem regressao nos testes de adapter existentes
  </acceptance_criteria>
  <done>Adapter coberto por teste sem rede nem credencial, e o pre-requisito operacional documentado no mesmo padrao dos demais providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend -> internet (Copernicus) | Primeira chamada HTTP de saida assincrona do repo; hosts precisam ser fixos |
| variaveis de ambiente -> log/traceback | `COPERNICUS_CLIENT_SECRET` cruza para o corpo da request de token |
| adapter -> quota compartilhada CDSE | Excesso de concorrencia degrada/bloqueia o servico para todo o projeto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | `COPERNICUS_CLIENT_SECRET` em log/traceback | mitigate | Erro do token endpoint e convertido em `RuntimeError("Falha ao autenticar...") from None` (sem encadear a excecao original, que carrega a request); corpo da resposta de erro nunca e logado; `CopernicusUsageRecord` tem campos fixos, sem payload livre; teste `test_usage_records_never_contain_client_secret` |
| T-05-16 | Server-Side Request Forgery | URLs de saida | mitigate | `token_url`/`stac_url`/`base_url` sao defaults do `dataclass(frozen=True)`; nenhum metodo publico aceita host/URL como parametro; ASVS V10 (RESEARCH Security Domain) |
| T-05-17 | Denial of Service | quota CDSE estourada (429) | mitigate | `asyncio.Semaphore(copernicus_max_concurrent_requests=2)` dentro do adapter (D-11), nunca nos call sites; teste `test_concurrency_never_exceeds_two_in_flight` |
| T-05-18 | Denial of Service (auto-infligido) | token renovado a cada request / 401 em cascata | mitigate | Token cacheado com margem de 30s e `asyncio.Lock` unico; retry de 401 acontece no maximo uma vez por chamada; testes de cache e de refresh |
| T-05-19 | Spoofing | dado satelital simulado apresentado como real | mitigate | `assert_ready()` em todo metodo publico; nenhum caminho devolve valor default quando o provider nao esta configurado; `parse_statistics_response` nunca converte `None` em `0.0`; teste `test_statistics_http_error_never_returns_simulated_data` |
| T-05-20 | Tampering | resposta malformada do provider gerando indice invalido | mitigate | Parsers puros descartam features sem `id`/`datetime` e intervalos com `error`, em vez de preencher com zero; cobertos por teste unitario |
</threat_model>

<verification>
1. `uv run pytest -q tests/adapters/test_copernicus.py` — SATM-05 (adapter) verde
2. `uv run pytest -q tests/adapters/` — nenhum adapter existente regrediu
3. `grep -ic 'sentinelhub\|numpy\|rasterio' backend_app/adapters/copernicus.py` retorna 0
4. `uv run pytest -q` — suite completa verde
</verification>

<success_criteria>
- `SatelliteProvider` existe como contrato desacoplado e `CopernicusProvider` o implementa
- Fail-closed comprovado nos tres metodos publicos, sem nenhuma request emitida sem credencial
- Token cacheado com margem de 30s, semaforo de 2 requests concorrentes
- Consumo registrado por chamada sem jamais conter o client secret
- Pre-requisito operacional de credenciais documentado em `.planning/docs/providers/`
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-03-SUMMARY.md` registrando: assinatura final dos tres metodos e dos DTOs (o Plan 05 consome), shape exato dos bodies enviados a STAC/Statistical/Process, o contrato do `usage_recorder`, e qualquer ajuste feito no parsing em relacao ao esperado.
</output>
