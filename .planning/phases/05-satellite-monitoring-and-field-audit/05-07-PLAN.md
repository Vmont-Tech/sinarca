---
phase: 05-satellite-monitoring-and-field-audit
plan: 07
type: execute
wave: 5
depends_on: ["05-02", "05-04", "05-06"]
files_modified:
  - backend_app/modules/satellite/schemas.py
  - backend_app/modules/satellite/routes.py
  - backend_app/modules/satellite/service.py
  - backend_app/modules/credits_availability.py
  - backend_app/modules/audit/routes.py
  - backend_app/api/router.py
  - backend_app/modules/projects/service.py
  - tests/test_satellite_incident_recalc.py
  - tests/contract/test_backend_app_api_v1.py
autonomous: true
requirements: [SATM-06, SATM-07, SATM-08, SATM-09]

must_haves:
  truths:
    - "O painel interno le observacoes, serie temporal e eventos por /api/v1, sempre org-scoped"
    - "Confirmar um evento HIGH/CRITICAL recalcula o risco e, se o score virar CRITICAL, o Auto Hold ja existente coloca o projeto em ON_HOLD sem nenhuma escrita direta de integrity_status"
    - "Confirmar um evento com area afetada cria uma credit_adjustment_pendencies OPEN e torna os creditos indisponiveis para venda"
    - "Nenhuma decisao de evento altera project.carbon_stock"
    - "Nao existe transicao DETECTED -> CONFIRMED: so ANALYZED pode ser confirmado ou descartado"
    - "O desbloqueio exige decisao humana explicita registrada (ANOMALY_REVIEW_CLEARED) e nunca acontece por timeout"
    - "O dossie publico mostra baseline Sentinel real, NDVI medio, pontos analisados e hash de referencia — sem nenhum campo derivado de deterministic_baseline quando ha observacao real"
    - "Todas as rotas novas negam acesso a organizacao sem vinculo (403) e sem token (401)"
  artifacts:
    - path: "backend_app/modules/satellite/routes.py"
      provides: "Rotas /projects/{id}/satellite/* e /projects/{id}/environmental-events"
      contains: "environmental-events"
      min_lines: 220
    - path: "backend_app/modules/credits_availability.py"
      provides: "block_project_credits/unlock_project_credits compartilhados (D-23)"
      contains: "def block_project_credits"
      min_lines: 40
    - path: "tests/test_satellite_incident_recalc.py"
      provides: "Cobertura SATM-08/SATM-09"
      min_lines: 180
  key_links:
    - from: "backend_app/modules/satellite/routes.py"
      to: "IntegrityService.recalculate_risk_score"
      via: "decisao CONFIRMED dispara recalculo (D-20)"
      pattern: "recalculate_risk_score"
    - from: "backend_app/modules/satellite/service.py"
      to: "credit_adjustment_pendencies"
      via: "pendencia estruturada por evento confirmado (D-23)"
      pattern: "CreditAdjustmentPendency"
    - from: "backend_app/modules/projects/service.py"
      to: "satellite_observations"
      via: "bloco satellite minimizado no dossie publico (D-25/SATM-07)"
      pattern: "public_satellite_item"
---

<objective>
Expor o dominio satelital por `/api/v1` e fechar o ciclo humano: leitura de observacoes/serie temporal/eventos, decisao `CONFIRMED`/`DISMISSED` (D-18), Auto Hold por sinal de risco (D-20), pendencia estruturada de recalculo de credito com indisponibilizacao (D-23) e desbloqueio auditavel (D-22) — alem do bloco `satellite` minimizado no dossie publico (D-25/SATM-07).

Purpose: e o unico plano onde a decisao humana entra. Fecha SATM-06 (ciclo completo), SATM-07 (dossie publico com baseline real), SATM-08 (bloqueio/notificacao/desbloqueio) e SATM-09 (recalculo de credito).
Output: `satellite/schemas.py`, `satellite/routes.py`, `credits_availability.py`, extensao de `SatelliteService`, registro no `api_router`, `public_satellite_item` no dossie publico, testes de contrato.

**Nao faz parte deste plano:** frontend (Plans 08/09) e qualquer alteracao no pipeline automatico (Plan 06). Nenhuma linha deste plano escreve `project.integrity_status` nem `project.carbon_stock`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-04-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-05-SUMMARY.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-06-SUMMARY.md

<interfaces>
Guard org-scoped a copiar verbatim (`backend_app/modules/integrity/routes.py:22-40`):
```python
@router.get("/projects/{project_id}/claims", response_model=ProjectClaimsResponse)
async def get_project_claims(
    project_id: str,
    current_user: AuthenticatedUser = Depends(require_role("producer", "certifier", "admin")),
    session: AsyncSession = Depends(get_session),
) -> ProjectClaimsResponse:
    service = ProjectsService(session)
    project = await service._get_project_model(project_id)
    await service._assert_project_edit_permission(project, actor_id=current_user.id, actor_role=current_user.role)
```

Auto Hold (Phase 04.2, unico escritor — `backend_app/modules/integrity/service.py:707-852`):
`await IntegrityService(session).recalculate_risk_score(project, trigger=..., actor_id=..., actor_role=...)` le `project_events` com `status == "CONFIRMED"` (estendido no Plan 04) e escreve `project.integrity_status` UMA vez. Nunca faz `commit()`.

Helpers de credito a extrair (`backend_app/modules/audit/routes.py:116-127`):
```python
async def _unlock_credits(session, project) -> None:
    for credit in ...: credit.status = "AVAILABLE"; credit.quantity_available = credit.quantity_total - credit.quantity_retired
async def _block_credits(session, project) -> None:
    for credit in ...: credit.status = "SUSPENDED"; credit.quantity_available = Decimal("0")
```

Padrao de pendencia (Phase 4, `backend_app/modules/certifier/routes.py::pendency_item` e `CertificationPendency`) e a base do serializador de `CreditAdjustmentPendency`.

Dossie publico (`backend_app/modules/projects/service.py:287-360`): `ProjectPublicDossierResponse(... baseline=baseline_item(baseline), integrity=public_integrity_item(...), audits=[public_audit_item(...)] ...)`. O Plan 02 ja criou `public_audit_item`. Este plano acrescenta `satellite=public_satellite_item(...)`.
Campos que `MrcaDetails.tsx` ja le (`MrcaDetails.tsx:444-470`): `dossier.baseline.sentinelSceneId`, `.baselineHash`, `.ndviMean`, `.pointsAnalyzed`, e `metadata.baseline_source`/`metadata.sentinel_status`. Manter esses nomes — o Plan 08 so troca a cor do tile, nao o shape.

Modelos e servicos disponiveis: `SatelliteObservation`, `SatelliteAnomaly`, `ProjectEvent`, `SatelliteEvidence`, `CreditAdjustmentPendency`, `SatelliteJob`; `SatelliteService` (Plan 05), `SatelliteEvidenceService` (Plan 06), `IntegrityService` (Phase 04.2).
Constantes (Plan 01): `PROJECT_EVENT_TRANSITIONS`, `PROJECT_EVENT_RISK_SEVERITIES`, `CREDIT_ADJUSTMENT_PENDENCY_STATUSES`, `CREDIT_ADJUSTMENT_PENDENCY_CATEGORY`, `BASELINE_SOURCE_COPERNICUS`, `SENTINEL_STATUS_BLOCKED`.

Copy travada (UI-SPEC "Copywriting Contract"): CTA `"Confirmar anomalia"`, `"Descartar anomalia"`, `"Registrar revisão e liberar bloqueio"`; empty state bloqueado: `"Monitoramento satelital bloqueado."` / `"Faltam credenciais do provedor Copernicus neste ambiente. Nenhum dado simulado é exibido..."`; chip `"Pendência de recálculo de crédito"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas e rotas de leitura — observacoes, serie temporal, eventos e consumo Copernicus</name>
  <files>backend_app/modules/satellite/schemas.py, backend_app/modules/satellite/routes.py, backend_app/api/router.py</files>
  <read_first>
    - backend_app/modules/integrity/routes.py (arquivo inteiro — guard org-scoped e formato de resposta a copiar)
    - backend_app/modules/integrity/schemas.py (arquivo inteiro — estilo dos DTOs Pydantic)
    - backend_app/api/router.py (arquivo inteiro, 27 linhas)
    - backend_app/modules/satellite/service.py (Plan 05) e monitoring.py/evidence.py (Plan 06)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md (o que a Surface B precisa exibir)
  </read_first>
  <action>
**1. `backend_app/modules/satellite/schemas.py`** — DTOs Pydantic no estilo de `integrity/schemas.py`:
- `SatelliteSummaryResponse`: `success`, `project_id`, `summary: dict[str, Any]`
- `SatelliteObservationsResponse`: `success`, `project_id`, `total`, `observations: list[dict[str, Any]]`
- `ProjectEventsResponse`: `success`, `project_id`, `total`, `events: list[dict[str, Any]]`
- `ProjectEventDetailResponse`: `success`, `project_id`, `event: dict[str, Any]`
- `ProjectEventDecisionRequest`: `decision: Literal["CONFIRMED", "DISMISSED"]`, `notes: str = ""`
- `ProjectEventClearRequest`: `notes: str = ""`
- `CreditAdjustmentPendenciesResponse`: `success`, `project_id`, `total`, `pendencies: list[dict[str, Any]]`
- `CopernicusUsageResponse`: `success`, `usage: dict[str, Any]`

**2. Serializadores** (funcoes de modulo em `satellite/routes.py`, mesmo padrao de `certification_item`/`document_item`):
```python
def observation_item(row: SatelliteObservation) -> dict[str, Any]:
    return {
        "id": str(row.id), "sceneId": row.scene_id, "satellite": row.satellite,
        "product": row.product, "processingVersion": row.processing_version,
        "observedAt": row.observed_at.isoformat(),
        "cloudCoverage": float(row.cloud_coverage) if row.cloud_coverage is not None else None,
        "ndviMean": ..., "ndviMin": ..., "ndviMax": ..., "ndmiMean": ..., "nbrMean": ...,
        "validPixelPercentage": ...,
    }
```
`event_item(event, *, anomaly=None, evidence=())` acrescenta `type`, `status`, `severity`, `confidence`, `affectedAreaHa`, `ndviBefore`, `ndviAfter`, `summary`, `detectedAt`, `analyzedAt`, `decidedAt`, `decisionNotes`, `clearedAt`, `clearanceNotes`, `correlation` (de `metadata_["correlation"]`) e `evidence: [{"id", "kind", "sha256", "capturedAt", "mimeType"}]`. **Nunca** incluir `storage_object_path`/`storage_bucket` na resposta (Pitfall 4) — a imagem e servida por rota dedicada.
`pendency_item(row: CreditAdjustmentPendency)` no formato de `certifier/routes.py::pendency_item`.

**3. Rotas** em `backend_app/modules/satellite/routes.py` (`router = APIRouter(tags=["satellite"])`), todas com o guard duplo `require_role("producer", "certifier", "auditor", "admin")` + `_assert_project_edit_permission`, com docstring PT-BR citando a sensibilidade (RESEARCH Security V4: dado de anomalia revela estado operacional de projeto de terceiro, mesma classe de `boundary-overlaps`):

- `GET /projects/{project_id}/satellite/summary` -> ultima observacao, `cloudCoverage`, `ndviMean` atual, contagem de observacoes, `baselineSource` (`projects.metadata.baseline_source`), `sentinelStatus`, ultimo `SatelliteJob` (tipo/status/erro/`finishedAt`) e contagem de eventos por status. Quando `sentinel_status == SENTINEL_STATUS_BLOCKED` e nao ha observacao, devolver `"blocked": True` e `"blockedReason": "Faltam credenciais do provedor Copernicus neste ambiente."` — a UI usa isso para o empty state fail-closed travado no UI-SPEC.
- `GET /projects/{project_id}/satellite/observations?date_from=&date_to=&limit=` -> serie completa ordenada por `observedAt` (a UI monta o grafico NDVI/NDMI/NBR a partir desta unica resposta; nao criar endpoint separado de serie temporal).
- `GET /projects/{project_id}/environmental-events?status=` -> lista com `anomaly` e `evidence` embutidos.
- `GET /projects/{project_id}/environmental-events/{event_id}` -> detalhe.
- `GET /projects/{project_id}/environmental-events/{event_id}/evidence/{evidence_id}/image` -> devolve os bytes via `Response(content=..., media_type=evidence.mime_type)`, usando `SatelliteEvidenceService.load_evidence_bytes`. 404 quando a evidencia nao pertence ao evento/projeto (nunca 403 vazando existencia — seguir o padrao ja usado no download de certificado). Comentario: `# UI-SPEC: quando a imagem ainda nao existe, a UI mostra "Imagem antes/depois ainda não disponível — só é gerada quando a anomalia avança para análise."`
- `GET /projects/{project_id}/credit-adjustment-pendencies?status=` -> pendencias do projeto.
- `GET /satellite/usage?since=` -> `SatelliteService.usage_summary` (D-26), com `require_role("admin")` apenas — consumo de quota e dado operacional global, nao por projeto.

**4. `backend_app/api/router.py`** — `from backend_app.modules.satellite.routes import router as satellite_router` e `api_router.include_router(satellite_router)`, mantendo a ordem alfabetica ja usada nos imports.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run python -c "
from backend_app.main import app
paths = {r.path for r in app.routes}
required = [
 '/api/v1/projects/{project_id}/satellite/summary',
 '/api/v1/projects/{project_id}/satellite/observations',
 '/api/v1/projects/{project_id}/environmental-events',
 '/api/v1/projects/{project_id}/environmental-events/{event_id}',
 '/api/v1/projects/{project_id}/environmental-events/{event_id}/evidence/{evidence_id}/image',
 '/api/v1/projects/{project_id}/credit-adjustment-pendencies',
 '/api/v1/satellite/usage',
]
missing = [p for p in required if p not in paths]
assert not missing, missing
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_assert_project_edit_permission' backend_app/modules/satellite/routes.py` maior ou igual a 6
    - `grep -c 'require_role' backend_app/modules/satellite/routes.py` maior ou igual a 7
    - `grep -c 'storage_object_path\|storage_bucket' backend_app/modules/satellite/routes.py` retorna 0
    - `grep -c 'satellite_router' backend_app/api/router.py` retorna 2
    - `grep -c 'def observation_item\|def event_item\|def pendency_item' backend_app/modules/satellite/routes.py` retorna 3
    - O comando python do verify imprime `ok`
  </acceptance_criteria>
  <done>Toda a leitura do dominio satelital disponivel por /api/v1 com guard org-scoped e sem vazar caminho de storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decisao humana CONFIRMED/DISMISSED, Auto Hold por recalculo e desbloqueio auditavel (SATM-08, D-18/D-20/D-22)</name>
  <files>backend_app/modules/satellite/routes.py, backend_app/modules/satellite/service.py, tests/test_satellite_incident_recalc.py</files>
  <read_first>
    - backend_app/modules/integrity/service.py linhas 707-852 (`recalculate_risk_score`, o unico escritor de integrity_status)
    - backend_app/modules/integrity/risk_engine.py (Plan 04 — ProjectEventSnapshot e o sinal SATELLITE_ANOMALY_CONFIRMED_*)
    - backend_app/modules/audit/routes.py linhas 48-102 (padrao de decisao humana auditavel: guard, transicao, timeline, create_audit_event, commit)
    - backend_app/modules/satellite/constants.py (PROJECT_EVENT_TRANSITIONS, PROJECT_EVENT_RISK_SEVERITIES)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-18, D-20, D-21, D-22
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md (copy exata dos CTAs e das confirmacoes destrutivas)
  </read_first>
  <behavior>
    - `PATCH .../decision` com `decision="CONFIRMED"` sobre um evento ANALYZED -> 200, evento vira CONFIRMED com `decided_at`/`decided_by_profile_id`/`decision_notes`
    - `PATCH .../decision` sobre um evento DETECTED -> 400 citando a transicao invalida (nunca 200 — D-18)
    - `PATCH .../decision` sobre um evento ja CONFIRMED ou DISMISSED -> 400 (estados terminais)
    - Confirmar um evento CRITICAL faz `GET /projects/{id}/integrity` passar a listar o sinal `SATELLITE_ANOMALY_CONFIRMED_CRITICAL`
    - Confirmar um evento CRITICAL em projeto que ja tem outros sinais leva `integrityStatus` a `ON_HOLD` — e `project.status` (coluna operacional) permanece inalterado
    - Confirmar um evento de severidade `MEDIUM` nao gera sinal e nao muda `integrityStatus`
    - `DISMISSED` nao gera sinal, nao bloqueia e registra a justificativa
    - `PATCH .../clear` sobre um evento CONFIRMED -> 200, `cleared_at`/`cleared_by_profile_id`/`clearance_notes` preenchidos, `ANOMALY_REVIEW_CLEARED` em `audit_events`, e o recalculo seguinte remove o sinal
    - Apos o clear, `integrityStatus` deixa de ser `ON_HOLD` sem que nenhuma linha de codigo tenha escrito `integrity_status` fora de `recalculate_risk_score`
    - `PATCH .../clear` sobre evento nao CONFIRMED -> 400
    - Decisao por usuario sem papel auditor/certifier/admin -> 403; sem token -> 401; evento de outro projeto -> 403/404
    - Toda decisao acrescenta exatamente uma entrada publica e neutra na `project.timeline`
  </behavior>
  <action>
**1. `SatelliteService` (extensao)** — `backend_app/modules/satellite/service.py`:

`async def decide_event(self, project: Project, event: ProjectEvent, *, decision: str, notes: str, actor_id: str | None, actor_role: str | None, actor_profile_id=None) -> ProjectEvent`
1. Validar a transicao contra o vocabulario, com HTTP 400 e mensagem PT-BR explicita:
```python
        allowed = PROJECT_EVENT_TRANSITIONS.get(event.status, ())
        if decision not in allowed:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=(
                    f"Transição inválida: evento em {event.status} não pode ir para {decision}. "
                    "Somente eventos em ANALYZED podem ser confirmados ou descartados."
                ),
            )
```
   Comentario obrigatorio: `# D-18: nao existe DETECTED -> CONFIRMED. A correlacao automatica (Plan 06) para em ANALYZED; a decisao humana comeca dali.`
2. `notes` obrigatoria e nao vazia para `CONFIRMED` (a UI-SPEC exige justificativa na confirmacao destrutiva) — 400 quando vazia.
3. Aplicar: `event.status = decision`, `event.decided_at = now`, `event.decided_by_profile_id`, `event.decision_notes = notes`. Marcar a anomalia vinculada como `"ANALYZED"` quando `DISMISSED` e manter `"LINKED"` quando `CONFIRMED`.
4. Timeline publica e neutra (mesma disciplina do Plan 06 — `project.timeline` aparece no dossie publico):
```python
        titulo = "Evento ambiental confirmado por revisão" if decision == "CONFIRMED" else "Evento ambiental descartado por revisão"
        desc = (
            "Revisão humana confirmou o evento detectado pelo monitoramento Sentinel-2."
            if decision == "CONFIRMED"
            else "Revisão humana descartou o evento detectado pelo monitoramento Sentinel-2."
        )
```
   **Nunca** gravar `notes` na timeline (e nota interna, mesmo tratamento das notes do certificador na Phase 4).
5. `create_audit_event(action=f"SATELLITE_EVENT_{decision}", entity_type="projects", entity_id=project.id, actor_role=actor_role, before_data={"status": <anterior>}, after_data={"status": decision}, metadata={"actor_external_id": actor_id, "event_id": str(event.id), "severity": event.severity, "type": event.type, "notes": notes})` — a `notes` fica em `audit_events` (trilha interna), nao na timeline.
6. Quando `decision == "CONFIRMED"` e `event.severity in PROJECT_EVENT_RISK_SEVERITIES`: chamar `await self.raise_credit_adjustment_pendency(project, event, actor_id=..., actor_profile_id=...)` (Task 3).
7. **Sempre** (confirmado ou descartado) chamar:
```python
        # D-20: NAO existe segundo mecanismo de bloqueio. O recalculo le
        # project_events CONFIRMED (Plan 04) e, se o score virar CRITICAL, o
        # Auto Hold ja existente escreve integrity_status. Nenhuma linha deste
        # arquivo escreve integrity_status.
        await IntegrityService(self.session).recalculate_risk_score(
            project, trigger="SATELLITE_EVENT_DECISION", actor_id=actor_id, actor_role=actor_role
        )
```
8. Nao fazer `commit()` — a rota comita.

`async def clear_event_review(self, project, event, *, notes, actor_id, actor_role, actor_profile_id=None) -> ProjectEvent` (D-22)
- 400 quando `event.status != "CONFIRMED"` (`"Somente eventos confirmados podem ter a revisão registrada."`) ou quando `event.cleared_at is not None` (`"Revisão já registrada para este evento."`).
- `notes` obrigatoria e nao vazia.
- Setar `cleared_at`, `cleared_by_profile_id`, `clearance_notes`.
- `create_audit_event(action="ANOMALY_REVIEW_CLEARED", ...)` com `before_data={"cleared": False}` / `after_data={"cleared": True}`.
- Entrada de timeline: titulo `"Bloqueio liberado após revisão de anomalia"`, desc neutra.
- Chamar `recalculate_risk_score(project, trigger="ANOMALY_REVIEW_CLEARED", ...)` — o sinal some porque `ProjectEventSnapshot.cleared` passa a ser `True` (Plan 04), e o Auto Hold sai sozinho.
- Chamar `unlock_project_credits(...)` (Task 3) apenas quando, apos o recalculo, `project.integrity_status != "ON_HOLD"` **e** nao restar nenhuma `CreditAdjustmentPendency` OPEN. Comentario: `# D-22/D-23: liberar credito exige as duas condicoes. Uma pendencia OPEN mantem o projeto indisponivel mesmo fora do Auto Hold.`
- Comentario obrigatorio: `# D-22: desbloqueio SO por decisao humana explicita e auditavel. Nao existe caminho por timeout nem por nova observacao satisfatoria.`

**2. Rotas** em `satellite/routes.py`:
- `PATCH /projects/{project_id}/environmental-events/{event_id}/decision`, guard `require_role("auditor", "certifier", "admin")` + `_assert_project_edit_permission`. Produtor **nao** decide sobre o proprio incidente (mesmo principio de `verify_project`, que restringe a auditor/admin).
- `PATCH /projects/{project_id}/environmental-events/{event_id}/clear`, mesmo guard.
Ambas resolvem `event` filtrando por `ProjectEvent.project_id == project.id` (404 se nao pertencer — nunca aceitar `event_id` de outro projeto), chamam o servico, fazem `await session.commit()` e devolvem `ProjectEventDetailResponse` com o evento reserializado + o bloco `integrity` atualizado (`IntegrityService.integrity_summary(project)`), para que a UI atualize o badge de Auto Hold na mesma resposta.

**3. Testes** — criar `tests/test_satellite_incident_recalc.py` (fixtures HTTP copiadas de `tests/test_project_boundaries.py`). Helper que semeia o cenario: cria projeto com boundary, insere observacoes e um `ProjectEvent` em `ANALYZED` com a severidade desejada diretamente via `AsyncSession` (mais rapido e deterministico que rodar o job completo). Um teste por item de `<behavior>`, incluindo por nome:
- `test_confirm_analyzed_event_creates_risk_signal`
- `test_confirm_critical_event_triggers_auto_hold`
- `test_auto_hold_from_satellite_never_changes_operational_status`
- `test_detected_event_cannot_be_confirmed_directly`
- `test_terminal_event_cannot_be_decided_again`
- `test_medium_severity_confirmation_does_not_block`
- `test_clear_review_removes_signal_and_lifts_auto_hold`
- `test_clear_requires_confirmed_event`
- `test_event_decision_is_org_scoped` (403/401 e evento de outro projeto)
- `test_decision_timeline_entry_never_contains_internal_notes`
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_satellite_incident_recalc.py -x && uv run pytest -q tests/test_risk_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'integrity_status =' backend_app/modules/satellite/service.py` retorna 0
    - `grep -c 'integrity_status =' backend_app/modules/satellite/routes.py` retorna 0
    - `grep -c 'project.status =' backend_app/modules/satellite/service.py backend_app/modules/satellite/routes.py` retorna 0 em ambos
    - `grep -c 'recalculate_risk_score' backend_app/modules/satellite/service.py` retorna 2 (decisao + clear)
    - `grep -c 'PROJECT_EVENT_TRANSITIONS' backend_app/modules/satellite/service.py` retorna 1
    - `grep -c 'ANOMALY_REVIEW_CLEARED' backend_app/modules/satellite/service.py` retorna 1
    - `uv run pytest -q tests/test_satellite_incident_recalc.py` coleta 10 ou mais testes e todos passam
  </acceptance_criteria>
  <done>Ciclo DETECTED-ANALYZED-CONFIRMED/DISMISSED completo com Auto Hold reaproveitado e desbloqueio exclusivamente humano.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pendencia de recalculo de credito, indisponibilizacao e bloco satellite no dossie publico (SATM-09/SATM-07, D-23/D-25)</name>
  <files>backend_app/modules/credits_availability.py, backend_app/modules/audit/routes.py, backend_app/modules/satellite/service.py, backend_app/modules/projects/service.py, tests/test_satellite_incident_recalc.py, tests/contract/test_backend_app_api_v1.py</files>
  <read_first>
    - backend_app/modules/audit/routes.py linhas 105-135 (`_unlock_credits`/`_block_credits` a extrair)
    - supabase/migrations/202608150001_certification_workbench.sql linhas 13-34 (certification_pendencies, o modelo de D-23)
    - backend_app/modules/certifier/routes.py (`pendency_item`, o serializador analogo)
    - backend_app/modules/projects/service.py linhas 287-360 (get_public_dossier) e 2153-2170 (`baseline_item`)
    - src/pages/Dashboard/MrcaDetails.tsx linhas 440-475 (os campos exatos que o dossie publico ja alimenta)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-23, D-25
  </read_first>
  <behavior>
    - Confirmar um evento HIGH/CRITICAL com `affected_area_ha` cria uma `credit_adjustment_pendencies` OPEN com a area registrada e `category == "SATELLITE_INCIDENT"`
    - Confirmar o mesmo evento duas vezes nao cria pendencia duplicada (unique parcial por `project_event_id`)
    - Ao criar a pendencia, os creditos do projeto ficam `SUSPENDED` com `quantity_available == 0`
    - `project.carbon_stock` permanece exatamente o mesmo antes e depois da confirmacao
    - `GET /projects/{id}/credit-adjustment-pendencies` lista a pendencia com descricao PT-BR citando area e severidade
    - Registrar a revisao (clear) com a pendencia ainda OPEN NAO libera os creditos
    - Resolver a pendencia e registrar a revisao libera os creditos de volta a AVAILABLE
    - O dossie publico traz `satellite.baselineSource == "COPERNICUS"`, `ndviMean`, `pointsAnalyzed` e `referenceHash` quando ha observacao real
    - Com o provider bloqueado (sem observacao), o dossie publico traz `satellite.blocked == true` e `baselineSource` continua `deterministic_baseline`
    - O bloco `satellite` publico nunca traz cloud coverage por cena, geometria, `scene_id` bruto de todas as cenas, caminho de storage nem eventos internos
  </behavior>
  <action>
**1. `backend_app/modules/credits_availability.py`** (novo, compartilhado):
```python
from __future__ import annotations

# Phase 05 / D-23 -- disponibilidade de credito por decisao operacional.
# Extraido de audit/routes.py (_block_credits/_unlock_credits) porque agora
# existem DOIS acionadores: a decisao de auditoria (Phase 1) e o incidente
# satelital confirmado (Phase 05). Um unico ponto de verdade evita dois
# caminhos divergentes para o mesmo conceito.

async def block_project_credits(session: AsyncSession, project: Project) -> int: ...
async def unlock_project_credits(session: AsyncSession, project: Project) -> int: ...
```
Corpo identico ao dos helpers atuais (`status = "SUSPENDED"` / `quantity_available = Decimal("0")` e `status = "AVAILABLE"` / `quantity_available = quantity_total - quantity_retired`), devolvendo a contagem de linhas afetadas. Comentario obrigatorio: `# NUNCA altera project.carbon_stock: D-23 proibe derivar volume de credito de NDVI. Aqui so muda DISPONIBILIDADE.`

Em `backend_app/modules/audit/routes.py`: remover `_unlock_credits`/`_block_credits` e importar `block_project_credits`/`unlock_project_credits`, ajustando as tres chamadas em `verify_project`. Nenhuma mudanca de comportamento — `tests/test_api_integration.py` deve continuar verde sem alteracao.

**2. `SatelliteService.raise_credit_adjustment_pendency(project, event, *, actor_id, actor_profile_id)`**:
```python
        # D-23: "recalculo de creditos apos incidente" NAO calcula toneladas
        # a partir de NDVI (fora de escopo na Bible). Cria uma pendencia
        # estruturada de revisao MANUAL, analoga a certification_pendencies, e
        # torna o credito indisponivel para venda/mint enquanto durar.
        # project.carbon_stock NAO e alterado por nenhuma linha deste metodo.
```
- Descricao PT-BR determinística:
```python
        area = f"{float(event.affected_area_ha):.2f} ha" if event.affected_area_ha is not None else "área não estimada"
        description = (
            f"Evento ambiental confirmado ({EVENT_TYPE_LABELS[event.type]}, severidade {event.severity.lower()}) "
            f"afetando {area}. Revisar manualmente o volume de créditos do projeto antes de liberar novas vendas."
        )
```
  com `EVENT_TYPE_LABELS = {"VEGETATION_LOSS": "perda de vegetação", "VEGETATION_RECOVERY": "recuperação de vegetação", "POSSIBLE_FIRE": "possível incêndio"}` declarado no topo do arquivo.
- `pg_insert(CreditAdjustmentPendency).values(project_id=..., project_event_id=event.id, raised_by_profile_id=actor_profile_id, category=CREDIT_ADJUSTMENT_PENDENCY_CATEGORY, description=description, affected_area_ha=event.affected_area_ha, status="OPEN", metadata_={"event_type": event.type, "severity": event.severity, "confidence": ...}).on_conflict_do_nothing(index_elements=["project_event_id"])`.
- Quando a pendencia foi realmente criada: `await block_project_credits(self.session, project)` e `create_audit_event(action="CREDIT_ADJUSTMENT_PENDENCY_RAISED", entity_type="projects", entity_id=project.id, metadata={"pendency_id": ..., "event_id": ..., "affected_area_ha": ..., "credits_blocked": <contagem>})`.
- Entrada de timeline publica e neutra: titulo `"Créditos indisponíveis para venda após incidente"`, desc `"Uma pendência de recálculo de crédito foi aberta após confirmação de evento ambiental. As vendas ficam suspensas até a revisão manual."`

`async def list_credit_pendencies(self, project_id, *, status_filter=None) -> list[CreditAdjustmentPendency]` e
`async def has_open_credit_pendency(self, project_id) -> bool` — usados pela rota da Task 1 e pelo `clear_event_review` da Task 2.

**3. Dossie publico (D-25/SATM-07)** — em `backend_app/modules/projects/service.py`:
```python
def public_satellite_item(
    *, latest: SatelliteObservation | None, observation_count: int,
    ndvi_mean: float | None, reference_hash: str | None,
    baseline_source: str | None, sentinel_status: str | None,
) -> dict[str, Any]:
    """Bloco satelital minimizado do dossie publico (Phase 05 / D-25).

    Mesma disciplina de public_document_item/public_integrity_item: expoe
    apenas o suficiente para o cidadao verificar a existencia de observacao
    real. NUNCA expoe geometria, serie completa, cloud coverage por cena,
    caminho de storage nem eventos internos (esses ficam em /api/v1 interno).
    """
    return {
        "baselineSource": baseline_source,
        "sentinelStatus": sentinel_status,
        "blocked": baseline_source != BASELINE_SOURCE_COPERNICUS,
        "ndviMean": ndvi_mean,
        "pointsAnalyzed": observation_count,
        "referenceHash": reference_hash,
        "sentinelSceneId": latest.scene_id if latest is not None else None,
        "lastObservedAt": latest.observed_at.isoformat() if latest is not None else None,
    }
```
Acrescentar `satellite=` a `ProjectPublicDossierResponse` (campo `dict[str, Any] | None = None` em `projects/schemas.py`... **atencao**: `schemas.py` nao esta em `files_modified` deste plano. Adicionar o campo diretamente em `ProjectPublicDossierResponse` exige tocar `projects/schemas.py` — incluir esse arquivo na lista de arquivos alterados ao executar e registrar no SUMMARY). Popular em `get_public_dossier` lendo `SatelliteService(self.session)` para `latest_observation`/contagem/media, e `reference_hash` do `ProjectBaseline` mais recente (`baseline.baseline_hash` — que o Plan 05 ja passou a derivar da observacao real).

Os campos existentes `baseline_item(...)` continuam sendo servidos como estao: como o Plan 05 recria `ProjectBaseline` a partir das observacoes reais, `dossier.baseline.ndviMean`/`.pointsAnalyzed`/`.baselineHash`/`.sentinelSceneId` passam a refletir Copernicus automaticamente, sem mudanca de shape (e por isso o Plan 08 so troca a cor do tile).

**4. Testes:**
- Em `tests/test_satellite_incident_recalc.py`: um teste por item de `<behavior>` relativo a pendencia/creditos, incluindo por nome `test_confirmed_event_raises_credit_pendency_and_blocks_credits`, `test_confirmation_never_changes_carbon_stock`, `test_pendency_is_not_duplicated_on_repeated_confirmation`, `test_clear_with_open_pendency_does_not_unlock_credits`, `test_resolved_pendency_plus_clear_unlocks_credits`.
- Em `tests/contract/test_backend_app_api_v1.py` (nomes com `satellite` ou `dossier`): `test_public_dossier_exposes_real_satellite_baseline` e `test_public_dossier_satellite_block_is_minimized` — este ultimo assere que `json.dumps(dossier["satellite"])` nao contem `"supabase://"`, `"cloudCoverage"`, `"geometry"` nem `"coordinates"`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_satellite_incident_recalc.py -x && uv run pytest -q tests/contract/test_backend_app_api_v1.py -k "satellite or dossier" -x && uv run pytest -q tests/test_api_integration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def block_project_credits\|def unlock_project_credits' backend_app/modules/credits_availability.py` retorna 2
    - `grep -c '_block_credits\|_unlock_credits' backend_app/modules/audit/routes.py` retorna 0
    - `grep -c 'block_project_credits\|unlock_project_credits' backend_app/modules/audit/routes.py` maior ou igual a 3
    - `grep -c 'carbon_stock' backend_app/modules/satellite/service.py backend_app/modules/credits_availability.py` retorna 0 em ambos
    - `grep -c 'def public_satellite_item' backend_app/modules/projects/service.py` retorna 1
    - `grep -c 'CREDIT_ADJUSTMENT_PENDENCY_RAISED' backend_app/modules/satellite/service.py` retorna 1
    - `uv run pytest -q tests/test_satellite_incident_recalc.py` coleta 15 ou mais testes e todos passam
    - `uv run pytest -q` (suite completa) passa sem regressao
  </acceptance_criteria>
  <done>Incidente confirmado abre pendencia estruturada, indisponibiliza credito sem tocar carbon_stock, e o dossie publico mostra baseline Sentinel real minimizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizacao A -> eventos/observacoes da organizacao B | `project_id` e `event_id` sao manipulaveis na URL |
| ator sem papel -> decisao de incidente | Confirmar/descartar altera o risco e a disponibilidade de credito |
| decisao humana -> Auto Hold | Um caminho paralelo de escrita quebraria a auditabilidade do bloqueio |
| dossie interno -> dossie publico | Serie completa, geometria e caminho de storage nao podem cruzar |
| pendencia de credito -> volume de credito | Alterar `carbon_stock` automaticamente seria estimativa de carbono proibida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-41 | Elevation of Privilege | IDOR em `/environmental-events/{event_id}` | mitigate | Toda rota resolve o projeto primeiro, aplica `_assert_project_edit_permission` org-scoped e filtra o evento por `ProjectEvent.project_id == project.id` (404 se nao pertencer); teste `test_event_decision_is_org_scoped` |
| T-05-42 | Spoofing | decisao de incidente por ator sem papel | mitigate | `require_role("auditor", "certifier", "admin")` nas duas rotas de PATCH; produtor nao decide sobre o proprio incidente (mesmo criterio de `verify_project`) |
| T-05-43 | Tampering | Auto Hold escrito fora do recalculo | mitigate | Gate de aceite `grep -c 'integrity_status ='` = 0 em `satellite/service.py` e `satellite/routes.py`; o unico escritor continua sendo `recalculate_risk_score` (D-20); teste provando que `project.status` nao muda |
| T-05-44 | Elevation of Privilege | confirmacao pulando o estado ANALYZED | mitigate | Validacao contra `PROJECT_EVENT_TRANSITIONS` com 400 explicito; teste `test_detected_event_cannot_be_confirmed_directly` |
| T-05-45 | Repudiation | desbloqueio sem trilha ou por timeout | mitigate | `clear_event_review` exige evento CONFIRMED, `notes` nao vazia e grava `ANOMALY_REVIEW_CLEARED` em `audit_events`; nenhum job/timeout escreve `cleared_at` (o Plan 06 nao toca essa coluna) |
| T-05-46 | Information Disclosure | serie completa/geometria vazando no dossie publico | mitigate | `public_satellite_item` com allowlist explicita de 8 campos; teste asserindo ausencia de `supabase://`, `cloudCoverage`, `geometry` e `coordinates` no bloco publico |
| T-05-47 | Information Disclosure | nota interna de decisao na timeline publica | mitigate | `notes` vai apenas para `audit_events`; a timeline recebe texto fixo neutro; teste `test_decision_timeline_entry_never_contains_internal_notes` |
| T-05-48 | Tampering | volume de credito alterado automaticamente | mitigate | Gate de aceite `grep -c 'carbon_stock'` = 0 nos dois modulos novos; o incidente muda apenas `EnvironmentalCredit.status`/`quantity_available`; teste `test_confirmation_never_changes_carbon_stock` |
| T-05-49 | Information Disclosure | consumo de quota exposto por projeto | mitigate | `GET /satellite/usage` restrito a `require_role("admin")`; a tabela `copernicus_api_usage` nao tem policy de SELECT (Plan 01) |
</threat_model>

<verification>
1. `uv run pytest -q tests/test_satellite_incident_recalc.py` — SATM-08/SATM-09 verdes
2. `uv run pytest -q tests/contract/test_backend_app_api_v1.py -k "satellite or dossier"` — SATM-07 (dossie publico) verde
3. `uv run pytest -q tests/test_api_integration.py tests/test_risk_engine.py` — extracao dos helpers de credito e sinal de risco sem regressao
4. `uv run pytest -q` — suite completa verde
</verification>

<success_criteria>
- Leitura completa do dominio satelital por `/api/v1`, org-scoped, sem vazar caminho de storage
- Decisao humana obrigatoria para CONFIRMED/DISMISSED, com transicao validada contra o vocabulario
- Auto Hold acionado exclusivamente pelo recalculo de risco; desbloqueio somente por decisao humana registrada
- Pendencia de recalculo de credito criada com area afetada e credito indisponibilizado, sem tocar `carbon_stock`
- Dossie publico com baseline Sentinel real, NDVI medio, pontos analisados e hash de referencia
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-07-SUMMARY.md` registrando: lista final de rotas com metodo/guard, shape exato de `observation_item`/`event_item`/`pendency_item`/`public_satellite_item` (os Plans 08/09 consomem), o score observado no cenario de Auto Hold por anomalia, e as condicoes exatas que liberam o credito de volta.
</output>
