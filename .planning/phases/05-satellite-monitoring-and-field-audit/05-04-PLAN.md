---
phase: 05-satellite-monitoring-and-field-audit
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend_app/modules/satellite/anomaly_detector.py
  - backend_app/modules/integrity/constants.py
  - backend_app/modules/integrity/risk_engine.py
  - backend_app/modules/integrity/service.py
  - tests/modules/satellite/test_anomaly_detector.py
  - tests/test_risk_engine.py
autonomous: true
requirements: [SATM-06, SATM-08]

must_haves:
  truths:
    - "O detector de anomalia e uma funcao pura: mesma entrada produz sempre a mesma saida, sem banco e sem event loop"
    - "O detector nunca produz o rotulo DEFORESTATION — o tipo devolvido esta sempre em VEGETATION_LOSS/VEGETATION_RECOVERY/POSSIBLE_FIRE"
    - "Os thresholds vem de Settings, nunca hardcoded"
    - "Um ProjectEvent CONFIRMED de severidade HIGH ou CRITICAL vira sinal de risco explicavel no Risk Engine"
    - "Um ProjectEvent CONFIRMED ja revisado (cleared_at preenchido) deixa de contar como sinal de risco, sem que ninguem escreva integrity_status manualmente"
    - "compute_signals continua funcionando para todos os chamadores existentes sem alteracao (novo parametro tem default vazio)"
  artifacts:
    - path: "backend_app/modules/satellite/anomaly_detector.py"
      provides: "detect_anomaly puro (D-17), ObservationSnapshot/AnomalySignal"
      contains: "def detect_anomaly"
      min_lines: 120
    - path: "tests/modules/satellite/test_anomaly_detector.py"
      provides: "Cobertura SATM-06 sem banco"
      min_lines: 130
    - path: "backend_app/modules/integrity/risk_engine.py"
      provides: "ProjectEventSnapshot + sinais SATELLITE_ANOMALY_CONFIRMED_*"
      contains: "SATELLITE_ANOMALY_CONFIRMED_CRITICAL"
  key_links:
    - from: "backend_app/modules/satellite/anomaly_detector.py"
      to: "backend_app/core/config.py"
      via: "satellite_ndvi_drop_threshold injetado por Settings"
      pattern: "satellite_ndvi_drop_threshold"
    - from: "backend_app/modules/integrity/service.py"
      to: "project_events"
      via: "quarta query do recalculate_risk_score (D-20)"
      pattern: "ProjectEvent.status == \"CONFIRMED\""
    - from: "backend_app/modules/integrity/risk_engine.py"
      to: "Auto Hold existente"
      via: "novo sinal soma peso e pode levar o score a CRITICAL"
      pattern: "satellite_events"
---

<objective>
Escrever a regra de negocio pura de deteccao de anomalia (D-17) e conectar anomalia confirmada ao Auto Hold que ja existe desde a Phase 04.2 (D-20) — estendendo `compute_signals` com um terceiro snapshot e `recalculate_risk_score` com uma quarta query, sem criar nenhum segundo mecanismo de bloqueio.

Purpose: fecha a metade "logica" de SATM-06 e habilita SATM-08. O Auto Hold ja esta pronto: esta fase apenas alimenta o motor com um sinal novo. Escrever um `if severity == CRITICAL: project.integrity_status = "ON_HOLD"` seria a violacao mais grave possivel de D-20.
Output: `satellite/anomaly_detector.py` (puro), `ProjectEventSnapshot` + 2 codigos de sinal novos, `recalculate_risk_score` lendo `project_events`, testes unitarios sem banco.

**Nao faz parte deste plano:** persistir anomalia/evento (Plan 06), rotas de decisao (Plan 07), pendencia de credito (Plan 07). Nenhum arquivo de `backend_app/modules/satellite/` alem de `anomaly_detector.py` e tocado aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-01-SUMMARY.md

<interfaces>
Disciplina de modulo puro a copiar (`backend_app/modules/integrity/risk_engine.py:1-11`, cabecalho literal):
```
# Modulo PURO: proibido importar o driver assincrono de banco ou qualquer
# modelo do ORM. Entradas sao dataclasses simples; a leitura do banco
# (Claim/Evidence/Conflict) fica inteiramente no IntegrityService (service.py).
```

Assinatura atual a estender (`risk_engine.py:55-59`):
```python
def compute_signals(
    claims: Sequence[ClaimSnapshot],
    conflicts: Sequence[ConflictSnapshot],
    settings: Settings | None = None,
) -> list[RiskSignalDTO]:
```
Estilo de bucket a seguir (`risk_engine.py:72-94`): um sinal no maximo por bucket, contagem no `reason`/`metadata`, peso nunca multiplicado, `public_safe=code in PUBLIC_RISK_SIGNAL_CODES`, e ordenacao final ja existente (`risk_engine.py:187`) `signals.sort(key=lambda s: (-s.weight, _SIGNAL_ORDER.get(s.code, len(_SIGNAL_ORDER))))` — `_SIGNAL_ORDER` deriva de `RISK_SIGNAL_CODES`, entao basta acrescentar os codigos novos a tupla.

`risk_signals.code` e `text not null` **sem CHECK constraint** (`202608170002_integrity_risk_assessments.sql:21-24`, confirmado no PATTERNS.md) — **nenhuma migration nova e necessaria** para adicionar codigos de sinal. Isso resolve a Open Question 1 do RESEARCH.

Trecho exato de `recalculate_risk_score` a estender (`backend_app/modules/integrity/service.py:725-761`):
```python
claims = (await self.session.execute(select(Claim).where(Claim.project_id == project.id))).scalars().all()
evidence = (await self.session.execute(select(Evidence).where(Evidence.project_id == project.id))).scalars().all()
conflicts = (await self.session.execute(select(Conflict).where(Conflict.project_id == project.id))).scalars().all()
...
signals = compute_signals(claim_snapshots, conflict_snapshots)
```
Auto Hold (unico escritor, `service.py:812-813`): `project.risk_score = score; project.integrity_status = integrity_status`. **Nao criar um segundo caminho de escrita.**

Settings ja disponiveis (Plan 01): `satellite_ndvi_drop_threshold: float = 0.15`, `satellite_ndvi_recovery_threshold: float = 0.15`, `satellite_nbr_fire_threshold: float = 0.27`, `satellite_max_cloud_coverage_pct: float = 20.0`, `integrity_risk_weight_satellite_anomaly_critical: float = 50.0`, `integrity_risk_weight_satellite_anomaly_high: float = 30.0`.

Constantes ja disponiveis (Plan 01): `PROJECT_EVENT_TYPES`, `PROJECT_EVENT_SEVERITIES`, `PROJECT_EVENT_RISK_SEVERITIES`, `NDVI_DROP_SEVERITY_BOUNDS`, `FORBIDDEN_AUTOMATIC_EVENT_TYPES`.

Modelo `ProjectEvent` (Plan 01): colunas `status`, `severity`, `type`, `cleared_at`, `decided_at`, `affected_area_ha`.

Estilo de teste puro a copiar (`tests/test_risk_engine.py`, 208 linhas): sem `TestClient`, sem `asyncio`, sem sessao de banco; fabricas `_claim(...)`/`_conflict(...)`; teste de injecao de Settings (`test_weights_come_from_settings_not_hardcoded`, linhas 156-161); teste de assercao negativa (`test_signal_reasons_never_mention_other_projects`, linhas 140-153).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: anomaly_detector.py puro (D-17) com testes unitarios sem banco</name>
  <files>backend_app/modules/satellite/anomaly_detector.py, tests/modules/satellite/test_anomaly_detector.py</files>
  <read_first>
    - backend_app/modules/integrity/risk_engine.py (arquivo inteiro, 220 linhas — a disciplina de pureza e o formato de dataclass/funcao a copiar)
    - tests/test_risk_engine.py (arquivo inteiro, 208 linhas — estrutura de teste puro a copiar)
    - backend_app/modules/satellite/constants.py (Plan 01 — NDVI_DROP_SEVERITY_BOUNDS, PROJECT_EVENT_TYPES, FORBIDDEN_AUTOMATIC_EVENT_TYPES)
    - backend_app/core/config.py (bloco satellite_* do Plan 01)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-17
  </read_first>
  <behavior>
    - Sem observacao anterior (`previous is None`) -> devolve `None` (nao ha do que comparar)
    - NDVI estavel (queda relativa abaixo de `satellite_ndvi_drop_threshold`) -> `None`
    - Queda relativa de NDVI de 0.30 -> `AnomalySignal` com `event_type == "VEGETATION_LOSS"` e `severity == "MEDIUM"`
    - Queda relativa de 0.20 -> severidade `LOW`; 0.40 -> `HIGH`; 0.70 -> `CRITICAL`
    - Queda de NBR acima de `satellite_nbr_fire_threshold` junto com queda de NDVI -> `event_type == "POSSIBLE_FIRE"` (tem precedencia sobre VEGETATION_LOSS)
    - Queda de NBR isolada, sem queda de NDVI acima do threshold -> `None` (NBR sozinho nao caracteriza incidente nesta fase)
    - Alta relativa de NDVI acima de `satellite_ndvi_recovery_threshold` -> `event_type == "VEGETATION_RECOVERY"` com `severity == "LOW"`
    - Observacao atual com `cloud_coverage` acima de `satellite_max_cloud_coverage_pct` -> `None` (guarda defensiva; a observacao nem deveria ter sido persistida — D-13)
    - `ndvi_mean` ausente (`None`) em qualquer um dos dois lados -> `None`, nunca tratar ausencia como zero
    - `previous.ndvi_mean == 0` -> `None` (divisao por zero impossivel)
    - Chamar `detect_anomaly` duas vezes com a mesma entrada devolve resultados iguais
    - `Settings(satellite_ndvi_drop_threshold=0.90)` faz uma queda de 0.30 deixar de gerar anomalia (threshold vem de Settings, nao hardcoded)
    - `result.event_type` sempre pertence a `PROJECT_EVENT_TYPES` e nunca e `"DEFORESTATION"`
    - `confidence` fica entre 0 e 100 e cai conforme o `cloud_coverage` sobe
  </behavior>
  <action>
Criar `backend_app/modules/satellite/anomaly_detector.py` — modulo **PURO**, com o cabecalho no mesmo estilo de `risk_engine.py:1-11`:

```python
from __future__ import annotations

# Sinarca Satellite Monitoring -- Anomaly Detector (Phase 05 / SATM-06, D-17).
#
# Modulo PURO: proibido importar o driver assincrono de banco ou qualquer
# modelo do ORM. Entradas sao dataclasses simples; a leitura/escrita de
# SatelliteObservation/SatelliteAnomaly fica inteiramente no
# SatelliteService (Plan 05/06). Isso torna detect_anomaly testavel sem banco
# (tests/modules/satellite/test_anomaly_detector.py) e garante determinismo:
# mesma entrada -> mesma saida, sem estado acumulado.
#
# REGRA DE ACEITE NAO-DISCRICIONARIA (D-17/SATM-06): este modulo NUNCA produz
# o rotulo "DEFORESTATION". Classificacao juridica de desmatamento esta
# explicitamente fora de escopo (Bible). O vocabulario e fechado em
# PROJECT_EVENT_TYPES e ha um assert final que o comprova.

from dataclasses import dataclass, field
from datetime import datetime
from typing import Sequence

from backend_app.core.config import Settings, get_settings
from backend_app.modules.satellite.constants import (
    FORBIDDEN_AUTOMATIC_EVENT_TYPES,
    NDVI_DROP_SEVERITY_BOUNDS,
    PROJECT_EVENT_TYPES,
)


@dataclass(frozen=True)
class ObservationSnapshot:
    observed_at: datetime
    ndvi_mean: float | None = None
    ndmi_mean: float | None = None
    nbr_mean: float | None = None
    cloud_coverage: float | None = None
    valid_pixel_percentage: float | None = None


@dataclass(frozen=True)
class AnomalySignal:
    index_name: str        # SATELLITE_INDEX_NAMES
    event_type: str        # PROJECT_EVENT_TYPES
    severity: str          # PROJECT_EVENT_SEVERITIES
    value_before: float
    value_after: float
    drop_ratio: float      # positivo = queda; negativo = recuperacao
    confidence: float      # 0-100
    reason: str            # PT-BR, explicavel, sem citar projeto de terceiro
    metadata: dict = field(default_factory=dict)
```

Funcoes obrigatorias (todas puras):

`def severity_for_drop_ratio(drop_ratio: float) -> str` — primeiro rotulo de `NDVI_DROP_SEVERITY_BOUNDS` cujo limite seja `>= drop_ratio`; acima do ultimo limite devolve `"CRITICAL"`. Mesma tecnica de `risk_class_for_score` (`risk_engine.py`).

`def confidence_for(observation: ObservationSnapshot) -> float` — deterministica e documentada:
```python
    # Confianca = quanto da cena e realmente utilizavel. Prioriza
    # valid_pixel_percentage (vem da Statistical API); na ausencia dele usa o
    # complemento da cobertura de nuvem. Sem nenhum dos dois, 50.0 (neutro).
```
Resultado sempre limitado a `max(0.0, min(100.0, round(valor, 2)))`.

`def detect_anomaly(previous: ObservationSnapshot | None, current: ObservationSnapshot, settings: Settings | None = None) -> AnomalySignal | None` — na ordem exata:
1. `config = settings or get_settings()`
2. `if previous is None: return None`
3. `if current.cloud_coverage is not None and current.cloud_coverage > config.satellite_max_cloud_coverage_pct: return None` (comentario: guarda defensiva de D-13)
4. `if previous.ndvi_mean is None or current.ndvi_mean is None or previous.ndvi_mean <= 0: return None`
5. `ndvi_drop = (previous.ndvi_mean - current.ndvi_mean) / previous.ndvi_mean`
6. `nbr_drop` calculado do mesmo jeito quando ambos os NBR existem e `previous.nbr_mean > 0`, senao `None`
7. Prioridade de classificacao:
   - `POSSIBLE_FIRE` quando `ndvi_drop >= config.satellite_ndvi_drop_threshold` **e** `nbr_drop is not None and nbr_drop >= config.satellite_nbr_fire_threshold` — `index_name="NBR"`, severidade por `severity_for_drop_ratio(ndvi_drop)`
   - `VEGETATION_LOSS` quando `ndvi_drop >= config.satellite_ndvi_drop_threshold` — `index_name="NDVI"`, severidade por `severity_for_drop_ratio(ndvi_drop)`
   - `VEGETATION_RECOVERY` quando `-ndvi_drop >= config.satellite_ndvi_recovery_threshold` — `index_name="NDVI"`, severidade fixa `"LOW"` (recuperacao nunca e incidente)
   - caso contrario `None`
8. `reason` em PT-BR, explicavel, comecando pela variacao percentual e sem citar nenhum outro projeto. Exemplos exatos:
   - `f"Queda de {ndvi_drop * 100:.1f}% no NDVI médio entre {previous.observed_at.date().isoformat()} e {current.observed_at.date().isoformat()}"`
   - `f"Queda de {ndvi_drop * 100:.1f}% no NDVI e de {nbr_drop * 100:.1f}% no NBR — assinatura compatível com fogo"`
   - `f"Recuperação de {-ndvi_drop * 100:.1f}% no NDVI médio entre ..."`
9. Antes de devolver, guarda nao-discricionaria:
```python
    assert signal.event_type in PROJECT_EVENT_TYPES
    assert signal.event_type not in FORBIDDEN_AUTOMATIC_EVENT_TYPES
```
   com comentario explicando que estes asserts existem para travar SATM-06 em tempo de execucao, nao apenas em teste.

`def detect_anomalies(observations: Sequence[ObservationSnapshot], settings: Settings | None = None) -> list[tuple[int, AnomalySignal]]` — helper que percorre a serie ordenada por `observed_at` e devolve `(indice_da_observacao_atual, sinal)` para cada par consecutivo que gerou anomalia. O Plan 06 usa isso para varrer a serie inteira; a funcao continua pura.

**Testes** — criar `tests/modules/satellite/test_anomaly_detector.py` (o diretorio `tests/modules/satellite/` nasce com este arquivo; `tests/` nao usa `__init__.py`, entao o nome do arquivo precisa ser unico no repo — e). Cabecalho no estilo de `tests/test_risk_engine.py:1-16`, importando apenas `Settings` e o modulo puro. Fabrica:
```python
def _obs(ndvi: float | None = 0.80, *, day: int = 1, month: int = 1, nbr: float | None = None,
         ndmi: float | None = None, cloud: float | None = 5.0, valid: float | None = None) -> ObservationSnapshot:
    return ObservationSnapshot(
        observed_at=datetime(2024, month, day, tzinfo=timezone.utc),
        ndvi_mean=ndvi, ndmi_mean=ndmi, nbr_mean=nbr,
        cloud_coverage=cloud, valid_pixel_percentage=valid,
    )
```
Um teste por item de `<behavior>`, incluindo obrigatoriamente por nome:
- `test_anomaly_never_labels_deforestation_directly` — varre uma grade de quedas de 0.16 a 0.95 e assere que todo `event_type` produzido esta em `PROJECT_EVENT_TYPES` e nenhum e `"DEFORESTATION"`
- `test_ndvi_drop_threshold_comes_from_settings_not_hardcoded` — `Settings(satellite_ndvi_drop_threshold=0.90)` faz a queda de 0.30 devolver `None`
- `test_detect_anomaly_is_pure_and_deterministic` — duas chamadas iguais devolvem objetos iguais
- `test_high_cloud_observation_never_produces_anomaly`
- `test_missing_ndvi_is_not_treated_as_zero`
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/modules/satellite/test_anomaly_detector.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sqlalchemy\|AsyncSession\|backend_app.db\|httpx' backend_app/modules/satellite/anomaly_detector.py` retorna 0
    - `grep -c 'get_sessionmaker\|TestClient\|asyncio' tests/modules/satellite/test_anomaly_detector.py` retorna 0
    - `grep -v '^#' backend_app/modules/satellite/anomaly_detector.py | grep -c '"DEFORESTATION"'` retorna 0
    - `grep -c 'satellite_ndvi_drop_threshold' backend_app/modules/satellite/anomaly_detector.py` maior ou igual a 1
    - `grep -c '0.15\|0.27' backend_app/modules/satellite/anomaly_detector.py` retorna 0 (thresholds nunca literais no detector)
    - `uv run pytest -q tests/modules/satellite/test_anomaly_detector.py` coleta 13 ou mais testes e todos passam
    - `uv run python -c "from backend_app.modules.satellite.anomaly_detector import detect_anomaly, detect_anomalies, severity_for_drop_ratio as s; assert [s(0.20),s(0.30),s(0.40),s(0.70)]==['LOW','MEDIUM','HIGH','CRITICAL']; print('ok')"` imprime `ok`
  </acceptance_criteria>
  <done>Regra de deteccao determinista, configuravel e com vocabulario fechado provado por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sinal SATELLITE_ANOMALY_CONFIRMED_* no Risk Engine e quarta query em recalculate_risk_score (D-20)</name>
  <files>backend_app/modules/integrity/constants.py, backend_app/modules/integrity/risk_engine.py, backend_app/modules/integrity/service.py, tests/test_risk_engine.py</files>
  <read_first>
    - backend_app/modules/integrity/risk_engine.py (arquivo inteiro, 220 linhas)
    - backend_app/modules/integrity/constants.py linhas 96-130 (RISK_SIGNAL_CODES, RISK_CLASS_BOUNDS, PUBLIC_RISK_SIGNAL_CODES)
    - backend_app/modules/integrity/service.py linhas 707-852 (`recalculate_risk_score` inteiro)
    - tests/test_risk_engine.py (arquivo inteiro, 208 linhas)
    - supabase/migrations/202608170002_integrity_risk_assessments.sql linhas 15-35 (confirmar de novo que `risk_signals.code` e `text` sem CHECK — se houver CHECK, PARAR e reportar: seria necessaria migration nova, contradizendo o PATTERNS.md)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md D-20, D-22
  </read_first>
  <behavior>
    - `compute_signals(claims, conflicts)` sem o novo argumento continua devolvendo exatamente o que devolvia antes (nenhum teste existente muda)
    - Um `ProjectEventSnapshot(status="CONFIRMED", severity="CRITICAL", cleared=False)` produz um sinal `SATELLITE_ANOMALY_CONFIRMED_CRITICAL` com o peso de `integrity_risk_weight_satellite_anomaly_critical`
    - Dois eventos CONFIRMED CRITICAL produzem **um unico** sinal, com `metadata["count"] == 2` (nunca peso dobrado)
    - `severity="HIGH"` produz `SATELLITE_ANOMALY_CONFIRMED_HIGH` com peso menor
    - `severity="MEDIUM"`/`"LOW"` CONFIRMED nao produzem sinal nenhum (D-20: so HIGH/CRITICAL bloqueiam)
    - Eventos com `status` diferente de `CONFIRMED` (DETECTED/ANALYZED/DISMISSED) sao ignorados
    - Evento CONFIRMED CRITICAL com `cleared=True` e ignorado (D-22: revisao humana registrada remove o sinal, e o score recalculado derruba o Auto Hold sozinho)
    - Um evento CONFIRMED CRITICAL sozinho leva `score_from_signals` a 50 e, somado a um DOUBLE_CLAIM (60), satura em 100 -> classe CRITICAL -> `integrity_status_for` devolve `ON_HOLD`
    - Nenhum `reason` dos sinais novos menciona identificador de outro projeto
    - `Settings(integrity_risk_weight_satellite_anomaly_critical=99.0)` muda o peso do sinal (configuravel, nao hardcoded)
  </behavior>
  <action>
**1. `backend_app/modules/integrity/constants.py`** — acrescentar os dois codigos ao FINAL de `RISK_SIGNAL_CODES` (a ordem da tupla define o desempate da ordenacao, entao a posicao importa):
```python
RISK_SIGNAL_CODES: tuple[str, ...] = (
    "OVERLAP_CRITICAL",
    "OVERLAP_HIGH",
    "OVERLAP_MEDIUM",
    "OVERLAP_LOW",
    "DOUBLE_CLAIM",
    "LAND_CLAIM_UNVERIFIED",
    "CLAIM_EVIDENCE_PENDING",
    "POSSESSION_WITHOUT_TITLE",
    # NOVO -- Phase 05 / D-20: anomalia satelital confirmada por decisao humana.
    # risk_signals.code e text sem CHECK (202608170002), entao nenhuma migration
    # e necessaria para este acrescimo.
    "SATELLITE_ANOMALY_CONFIRMED_CRITICAL",
    "SATELLITE_ANOMALY_CONFIRMED_HIGH",
)
```
`PUBLIC_RISK_SIGNAL_CODES = frozenset(RISK_SIGNAL_CODES)` ja deriva automaticamente — nao editar.

**2. `backend_app/modules/integrity/risk_engine.py`:**

a) Novo snapshot, ao lado de `ConflictSnapshot`:
```python
@dataclass(frozen=True)
class ProjectEventSnapshot:
    """Evento ambiental satelital (Phase 05 / D-20).

    `cleared` reflete project_events.cleared_at: uma anomalia confirmada que
    ja passou por revisao humana auditavel (D-22) deixa de pesar no risco. O
    Auto Hold sai sozinho no proximo recalculo -- ninguem escreve
    integrity_status na mao para "desbloquear".
    """
    type: str
    status: str
    severity: str
    cleared: bool = False
```

b) Novo parametro **com default vazio**, posicionado ANTES de `settings` para nao quebrar chamadas posicionais existentes... **atencao**: `compute_signals(claim_snapshots, conflict_snapshots)` e chamada posicionalmente em `service.py:761`, e `tests/test_risk_engine.py` chama `compute_signals([], [_conflict(...)], settings=custom_settings)` com `settings` nomeado. Inserir o parametro novo na terceira posicao e seguro para ambos:
```python
def compute_signals(
    claims: Sequence[ClaimSnapshot],
    conflicts: Sequence[ConflictSnapshot],
    satellite_events: Sequence[ProjectEventSnapshot] = (),
    settings: Settings | None = None,
) -> list[RiskSignalDTO]:
```
Antes de escrever, rodar `grep -rn "compute_signals(" backend_app tests` e confirmar que nenhuma chamada existente passa `settings` posicionalmente. Se alguma passar, converter para nomeada no mesmo commit.

c) Bloco de sinal, seguindo o estilo de bucket ja usado para overlap (um sinal no maximo por bucket), inserido apos o bucket `POSSESSION_WITHOUT_TITLE` e antes do `signals.sort(...)`:
```python
    # D-20: so evento CONFIRMED, de severidade HIGH/CRITICAL e ainda NAO
    # revisado (cleared=False) vira sinal. Nao existe segundo mecanismo de
    # bloqueio: o Auto Hold ja existente dispara sozinho se o score chegar a
    # classe CRITICAL.
    active_confirmed = [
        e for e in satellite_events if e.status == "CONFIRMED" and not e.cleared
    ]
    satellite_specs = (
        ("CRITICAL", "SATELLITE_ANOMALY_CONFIRMED_CRITICAL",
         config.integrity_risk_weight_satellite_anomaly_critical, "crítica"),
        ("HIGH", "SATELLITE_ANOMALY_CONFIRMED_HIGH",
         config.integrity_risk_weight_satellite_anomaly_high, "alta"),
    )
    for severity, code, weight, rotulo in satellite_specs:
        matches = [e for e in active_confirmed if e.severity == severity]
        if matches:
            n = len(matches)
            noun = "anomalia satelital confirmada" if n == 1 else "anomalias satelitais confirmadas"
            signals.append(
                RiskSignalDTO(
                    code=code,
                    weight=float(weight),
                    reason=f"+{weight:.0f} {n} {noun} de severidade {rotulo}",
                    public_safe=code in PUBLIC_RISK_SIGNAL_CODES,
                    metadata={"count": n, "types": sorted({e.type for e in matches})},
                )
            )
```
Nao alterar `score_from_signals`, `risk_class_for_score`, `integrity_status_for` nem a ordenacao final.

**3. `backend_app/modules/integrity/service.py` — `recalculate_risk_score`:**

a) Quarta query, ao lado das tres existentes (`service.py:725-733`):
```python
        # Phase 05 / D-20: quarta fonte de sinal. Le so os CONFIRMED -- os
        # estados DETECTED/ANALYZED sao trabalho em andamento e nao influenciam
        # o risco ate a decisao humana (D-18).
        project_events = (
            await self.session.execute(
                select(ProjectEvent).where(
                    ProjectEvent.project_id == project.id,
                    ProjectEvent.status == "CONFIRMED",
                )
            )
        ).scalars().all()
```
Importar `ProjectEvent` de `backend_app.db.models` no topo do arquivo.

b) Snapshots, junto de `claim_snapshots`/`conflict_snapshots`:
```python
        satellite_event_snapshots = [
            ProjectEventSnapshot(
                type=event.type,
                status=event.status,
                severity=event.severity,
                cleared=event.cleared_at is not None,
            )
            for event in project_events
        ]
```

c) Passar como argumento NOMEADO (`service.py:761`):
```python
        signals = compute_signals(
            claim_snapshots,
            conflict_snapshots,
            satellite_events=satellite_event_snapshots,
        )
```

d) Acrescentar ao `metadata_` do `ProjectRiskAssessment` a chave `"satellite_event_count": len(satellite_event_snapshots)`, ao lado de `claim_count`/`conflict_count`/`evidence_count`.

e) **Nao alterar mais nada**: o `ProjectRiskAssessment` append-only, os `RiskSignal` por sinal, o `create_audit_event(action="RISK_RECALCULATED")`, o `INTEGRITY_AUTO_HOLD` condicional e a escrita unica de `project.integrity_status` continuam exatamente como estao. Nenhuma linha nova pode escrever `integrity_status`.

**4. Testes** — acrescentar a `tests/test_risk_engine.py` (todos com `satellite` no nome, para casar com o comando `-k satellite` de VALIDATION.md), com a fabrica:
```python
def _event(status: str = "CONFIRMED", severity: str = "CRITICAL", type: str = "VEGETATION_LOSS",
           cleared: bool = False) -> ProjectEventSnapshot:
    return ProjectEventSnapshot(type=type, status=status, severity=severity, cleared=cleared)
```
Um teste por item de `<behavior>`, incluindo por nome:
- `test_satellite_confirmed_critical_produces_signal`
- `test_satellite_two_confirmed_criticals_produce_one_signal_with_count`
- `test_satellite_medium_confirmed_produces_no_signal`
- `test_satellite_non_confirmed_events_are_ignored`
- `test_satellite_cleared_event_stops_counting` (D-22)
- `test_satellite_confirmed_critical_can_reach_auto_hold` — combina com um `_conflict(type="DOUBLE_CLAIM")`, assere `score_from_signals(...) == 100`, `risk_class_for_score(100) == "CRITICAL"` e `integrity_status_for([], risk_class="CRITICAL") == "ON_HOLD"`
- `test_satellite_signal_weight_comes_from_settings_not_hardcoded`
- `test_satellite_signal_reasons_never_mention_other_projects`
- `test_compute_signals_backwards_compatible_without_satellite_events` — chamada com dois argumentos posicionais produz exatamente a mesma lista de antes
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_risk_engine.py -x && uv run pytest -q tests/contract/test_backend_app_api_v1.py -k "integrity or risk" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SATELLITE_ANOMALY_CONFIRMED_CRITICAL' backend_app/modules/integrity/constants.py` retorna 1
    - `grep -c 'SATELLITE_ANOMALY_CONFIRMED' backend_app/modules/integrity/risk_engine.py` retorna 2
    - `grep -c 'class ProjectEventSnapshot' backend_app/modules/integrity/risk_engine.py` retorna 1
    - `grep -c 'sqlalchemy\|AsyncSession\|backend_app.db' backend_app/modules/integrity/risk_engine.py` retorna 0 (pureza preservada)
    - `grep -c 'ProjectEvent.status == "CONFIRMED"' backend_app/modules/integrity/service.py` retorna 1
    - `grep -c 'project.integrity_status =' backend_app/modules/integrity/service.py` retorna 1 (Auto Hold continua com escritor unico — D-20)
    - `grep -c 'project.status' backend_app/modules/integrity/service.py` retorna 0
    - `grep -c 'satellite_events=' backend_app/modules/integrity/service.py` retorna 1
    - `uv run pytest -q tests/test_risk_engine.py -k satellite` coleta 9 ou mais testes e todos passam
    - `uv run pytest -q tests/test_risk_engine.py` passa por inteiro (nenhum teste antigo quebrou)
  </acceptance_criteria>
  <done>Anomalia confirmada e um sinal de risco explicavel que alimenta o Auto Hold ja existente, sem segundo mecanismo de bloqueio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado satelital -> classificacao de evento | Um rotulo errado (`DEFORESTATION`) teria consequencia juridica fora do escopo autorizado |
| sinal de risco -> Auto Hold | O sinal novo pode bloquear um projeto automaticamente |
| evento revisado -> desbloqueio | Um caminho de "clear" mal desenhado permitiria desbloqueio nao auditavel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Tampering | rotulo automatico de evento | mitigate | Vocabulario fechado em `PROJECT_EVENT_TYPES` + `assert` em runtime no detector + check constraint no banco (Plan 01) + teste `test_anomaly_never_labels_deforestation_directly` |
| T-05-22 | Tampering | score inflado por multiplos eventos | mitigate | Um sinal no maximo por bucket de severidade, contagem em `metadata` (mesmo estilo dos buckets de overlap); teste com dois CRITICAL provando peso unico |
| T-05-23 | Elevation of Privilege | segundo caminho de escrita de `integrity_status` | mitigate | Gate de aceite `grep -c 'project.integrity_status ='` = 1; o plano proibe explicitamente qualquer escrita nova; recalculo continua sendo o unico escritor (D-20) |
| T-05-24 | Repudiation | desbloqueio silencioso por timeout | mitigate | `cleared` deriva de `cleared_at`, coluna preenchida apenas pela rota de decisao humana do Plan 07; nenhum caminho automatico a preenche; teste `test_satellite_cleared_event_stops_counting` prova o efeito, e a rota que a preenche gera `audit_event` (Plan 07) |
| T-05-25 | Information Disclosure | `reason` do sinal vazando projeto de terceiro | mitigate | `reason` construido so com contagem e severidade; teste `test_satellite_signal_reasons_never_mention_other_projects` (mesma tecnica de T-04.2-16) |
| T-05-26 | Denial of Service | leitura de `project_events` a cada recalculo | accept | Consulta indexada por `(project_id, status, detected_at desc)` sobre dezenas de linhas por projeto; mesma ordem de custo das tres queries ja existentes. Reavaliar se `project_events` passar de ~10^5 linhas |
</threat_model>

<verification>
1. `uv run pytest -q tests/modules/satellite/test_anomaly_detector.py` — SATM-06 (regra pura) verde
2. `uv run pytest -q tests/test_risk_engine.py` — sinal novo verde e nenhum teste antigo quebrado
3. `uv run pytest -q tests/contract/test_backend_app_api_v1.py -k "integrity or risk"` — contrato de risco/Auto Hold sem regressao
4. `uv run pytest -q` — suite completa verde
</verification>

<success_criteria>
- `detect_anomaly` puro, deterministico, configuravel e com vocabulario fechado (nunca `DEFORESTATION`)
- `compute_signals` aceita eventos satelitais sem quebrar nenhum chamador existente
- Evento CONFIRMED HIGH/CRITICAL nao revisado vira sinal explicavel; revisado deixa de contar
- Auto Hold continua com escritor unico em `recalculate_risk_score`
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-04-SUMMARY.md` registrando: assinatura final de `detect_anomaly`/`detect_anomalies` e de `AnomalySignal` (o Plan 06 consome), a assinatura final de `compute_signals`, os pesos observados nos testes e o score exato em que o Auto Hold dispara por anomalia satelital.
</output>
