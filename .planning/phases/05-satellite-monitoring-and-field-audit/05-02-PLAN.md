---
phase: 05-satellite-monitoring-and-field-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_app/modules/audit/routes.py
  - backend_app/modules/audit/signature.py
  - backend_app/modules/projects/service.py
  - backend_app/modules/projects/schemas.py
  - tests/test_audit_field_evidence.py
  - tests/test_api_integration.py
autonomous: true
requirements: [SATM-01, SATM-02, SATM-03]

must_haves:
  truths:
    - "Um arquivo enviado pelo auditor vira um Document real com sha256_hash calculado no servidor e uma Evidence vinculada ao projeto"
    - "Nenhuma evidencia de auditoria persiste string local:// ou mock://"
    - "PATCH /audit/verify aceita apenas Document.id de documentos AUDIT_EVIDENCE do MESMO projeto; qualquer outro id e rejeitado com 400"
    - "Audit.digital_signature e sempre recalculada no servidor a partir de auditor_id|project_id|laudo|timestamp|evidence_ids ordenados — o valor enviado pelo cliente e descartado"
    - "A mesma entrada produz sempre a mesma assinatura (determinismo verificavel)"
    - "O dossie publico expoe existencia, data e conclusao da auditoria, mas nunca reportText interno, coordenadas do auditor, hash de storage ou a assinatura completa"
  artifacts:
    - path: "backend_app/modules/audit/signature.py"
      provides: "compute_audit_signature puro e deterministico (D-03)"
      contains: "def compute_audit_signature"
      min_lines: 30
    - path: "backend_app/modules/audit/routes.py"
      provides: "POST /audit/{project_id}/evidence + verify com Document.id reais e assinatura server-side"
      contains: "AUDIT_EVIDENCE"
    - path: "tests/test_audit_field_evidence.py"
      provides: "Cobertura SATM-01/SATM-02"
      min_lines: 120
  key_links:
    - from: "backend_app/modules/audit/routes.py"
      to: "IntegrityService.create_evidence_for_document"
      via: "mesmo pipeline de upload_project_document (D-01)"
      pattern: "create_evidence_for_document"
    - from: "backend_app/modules/audit/routes.py"
      to: "Audit.digital_signature"
      via: "compute_audit_signature recalculada no servidor (D-03)"
      pattern: "compute_audit_signature"
    - from: "backend_app/modules/projects/service.py"
      to: "ProjectPublicDossierResponse.audits"
      via: "public_audit_item minimizado (D-05)"
      pattern: "def public_audit_item"
---

<objective>
Substituir o rascunho local da auditoria de campo por evidencia real: upload multipart reaproveitando integralmente o pipeline de `upload_project_document` (D-01), `evidencias_url` passando a referenciar `Document.id` reais validados (D-02), assinatura verificavel recalculada no servidor como stub SHA-256 deterministico (D-03) e minimizacao do laudo/evidencia no dossie publico (D-05).

Purpose: fecha SATM-01/02/03 e elimina o ultimo `local://` operacional do sistema. Tudo o que esta fase precisa ja existe no repo desde a Phase 3/04.2 — este plano so conecta a auditoria ao mesmo pipeline, sem criar um segundo caminho de verdade para "documento com hash".
Output: `POST /api/v1/audit/{project_id}/evidence`, `PATCH /api/v1/audit/verify/{project_id}` evoluido, `public_audit_item`, testes de integracao novos.

**Nao faz parte deste plano:** qualquer mudanca de frontend (Plan 08), qualquer coisa de satelite. Este plano nao toca `backend_app/modules/satellite/` nem migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-CONTEXT.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-PATTERNS.md
@.planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md

<interfaces>
Pipeline de upload a REAPROVEITAR (nunca reimplementar) — `backend_app/modules/projects/routes.py`:

```python
# routes.py:49-73
async def _validated_upload_payload(document_type: str, file: UploadFile) -> dict[str, object]:
    # valida extensao contra ALLOWED_EXTENSIONS, tamanho contra MAX_UPLOAD_BYTES,
    # magic bytes via validate_magic_bytes, devolve dict com sha256/mime_type/size_bytes
```
```python
# routes.py:519-550
location = project_document_location(project.friendly_id, document_type, sha256, extension)
await upload_storage_object(location.bucket, location.object_path, content, mime_type)
document = Document(project_id=..., document_type=..., storage_bucket=location.bucket,
                    storage_object_path=location.object_path, storage_path=location.uri,
                    sha256_hash=..., mime_type=..., size_bytes=...,
                    metadata_={"filename": ..., "content_type": ...})
session.add(document); await session.flush()
await IntegrityService(session).create_evidence_for_document(document, project=project,
                                                             actor_id=current_user.id, actor_role=current_user.role)
```
Branch de deduplicacao por hash a preservar verbatim (`routes.py:495-517`): se ja existe `Document` com `(project_id, document_type, sha256)`, devolve o existente — e o que torna reenvio idempotente. `Document.__table_args__` tem `UniqueConstraint("project_id","document_type","sha256_hash")`.

Allowlists globais (`backend_app/modules/inventory/routes.py:22-31`, importadas por `projects/routes.py`):
```python
MAX_UPLOAD_BYTES = 10 * 1024 * 1024
ALLOWED_EXTENSIONS = {".pdf", ".png", ".jpg", ".jpeg", ".csv", ".xlsx"}
MIME_BY_EXTENSION = {".pdf": "application/pdf", ".png": "image/png", ".jpg": "image/jpeg",
                     ".jpeg": "image/jpeg", ".csv": "text/csv", ".xlsx": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"}
def validate_magic_bytes(extension: str, content: bytes) -> None:  # %PDF-, \x89PNG, \xff\xd8\xff
```
`.mp4` NAO esta na allowlist global hoje. SATM-01 exige video.

Estado atual da auditoria (`backend_app/modules/audit/routes.py`, 135 linhas):
```python
class AuditVerifyRequest(BaseModel):
    status: Literal["APPROVED", "BLOCKED", "RECALCULATED"]
    laudo_texto: str = ""
    latitude: float | None = None
    longitude: float | None = None
    evidencias_url: list[str] = []              # <- strings livres hoje (local://...)
    assinatura_digital: str = "assinatura-digital-pendente"   # <- texto livre do cliente
    auditor_id: str | int | None = None
```
`audit.evidence_urls = payload.evidencias_url` (linha 73) e `audit.digital_signature = payload.assinatura_digital` (linha 74). O restante (`project.timeline` append, `create_audit_event(action=f"AUDIT_{status}")`, `_unlock_credits`/`_block_credits`) deve ser preservado exatamente.

Modelo `Audit` (`backend_app/db/models.py:325-341`): `evidence_urls: Mapped[list] = mapped_column(JSONB, ...)` — ja aceita lista de UUIDs como strings, sem migration (RESEARCH Open Question 3, recomendacao: comecar por JSONB de `Document.id`, integridade referencial garantida na aplicacao).

Serializador atual do dossie publico (`backend_app/modules/projects/service.py:2256`, usado em `get_public_dossier` linha 353):
```python
def audit_item(audit: Audit) -> dict[str, Any]:
    return {"id": ..., "status": ..., "reportText": audit.report_text,
            "latitude": ..., "longitude": ..., "evidenceUrls": list(audit.evidence_urls or []),
            "digitalSignature": audit.digital_signature, "auditedAt": ..., "createdAt": ...}
```
Padrao de dualidade interno/publico ja estabelecido na Phase 4: `document_item` (interno) vs `public_document_item` (publico, sem bucket/caminho/hash/metadata) — `projects/service.py:2216-2255`.

Guard org-scoped a reaproveitar: `ProjectsService.get_editable_project_model(project_id, actor_id=..., actor_role=...)` (`projects/service.py:277-285`), que ja chama `_assert_project_edit_permission`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /audit/{project_id}/evidence — upload real de evidencia de campo (D-01)</name>
  <files>backend_app/modules/audit/routes.py, tests/test_audit_field_evidence.py</files>
  <read_first>
    - backend_app/modules/audit/routes.py (arquivo inteiro, 135 linhas)
    - backend_app/modules/projects/routes.py linhas 1-75 (imports e `_validated_upload_payload`) e 484-570 (`upload_project_document` completo)
    - backend_app/modules/inventory/routes.py linhas 20-32 e 168-180 (allowlists e `validate_magic_bytes`)
    - backend_app/modules/storage_paths.py (arquivo inteiro, 51 linhas)
    - tests/test_api_integration.py (arquivo inteiro — fixtures de login/TestClient a copiar; `tests/` nao tem `__init__.py`, entao fixtures sao copiadas verbatim, nunca importadas — desvio ja documentado em `.planning/phases/04.1-geospatial-foundation/04.1-01-SUMMARY.md`)
  </read_first>
  <behavior>
    - Upload de PNG valido por auditor autenticado -> 201 com `id` (UUID de Document), `sha256` (64 hex), `document_type == "AUDIT_EVIDENCE"`, `storage_path` comecando com `supabase://projects/`
    - O mesmo arquivo enviado duas vezes -> segunda resposta devolve o MESMO `id` e `sha256` (dedup por hash preservada), sem criar Document novo
    - Cada upload aceito cria exatamente uma `Evidence` vinculada ao projeto (visivel em `GET /api/v1/projects/{id}/evidence`)
    - Upload de `.exe`/extensao fora da allowlist -> 415
    - Upload de arquivo com extensao `.png` mas magic bytes de PDF -> 400
    - Upload de `.mp4` valido (magic bytes `ftyp` no offset 4) -> 201 com `mime_type == "video/mp4"`
    - Upload acima do limite de evidencia de auditoria -> 413
    - Upload por usuario sem papel auditor/admin -> 403; sem token -> 401
    - Upload para projeto de outra organizacao -> 403 (guard org-scoped)
    - Nenhuma resposta contem a substring `local://` nem `mock://`
  </behavior>
  <action>
Criar o endpoint `POST /audit/{project_id}/evidence` em `backend_app/modules/audit/routes.py`, com `status_code=201`.

**Constantes especificas de evidencia de campo** (declaradas no topo de `audit/routes.py`, com comentario explicando por que nao entram na allowlist global):
```python
# D-01/SATM-01: a evidencia de campo aceita VIDEO alem dos tipos ja permitidos
# para documento de projeto. A allowlist global (inventory/routes.py) NAO e
# alterada de proposito: video so faz sentido em auditoria de campo, e ampliar
# a allowlist global liberaria .mp4 para todo upload de documento do sistema.
AUDIT_EVIDENCE_DOCUMENT_TYPE = "AUDIT_EVIDENCE"
MAX_AUDIT_EVIDENCE_BYTES = 50 * 1024 * 1024
AUDIT_EVIDENCE_EXTENSIONS: frozenset[str] = frozenset({".pdf", ".png", ".jpg", ".jpeg", ".mp4"})
AUDIT_EVIDENCE_MIME_BY_EXTENSION: dict[str, str] = {
    ".pdf": "application/pdf",
    ".png": "image/png",
    ".jpg": "image/jpeg",
    ".jpeg": "image/jpeg",
    ".mp4": "video/mp4",
}
```

**Validacao** — funcao `_validated_audit_evidence_payload(file: UploadFile) -> dict[str, object]`, que reaproveita `validate_magic_bytes` importada de `backend_app.modules.inventory.routes` e so acrescenta a checagem de MP4:
```python
def _validate_mp4_magic_bytes(content: bytes) -> None:
    # ISO BMFF: box "ftyp" no offset 4 dos primeiros bytes.
    if len(content) < 12 or content[4:8] != b"ftyp":
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Magic bytes invalidos para MP4")
```
A funcao deve, na ordem: normalizar `filename`/`extension` com `PurePath(filename).suffix.lower()`; rejeitar extensao fora de `AUDIT_EVIDENCE_EXTENSIONS` com 415 e detail `"Extensão de arquivo não permitida"`; ler `await file.read(MAX_AUDIT_EVIDENCE_BYTES + 1)` e rejeitar com 413 e detail `"Arquivo excede o limite configurado"`; chamar `validate_magic_bytes(extension, content)` para `.pdf/.png/.jpg/.jpeg` e `_validate_mp4_magic_bytes(content)` para `.mp4`; devolver o mesmo shape de `_validated_upload_payload` (`document_type`, `filename`, `extension`, `content`, `content_type`, `sha256` via `hashlib.sha256(content).hexdigest()`, `mime_type` de `AUDIT_EVIDENCE_MIME_BY_EXTENSION`, `size_bytes`), com `document_type` fixo em `AUDIT_EVIDENCE_DOCUMENT_TYPE`.

**Handler** — copiar a sequencia de `upload_project_document` (`projects/routes.py:484-565`) verbatim, trocando apenas o guard de papel e a validacao:
```python
@router.post("/audit/{project_id}/evidence", status_code=status.HTTP_201_CREATED)
async def upload_audit_evidence(
    project_id: str,
    file: UploadFile = File(...),
    current_user: AuthenticatedUser = Depends(require_role("auditor", "admin")),
    session: AsyncSession = Depends(get_session),
) -> dict[str, object]:
```
Corpo, na ordem:
1. `upload = await _validated_audit_evidence_payload(file)`
2. `project = await ProjectsService(session).get_editable_project_model(project_id, actor_id=current_user.id, actor_role=current_user.role)` — guard org-scoped, nao apenas `require_role` (RESEARCH Security V4, IDOR)
3. Branch de deduplicacao por `(project.id, AUDIT_EVIDENCE_DOCUMENT_TYPE, sha256)` — copiado verbatim de `projects/routes.py:495-517`, devolvendo o Document existente com o mesmo shape de resposta
4. `location = project_document_location(project.friendly_id, AUDIT_EVIDENCE_DOCUMENT_TYPE, sha256, extension)` + `await upload_storage_object(...)`
5. `Document(...)` com `metadata_={"filename": ..., "content_type": ..., "captured_in": "FIELD_AUDIT"}` + `session.add` + `await session.flush()`
6. `await IntegrityService(session).create_evidence_for_document(document, project=project, actor_id=current_user.id, actor_role=current_user.role)` (D-25 da Phase 04.2 — mesmo gancho automatico)
7. `await create_audit_event(session, action="AUDIT_EVIDENCE_UPLOADED", entity_type="documents", entity_id=document.id, actor_role=current_user.role, metadata={"actor_external_id": current_user.id, "friendly_id": project.friendly_id, "document_type": AUDIT_EVIDENCE_DOCUMENT_TYPE, "sha256": upload["sha256"]})`
8. `await session.commit()`
9. Resposta: `{"success": True, "id": str(document.id), "project_id": project.friendly_id, "document_type": ..., "filename": ..., "sha256": ..., "mime_type": ..., "size_bytes": ..., "storage_bucket": ..., "storage_object_path": ..., "storage_path": ..., "status": "UPLOADED"}`

**Testes** — criar `tests/test_audit_field_evidence.py` copiando as fixtures de login/`TestClient` de `tests/test_api_integration.py` (nunca importar de outro arquivo de teste). Cobrir cada item de `<behavior>`. Fixtures binarias minimas a construir inline:
- PNG valido: `b"\x89PNG\r\n\x1a\n" + b"\x00" * 64`
- PDF valido: `b"%PDF-1.4\n" + b"\x00" * 64`
- MP4 valido: `b"\x00\x00\x00\x18ftypisom" + b"\x00" * 64`
- PNG falso (extensao `.png`, conteudo PDF): `b"%PDF-1.4\n" + b"\x00" * 64`
Incluir explicitamente `test_audit_evidence_response_never_contains_local_scheme`, que assere `"local://" not in json.dumps(response.json())` e `"mock://" not in json.dumps(response.json())`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_audit_field_evidence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AUDIT_EVIDENCE' backend_app/modules/audit/routes.py` maior ou igual a 3
    - `grep -c 'create_evidence_for_document' backend_app/modules/audit/routes.py` retorna 1
    - `grep -c 'project_document_location' backend_app/modules/audit/routes.py` retorna 1
    - `grep -c 'get_editable_project_model' backend_app/modules/audit/routes.py` maior ou igual a 1
    - `grep -c 'local://' backend_app/modules/audit/routes.py` retorna 0
    - `grep -c 'ALLOWED_EXTENSIONS' backend_app/modules/inventory/routes.py` continua 1 e `grep -c 'mp4' backend_app/modules/inventory/routes.py` retorna 0 (allowlist global intocada)
    - `uv run python -c "from backend_app.main import app; p={r.path for r in app.routes}; assert '/api/v1/audit/{project_id}/evidence' in p, sorted(x for x in p if 'audit' in x); print('ok')"` imprime `ok`
    - `uv run pytest -q tests/test_audit_field_evidence.py` coleta 10 ou mais testes e todos passam
  </acceptance_criteria>
  <done>Evidencia de auditoria e um Document real com hash SHA-256 no servidor, com Evidence automatica e sem nenhum caminho `local://`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify com Document.id validados e assinatura stub SHA-256 recalculada no servidor (D-02/D-03)</name>
  <files>backend_app/modules/audit/signature.py, backend_app/modules/audit/routes.py, tests/test_audit_field_evidence.py</files>
  <read_first>
    - backend_app/modules/audit/routes.py (apos a Task 1)
    - backend_app/adapters/stellar.py linhas 36-60 (principio fail-closed: nunca simular sucesso de mecanismo inexistente — citado literalmente por D-03)
    - backend_app/db/models.py linhas 325-341 (modelo Audit: evidence_urls JSONB, digital_signature String)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md linha 141 (copy exata da assinatura, travada por D-03)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-RESEARCH.md "Pitfall 4" (nao vazar UUID interno no contrato publico)
  </read_first>
  <behavior>
    - `compute_audit_signature` com os mesmos inputs devolve sempre o mesmo hash de 64 caracteres hexadecimais
    - A ordem em que os evidence_ids sao passados NAO altera o hash (lista ordenada antes do join)
    - Mudar qualquer um dos 5 componentes (auditor_id, project_id, laudo, timestamp, evidence_ids) muda o hash
    - `PATCH /audit/verify/{project_id}` com `evidencias_url` contendo um Document.id valido de AUDIT_EVIDENCE do proprio projeto -> 200, e `Audit.evidence_urls` guarda esse mesmo id
    - `evidencias_url` contendo um UUID inexistente -> 400 com detail citando evidencia nao encontrada
    - `evidencias_url` contendo um Document.id de OUTRO projeto -> 400 (nunca 200)
    - `evidencias_url` contendo uma string que nao e UUID (ex. `local://auditoria/x.png`) -> 400
    - O `assinatura_digital` enviado pelo cliente e IGNORADO: a resposta e o registro persistido trazem o hash recalculado no servidor, diferente do valor enviado
    - A resposta do verify inclui `assinatura_digital` (hash), `assinatura_tipo == "STUB_SHA256"` e `assinatura_verificavel_em` (timestamp usado no calculo), para que o cliente possa reproduzir a verificacao
    - `project.timeline`, `create_audit_event(action=f"AUDIT_{status}")` e `_unlock_credits`/`_block_credits` continuam funcionando exatamente como antes (sem regressao em `tests/test_api_integration.py`)
  </behavior>
  <action>
**1. Criar `backend_app/modules/audit/signature.py`** — modulo PURO (proibido importar `AsyncSession`/ORM), no mesmo espirito de `integrity/risk_engine.py`:

```python
from __future__ import annotations

# Phase 05 / D-03 -- Assinatura verificavel de auditoria de campo.
#
# Modulo PURO: sem banco, sem ORM. Nao existe hardware biometrico disponivel
# neste ambiente (mesma realidade documentada na Phase 3 para SUN/Sentinel),
# entao a assinatura desta fase e um STUB DETERMINISTICO VERIFICAVEL, nunca
# uma assinatura biometrica ou criptografica real. Segue o principio
# fail-closed de backend_app/adapters/stellar.py: nunca simular o sucesso de
# um mecanismo que nao existe. A UI rotula explicitamente como
# "Assinatura verificável (stub SHA-256)".

import hashlib
from datetime import datetime
from typing import Sequence

AUDIT_SIGNATURE_KIND = "STUB_SHA256"


def audit_signature_payload(
    *,
    auditor_id: str,
    project_id: str,
    report_text: str,
    signed_at: datetime,
    evidence_ids: Sequence[str],
) -> str:
    """String canonica assinada (D-03). Ordem dos ids nunca altera o resultado."""
    joined = ",".join(sorted(str(item) for item in evidence_ids))
    return f"{auditor_id}|{project_id}|{report_text}|{signed_at.isoformat()}|{joined}"


def compute_audit_signature(
    *,
    auditor_id: str,
    project_id: str,
    report_text: str,
    signed_at: datetime,
    evidence_ids: Sequence[str],
) -> str:
    raw = audit_signature_payload(
        auditor_id=auditor_id,
        project_id=project_id,
        report_text=report_text,
        signed_at=signed_at,
        evidence_ids=evidence_ids,
    )
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()
```

**2. Evoluir `PATCH /audit/verify/{project_id}`** em `audit/routes.py`:

a) `AuditVerifyRequest`: manter o nome `evidencias_url` (compatibilidade exigida por D-02) mas documentar em docstring/`Field(description=...)` que o campo agora carrega `Document.id` de evidencias `AUDIT_EVIDENCE` do proprio projeto. Manter `assinatura_digital` no schema por compatibilidade de contrato, marcando com comentario `# D-03: valor recebido e IGNORADO; a assinatura e recalculada no servidor.`

b) Antes de qualquer escrita, resolver e validar os ids — nova funcao:
```python
async def _resolve_audit_evidence_documents(
    session: AsyncSession, project: Project, raw_ids: list[str]
) -> list[Document]:
```
Regras (todas com HTTP 400 e detail PT-BR):
- Cada item precisa ser um UUID valido (`uuid.UUID(item)`), senao `"Evidência de auditoria inválida: {item}"` — isso e o que rejeita `local://...` explicitamente.
- Buscar `select(Document).where(Document.id.in_(uuids), Document.project_id == project.id, Document.document_type == AUDIT_EVIDENCE_DOCUMENT_TYPE)`.
- Se a contagem encontrada for menor que a de ids distintos, `"Evidência de auditoria não encontrada para este projeto: {ids_faltantes}"` — cobre simultaneamente id inexistente e id de outro projeto (RESEARCH Security V4/IDOR).
- Lista vazia e permitida (auditoria sem anexo continua valida, como hoje).
- Devolver os documentos ordenados por `id` (string) para determinismo.

c) Persistencia:
```python
signed_at = datetime.now(timezone.utc)
evidence_ids = [str(document.id) for document in documents]
audit.evidence_urls = evidence_ids
audit.digital_signature = compute_audit_signature(
    auditor_id=str(payload.auditor_id or current_user.id),
    project_id=str(project.id),
    report_text=payload.laudo_texto,
    signed_at=signed_at,
    evidence_ids=evidence_ids,
)
audit.audited_at = signed_at
```
Guardar tambem os metadados de reproducao em `create_audit_event(...)`: acrescentar ao `metadata` ja existente as chaves `"signature_kind": AUDIT_SIGNATURE_KIND`, `"signed_at": signed_at.isoformat()`, `"evidence_ids": evidence_ids` (substituindo o atual `"evidence_count"` por `"evidence_count": len(evidence_ids)` mantido + as novas chaves).

d) Resposta do verify — acrescentar aos campos ja existentes:
```python
"assinatura_digital": audit.digital_signature,
"assinatura_tipo": AUDIT_SIGNATURE_KIND,
"assinatura_verificavel_em": signed_at.isoformat(),
"evidencias_url": evidence_ids,
```
**Proibido** devolver `storage_object_path`/`storage_bucket` nesta resposta (Pitfall 4).

e) Nao alterar: o append em `project.timeline`, o `create_audit_event(action=f"AUDIT_{payload.status}")`, `_get_or_create_audit`, `_unlock_credits`, `_block_credits`, `_audit_title`.

**3. Testes** — acrescentar a `tests/test_audit_field_evidence.py`:
- `test_signature_stub_deterministic` (nome exato exigido por VALIDATION.md): duas chamadas de `compute_audit_signature` com os mesmos inputs produzem o mesmo hash de 64 hex; ordem invertida de `evidence_ids` produz o mesmo hash; alterar cada um dos 5 componentes produz hash diferente. Teste PURO, sem banco.
- `test_verify_rejects_local_scheme_evidence` — `evidencias_url=["local://auditoria/foto.png"]` devolve 400.
- `test_verify_rejects_document_from_other_project` — 400.
- `test_verify_rejects_unknown_document_id` — 400 com UUID aleatorio.
- `test_verify_persists_document_ids_and_server_signature` — envia `assinatura_digital="assinatura-falsa-do-cliente"` e assere que a resposta traz um hash de 64 hex diferente dessa string, e que `assinatura_tipo == "STUB_SHA256"`.
- `test_verify_signature_is_reproducible_from_response` — recalcula localmente com `compute_audit_signature` usando `assinatura_verificavel_em` da resposta e confere igualdade.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_audit_field_evidence.py -x && uv run pytest -q tests/test_api_integration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sqlalchemy\|AsyncSession\|backend_app.db' backend_app/modules/audit/signature.py` retorna 0
    - `grep -c 'compute_audit_signature' backend_app/modules/audit/routes.py` retorna 1
    - `grep -v '^ *#' backend_app/modules/audit/routes.py | grep -c 'payload.assinatura_digital'` retorna 0
    - `grep -c '_resolve_audit_evidence_documents' backend_app/modules/audit/routes.py` retorna 2
    - `grep -c 'storage_object_path' backend_app/modules/audit/routes.py` retorna 1 (apenas na resposta do endpoint de upload da Task 1, nunca no verify)
    - `uv run pytest -q tests/test_audit_field_evidence.py::test_signature_stub_deterministic` passa
    - `uv run pytest -q tests/test_api_integration.py` passa sem regressao
  </acceptance_criteria>
  <done>Assinatura verificavel calculada no servidor, evidencias referenciando Documents reais validados e contrato antigo preservado.</done>
</task>

<task type="auto">
  <name>Task 3: Minimizacao do laudo e da evidencia de auditoria no dossie publico (D-05/SATM-03)</name>
  <files>backend_app/modules/projects/service.py, backend_app/modules/projects/schemas.py, tests/test_api_integration.py</files>
  <read_first>
    - backend_app/modules/projects/service.py linhas 287-360 (`get_public_dossier`, onde `audit_item` e usado hoje na linha 353)
    - backend_app/modules/projects/service.py linhas 2216-2270 (`public_document_item`, `document_item`, `audit_item` — o padrao de dualidade a replicar)
    - backend_app/modules/projects/schemas.py linhas 96-130 (`ProjectPublicDossierResponse`)
    - .planning/phases/04-certification-workbench/04-CONTEXT.md (regra de minimizacao `PUBLIC_DOCUMENT_TYPES` que serve de modelo)
    - .planning/phases/05-satellite-monitoring-and-field-audit/05-UI-SPEC.md secao "Surface C" (o que o dossie publico pode exibir)
  </read_first>
  <action>
Hoje `get_public_dossier` (linha 353) serializa auditorias com `audit_item`, que expoe `reportText` (laudo interno completo), `latitude`/`longitude` do auditor e `digitalSignature` inteira — violando D-05 e o anti-padrao "misturar dossie publico e interno" ja documentado em `ARCHITECTURE.md`.

**1. Criar `public_audit_item(audit: Audit) -> dict[str, Any]`** em `projects/service.py`, imediatamente apos `audit_item` (que permanece intocado para uso interno), seguindo exatamente o estilo de `public_document_item`:

```python
def public_audit_item(audit: Audit) -> dict[str, Any]:
    """Auditoria minimizada para o dossie publico (Phase 05 / D-05).

    Expoe apenas EXISTENCIA, DATA e CONCLUSAO da auditoria. Nunca o laudo
    interno completo (report_text), nunca as coordenadas do auditor, nunca a
    assinatura inteira e nunca caminho/hash de storage das evidencias.
    Mesmo principio de minimizacao de public_document_item/PUBLIC_DOCUMENT_TYPES
    (Phase 4) e do bloco integrity publico (Phase 04.2 / D-16).
    """
    signature = audit.digital_signature or ""
    return {
        "id": str(audit.id),
        "status": audit.status,
        "conclusion": AUDIT_PUBLIC_CONCLUSION_LABELS.get(audit.status, "Auditoria registrada"),
        "auditedAt": audit.audited_at.isoformat() if audit.audited_at else None,
        "createdAt": audit.created_at.isoformat(),
        "evidenceCount": len(audit.evidence_urls or []),
        "signatureKind": AUDIT_SIGNATURE_KIND if signature else None,
        "signaturePreview": f"{signature[:12]}…" if signature else None,
    }
```
Com o mapa de rotulos publicos declarado junto de `PUBLIC_DOCUMENT_TYPES` (linha 167):
```python
# D-05: conclusao publica fixa por decisao. O texto do laudo interno NUNCA
# chega ao dossie publico (mesmo principio de project.timeline na Phase 4,
# que usa descricao publica fixa em vez das notes do certificador).
AUDIT_PUBLIC_CONCLUSION_LABELS: dict[str, str] = {
    "APPROVED": "Auditoria de campo aprovada",
    "BLOCKED": "Projeto bloqueado por auditoria de campo",
    "RECALCULATED": "Recálculo solicitado pela auditoria de campo",
}
```
Importar `AUDIT_SIGNATURE_KIND` de `backend_app.modules.audit.signature` (modulo puro, sem ciclo de import — `audit/signature.py` nao importa nada de `projects`).

**2. Trocar o uso no dossie publico** (`get_public_dossier`, linha 353): `audits=[audit_item(item) for item in audits]` passa a ser `audits=[public_audit_item(item) for item in audits]`. Nenhuma outra chamada de `audit_item` muda (ela continua servindo a visao interna).

**3. Expor o laudo completo no caminho INTERNO** — confirmar por leitura que nao existe hoje nenhuma rota autenticada que devolva o laudo interno; se `audit_item` ficar sem nenhum consumidor apos a troca, mante-la exportada e documentada como serializador interno para a bancada (consumida pelo frontend no Plan 08 via `GET /audit/queue`), e acrescentar `evidenceUrls`/`reportText` a resposta ja existente de `GET /api/v1/audit/queue`? **Nao** — nao alterar `audit/queue` neste plano. Registrar em uma linha de comentario acima de `audit_item` que ela e o serializador interno e nao deve ser usada no dossie publico.

**4. Testes** — acrescentar a `tests/test_api_integration.py` (nomes contendo `dossier`, para casar com o comando `-k dossier` de VALIDATION.md):
- `test_public_dossier_audit_block_is_minimized`: cria projeto, roda o fluxo de auditoria (upload de evidencia + verify APPROVED com um laudo que contenha uma frase-sentinela unica, ex. `"NOTA-INTERNA-CONFIDENCIAL-XYZ"`), busca `GET /api/v1/projects/{id}/public-dossier` e assere:
  - `dossier["audits"][0]["conclusion"] == "Auditoria de campo aprovada"`
  - `dossier["audits"][0]["evidenceCount"] == 1`
  - `dossier["audits"][0]["signatureKind"] == "STUB_SHA256"`
  - `"reportText" not in dossier["audits"][0]` e `"latitude" not in dossier["audits"][0]`
  - `"NOTA-INTERNA-CONFIDENCIAL-XYZ" not in json.dumps(dossier)`
  - `"supabase://" not in json.dumps(dossier["audits"])` e nenhum sha256 de 64 hex aparece no bloco `audits`
- `test_public_dossier_audit_signature_is_never_complete`: o valor de `signaturePreview` tem no maximo 13 caracteres e o hash completo (64 hex) nao aparece em `json.dumps(dossier["audits"])`.
  </action>
  <verify>
    <automated>cd "/Volumes/External SSD/Projects/sinarca" && uv run pytest -q tests/test_api_integration.py -k dossier -x && uv run pytest -q tests/test_certifier_workbench.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def public_audit_item' backend_app/modules/projects/service.py` retorna 1
    - `grep -c 'audits=\[public_audit_item' backend_app/modules/projects/service.py` retorna 1
    - `grep -c 'audits=\[audit_item' backend_app/modules/projects/service.py` retorna 0
    - `grep -c 'reportText' backend_app/modules/projects/service.py` retorna 1 (apenas dentro de `audit_item`, o serializador interno)
    - `uv run pytest -q tests/test_api_integration.py -k dossier` coleta 2 ou mais testes e todos passam
    - `uv run pytest -q` (suite completa) passa sem regressao
  </acceptance_criteria>
  <done>Dossie publico expoe existencia/data/conclusao da auditoria sem vazar laudo interno, coordenadas, hash ou caminho de storage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor (cliente) -> POST /audit/{id}/evidence | Arquivo binario nao confiavel cruza para o Storage e para o banco |
| cliente -> PATCH /audit/verify | `evidencias_url` e `assinatura_digital` sao entrada arbitraria do cliente |
| dossie interno -> dossie publico | Laudo, coordenadas do auditor e caminho de storage nao podem cruzar |
| organizacao A -> projeto da organizacao B | `project_id` na URL e manipulavel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | upload de evidencia com extensao trocada | mitigate | Reaproveita `validate_magic_bytes` existente + `_validate_mp4_magic_bytes` (`ftyp` no offset 4); allowlist fechada `AUDIT_EVIDENCE_EXTENSIONS`; teste de PNG com conteudo PDF retornando 400 |
| T-05-09 | Spoofing | assinatura de auditoria forjada pelo cliente | mitigate | `payload.assinatura_digital` e descartado; `compute_audit_signature` roda no servidor sobre dados ja persistidos; gate de aceite `grep -c 'payload.assinatura_digital'` = 0. A UI rotula como "stub SHA-256", nunca como assinatura biometrica (D-03) |
| T-05-10 | Elevation of Privilege | IDOR via `project_id`/`Document.id` de outro projeto | mitigate | `get_editable_project_model` org-scoped no upload; `_resolve_audit_evidence_documents` filtra por `Document.project_id == project.id` e retorna 400 quando faltar id; testes dedicados |
| T-05-11 | Information Disclosure | laudo interno / coordenadas do auditor no dossie publico | mitigate | `public_audit_item` com allowlist explicita de campos; teste com frase-sentinela provando ausencia no JSON inteiro (RESEARCH Pitfall 4) |
| T-05-12 | Information Disclosure | caminho/hash de storage vazando no dossie publico | mitigate | `public_audit_item` nao inclui `evidenceUrls`, apenas `evidenceCount`; teste assere ausencia de `supabase://` e de hash de 64 hex no bloco `audits` |
| T-05-13 | Denial of Service | upload de video muito grande | mitigate | `MAX_AUDIT_EVIDENCE_BYTES = 50 MiB` com leitura de `limite + 1` byte e 413 antes de qualquer hash/IO de Storage (mesma tecnica ja usada em `_validated_upload_payload`) |
| T-05-14 | Repudiation | evidencia anexada sem trilha | mitigate | `AUDIT_EVIDENCE_UPLOADED` em `audit_events` por upload e `AUDIT_{status}` com `evidence_ids`/`signed_at`/`signature_kind` no verify |
</threat_model>

<verification>
1. `uv run pytest -q tests/test_audit_field_evidence.py` — SATM-01/SATM-02 verdes
2. `uv run pytest -q tests/test_api_integration.py -k dossier` — SATM-03 verde
3. `uv run pytest -q tests/test_api_integration.py tests/test_certifier_workbench.py` — sem regressao no contrato existente
4. `uv run pytest -q` — suite completa verde
</verification>

<success_criteria>
- Evidencia de auditoria e `Document` + `Evidence` reais com SHA-256 calculado no servidor; zero `local://`
- `evidencias_url` so aceita `Document.id` `AUDIT_EVIDENCE` do proprio projeto
- `Audit.digital_signature` sempre recalculada no servidor e reproduzivel pelo cliente
- Dossie publico mostra existencia/data/conclusao da auditoria sem laudo, coordenadas, hash ou caminho de storage
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/05-satellite-monitoring-and-field-audit/05-02-SUMMARY.md` registrando: shape exato da resposta de `POST /audit/{id}/evidence` e do `PATCH /audit/verify` (o Plan 08 consome), a string canonica assinada, os campos exatos de `public_audit_item`, e o limite/allowlist finais de evidencia de campo.
</output>
