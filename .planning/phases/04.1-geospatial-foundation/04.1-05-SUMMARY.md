---
phase: 04.1-geospatial-foundation
plan: 05
subsystem: frontend
tags: [react, leaflet, geojson, geometry, geofence]
status: IN_PROGRESS_AWAITING_CHECKPOINT

# Dependency graph
requires:
  - phase: 04.1-04
    provides: "boundary field (public_boundary_item/boundary_item GeoJSON) on public-dossier and certifier review payloads"
provides:
  - "ProjectGeofencePreview optional boundary prop -- renders persisted GeoJSON polygon instead of client-side vertex recomputation, with [lng,lat] -> [lat,lng] conversion for Leaflet"
  - "PersistedBoundaryPolygon / ProjectBoundaryPayload types in src/services/database.ts, reused by src/services/certifierReview.ts"
  - "MrcaDetails.tsx (public dossier) and CertifierReview.tsx (QTAGs/Geofence tab) both wired to server geometry"
affects: [04.2-integrity-layer-foundation, 05-satellite-monitoring-and-field-audit]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Persisted-vs-computed fallback pattern in a shared preview component: usesPersistedBoundary gates hasArea/shouldRenderMap/polygonPoints/mapPoints so the same component serves both a read-only server-backed render (dossier, certifier review) and a live client-side draft render (origination wizard) without branching the component itself."

key-files:
  created: []
  modified:
    - src/components/ProjectGeofencePreview.tsx
    - src/services/database.ts
    - src/services/certifierReview.ts
    - src/pages/Dashboard/MrcaDetails.tsx
    - src/pages/Dashboard/CertifierReview.tsx
    - src/pages/Dashboard/AddProject.tsx

key-decisions:
  - "AddProject.tsx's two wizard call sites deliberately receive no boundary prop (unchanged behavior), documented with an explicit code comment at each render location per the plan's call-site disposition table."

requirements-completed: []
# GEOF-05 is NOT marked complete yet -- Task 3 (human verification checkpoint) is pending.

# Metrics
duration: in-progress
completed: null
---

# Phase 04.1 Plan 05: Render Persisted Boundary Geometry in the Frontend Summary

**`ProjectGeofencePreview` now accepts an optional `boundary` GeoJSON Polygon prop and renders server-persisted geometry (converting `[lng,lat]` to `[lat,lng]`) at the two call sites showing a persisted project, while the origination wizard keeps its live client-side recompute -- Task 3 (human visual verification) is the only remaining step.**

## Status: Tasks 1-2 complete and committed. Task 3 (checkpoint:human-verify) is pending -- awaiting real browser verification before this plan can be marked done.

## Performance (partial, tasks 1-2 only)

- **Tasks:** 2/3 (Task 3 is a blocking human-verify checkpoint, not yet resolved)
- **Files modified:** 6 across the two completed tasks

## Accomplishments (Tasks 1-2)

- Added `PersistedBoundaryPolygon` (`{ type: 'Polygon'; coordinates: number[][][] }`) and `ProjectBoundaryPayload` types to `src/services/database.ts`; added `boundary: ProjectBoundaryPayload | null` to `ProjectPublicDossier`, right after `baseline`.
- `src/services/certifierReview.ts` imports `ProjectBoundaryPayload` from `./database` and adds `boundary: ProjectBoundaryPayload | null` to `CertifierReviewDossier`, right after `baseline`.
- `ProjectGeofencePreview` gained an optional `boundary?: PersistedBoundaryPolygon | null` prop (default `null`). A new `persistedPoints` memo derives Leaflet-ready `CoordinateTag[]` from `boundary.coordinates[0]`, guarding against a missing/too-short ring (`ring.length < 4` -> `[]`), dropping the GeoJSON-closed ring's duplicated last point (`slice(0, -1)`), and explicitly destructuring `[lng, lat]` into `latitude: lat, longitude: lng` (coordinate-swap guard, T-041-21).
- `usesPersistedBoundary` (`persistedPoints.length >= MIN_PROJECT_TAGS`) now gates `hasArea`, `shouldRenderMap`, `polygonPoints` and `mapPoints`, so the Leaflet map, SVG technical preview and "área válida" badge all render from server geometry when present, and transparently fall back to the pre-existing client-side `orderTagsForPolygon`/`calculatePolygonArea` computation when `boundary` is absent or null (T-041-19 malformed-ring guard).
- `orderTagsForPolygon`, `calculatePolygonArea`, `MIN_PROJECT_TAGS`, and the rest of `src/services/projectOrigination.ts` were left untouched -- confirmed by `git diff --name-only` not including that file.
- `MrcaDetails.tsx` (~line 302): `<ProjectGeofencePreview tags={qtagDrafts} boundary={dossier.boundary?.declared ?? null} />`, with an explanatory code comment.
- `CertifierReview.tsx` (~line 484, "QTAGs / Geofence" tab): `<ProjectGeofencePreview tags={qtagDrafts} boundary={review.boundary?.declared ?? null} />`. Confirmed `review` is non-null at that JSX position (guarded by the enclosing `{review && (...)}` block at ~line 382), so no optional-chaining-on-possibly-undefined risk beyond the already-optional `.boundary`.
- `AddProject.tsx`'s two wizard call sites (`~1596`, `~1741`) were left as `<ProjectGeofencePreview tags={tags} />` with no `boundary` prop, each preceded by an explicit rationale comment (`GEOF-05: intencionalmente SEM a prop boundary...`), per the plan's locked call-site disposition table. `applyProjectForEditing`/`editingProjectId` flow untouched.
- `npm run build` passes after each task (vite build, no TypeScript errors surfaced by the build step; `npx tsc -b` intentionally not used per plan and STATE.md's documented pre-existing legacy-debt block).
- All grep-level structural acceptance criteria from both tasks passed verbatim (call-site counts, coordinate-swap guard, `usesPersistedBoundary` reference count >= 5, fallback path preserved, wizard rationale comment count == 2, no `ProjectGeofencePreview` line was deleted in `AddProject.tsx`).

## Task Commits

1. **Task 1: Optional persisted-boundary prop on ProjectGeofencePreview + API types** - `baa2e7b` (feat)
2. **Task 2: Wire the two persisted call sites; document AddProject as intentionally unchanged** - `8901013` (feat)
3. **Task 3: Human verification of persisted-geometry rendering** - PENDING (checkpoint:human-verify, not yet resolved)

## Files Modified (Tasks 1-2)

- `src/components/ProjectGeofencePreview.tsx` - optional `boundary` prop, `persistedPoints`/`usesPersistedBoundary` derivation, four downstream values (`hasArea`, `shouldRenderMap`, `polygonPoints`, `mapPoints`) now persisted-boundary-aware
- `src/services/database.ts` - `PersistedBoundaryPolygon`, `ProjectBoundaryPayload` types; `boundary` field on `ProjectPublicDossier`
- `src/services/certifierReview.ts` - import of `ProjectBoundaryPayload`; `boundary` field on `CertifierReviewDossier`
- `src/pages/Dashboard/MrcaDetails.tsx` - `boundary={dossier.boundary?.declared ?? null}` wired into the QTAG/geofence card
- `src/pages/Dashboard/CertifierReview.tsx` - `boundary={review.boundary?.declared ?? null}` wired into the "QTAGs / Geofence" tab
- `src/pages/Dashboard/AddProject.tsx` - two explanatory comments added above the unchanged wizard render calls; no behavior change

## Deviations from Plan

None so far - Tasks 1-2 executed exactly as written, including the exact type placements, derivation ordering, and call-site wiring specified in the plan's `<action>` blocks.

## Task 3: Checkpoint Pending

Task 3 is a `checkpoint:human-verify` (`gate="blocking"`) requiring real browser confirmation that:
1. The public dossier (`/projeto/PRC-2024-002`) renders the persisted polygon over Brazil (lat ~ -10.18, lng ~ -48.33), matching `GET /api/v1/projects/PRC-2024-002/public-dossier`'s `boundary.declared.coordinates`.
2. The certifier review "QTAGs / Geofence" tab renders the polygon (login `certificadora@sinarca.com.br` / `certificadora`).
3. The origination wizard preview still updates live while typing vertex coordinates (login `produtor@sinarca.com.br` / `produtor`) -- proving it was NOT switched to persisted geometry.
4. The fallback path renders from vertices (not empty) when a project has QTAGs but no persisted boundary.

This cannot be self-approved by the executing agent -- it requires actual visual confirmation in a running browser, which is outside this agent's capability in this session. See the checkpoint report returned alongside this summary for exact verification steps and the API cross-check command.

This SUMMARY.md will be updated with the verbatim human-verification result (coordinates observed, pages checked) once Task 3 resolves, and STATE.md/ROADMAP.md/REQUIREMENTS.md will be updated only after that resolution, per this plan's success criteria.

## Self-Check: PASSED (partial, tasks 1-2 only)

- `src/components/ProjectGeofencePreview.tsx` - FOUND
- `src/services/database.ts` - FOUND
- `src/services/certifierReview.ts` - FOUND
- `src/pages/Dashboard/MrcaDetails.tsx` - FOUND
- `src/pages/Dashboard/CertifierReview.tsx` - FOUND
- `src/pages/Dashboard/AddProject.tsx` - FOUND
- Commit `baa2e7b` - FOUND in git log
- Commit `8901013` - FOUND in git log

---
*Phase: 04.1-geospatial-foundation*
*Status: awaiting checkpoint resolution (Task 3)*
