---
phase: 04.1
slug: geospatial-foundation
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-15
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest ≥9.0.3 (backend). No frontend test framework configured (no `test` script, no vitest/jest dependency). |
| **Config file** | No `[tool.pytest.ini_options]` in `pyproject.toml` — discovery relies on pytest defaults (`tests/` dir, `test_*.py`). |
| **Quick run command** | `uv run pytest tests/test_api_integration.py -x` (requires local Supabase Postgres reachable at `127.0.0.1:54322`, per `tests/conftest.py:11-13`) |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~30-60s quick / a few minutes full |

Frontend (`ProjectGeofencePreview.tsx`, GEOF-05) has no automated test harness in this repo — verified manually/via browser automation, consistent with Phase 04's own precedent (`bc4317d docs(04): confirm manual UI verification via browser automation`).

---

## Sampling Rate

- **After every task commit:** `uv run pytest tests/test_project_boundaries.py -x -q` (phase-scoped) or `uv run pytest tests/test_api_integration.py -x` for non-boundary tasks
- **After every plan wave:** `uv run pytest -q`
- **Before `/gsd-verify-work`:** Full backend suite must be green; GEOF-05 frontend render verified manually/via browser automation
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01/T1 | 04.1-01 | 1 | GEOF-01 | T-041-01, T-041-02 | `project_boundaries` created with 4 geometry(Polygon,4326) columns, GiST index, RLS enabled and no select policy | static SQL contract | `uv run pytest tests/db/test_project_boundaries_schema.py -x -q` | created by 01/T2 | ⬜ pending |
| 01/T2 | 04.1-01 | 1 | GEOF-01, GEOF-02 | T-041-03, T-041-04 | Backfill is an idempotent upsert, reads `project_tags` read-only, reuses polar-angle ordering, lng-before-lat, `::geography` cast | static SQL contract | `uv run pytest tests/db/test_project_boundaries_schema.py -x -q` | ✅ created here | ⬜ pending |
| 01/T3 **[BLOCKING]** | 04.1-01 | 1 | GEOF-01, GEOF-02 | T-041-04, T-041-05 | PostGIS live; `PRC-2024-002` backfilled with 5-point closed ring; vertex set and shoelace area identical to the previous calculation | integration (live DB) | `uv run pytest tests/test_project_boundaries.py -x -q` | ✅ created here | ⬜ pending |
| 02/T1 | 04.1-02 | 2 | GEOF-03 | T-041-06, T-041-07 | WKT built in exactly one place (lng first), bound parameters only, vertex cap 500 | unit + inline | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 02/T2 | 04.1-02 | 2 | GEOF-03 | T-041-08 | create/update persist declared + mirrored active boundary in the same transaction; upsert never duplicates | integration | `uv run pytest tests/test_certifier_workbench.py -x -q` | ✅ exists | ⬜ pending |
| 02/T3 | 04.1-02 | 2 | GEOF-03 | T-041-06, T-041-07, T-041-08 | Invalid / self-intersecting / duplicate-vertex / >500-vertex geometry rejected with 400; area divergence flagged, not blocking | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 03/T1 | 04.1-03 | 3 | GEOF-04 | T-041-12, T-041-13 | `ST_Intersects` GiST pre-filter + `ST_Area(::geography)`; read-only, no Conflict creation | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 03/T2 | 04.1-03 | 3 | GEOF-04 | T-041-11 | Endpoint org-scoped by `_assert_project_edit_permission`; anonymous access refused | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 03/T3 | 04.1-03 | 3 | GEOF-04 | T-041-11, T-041-14 | Two overlapping projects yield ≈25% `overlapPercentage`; disjoint projects excluded | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 04/T1 | 04.1-04 | 4 | GEOF-05 | T-041-15, T-041-17 | Public dossier ships minimized `boundary` GeoJSON; internal telemetry withheld | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 04/T2 | 04.1-04 | 4 | GEOF-05 | T-041-15 | Certifier review ships the full `boundary` object | integration | `uv run pytest tests/test_certifier_workbench.py -x -q` | ✅ exists | ⬜ pending |
| 04/T3 | 04.1-04 | 4 | GEOF-05 | T-041-15, T-041-16 | Ring closed, coordinates `[lng, lat]` within Brazil bounds, `boundary: null` when no geometry | integration | `uv run pytest tests/test_project_boundaries.py -x -q` | extends 01/T3 file | ⬜ pending |
| 05/T1 | 04.1-05 | 5 | GEOF-05 | T-041-19, T-041-21 | Component renders persisted GeoJSON, converts `[lng,lat]`→`[lat,lng]`, falls back when null | build + grep | `npm run build` | n/a (no FE framework) | ⬜ pending |
| 05/T2 | 04.1-05 | 5 | GEOF-05 | T-041-20 | 2 call sites pass `boundary`; AddProject verifiably passes none | build + grep | `npm run build` | n/a (no FE framework) | ⬜ pending |
| 05/T3 (checkpoint) | 04.1-05 | 5 | GEOF-05 | T-041-21 | Rendered polygon matches API coordinates and sits over Brazil; wizard preview still live-updates | manual / browser | `npm run build` (automated gate) + human resume signal | n/a | ⬜ pending |

*Filled by the planner on 2026-08-15. `NN/TN` = plan number / task number within that plan.*

---

## Wave 0 Requirements

- [ ] Verify PostGIS actually loads in the local Supabase image (`CREATE EXTENSION postgis; SELECT postgis_full_version();`) — blocks everything else in this phase if it fails
- [ ] `tests/db/test_project_boundaries_schema.py` — new file, covers GEOF-01 (schema/index assertions)
- [ ] Test fixtures/factory for creating a project with N `project_tags` in tests (check `tests/test_api_integration.py` / `tests/test_certifier_workbench.py` for a reusable helper before writing a new one)
- [ ] Fixture data for two intentionally-overlapping projects, to exercise GEOF-04 deterministically
- [ ] Framework install: none — pytest already present

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| `ProjectGeofencePreview` renders persisted geometry (not client recompute) | GEOF-05 | No frontend test framework configured in this repo | Open a project dossier (`MrcaDetails.tsx`), certifier review (`CertifierReview.tsx`), and `AddProject.tsx` in a browser; confirm the rendered polygon matches `declared_boundary`/`active_boundary` returned by the API, not a client-side shoelace recompute |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (created in 04.1-01 T2/T3)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-signed 2026-08-15 — every task has an `<automated>` verify or an explicit manual justification; no 3 consecutive tasks lack automated verification; Wave 0 scaffolding (`tests/db/test_project_boundaries_schema.py`, `tests/test_project_boundaries.py`, overlapping-project fixtures via `rectangle_tags`) is created inside plan 04.1-01 tasks 2 and 3, before any task depends on it.

**Final verification:** verified 2026-08-18 via `04.1-VERIFICATION.md`; phase-scoped tests passed (`41 passed`) and UAT/security artifacts are complete.
