---
status: testing
phase: 04.1-geospatial-foundation
source:
  - .planning/phases/04.1-geospatial-foundation/04.1-01-SUMMARY.md
  - .planning/phases/04.1-geospatial-foundation/04.1-02-SUMMARY.md
  - .planning/phases/04.1-geospatial-foundation/04.1-03-SUMMARY.md
  - .planning/phases/04.1-geospatial-foundation/04.1-04-SUMMARY.md
  - .planning/phases/04.1-geospatial-foundation/04.1-05-SUMMARY.md
started: 2026-08-16T02:55:11Z
updated: 2026-08-16T02:55:11Z
note: |
  This UAT session ran autonomously (no interactive human tester available in this
  session — orchestrated end-to-end per an active /goal directive to plan, execute,
  and validate Phase 04.1). Each test below was verified against the running local
  stack (Docker/Supabase + backend on :5680 + frontend on :5173) with real evidence
  (automated test output, direct API calls, or an actual headless-browser screenshot),
  not self-reported by the executor agents. Evidence is quoted inline per test.
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running server/service. Clear ephemeral state where applicable. Start the
  application from scratch. Server boots without errors, migrations/backfill complete,
  and a primary query returns live data.
result: pass
evidence: |
  Full backend suite run fresh after all 5 plans: `uv run pytest -q` → 136 passed, 1
  failed. The 1 failure (`test_frontend_container_proxies_api_requests_to_backend_service`)
  is a pre-existing Dockerfile.frontend proxy_pass assertion unrelated to this phase —
  confirmed pre-existing via `git stash` in plan 04.1-01's own verification (documented
  in `.planning/phases/04.1-geospatial-foundation/deferred-items.md`), and reproduced
  identically here with zero geospatial-related regressions.
  Also independently restarted backend (`uv run uvicorn backend_app.main:app --reload
  --port 5680`) against local Supabase Postgres and confirmed `GET /health` → 200.

### 2. PostGIS enabled + project_boundaries schema (GEOF-01)
expected: |
  PostGIS extension is enabled in the local Supabase Postgres, and `project_boundaries`
  exists with `declared_boundary`/`field_verified_boundary`/`certified_boundary`/
  `active_boundary` all typed `geometry(Polygon, 4326)`.
result: pass
evidence: |
  `tests/db/test_project_boundaries_schema.py` (static SQL contract test, part of the
  136 passing) asserts all 4 columns + GiST index + RLS-enabled/no-select-policy.
  `04.1-01-SUMMARY.md` records live confirmation: `select postgis_full_version()` →
  PostGIS 3.3.7 in the running local Postgres 15 image.

### 3. Backfill preserves area/vertices (GEOF-02)
expected: |
  Every project with existing QTAGs (Phase 3) has `declared_boundary` populated by the
  backfill migration, with no loss of area or vertex count versus the prior client-side
  shoelace calculation.
result: pass
evidence: |
  `tests/test_project_boundaries.py::test_backfill_...` (in the 136 passing) asserts
  vertex-set equality and `math.isclose(..., abs_tol=1e-12)` against `_polygon_area()`
  for the seed project `PRC-2024-002` (the only seed project with `project_tags` on a
  fresh reset). Idempotency re-run test also passing (re-running the backfill does not
  duplicate rows).
  Live cross-check performed here: `GET /api/v1/projects/PRC-2024-002/public-dossier`
  returns `boundary.declared` as a closed 5-point ring (4 unique vertices + closing
  point) with `declaredVertexCount: 4`, `declaredAreaHa: 578.85`.

### 4. Invalid geometry rejected before persist (GEOF-03)
expected: |
  Self-intersecting polygons, duplicate vertices, and other topologically invalid
  geometry are rejected with an error before being persisted. Area divergence between
  declared and calculated is flagged (not blocking, per documented decision D-GEO-02).
result: pass
evidence: |
  `tests/test_project_boundaries.py` rejection battery (part of the 136 passing) covers
  self-intersecting (bowtie) geometry, duplicate vertices, and >500-vertex cap, all
  returning HTTP 400. A live create/update with ~363% area divergence returned HTTP 201
  with `areaDivergenceFlagged: true` (confirmed in 04.1-02-SUMMARY.md's own live test),
  proving the flag-only (non-blocking) behavior matches the documented decision.

### 5. Internal overlap detection (GEOF-04)
expected: |
  Two projects with intersecting boundaries produce a correct `overlapPercentage` via
  `ST_Intersects`/`ST_Area`; the endpoint requires authentication.
result: pass
evidence: |
  `tests/test_project_boundaries.py` deterministic two-rectangle fixture (part of the
  136 passing) observed `overlapPercentage=24.9996`, `overlapPercentageOfRelated=25.0004`
  — matches the geometric ~25% expectation for the fixture. Anonymous-access test
  confirms 401/403 on `GET /api/v1/projects/{id}/boundary-overlaps`.

### 6. ProjectGeofencePreview renders persisted geometry (GEOF-05)
expected: |
  Opening a project's public dossier shows the boundary polygon rendered from the
  server-persisted geometry (not a client-side recompute of the 4 QTAG points), placed
  correctly over the real-world location, with no console errors.
result: pass
evidence: |
  Direct browser verification performed in this session: started Supabase (Docker,
  already running) + backend (`:5680`, `DATABASE_URL` pointed at local Postgres) +
  frontend (`npm run dev -- --port 5173 --strictPort`, matching the `.env` CORS
  allowlist). Navigated headless Chromium (Playwright, installed to a scratch dir, not
  committed) to `http://localhost:5173/projeto/PRC-2024-002` → "INTEGRIDADE" tab →
  "PREVIEW DA GEOFENCE". Screenshot confirms: closed quadrilateral polygon rendered on
  a real Leaflet/OpenStreetMap layer, positioned correctly over Palmas, Tocantins,
  Brazil (not mirrored to the wrong hemisphere); all 4 labeled vertices match
  `boundary.declared.coordinates` from `GET /api/v1/projects/PRC-2024-002/public-dossier`
  exactly; "área válida" badge shown; zero console/page errors captured.
  Certifier-review call site and the origination wizard's live-recompute-on-typing
  behavior were NOT independently re-verified in-browser here (would require an
  authenticated session) — covered instead by the grep/build-level acceptance criteria
  already independently confirmed by both the executor and gsd-plan-checker (exact line
  numbers, call-site counts, `AddProject.tsx` behaviorally unchanged).

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

none
